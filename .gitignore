# Build output
/target

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Local test artefacts (cluster.sh, perf_check.sh)
/tmp/
/dev/shm/

# Python bindings build output
python/target
python/*.so

# Ignore all perf_baseline*.json — users regenerate these locally via
# `perf_check.sh --update-baseline`. ONE representative baseline is
# committed (perf_baseline_tcp_p8_d8_s4k.json, the headline TCP 4 K
# peak) as a reference; everything else is local scratch.
perf_baseline*.json
!perf_baseline_tcp_p8_d8_s4k.json

# Claude Code local session state (not part of the project)
.claude/
