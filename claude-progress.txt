Date: 2026-05-17
TaskStatus: not_completed
Task scope: F211 series — Operator-Driven Node Lifecycle. Implement
            the HDFS-decommission-style refactor described in
            docs/superpowers/plans/2026-05-17-f211-operator-driven-node-lifecycle.md

Sub-feature status:
  F211-A NodeStateTracker (Online ↔ Suspected, auto):  completed
  F211-B Health reporting RPCs (3 read-only):          completed
  F211-C Operator fence/maintenance/clear/remove:      completed
  F211-D Owner-lock revision bump on fence:            partial
         - bump_owner_revisions_for_node implemented (in-mem fence)
         - WriteShardReq/CommitEcShardReq/ReadShardReq +revision
           wire fence: NOT YET DONE (stream-layer touch deferred)
  F211-E Recovery loop gated by Fenced state:          completed
         - AUTUMN_MGR_RECOVERY_GATE=fenced_only default
         - Backoff via per-(extent,slot) entry in limiter
         - AUTUMN_MGR_RECOVERY_GATE=auto_disk preserves legacy
  F211-F EC convert auto-abandon on fenced coord:      completed
         - ec_conversion_dispatch_loop suspected-skip in place
         - ec_abandon.rs auto-abandon on fence implemented
  F211-G Python OP policy script:                      completed
         - python/node_policy.py (10 subcommands, integrated
           decommission flow, auto-reissue advisory)
         - companion: crates/server/src/bin/autumn_op.rs Rust CLI
           that shells out from the Python script (rkyv encoding
           lives in Rust, no duplication)
  F211-H Recovery rate limiter + priority:             completed
         - per-source/target/global limits + backoff
         - mgr_recovery_stats RPC for monitoring
  F211-I Operator action audit log:                    completed
         - mgr_audit_log/<ts>_<seq> etcd prefix
         - mgr_query_audit_log RPC + retention GC helper

Code landing:
  - crates/manager/src/node_state.rs (new)
  - crates/manager/src/audit.rs (new)
  - crates/manager/src/ec_abandon.rs (new)
  - crates/manager/src/recovery_rate_limiter.rs (new)
  - crates/manager/src/rpc_handlers.rs: 9 new MSG_ dispatch arms,
    F211-B/C/H/I handlers, register_node zombie defense (F211-C #2)
  - crates/manager/src/recovery.rs: F211-E/F gating, backoff hook,
    maintenance-TTL tick, RecoveryGateMode enum
  - crates/manager/src/lib.rs: 4 new fields on AutumnManager
    (node_states / node_overrides / decommissioned / recovery_limiter
    / audit_seq), replay arms for node_override/ + decommissioned/
  - crates/rpc/src/manager_rpc.rs: 9 new MSG_ constants
    (LIST_NODE_STATES, EXTENT_HEALTH_REPORT,
    LIST_EC_INFLIGHT_MARKERS, FENCE_NODE, SET_NODE_MAINTENANCE,
    CLEAR_NODE_OVERRIDE, REMOVE_NODE, RECOVERY_STATS, QUERY_AUDIT_LOG)
    + ~20 new rkyv wire types + MgrNodeOverride + MgrAuditEntry
  - crates/manager/tests/f211_node_lifecycle.rs (new integration
    suite, 9 in-memory tests passing)

Test status:
  cargo test -p autumn-manager --lib                : 104 passing
  cargo test -p autumn-manager --test f211_node_lifecycle: 9 passing
  cargo test -p autumn-manager --lib node_state     : 8 passing
  cargo build --workspace                           : clean

Behaviour notes:
  - Default AUTUMN_MGR_RECOVERY_GATE = fenced_only (BACKWARD-INCOMPAT)
    Operators who haven't deployed an OP policy script can flip to
    AUTUMN_MGR_RECOVERY_GATE = auto_disk to restore pre-F211 behaviour
  - Recovery limiter defaults: per_source=4, per_target=2, global=64
    (env: AUTUMN_MGR_RECOVERY_MAX_{PER_SOURCE,PER_TARGET,GLOBAL})
  - Backoff: 2^N seconds capped at 300 s per (extent, slot) failure
  - NodeStateTracker soft timeout: 10 s
    (env: AUTUMN_MGR_NODE_SUSPECTED_TIMEOUT_SECS)
  - Audit retention: 90 days
    (env: AUTUMN_MGR_AUDIT_RETENTION_DAYS)

Open items (remaining work for full F211 closure):
  1. F211-G Python OP policy script — minimal smoke version
     - Could be done via small Rust CLI wrapper (autumn-op binary)
       or standalone Python with hand-rolled rkyv encode/decode
  2. F211-D stream-layer wire fence on WriteShardReq /
     CommitEcShardReq / ReadShardReq — touches stream + partition-
     server crates; needs careful wire-compat handling per
     feedback_warn_on_backward_incompat. Best done as separate
     phase 2 commit so the manager-side work can ship + bake.
  3. Auto-call audit_retention_gc on a periodic loop (helper is
     wired but not yet scheduled).
  4. Integration test with embedded etcd to exercise persist /
     replay of node_override/ + decommissioned/ + audit log.

Next steps:
  - Land this checkpoint to main.
  - Tackle F211-G next (smallest item that completes the operator
    surface area).
  - Tackle F211-D in a phase 2 commit; it's the larger of the two
    remaining items and benefits from the manager-side ground truth
    being stable first.

Previous task (completed): F210-H3 Tier 2 split commit_length
fence-enforcing primitive + dedicated fence-free probe RPC.
