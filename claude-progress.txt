Date: 2026-05-04
TaskStatus: completed
Task scope: feature_list.md prune — drop work-tracking entries with no value to a fresh
            contributor; keep root-cause analyses, architectural decisions, and active
            designs. F136 / F138 / F139 from the prior session are intact.

Completed:
  - feature_list.md: 1322 → 446 lines (-66 %; -876 net lines).
  - Demoted to the ✅ Completed table (rationale lives in code + crate CLAUDE.md):
    F010, F012, F019, F020, F021, F024, F040, F041, F042-F047, F048, F049, F050-F060,
    F061, F062-F076 (system tests), F077, F078, F079, F082, F083, F084, F085, F086,
    F087-bulk-mux, F088-F094 (PS thread isolation perf series), F095, F096,
    F098-4.2 / 4.3 / 4.4 / R4-B, F099-A / B / C / D / H / I / I-fix / N-a,
    F101 / 101-b / 101-c / 101-d / 101-e (UCX experiments), F102, FOPS-01 / 02 / 03 / 04,
    FGA-01.
  - Kept verbatim (high reference value):
    * core arch: F011, F028-F038 (already brief)
    * production root-cause fixes: F103-F107, F108, F109, F111, F112, F113, F116-F121,
      F122, F123-F128, F135, F136
    * open / deferred: F129-F134, F138, F139, FCI-01
    * perf-series architectural lessons (small set): F099-J, F099-K-diagnosis, F099-M,
      F099-N-b — these document non-obvious decisions / measured root causes that
      cannot be derived from code alone
    * P5 transport: F100-UCX (umbrella + cross-host A/B deferral)
  - All entries kept retain the Target/Evidence/passes shape; "passes" is unchanged for
    every entry. No status flips. The prune is a simplification, not a state change.
  - Order: ✅ Completed table → P0 Core Architecture → P1 Performance & Space (incl.
    production fixes) → P0 Code Review Fixes → Open/Deferred → Perf-series lessons →
    P5 transport → P3 CI cleanup. The previous file's repeated "P0", "P1", "P2", "P3"
    section markers (the original was a chronological log with reused headers) were
    consolidated.

Out of scope:
  - F136 / F138 / F139 (prior session, kept).
  - Source-code changes (this session is documentation only).
  - autumn-rs/CLAUDE.md, crate-level CLAUDE.md (untouched — they remain the source of
    truth for current architecture, and feature_list.md now defers to them).

Verification:
  - feature_list.md still parses as well-formed Markdown (verified by `grep -nE '^(##|###)'`
    outline: 8 sections, 48 detailed entries, 1 completed-table block).
  - All 12 root-cause entries (F103-F121 / F108 / F109) still cite their file:line evidence.
  - All 8 deferred entries (F129-F134, F138, F139) retain their Trigger: clauses per the
    project's "deferred items go into feature_list.md" rule.
  - No source changes → no build / test runs needed; the repository builds the same as it
    did at HEAD before this commit (commit 8661dfa).
