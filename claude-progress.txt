Date: 2026-09-25 (5)
TaskStatus: completed
Task scope: "实现网关的写入接口" — the gateway write APIs of F-LANCEDB-S3-GATEWAY (the LanceDB e2e is not wanted, user 2026-09-25).
Current summary: autumn-s3 now serves PutObject (If-None-Match: *, If-Match), CopyObject, DeleteObject (If-Match), DeleteObjects, Create/UploadPart/Complete/AbortMultipartUpload, conditional GET/HEAD (RFC 9110 order), IMF-fixdate Last-Modified (was ISO 8601), generation-based ETags, aws-chunked bodies, Content-MD5 when sent. New write.rs; objects.rs gains STABLE pins for GET (held_leases, per-worker refcount, 2 s linger, deferred reclaim of inodes unlinked while pinned); every worker runs the lease heartbeat; a sweeper thread runs recover_dead_sessions / sweep_uploads / sweep_unlink_tombstones / sweep_garbage (--sweep-interval-secs, default 30). autumn-fs: ObjectStream/PartWriter writes need only the client, NewFile::start_object + write_streamed, so a body streams without the worker's state lock; hold_for_swap counts only this client's WRITERS as a conflict (a GET pin on the same worker used to make a PUT 409).
Verification: gateway unit tests 18 pass (dates, conditions, XML, aws-chunked at every split, keys). scripts/s3_write_check.py (boto3) 66/66 on the final binary. Ablations: ignoring If-None-Match makes all 8 racing writers win (race check red); releasing the manager lease right after the pin makes a GET break at 17.5 MB when a concurrent delete lands on another worker (4/4 intact with the pin). kill -9 of the gateway mid-PUT and mid-UploadPart with 29 data keys written: after restart the sweeper reclaimed every key and pending record, upload still usable. Exact KV accounting after the SDK run: no rmtomb/pend/mpu/mpa/mpp leftovers, segc and data keys match the live objects. autumn-fs 27 unit tests + system_multipart/publish/segmented/fuse_read pass; python/ cargo check passes. Independent review not run (the user said review is not needed for this work).
Main gaps (feature not passed): Complete retried after success gets NoSuchUpload (AWS: success); an unknown publish outcome settles only after the worker's session is recovered (gateway restart); Abort and large deletes reclaim data under the worker's lock; DeleteObjects is serial; UploadPartCopy/ListParts/ListMultipartUploads/object metadata not served; no GET-vs-mount-writer test through a real mount; no perf measurement of write throughput or of the pin's extra manager round trips.
Env notes: the disk filled up during this session (root volume); the scratch test cluster used ~2.5 GB with 3 ENs. Test with `cluster.sh reset 1` when space is short.
Next steps: the remaining gaps above, as the user schedules them.

Date: 2026-09-25 (4)
TaskStatus: completed
Task scope: "继续做multipart" — bring the multipart draft (formerly outside the tree in the 6a58b750 scratchpad fstage/) into autumn-fs. This supersedes the "Multipart (F) is drafted OUTSIDE the tree" line of the 2026-09-24 (3) entry.
Current summary: crates/fs/src/multipart.rs + schema (UploadRecord with `key`, UploadState Open/Completing/Completed/Aborted, PartRecord, PendingOp::Part/Complete appended) + keys (mpu/, mpp/, mpa/ keyed by session, parse_pending_key) + recover_session arms for Part and Complete. Five defects in the draft fixed while integrating: (1) Complete undid the file on PublishError::Other, whose swap may still land (dangling name) — now left Completing for recovery; (2) a failure after a landed publish failed the Complete — the post-publish steps are best-effort now; (3) a same-session retry after (2) was Busy forever — it finishes the upload when the dirent names the file; (4) cleanup compared frozen with Vec::contains, quadratic up to 10000x10000 — HashSet; (5) sweep_uploads only ever read the first 256 uploads — pages through all.
Verification: system_multipart passes (metadata-only Complete proved by deleting every part body first and diffing all [0x03] keys; Complete/Abort race; late part; list validation; unknown publish outcome; same-session retry; dead session part + Complete). Ablations: undoing on Other turns it red at "leaves the upload Completing"; Busy for the own-session retry turns it red at "the retry finishes a landed Complete". system_publish and system_segmented still pass; autumn-fs unit tests 27 pass. Independent review not run (the user said review is not needed for this work).
Main gaps: the gateway still routes none of it (PUT/DELETE/Copy/multipart HTTP routes, conditional GET/HEAD with STABLE leases, periodic recover_dead_sessions / sweep_uploads / sweep_garbage). Known multipart limits recorded in crates/fs/CLAUDE.md: a Complete retry after cleanup gets NoSuchUpload (AWS returns success); an Open upload whose creator died is never reclaimed; an UploadPart during a Complete that later fails must be re-sent.
Next steps: gateway write APIs (G). LanceDB e2e is not wanted (user, 2026-09-25).

Date: 2026-09-25 (3)
TaskStatus: completed
Task scope: user decision — "用autumn-fs一个crate，另外是autumn-fuse和s3的依赖autumn-fs". Split the fuser-free filesystem layer (the old `core` feature of autumn-fuse) into its own crate before multipart goes in, since multipart and the S3 gateway have nothing to do with the kernel mount.
Current summary: new crate `crates/fs` (autumn-fs): key/schema/geom/dir/extent/lease_tasks/meta/publish/read/segment/state/write + the read_plan bench, moved with git mv. autumn-fuse is mount-only (attr/bridge/dispatch/ops/read_pool/main), depends on autumn-fs, and lost its `core`/`fuse` features (`ucx` kept). autumn-server (autumnfs, autumn-s3, migratev3_v4, autumn-op) and the PyO3 binding depend on autumn-fs directly. Manager: autumn-fs is a dev-dependency; seven filesystem tests (system_publish, system_segmented, system_fuse_ns/read/eof_clobber/flush_error_sticky, bug_lease_3) no longer need `fuse-tests` or libfuse and call `meta::ensure_root` (what `dispatch::init_root` wrapped); only fuse_lease_1/2 and system_fuse_release_best_effort still need `fuse-tests`. crates/fuse/CLAUDE.md split into crates/fs/CLAUDE.md (filesystem sections moved verbatim) + a mount-only guide. python/Cargo.lock also picked up autumn-rpc's `libc`, which an earlier upstream commit had left stale.
Verification: cargo check fs/fuse/server all-targets clean; fs 26 + fuse 42 unit tests pass (HEAD's crates/fuse had 68); manager tests compile with and without fuse-tests; python/ cargo check clean; the ten affected manager system tests pass with --include-ignored (60 tests). Independent review skipped on the user's instruction.
Main gaps: none for the split. Multipart is next: the integrated draft is in `git stash` ("multipart WIP before autumn-fs split") and must be re-applied under crates/fs.
Next steps: apply the multipart WIP to crates/fs, run system_multipart, commit.

Date: 2026-09-25 (2)
TaskStatus: not_completed
Task scope: "fix F-DEAD-PEER-DETECT" — connections whose peer stopped answering while TCP looks healthy wedged fuse reads (30 s EIO for hours, 2026-09-22 incident).
Current summary: RpcClient now detects a silent peer itself (`client::Keepalive`, 2 s / 8 s). Signs of life = any received byte, or on TCP the peer ACKing our bytes while more wait UNSENT behind them (`TCP_INFO` tcpi_bytes_acked + tcpi_notsent_bytes via a local repr(C) prefix — glibc's libc struct stops early). Silent 2 s → MSG_TYPE_PING; TCP closes 8–10 s / UCX ~10–12 s after the last sign: closed=true, pending cleared, an in-progress bulk receive released through the `receiving` slot (NOT by cancelling the reader — on UCX that can return a pool slab UCX still writes into). EN/PS/manager answer the ping in their decode loops (PS before authz_gate, documented as the one exception); an older server's `unknown msg_type` reply counts too, so no wire bump. Manager's hand-rolled RpcConn (which also aliased one &mut across tasks via a raw pointer) replaced by an RpcClient pool; SDK ps_conns/mgr_conn skip is_closed() entries.
THE LEDGER'S SIGSTOP TEST DOES NOT DISCRIMINATE: HEAD already recovers from a SIGSTOPped EN (manager marks it Suspected, descriptors stop naming it) on TCP and UCX. The discriminating scenario is `mgr-freeze` (scripts/fuse_dead_peer_chaos.sh + scripts/freeze_proxy.py: freeze the mount's existing manager flows, new flows pass, then split so routing is stale): HEAD = every read 30 s EIO for the whole run; fixed = worst 8.3 s then ≤0.2 s. Root cause of the wedge: mgr_call's full 30 s rpc_timeout always loses the race to fuse's 30 s REPLY_TIMEOUT, so the call is cancelled before it can evict the dead connection.
Four independent reviews; each round's real findings fixed with a regression test ablated red: ping queued behind a large frame on a slow link (host autotunes 32 MiB snd+rcv buffers) → ACK signal; bulk receive not released by close → `receiving` slot; ACK from a frozen peer's kernel kept it alive under caller traffic → count ACKs only with a send backlog (tcpi_unacked is not enough: delayed ACKs keep a segment in flight). 7 keepalive tests + 3 server ping tests, each mechanism's ablation reds only its own test.
Verification: rpc 87 + keepalive 7 + teardown 2 + others; stream 204; manager 411 + keepalive_ping; client 63; PS 259; fuse 63. E2E TCP mgr-freeze + en-stop PASS (concurrent reads, closes named only faulted peers); UCX en-stop PASS (worst 13.2 s; UCX config verified from the fuse's own context dump: self + rc_mlx5/ud_mlx5 on mlx5_1:1 only, mlx5_1 = eth0 = the bind IP). UCX cancel-drain "leaking slot" warnings present equally on HEAD (2/2/2/15/40/10 vs 2/2/2/16/37/7) — pre-existing. Perf A/B TCP p8 d8 cpuset-isolated, 7 interleaved pairs at 4K: write median HEAD 48.0K / fix 47.1K, read 504K / 599K (noisy both sides); 8M unchanged (2 pairs).
Main gaps: (1) ledger Acceptance (e) 24 h no-false-close soak NOT run. (2) Acceptance (b) "worker-evict" deliberately NOT implemented — mgr-freeze passes without it and root rule 16 forbids defence without a reproduced need; the ledger text can't be met literally, needs the user's call. (3) Residuals: a server back-pressured at its in-flight cap (PS 4 / EN 64) stops reading and can't answer pings (SDK's 5 s first attempt already evicts PS conns); UCX writer stuck behind a non-reading peer never gets the ping out. (4) CPU cost not measured separately.
Decision (user, 2026-09-25): worker-evict is NOT to be added — unnecessary now that the keepalive removes the root cause; gap (2) is closed as not-applicable.
Next steps: run the 24 h soak with `grep "rpc peer stopped answering"` across all logs (must name only faulted peers).

Date: 2026-09-25
TaskStatus: completed
Task scope: (1) "你这个性能不是没变化吗？！Try again" + "测试的是cpuset要先躲开正在跑的python" — redo the compio 0.18 vs 0.19 comparison with the cluster pinned away from the tenants; (2) "所以0.19不用revert？ reset 然后再push -f吧" — undo the revert; (3) the lone-writer regression the reviewer predicted for yesterday's coalescing window, now measured and fixed. Ledger: BUG-COALESCE-WINDOW-HOLDS-LONE-WRITER (+ a dated correction under BUG-GROUP-COMMIT-SPLIT-AFTER-RUNTIME-UPGRADE, F-COMPIO-UPGRADE status).
Current summary: THE WHOLE 2026-09-24 DIAGNOSIS WAS A TENANT ARTIFACT. Every number taken that day came from a cluster pinned to 96-151 — NUMA node 0, where sglang's schedulers are pinned (0-47,96-143) next to a 100%-busy python on cpu 130. Moved to quiet node-1 cores (EN 59-66 / 83-90 / 72-75,78-81, PS 48-53,55-57,67-69,91-93,95; layout chosen from a 30 s per-CPU busy sample + the tenants' thread affinities; runs interleaved A B A B with a tenant snapshot at each start/end) the same binaries went from 33-48K to 67-84K 4K-write ops/s, and:
  - compio 0.18 + gate 67.0/74.1/73.9/84.3K (median 74.0K) vs 0.19 + gate 72.3/68.1/77.5/81.2K (74.9K); batch 8.00 both; phase2 0.72 vs 0.73 ms. NO DIFFERENCE. The "0.19 adds 0.2-0.3 ms per append" claim is withdrawn.
  - gate ablation (MIN_PIPELINED_BATCH=1, which disables the in-flight gate AND the window): 0.18 52.8/56.2/59.6K batch 4.25-4.45; 0.19 56.6/52.0/53.3K batch 3.83-3.90. The gate is worth +30% on BOTH runtimes and stays. Yesterday's "0.18 = 6.2 per batch" was contention piling frames up by accident — contention distorts the batch-size signal too.
  - so the revert (3279694) was undone as instructed: `git reset --hard f004ab3` + `git push -f origin main` (origin had only that one commit past f004ab3). Back on compio 0.19.2 via the thesues fork branch — still required, crates.io compio-driver is at 0.12.5 (2026-08-18) without the CreateSocket fix — and the opt-in replica TCP zerocopy code is back (default off). If the user still wants zerocopy and/or the patch gone, that is a separate commit on top of 0.19.
THE LONE-WRITER REGRESSION, predicted by the revert's reviewer and confirmed: at depth 1 with one connection per partition the fixed-count window (wait while pending < 8) held every op for the full 200 µs: p50 0.30 → 0.43 ms, 15.4K → 12.3K ops/s. Fix: `CoalesceWindow` sizes the wait by what the completion's acks will bring — target = ops held at completion + client FRAMES answered (`WriteResponder::send_ok` now returns whether it answered a frame; a batch-put frame's N ops are one slot and one reply on the last op, a fence record none; `BatchStats.replies`, `handle_completion` returns it). Lone writer: target 1, launches on arrival. Desynchronized pair: 4 held + 4 answered → waits for 8. Unit tests `the_window_waits_for_the_acked_refill_not_a_fixed_count` (red with a fixed target — ablated) and `a_reply_is_counted_per_client_frame_not_per_op`. The independent review (fable) found no high-severity issue; it caught the ops-vs-frames premise (fixed as above), two doc inaccuracies (fixed), the stale "conn cap default 64" in the guide (4), and the missing (B0) line in the loop pseudo-block.
Measured on 0.18 + fix, same layout: depth-8 87.4/78.6/76.5K (median 78.6K) batch 8.00 vs the fixed-count 0.19 partner 81.2/77.8/76.7K; depth-1 p50 0.29/0.29 ms, 13.2/16.6K vs the ungated floor 0.29/0.31 ms, 14.3/14.4K. Both acceptance bars met. ON 0.19 + FIX (what ships), same layout, interleaved: depth-8 76.8K / 73.1K, batch 7.99, phase2 0.66-0.71 ms vs the fixed-count 0.19 partner 71.3K / 59.0K; depth-1 p50 0.31 / 0.29 ms vs the ungated 0.19 floor 0.30 / 0.29 ms, 12.7K / 12.0K vs 12.5K / 15.3K (the floor's 15.3K run is the one where the tenant's bench_serving ended mid-run). The lone-writer p50 is at the floor in all four comparisons; depth-8 is unchanged.
Verification: `cargo test -p autumn-partition-server --lib` 258 passed on the 0.19 tree; ablations as above; measurements as above.
Main gaps: (1) the measurements' read ops/s are ≈0.5M in the node-1 layout vs ≈1.3M yesterday — the unpinned bench client crossing NUMA to a node-1 PS; equal on both sides of every A/B, so not a version effect, but read baselines are layout-bound. (2) presplit first-write `alloc_new_extent` refusals (1-3 per run) still there. (3) compare_put's inline drain launches without `window.clear()` — bounded by the 200 µs deadline, rare path, left as is. (4) the HTTPS-embedder regression test from the dropped revert commit (`an_https_embedder_fails_without_panicking`) is gone with it; `compio/ring` is present on 0.19 so nothing is broken, but the guard is worth re-adding.
Next steps: none for this task. Scratch worktrees /tmp/wt-019, /tmp/wt-018n, /tmp/wt-019n, /tmp/wt-reset (each with a release build) can be removed with `git worktree remove --force`. Re-add the HTTPS-embedder guard test when touching autumn-memory next.

Date: 2026-09-24
TaskStatus: completed
Task scope: "4K write 太差了" — perf-check reported 4K TCP --3disk writes at 61% of the June baseline. Find the cause, fix it (user chose option A2, review skipped on the user's instruction).
Current summary: root cause is the compio 0.19 runtime upgrade (`c48bb5f`) changing the partition loop's wake-up order so that a partition's two client connections deliver their 4 admitted ops in separate wake-ups; the unconditional launch then split every 8-op burst into two 4-op appends, and the EN serializes appends to one extent. Fixed in `partition_loop` with a launch gate (`MIN_PIPELINED_BATCH` = 8: no second batch below 8 ops while one is in flight) plus a 200 µs coalescing window after each completion (`LAUNCH_COALESCE_WINDOW`) so the two connections re-synchronize. Measured with the same instrumented probe: avg batch 3.97 → 8.00/7.99, p50 1.15-1.58 → 0.96-0.99 ms, write ops/s 41.5-42K → 48.3K/44.5K. Ledger: BUG-GROUP-COMMIT-SPLIT-AFTER-RUNTIME-UPGRADE.
HOW IT WAS FOUND, because the method matters more than the number here. Two blind throughput bisects over the 976-commit range both "converged" on a docs-only-adjacent commit whose diff could not plausibly matter; re-measuring the "good" endpoint with the same 3-sample protocol showed it sitting in the same 32K-49K band as the "bad" one — single-sample throughput on this host is not a bisectable signal. What was: a same-day, same-box A/B of the baseline commit vs HEAD (53.3K vs 36.8K medians, non-overlapping samples), then per-op decomposition (CPU/op, block writes/op, EN pwrite+sync per append, PS group-commit batch size from the `partition write summary` log line). Batch size (6.2-6.7 vs 3.9-4.0) was stable to a few percent while ops/s swung 30-50%, so the last step was a two-commit A/B on that signal at the prime suspect, which flipped cleanly (6.24 → 4.13, EN time unchanged). Ruled out on the way, each by measurement not argument: CPU tenants (cpuset-isolated run identical), disks (`fsync_isolated` healthy on all three), authz (off), partition count/shards (8/8 in every log), the bench client (both versions send per-op `put` through the same sliding window), `--ps-inflight-cap` (2 made it worse), and MAX_WRITE_BATCH_BYTES (parent and child equal).
WHAT THE FIRST FIX ATTEMPT TAUGHT: the gate alone left the batch at exactly 4.00 with ~0.7 in flight — the connections had gone into strict alternation and nothing re-synchronized them; the completion-time coalescing window is the part that actually restores lock-step. Recorded in the crate guide so the gate is not "simplified" back out.
Verification: `cargo check` clean; `cargo test -p autumn-partition-server --lib` 256 passed; the new test `a_batch_in_flight_holds_a_partial_burst_until_it_is_whole` goes red without the gate (ablated, restored); two instrumented probes of the release build as above. `docs/ops.md` gained the executable batch-size check (`avg_batch_size` over the write phase; expect ≈8, ≈4 = fragmentation).
Main gaps: (1) per-append PS↔EN round trip is still ~0.2-0.3 ms slower than before the runtime upgrade at equal batch size (fan-out 1.01 → 1.21 ms with EN disk time unchanged) — that is the rest of the gap and it is NOT located; (2) after presplit every child partition's log tail is a sealed empty CoW extent, and the first write's 8-way concurrent `alloc_new_extent` gets 3 refusals ("no healthy node available") every run, costing a 100 ms client backoff — small, real, unexplained; (3) `--conn-inflight-cap 4` was justified by read measurements only; 16 gave +23-37% on writes here and its comment should say so before anyone changes it. Investigation scaffolding (`ab_probe.sh`, `ab_analyze.py`) lives in the session scratchpad, not the tree.
Next steps: locate gap (1) with a per-hop timing breakdown of one append (PS worker → EN handler → reply) on the two commits; fix (2) at the presplit (give each child a fresh tail extent) or make the manager tolerate the burst.

Date: 2026-09-24 (3)
TaskStatus: not_completed
Task scope: implement F-LANCEDB-S3-GATEWAY per docs/lancedb_s3_gateway_plan.md (section 9 = the concrete design). F-LANCEDB-OBJECT-STORE is not wanted (already retired; leftover dirs removed).
Committed so far: plan + pinned LanceDB harness; macOS CPU-pin fix; ordered resumable ListObjectsV2 + bounded readdir + HeadBucket; MSG_COMPARE_WRITE (wire 47) with durable fence floors; lease modes STABLE/REPLACE/EXCLUSIVE (wire 48, a forced WRITE never deposes REPLACE/EXCLUSIVE, lone-writer epoch fix); fs schema v4 segmented files + migratev3_v4 + deferred reclaim + transient lease for path truncate; publish.rs (sessions, conditional publish, Retire records with successor, dead-session recovery, fence_all with region refresh). Each went through independent review rounds; every high finding has an ablation-verified regression test.
Multipart (F) is drafted OUTSIDE the tree at <scratchpad>/fstage/{multipart.rs,system_multipart.rs,schema.rs,key.rs}: the staged schema.rs/key.rs carry the multipart records (UploadState/UploadRecord/PartRecord, PendingOp::Part/Complete, mpu/mpp/mpa keys) that were held back so C/D/E commit and build on their own. Still to change in the draft: PendingOp::Part gets `part`, UploadState gets Completed{new_ino,frozen}, UploadRecord gets `key`, upload_alloc_key(id, session, data_ino) + parse + upload_prefix/parse_upload_key; recover_session arms for Part/Complete.
Main gaps: multipart (F) in tree + tests (zero body I/O via deleting bodies before Complete + data-key count; Complete/Abort race; late part; dead session); gateway write APIs and conditional GET/HEAD with STABLE leases, gateway sweeps (recover_dead_sessions, sweep_uploads, sweep_garbage have no production caller yet); LanceDB e2e on the local cluster; fault and perf tests; docs/README; feature pass flag.
Known limits (documented in crates/fuse/CLAUDE.md, crates/manager/CLAUDE.md): failover loses EXCLUSIVE/STABLE; FUSE unlink/rename are unconditional; a Retire crash after the nlink decrement double-decrements; close-time live reclaim scans all segc records (accepted cost).
Unexplained once: in the final full regression run, system_publish::publish_conditions failed at its 20 MiB put after 348 s (RPC timeouts), while a stopped earlier run may still have had test processes alive; it passed in 4 later runs (alone and with its file). Not diagnosed; watch for it.
Env notes: local cluster at AUTUMN_DATA_ROOT=<scratchpad>/cluster (v4). cluster.sh stop kills every local autumn process by name. crates/stream bench fsync_isolated does not build on macOS (Linux-only libc calls, pre-existing). Manager alloc_fs_inodes CAS exhausts under ~32 concurrent allocator processes (pre-existing).
Re-investigated 2026-09-25: the publish_conditions failure did not reproduce in 7 clean runs (alone, both tests of the file together, 4 concurrent copies). Per-partition listener ports cannot collide in that harness (bind fallback to +1024 band then :0, re-registered), and no in-tree path stalls an 8 MiB append for 348 s with 2 ENs (bad_nodes is a soft hint, the failure-report quorum is advisory, Suspected clears on the next df). Most likely environmental (leftover processes from the stopped run). No code change. The manager test harness installs no tracing subscriber, so a recurrence leaves no PS/EN/stream logs; an RUST_LOG-gated subscriber in tests/support is the instrumentation to add if it comes back.
Decision (user, 2026-09-25): the LanceDB e2e on the local cluster is NOT to be done. Multipart integration from fstage is deferred to a later session by the user. NOTE the fstage draft lives under /private/tmp (the 6a58b750 session scratchpad) and does not survive a reboot.
Next steps: (user-scheduled) integrate multipart from fstage, test, review, commit; then the gateway write APIs (G).

Date: 2026-09-24
TaskStatus: not_completed
Task scope: record the user-approved LanceDB S3 gateway implementation plan; the latest user instruction is to write the plan only.
Current summary: Added docs/lancedb_s3_gateway_plan.md and linked the user-added requirements from feature_list.md. CompleteMultipartUpload must only publish segment metadata: zero part-body reads, copies, or rewrites, with no deferred full-file materialization. The plan also records shared FS segmented files, mixed S3/FUSE/Python access, immediate conflicts, stable GET reads, durable multipart Abort, upgrades, implementation order, and acceptance tests.
Verification: documentation-only change; reviewed the plan and ledger linkage and checked whitespace. No implementation, runtime tests, dependency installation, or deployment performed.
Main gaps: gateway write APIs, shared segmented-file layout and fencing, multipart lifecycle, and all runtime acceptance remain unimplemented. Feature passes remains false.
Next steps: implement docs/lancedb_s3_gateway_plan.md, starting with the pinned official LanceDB request trace and the shared FS primitives; retain the hard zero-body-I/O Complete acceptance.

Date: 2026-09-23 (2)
TaskStatus: not_completed
Task scope: define the LanceDB S3 gateway write feature in the backlog only; implementation was explicitly deferred by the user.
Current summary: Added F-LANCEDB-S3-GATEWAY to feature_list.md with the required S3 operations, atomic conditional publication, multipart visibility, scope boundary, and LanceDB end-to-end acceptance.
Verification: reviewed the current read-only gateway routes and the feature ledger diff; no implementation or runtime test was performed.
Main gaps: all gateway write APIs and LanceDB end-to-end verification remain to be implemented in a later task.
Next steps: trace requests from a pinned LanceDB/Lance version, then implement and verify against this feature's acceptance criteria.

Date: 2026-09-20 (8)
TaskStatus: completed
Task scope: the three follow-ons the operator asked for after the rollout — bump `cluster_version`, delete the converter, and roll the dependent services.
Current summary: `cluster_version` 42 -> 45 and the converter is gone from the tree. Dependent-service rollout is tracked separately below.
CLUSTER_VERSION 42 -> 45, in three calls because `bump_cluster_version` enforces exactly current+1 and caps at this binary's `WIRE_VERSION`. Checked first that it was safe to do with wire-43 clients still connected: NOTHING in the tree gates behaviour on `cluster_version` today — it is the forward-looking R1 feature gate ("formats versioned N may only be EMITTED once cluster_version >= N") and no emission path reads it yet. So the real cost is the one the CLI prints: rollback to pre-45 binaries is now unsupported, which the record conversion had already made true.
THE CONVERTER IS DELETED, with the `autumn-etcd` dep it had added to crates/server. That is the contract — `crates/manager/CLAUDE.md` "Upgrade safety" says it is run once by hand and then DELETED, so a one-time migration cannot decay into the permanent compatibility code the rule exists to prevent. Recovery pointer, verified resolvable and byte-identical to the deleted blob: `git show fb47730e:crates/server/src/bin/migratev0_v1.rs`.
WHAT THE INDEPENDENT REVIEW CAUGHT, and it was the bookkeeping rather than the code. The deletion itself came back clean: nothing in code, tests, scripts, CI, the Dockerfile's explicit binary list, `python/` (excluded from the workspace) or the lockfile depended on the bin or the dep beyond the removed lines, and the Cargo.lock diff is exactly one line. Three things did need fixing:
  - entry (7) above still said the converter was STILL in the tree. It was the top block, the one the next session reads. Corrected in place.
  - `persist/mod.rs`'s leadership-refusal message tells the operator to "run `migratev0_v1`" — the one live instruction in the tree now pointing at nothing. It now also says the bin was deleted and gives the `git show` recovery line. The existing test asserts `contains("migratev0_v1")`, so appending keeps it green rather than needing a new assertion.
  - the "What the conversion looked like" section in `docs/ops.md` leaned on "the steps above", "the dry-run" and "the summary line", all of which the rewritten runbook section no longer defined. The rewrite now restates the flags and the summary shape for whoever restores a pre-conversion snapshot.
It also corrected a citation I had made: the delete-the-converter mandate lives in `crates/manager/CLAUDE.md` and in ops.md, NOT in the root CLAUDE.md, which never mentions it.
Verification: `cargo check -p autumn-server --all-targets` clean; `cargo check -p autumn-manager --lib` clean. The manager's persist TESTS were NOT run and I am not claiming they were — `cargo test -p autumn-manager` cannot build on this Mac at all, because a dev-dependency pulls `autumn-fuse` with the `fuse` feature and fuser needs libfuse/pkg-config (pre-existing, unrelated to this change). What the message edit can break is exactly two substring assertions, `contains("no AUMG envelope")` and `contains("migratev0_v1")`, and both survive because the edit APPENDS; that is read from the test, not executed.
DEPENDENT ROLLOUT, and it is not the flat image swap it looked like. `memory-mcp` and `code-index-mcp` are plain autumn-rs images and are DONE — both now run e7a554fa, the same tag as the core. The other two are a CHAIN: autumn-rs --(AUTUMN_IMAGE build arg)--> freetoken-l3 --(BASE_IMAGE)--> comfyui-autumn. comfyui is built FROM the freetoken image, so freetoken has to be rebuilt correctly first.
WHAT THE PROBE FOUND, by connecting each embedded client to the live cluster rather than reading logs (the logs are silent either way, and freetoken's last L3 summary was hours before the rollout, so silence proved nothing):
  - freetoken's client CONNECTS fine. Its running image was built from a newer autumn than the pipeline's pin implies.
  - comfyui's client is REFUSED: `local=[40,40], peer=[43,45] — no common wire version`. It is wire 40. This is PRE-EXISTING, not caused by the rollout: against the old cluster it was [40,40] vs [43,43], also no overlap. comfyui's autumn integration has been dead for a while.
THE TRAP, caught before pulling the trigger: the `freetoken-l3` CP pipeline still pins `AUTUMN_IMAGE=autumn-rs:633a4832`, which is **wire 40** — below the cluster's client floor of 43. Running that build as-is would have REPLACED a working freetoken client with a broken one. The pin has to move to e7a554fa first, and `volc-cp` is read-only for pipelines (ls/get only), so that edit is the operator's in the CP console.
PREPARED AND WAITING in the session scratchpad (`freetoken/`): both StatefulSets patched to `--direct-read true` with the fusebin initContainer moved aef13927 -> e7a554fa, plus `apply.sh <new-tag>` that stamps the rebuilt image and applies both in ONE restart (the operator's call: freetoken is a live GPU service and each apply reloads a model). The EC comment that justified `--direct-read false` was rewritten rather than deleted — it was right when written, the EC/direct-read path has since been fixed, and the next reader needs to know which of those is true now.
THE CHAIN IS DONE. Operator moved the pin; then freetoken-l3 rebuilt against e7a554fa, ComfyUI's `BASE_IMAGE` bumped to that image (thesues/ComfyUI autumn-dev `685dd0ef`), comfyui-autumn rebuilt on top, both redeployed. Every autumn-coupled workload now runs a client the cluster accepts.
EACH REBUILT IMAGE WAS PROBED IN A THROWAWAY POD BEFORE IT WENT NEAR A LIVE SERVICE, and the probe checked TWO things, because one of them alone is a broken instrument: the client connects, AND the pulled digest differs from the one running. freetoken's tag did NOT change (FreeToken's branch HEAD was already 87141c68, so the build overwrote the tag in place), so "it connects" would have proved nothing if the node had served a cached old layer. digests: freetoken aa2e8a5d -> 598f97b6, comfyui 465cef55 -> 77b03553. Both old digests are written down as the rollback anchor, since neither tag can name the old image any more.
DIRECT READS ON EC DATA REALLY ARE FIXED — I was wrong to doubt it, and the evidence is not a diff. `autumn-fuse` at e7a554fa says so in its own startup line ("EC-converted extents are read the same way, by fetching the data shards the range covers straight from their nodes; a shard whose node will not answer needs reconstruction, which only the PS can do, so those reads fall back"), and on the live mount over EC-converted weights the 32 MiB read that PREVIOUSLY NEVER RETURNED now takes 0.174 s at 192 MB/s — faster than the 0.48 s the old comment recorded for the proxy fallback. mm2 then streamed ~12 GB through the mount during model load. The manifests' EC comment was rewritten, not deleted: it was correct when written, and what changed is the premise.
comfyui's autumn integration had been DEAD, not degraded, and not because of this rollout: wire 40 against a 43-floor cluster, so it was already refused before the 43 -> 45 upgrade. It is alive for the first time in a while.
Verification: cluster_version 45 / wire 45 / window 43..=45; all 9 autumn-rs workloads on e7a554fa; freetoken l3+mm2 on the rebuilt image with `--direct-read true` and fusebin at e7a554fa, both Ready, both clients OK; both comfyui Deployments 1/1 on 685dd0ef, both clients OK.
Main gaps: `scripts/client_window_verify.sh` still not re-run at 45. The dormant `freetoken` StatefulSet (replicas=0) still points at an old `freetoken-autumn` image — harmless, nothing runs it, but it will need the same treatment if anyone scales it up.
Next steps: none outstanding for this rollout.

Date: 2026-09-20 (7)
TaskStatus: completed
Task scope: the operator's stop-the-world deploy of the VKE cluster onto e7a554fa — stop the world, run `migratev0_v1`, bring the core back. Downstream services deliberately untouched this round; they get their own rollout only once autumn-rs is proven healthy.
Current summary: DONE. The cluster (namespace `autumn`, VKE) went aef13927 -> e7a554fa, wire 43 -> 45, and all 455 manager records are now enveloped. Every autumn-rs workload runs the new tag; `code-index-mcp` and `memory-mcp` stay on ead1bb2b on purpose.
THE THING THIS ROLLOUT WAS ACTUALLY TESTING, and it held: a wire bump that does NOT force the fleet to rebuild. `MIN_CLIENT_WIRE_VERSION` stayed 43 while `WIRE_VERSION` went to 45, so the client window is open two wide. Proven live rather than argued: a throwaway pod on the OLD image (its own `WIRE_VERSION_MAX = 43`) read a canary off the wire-45 cluster and returned the same sha256 as the pre-rollout read, `31047d7c…`. freetoken, comfyui, memory-mcp and code-index-mcp were never restarted and logged no version error.
THE TRAP THE SAME OPEN WINDOW SETS, checked before stopping anything: the DEPLOYED 43 binaries predate `f17f5333`, so their PS/EN startup check is the old interval OVERLAP, not equality. `[43,43] ∩ [43,45] = {43}` — a PS or EN that survived the window would have JOINED the wire-45 manager silently and then decoded garbage. So "stopped" was verified as zero pods matching the three component labels, not as `replicas=0`.
PRE-FLIGHT THAT PAID FOR ITSELF. All nine persisted structs were diffed 43 -> 45 field by field and are IDENTICAL, which is what makes the tool's pure prefix insertion correct on this data. `OpRecord` was diffed too and is also identical — so `opLog/` was NOT wiped, against the runbook's default advice, and `ops history` afterwards decoded all 21 records (17 old + 4 new) with zero skip warnings. The bare-persisted types were checked the same way (`MgrExtentInflightRecord`, `MgrAutoPolicyEntry` identical; `extentLayout/` and `extentCorrupt/` are raw bytes, not rkyv).
THE MIGRATION: dry-run 455 converted / 0 already, matching a prefix-by-prefix etcd key count taken beforehand — the cheap check that proves the tool is looking at the cluster you think it is. Convert, then re-run: 0 converted / 455 already. Spot-checked the raw bytes: `regions/144` now starts `41554d47 09 01` = `AUMG` + type 9 + format 1. The manager then took leadership on the first try, which is the real acceptance — a bare value would have refused it.
BACKUPS, both taken before a byte moved: a full `etcdctl snapshot save` to the etcd PVC (111 MB, `/var/run/etcd/pre-migrate-20260920.db`) and a targeted local dump of exactly the nine prefixes, 455 keys verified by count.
PARTITION SPREAD COLLAPSES ON A FULL RESTART, and it is structural, not bad luck: `handle_register_ps` calls `rebalance_regions` the moment the FIRST PS registers, and with one PS in `ps_nodes` every region whose owner is not yet back is reassigned to it. 8/9/7 became 24/0/0. `autumn-op rebalance` fixed it — but it moves at most 4 per op, so it took four passes to reach 8/8/8.
A DISPLAY LAG WORTH KNOWING ABOUT, because it reads exactly like a failed rebalance: after the moves, `info` still showed 10/6/8 while `--json` showed 8/8/8. A move DELETES the stale per-partition listener addr and the new PS re-registers it later, so the text view lags. The same lag makes sizes look wrong right after the PS come up — partitions 341 and 152 read 48.1 GB and 16.5 GB mid-open and were back to 62.3 and 31.2 minutes later. I nearly reported those as data loss.
kvc LOST ITS CACHED ENTRIES and the fs/mem data did not. All 17 fs partitions match their pre-rollout size and extent count exactly; the mem canary is byte-identical; mem partitions grew, which is live traffic. The `dsv4-flash-ftw/` kvc entries are gone and partition 168 shrank ~2 GB with them. kvc is the one namespace the connector writes with `ttl_secs` (marker at ttl, layer pages at ttl + 300 s grace), it is regenerable by design, and the migration never touched partition data — only the nine manager metadata prefixes. I did not prove TTL expiry was the cause, and it should be stated that way rather than dressed up.
A `ls` BEHAVIOUR that made the above look worse than it was: a bare `ls` on a namespace scans from the namespace head and does NOT continue into later partitions, so with the head empty it returned nothing even for a key that existed and read back fine by point get. `ls --prefix` found it immediately. Worth remembering before concluding a namespace is empty.
Verification: cluster wire 45, window 43..=45, 3 PS, 24 partitions, 5 nodes, all pods Running. Fresh 64 KiB write into kvc read back byte-identical, then deleted.
Main gaps: `scripts/client_window_verify.sh` still has not been re-run at 45. (The two gaps this line used to carry — the converter still being in the tree, and `cluster_version` still at 42 — were both closed the same day; see entry (8).)
Next steps: downstream rollout (freetoken / comfyui / vllm / hermes / the two mcp pods) if and when it is wanted — none of them NEEDS it, which was the point. Then delete the converter.
Date: 2026-09-20 (6)
TaskStatus: completed
Task scope: F-SCHEMA-HOMES — run the FIRST acceptance row on a live local cluster, the one thing standing between the feature and `passes`. Then the row-6 ablation, then close the entry out.
Current summary: DONE, on a real cluster, and the feature is now deleted from `feature_list.md` per that file's own rule (a `passes: true` entry is removed; git history and this file are the record).
THE DEMONSTRATION, exactly as the row words it. Local cluster: real etcd, manager, 3 extent nodes (a 4th added later, see below), 1 partition server, RF=3, 12 x 4 KiB values written. Then a persist-only field added to `AuditRecord` with its OWN `FORMAT_VERSION` 1 -> 2 and NOTHING else: `git diff crates/rpc/` came back EMPTY and `WIRE_VERSION` stayed 45. Only the MANAGER binary was rebuilt and swapped. EN and PS kept their original PIDs and original 05:58 start times across the swap — verified by `ps -o lstart`, not assumed. After it: reads 12/12 byte-correct, a fresh write read back, `autumn-op split` succeeded, and recovery rebuilt three extents' slots onto a new node. Reads 12/12 again afterwards.
THE VERSION MECHANISM, seen working rather than argued: the two audit rows manager A had written at format 1 were SKIPPED by manager B with a WARN naming the key, both version numbers and the bin to run; manager B's own rows at format 2 read fine. That is the documented audit leniency (audit is diagnostic and never replayed) and it is what a `FORMAT_VERSION` bump looks like from the operator's side.
The guard fired on me on the way through, which is the point of having it: adding the field broke the build at `persist/freeze.rs` and `persist/mod.rs` before any byte comparison could run, because both fixtures are struct literals. Following the file header's own three steps (bump the version, add a converter step, re-record IN THE SAME COMMIT) produced an encoding whose sixth byte is now `02` — the envelope carrying its own version.
ONE REAL IMPROVEMENT KEPT from the demo: the impersonation fixtures in `persist/mod.rs` spelled the envelope out as a literal (`"AUMG\u{2}\u{1}longname"`), so a `FORMAT_VERSION` bump silently stopped them impersonating anything while they went on passing. They are built from `RECORD_TYPE` and `FORMAT_VERSION` now (`impersonating_name`). The demo field itself is reverted.
ACCEPTANCE ROW 6, the ablation I had not run: moving a split record back into the wire file. Simulated by adding the same field to the WIRE `MgrAuditEntry` — `crates/rpc/` immediately shows 1 file changed, so `WIRE_VERSION` must bump and every PS and EN must be redeployed: row 1 turns red, which is what the ablation asks. Worth recording what ELSE it showed: `client_surface_freeze` stayed GREEN, because audit rides `MSG_QUERY_AUDIT_LOG`, an OPERATOR-surface message deliberately outside that freeze. So nothing automated would have caught the forgotten bump — the fingerprint that used to is gone. The SEPARATION is what buys row 1; no test substitutes for it.
A CAPACITY BEHAVIOUR confirmed on the way, pre-existing and NOT from this work: with RF=3 on exactly 3 ENs, fencing one leaves 2 allocatable, so `stream_alloc_extent` refuses "not enough nodes: need 3, got 2", tails cannot roll, nothing seals, and recovery — which only rebuilds SEALED extents — has nothing to dispatch. Adding a 4th EN made recovery run immediately. See project_en_down_write_stall_is_capacity.
All six acceptance rows are now met: (1) live above; (2) exhaustive-destructure compile failure, ablated twice; (3) the converter plus the reverse-direction test; (4) one shared envelope check rather than nine per-struct tests, which the row's own "或记录它靠什么别的机制兜底" allows, and which today's run exercised concretely; (5) `ClientRegion` is four-field and type-enforced, with the fallback decode documented; (6) above.
Verification: workspace check clean, warnings at baseline (17); `cargo test -p autumn-manager --lib persist::` 14, rpc lib 87. Tree carries only the `impersonating_name` improvement plus the ledger edits.
Main gaps: `migratev0_v1` is still in the tree and should be deleted once the single production cluster is converted, along with the `autumn-etcd` dep it added to crates/server. `scripts/client_window_verify.sh` derives its constants from the tree and needed no edit, but has not been re-run at wire 45. Two OTHER entries in `feature_list.md` are `passes: true` and by that file's own rule should also be deleted — they predate this work and I did not touch them; that is the maintainer's call.
Next steps: nothing in F-SCHEMA-HOMES is open. The natural follow-ons are the stop-the-world deploy (the operator's, and the wire gap is now 35 -> 45) and deleting the converter after it.

Date: 2026-09-20 (5)
TaskStatus: completed
Task scope: F-SCHEMA-HOMES Scope 3 — the last open scope. An embedded client's routing record drops the three `*_stream` ids it never read. Served as a SECOND FORM under a new opcode, so no client has to be rebuilt.
Current summary: `MSG_GET_CLIENT_REGIONS` (0x60) + `ClientRegion` (4 fields) + `ClientRegionsResp`. `MSG_GET_REGIONS` (0x2E) and its seven-field `MgrRegionInfo` are UNTOUCHED and still serve the partition server — `sync_regions_once` opens a partition from those stream ids — plus `autumn-op`, plus any client built below 45. `WIRE_VERSION` 44 -> 45, floor held at 43, window `[43, 45]`.
WHY A NEW OPCODE AND NOT A SECOND SHAPE BEHIND THE OLD ONE: design §7. msg_type is settled in the frame header before any decode and a reply carries the msg_type its request named, so both directions are self-describing. Reusing one opcode and branching on the connection's negotiated version is what the rule forbids — a frame's meaning would depend on connection state, and an rkyv mis-decode is silent, so a missed hello reads bytes at the wrong version and says nothing. Choosing WHICH OPCODE TO SEND from that number is a different act; the reviewer enumerated the failure modes and confirmed no path yields a silent mis-decode (stale-low or 0 -> old opcode, correct; stale-high -> the manager refuses the unknown msg_type loudly; the validated `from_bytes` is a backstop that never has to fire).
It fails CLOSED: `negotiated_cluster_wire` is 0 until a hello succeeds and a silent connection never raises it, so an unknown cluster gets the old opcode, which every cluster in the window serves.
LIVE, BOTH FORMS AT ONCE — the evidence this needed and the only kind that counts here: real etcd + manager + EN + PS at wire 45. A client from this tree wrote and read 6/6. Then a client binary built from 45b70b9, a REAL wire-44 build in a git worktree, read all six back and wrote one of its own, which the wire-45 client then read. Old opcode and new one interoperating on one cluster, not argued from a diff.
THE ACCEPTANCE, STATED HONESTLY because the reviewer was right to push on it: the routing record the client HOLDS is `Vec<(u64, ClientRegion)>` and `ClientRegion` has no stream field, so client code cannot name one — type-enforced, not a runtime assertion, which is what the row asks. The caveat I had glossed: against a cluster at 44 the client still DECODES a full seven-field `GetRegionsResp` in-process before narrowing it. So the right claim is "the record is four-field by type; the seven-field form is decoded in exactly one place, the fallback" — not "unreachable from the client". That one place disappears the day the floor passes 45.
WHAT THE REVIEW FOUND. No HIGH. The one that mattered: NOTHING PINNED that 0x60 is in `is_client_surface_mgr_msg`. Deleting it left every test green — the freeze records its bytes independently of the set — while a below-floor client would quietly keep its routing, and my own doc comment claimed the residue was narrowed. Two tests now pin it (`the_sdk_data_plane_is_covered` lists it; `the_narrow_routing_opcode_is_wire_gated_while_the_shared_one_is_not` drives a real refused connection and asserts FailedPrecondition on 0x60), both ablated red.
Also fixed from the review: `docs/ops.md`'s cluster-version block was an EXECUTABLE step that now failed (44s everywhere, and the refusal text) — re-recorded from a live manager, not recalculated: cluster_version 45, window 43..=45, "46 exceeds WIRE_VERSION=45". Stale guides corrected in `crates/rpc/CLAUDE.md` (two version claims), `crates/client/CLAUDE.md` ("nothing branches on it yet" — this IS the first branch), `client_hello.rs`'s `server_hello_resp` doc (a swapped pair is no longer merely latent: it would push every client silently onto the old opcode), and design §7's "nothing branches on it until §7". Three comments said an old cluster answers `NotFound`; the manager answers `InvalidArgument` — NotFound is the PS's shape. A redundant `want()` in the freeze went away with its wrong comment.
THE BUMP CONTRADICTED OUR OWN RULE, and the exception is now written where the rule lives. `crates/rpc/CLAUDE.md` says a pure msg_type ADDITION is not a bump. This added an opcode and two structs, touched no existing struct, and bumped anyway — because the version integer is the SDK's only signal that the handler EXISTS. Without it the client must ask blind (every refresh fails against an older cluster) or never use the new form. The cost is stated in the same paragraph rather than hidden: this bump stops every PS and EN for a change neither can see, one of the 68% of intervals where the extent node need not have moved. It buys the thing that matters more — no embedded image rebuilt.
Verification: workspace check clean, warnings at baseline (17); clippy over rpc + client + manager byte-identical to a `git stash -u` baseline. Lib: rpc 87, client 61, manager 399, PS 254. Integration: client_wire_admission 6 (+1), e2e_lifecycle 11, graceful_shutdown 4, system_bulk_verification 4, client_surface_freeze 8, negotiation_freeze 3. Ablations red-then-restored: `narrow = true` (dropping the version gate) reds the new pair test with 0x60 where 0x2E was required; removing 0x60 from the client-surface set reds both new gating tests.
Main gaps: the seven-field decode in the fallback arm lives until the floor passes 45, which is a deliberate consequence of serving two forms and not a defect. `scripts/client_window_verify.sh` derives both constants from the tree and needed no edit, but has NOT been re-run at 45. F-SCHEMA-HOMES now has every scope done — 1, 2, 4, 5 and Scope A/B in the earlier entries, 3 here — so the only thing between it and `passes: true` is whether the first Acceptance row (swap only the manager binary on a live cluster, PS/EN untouched) has been demonstrated; it has NOT, and that row is why the flag stays false.
Next steps: either demonstrate that first Acceptance row on a live cluster and flip `passes`, or delete `migratev0_v1` once the single cluster is converted. Nothing else in the feature is open.

Date: 2026-09-20 (4)
TaskStatus: completed
Task scope: F-SCHEMA-HOMES Scope B — the remaining SIX persisted records (extents / streams / nodes / disks / partitions / regions, plus a nested RangeRecord), and `MetadataState` moved into the manager so it can hold them. With Scope A this makes all nine split; the entry still stays `passes: false` (Scope 3's client-facing MgrRegionInfo narrowing is untouched).
Current summary: Two commits. B1 was a PURE MOVE of `MetadataState`/`MetadataStore` from `autumn-common` to `crates/manager/src/store.rs` — test counts prove it (common 16->12, manager 394->398, the same four tests on the other side). It has to happen first: a persisted record is `pub(crate)` to the manager, and a state struct in a SHARED crate cannot hold a type only the manager may name. B2 then split the six and switched the maps to hold records.
THE SHARPEST HAZARD, and it is why the helpers exist: `Cmp::value` compares stored bytes BYTE FOR BYTE against a baseline the manager RE-ENCODES from an in-memory snapshot. A baseline still produced by bare `rkyv_encode` while the value carries an envelope can NEVER match — every CAS fails, and split / merge / GC / recovery retry forever against a conflict that is not there. `persist_kv_entry` and `replay_decode_persist_id_map` are the two helpers every record now goes through, and the hazard is written at the function rather than left to be rediscovered. The bare-rkyv `kv_entry` and `replay_decode_id_map` are DELETED: with all nine enveloped, a writer that skipped the envelope could only be a mistake, so the tool for making it is gone.
MEASURED COST, which is why the disks pilot was worth doing alone: about twelve real call sites per type plus fixtures, all enumerated by the COMPILER. Most of the 530 reads of the six maps compiled untouched, because a record carries the same field names — only whole-struct crossings to the wire break. That is the guard working, not luck.
LIVE CLUSTER, because no automated test executes `Cmp::value` on the split/merge path (memory-mode tests never reach etcd): real etcd + manager + EN + PS on release binaries. format, bootstrap, 12 x 2 KiB puts, `autumn-op split` SUCCEEDED with 12/12 byte-correct after, `autumn-op merge` SUCCEEDED with 12/12 after, manager restarted and replayed all nine record kinds clean, 12/12 after that, namespace-list intact. That is the CAS evidence; the unit suite cannot supply it.
`sealed_empty_sweep_etcd` failed FIRST with `unaligned pointer` — the TEST decoding a persisted stream value bare. Production was fine, and the failure re-confirmed the property already pinned in Scope A: an envelope in front of an rkyv archive is a LOUD failure, not a silent misread, because rkyv pads to its root alignment and a six-byte displacement always lands misaligned.
WHAT THE REVIEW FOUND, and it was the round's value. No HIGH; the hazard was verified closed by enumerating every `Cmp::value` / `Op::put` / `put_*_txn` / `format!("<prefix>/…")` / `rkyv_encode` site. The one real finding was a file the diff never touched: `crates/manager/tests/system_chaos.rs` still decoded `extents/` and `streams/` bare at FIVE sites. Two would have failed loudly every round; the one that mattered, `layout_still_serves`, swallowed the decode error with `continue` and would have DOWNGRADED a real "the file vanished under a layout that still serves it" accusation into the benign branch. A harness that quietly stops accusing is worse than one that fails. All five now go through `support::decode_persisted_{extent,stream}`, which PANICS on a bad envelope rather than returning an Option, with the fallible twins reserved for the accounting sweep that collects findings.
Also from the review and fixed: the exhaustive destructure blocks a DROPPED field but not a SWAP between two same-typed fields — and these records are full of them (partition and region each carry three adjacent u64 stream ids, an extent two bools and four Vec<u64>). The freeze test cannot see it either, because a swapped conversion never reaches the encoder. `the_second_round_conversions_carry_every_field_both_ways` closes it, ablated: swapping `log_stream`/`row_stream` in one conversion reds it (left 22, right 11). All nine RECORD_TYPE numbers are now pinned by name, and five stale doc claims were corrected.
One self-inflicted break worth recording: the new `crates/object-store` (added upstream DURING this session) `#[path]`-includes the manager's `tests/support/mod.rs` and does not depend on rkyv, so my first helper — generic, with `rkyv::` in its bounds — broke that crate while `cargo test -p autumn-manager` stayed green. The helpers are concrete now (two record types are ever read this way from a test) and the rkyv coupling is gone. The lesson is the verification command: `--workspace --all-targets`, not the crate under edit.
Verification: workspace check clean, warnings identical to baseline (17); clippy over manager + server byte-identical to a `git stash -u` baseline. Lib: manager 399, every other crate unchanged. Integration: e2e_lifecycle 11, system_merge 3, integration 13, system_extent_recovery 8, ec_integration 4, apply_done_atomicity 3, physical_deletion 4, node_lifecycle 9. Etcd-backed (the only ones that execute `Cmp::value`): etcd_stream_integration 4, sealed_empty_sweep_etcd 1, namespace_registry_etcd 1, leader_fence 1.
Main gaps: Scope 3 — narrowing what a CLIENT receives for a region to four fields, dropping the three `*_stream` ids the SDK never reads — is NOT done and is a different kind of change: it alters what a client decodes, so it needs the two-form rule from F-CLIENT-WIRE-COMPAT, not this machinery. `passes` stays false for that reason. The nine records' `FORMAT_VERSION` are all 1 and nothing has exercised a BUMP end to end; the first real one will be the test of the converter's second step, which unlike the first cannot be a pure prefix insertion and must vendor the old definition itself. `migratev0_v1` now covers all nine prefixes and should be deleted after the single cluster is converted, along with the `autumn-etcd` dep it added to crates/server.
Next steps: either Scope 3 (client region record, behind the two-form rule) or delete the converter once the cluster is migrated. Scope 5's replacement guard is done (`persist/freeze.rs`), Scope 1/2/4 are done.

Date: 2026-09-20 (3)
TaskStatus: completed
Task scope: F-LANCEDB-PY-NATIVE — Python LanceDB reaches Autumn through an autumn:// provider instead of a FUSE mount. All four acceptance rows now run against a live cluster on H200-1.
WHAT IT REPLACES: the adapter had two code paths and only one was native. Rust handed lance a store OBJECT through the deprecated ObjectStoreParams::object_store, which a prebuilt wheel can never receive; Python resolved a store from the URL SCHEME, so its only route was a FUSE mount — the path docs/lancedb_validation.md already marked weaker (nontransactional multi-key metadata, per-mount serialization, single mount only). Both languages now go through one provider, and the Rust demo covers it without a wheel build.
VERIFIED ON A LIVE RF2 CLUSTER (disposable, random ports, task root /data08/autumn-pynative-20260920-60d1d3ce). (1) Python: create/append/vector search/delete/concurrent reader+writer pass through both session paths — connect()'s own session AND an explicit lancedb.Session(); `mount | grep -c $TASK_ROOT` is 0, so "no FUSE" is measured rather than asserted. (2) Registration ablation: an unregistered scheme fails with "No object store provider found for scheme: 'notautumn'". (3) Bytes land in Autumn, checked ACROSS languages: the same scope reads objects=0 from the Rust client before any write, then 3 objects / 2501 bytes after Python writes 50 rows, with Lance's real key layout (_transactions/, _versions/*.manifest, data/*.lance). (4) Rust demo: 210 rows, 15 objects, vacuum reclaimed 17 chunks.
THE ABLATION THAT MATTERS, and it is a pair rather than an assertion: with autumn_commit_handler forced to None the demo fails `left: 200, right: 210` — exactly one 10-row append lost, the signature of UnsafeCommitHandler putting a manifest unconditionally. Restoring the line turns it green again. Same binary path, same cluster, a fresh empty scope each time, one line different. So the 210 assertion has detection power; the earlier reasoning about v3->v4 was right but was only reasoning until this ran.
MY OWN ERRORS THIS ROUND, each caught before it could mislead: ssh direct to the IP timed out and looked like an unreachable host — the real cause was the local sandbox blocking IPv6, and the ssh config alias worked once that was bypassed. Nested shell quoting ate $T and wrote the Cargo patch path as /lancedb/..., which failed loudly. The first ablation was written as `None.or_else(|| Arc::new(..))` — a type error that would ALSO have returned Some, i.e. an ablation that silently did nothing; had it compiled it would have produced a green "ablated" run and a false conclusion.
A REAL MISJUDGEMENT, not a slip: I built the wheel with --release. 35m11s, of which ~15 minutes was one single-threaded rustc on the _lancedb cdylib monomorphising DataFusion. Functional verification needs no optimisation; debug would have been far faster. I also briefly claimed the build had died when my ssh timed out — it had not, and CPU time was the thing that settled it.
KNOWN LIMITS, recorded rather than glossed: the commit-handler selection covers only ListingDatabase's create and open paths; namespace-backed tables (connect_namespace*, manifest_enabled) and clone_table still reach lance's UnsafeCommitHandler though the scheme resolves for them. ObjectStore::new resolves store_prefix through a private global registry with no autumn entry, so every store creation logs "Guessing that object store prefix is" — cosmetic, affects a metrics label only. Performance was not measured: no benchmark was run this round, so nothing is claimed about it.
Cleanup: the cluster process (413099) was stopped by its own PID, never by binary name. Verified afterwards: no process under the task root, port 36671 not listening, no mounts. Logs, cluster.json and the wheel are retained under the task root (9.9G).
Next steps: deleting fuse_demo.py is now unblocked on the acceptance side but still needs the linkat regression moved into crates/fuse, which has no tests/ directory at all. Namespace and clone_table commit handling is the next real gap.

Date: 2026-09-20 (2)
TaskStatus: not_completed
Task scope: F-LANCEDB-PY-NATIVE — Python LanceDB reaches Autumn through an autumn:// ObjectStoreProvider instead of a FUSE mount. Code written; NOT verified, because nothing here can compile it. Status is not_completed per rule 5.
Why this exists: the adapter had two code paths, and only one was native. Rust hands LanceDB a store OBJECT through ObjectStoreParams.object_store — a field only a caller compiled against lance can reach. A prebuilt wheel never sees it; Python resolves a store from the URL SCHEME inside its own bundled Rust core. So Python's only route was a FUSE mount, which docs/lancedb_validation.md itself marks as the weaker path (nontransactional multi-key metadata, per-mount serialization, single-mount only).
WHAT WAS VERIFIED BY READING lance v13.0.0-beta.6 AND lancedb df5709ef, not inferred: ObjectStoreProvider (lance-io object_store/providers.rs:42) and ObjectStoreRegistry::insert (:418, takes &self) are public; Session::store_registry() (lance/src/session.rs:301) reaches a live session's registry; ObjectStore::new(Arc<DynObjectStore>, Url, ...) (object_store.rs:1800) is a public constructor. Therefore NO lance fork is needed — the patch is confined to lancedb-python. The ObjectStore struct's own fields are mostly private, so the struct-literal form the in-tree providers use is not available out of crate; the public constructor is the only door.
THE SECOND REGISTRATION SITE, which is the easy thing to get wrong: lancedb.connect(session=None) does NOT go through the Session class. python/src/connection.rs:926 only set a session when one was passed, and the builder's own fallback is Arc::new(Session::default()) (verified at database/listing.rs:464,569,644) carrying the stock registry. Patching Session alone would leave the default connect() unable to resolve the scheme. Both connect entry points now install the provider, and the demo takes --with-session precisely so both paths get exercised.
URL shape, and why the scope is NOT in it: the manager rides in the authority (autumn://host:port/path) so a dataset URI is self-contained. The scope is a storage option because new_store receives the TABLE url — autumn://mgr/objects/demo/vectors.lance gives no rule for where scope ends and object path begins, and a segment-counting guess writes to the wrong prefix silently when wrong. A missing scope is an error, not a default. The scope is also folded into calculate_object_store_prefix, which is the registry cache key (providers.rs:290) and build_store's metrics base. CORRECTED after review: it does NOT reach the ObjectStore struct's own store_prefix field, because ObjectStore::new resolves that through a PRIVATE static registry that has no autumn entry (object_store.rs:1795-1812) — so that field stays scope-less and every store creation logs a "Guessing that object store prefix is" warning. Saying it was "the metrics label" was half true.
Verification actually performed. SUPERSEDED, and worth saying why: the first round could only stub — helpers extracted with a fake error type, a stubbed lancedb for the Python demo, a fake maturin for the build script. Once the Rust demo was moved onto the provider the whole graph resolved and real compilers replaced all of it: autumn-lance-provider checks clean against real lance v13.0.0-beta.6 with 7/7 unit tests; examples/lancedb checks clean with --locked (which compiles the fork's rust/lancedb, so the commit-handler change is type-checked too); and PYO3_PYTHON=python3.11 cargo check -p lancedb-python is clean over 387 packages, covering the registration patch. protoc was installed via brew for lance-encoding/lance-file (reversible: brew uninstall protobuf). Two things only the REAL build could show: the fork's lockfile held cfg_aliases and io-uring below what the compio fork needs — invisible while the compio patch sat in a manifest Cargo ignores, which is exactly the silent stock-compio wheel the patch approach would have shipped onto Linux; and the root .gitignore anchors /target, so neither workspace under examples/lancedb was covered and a build almost went into the commit.
COMPILED AFTER ALL, and it changes the honest gap. Making the Rust demo go through the provider forced the dependency graph to resolve, and once it did the provider crate type-checks against real lance v13.0.0-beta.6 (cargo check --all-targets, 50s) and its 7 unit tests run for real — including the two credential cases that had only ever run against a stub. So the review's biggest open question, "will it compile against lance's real signatures", is now answered by the compiler rather than by reading. The demo's own check needed protoc (lance-encoding/lance-file build scripts); installed via brew, which is a change to this machine and reversible with brew uninstall protobuf.
WHY THE DEMO NOW USES THE PROVIDER: it was injecting a store through ObjectStoreParams::object_store while the provider registered a scheme — two mechanisms side by side in one directory, with nothing exercising the second from Rust. That left the provider's only proof behind a full pyo3 wheel build. The demo now connects with autumn:// through a session registry, and it builds against the FORK rather than upstream lancedb, so it also covers the commit-handler selection. It deliberately no longer names ConditionalPutCommitHandler: upstream hands an unknown scheme UnsafeCommitHandler, the fork picks ConditionalPut, and spelling it out in the demo would mask the thing under test. The demo's Cargo.lock was regenerated (707 packages) — it is no longer the lockfile the earlier Linux run validated.
STILL NOT VERIFIED: nothing has RUN against a cluster. No wheel was built (that still needs pyo3 and a Linux target), no cluster was contacted, and every acceptance row in F-LANCEDB-PY-NATIVE is still open.
The lancedb work is now a FORK, pushed: github.com/thesues/lancedb branch autumn-native (570c0ae3 the integration, 82bc2720 the provider pinned by rev instead of a sibling path, 431a7e60 the lockfile bumps). The local checkout sits on that branch; its main is untouched at df5709ef.
DECISION (user, after review): drop the online-patch machinery and FORK lancedb. The patch form existed only to avoid touching upstream, and the commit-handler finding showed that constraint never held — the change is not confined to the Python bindings. patches/ and build_patched_wheel.sh are deleted; the work lives on branch autumn-native (570c0ae3) in the lancedb checkout, with main untouched.
WHAT THE REVIEW CAUGHT, all confirmed by re-reading the source myself, not taken on trust. (1) HIGH, and mine: autumn-op principal-create prints a LABELED pair (`principal:` / `credential:`, autumn_op/main.rs:559-560); my hand-written parser assumed two bare lines and would have failed every authenticated open on "not hex". Fixed by calling the client's existing read_credential_file, which autumnfs already uses — PROVEN here by a throwaway test against crates/client, since that crate does compile on this box: the labeled form parses. That also deleted decode_hex and with it a panic on a non-ASCII second line. (2) HIGH: lance picks a commit handler from a hard-coded scheme table, `_ => UnsafeCommitHandler` (lance-table commit.rs:1193-1261) — an unconditional manifest put. Python writers would have bypassed the adapter's CAS entirely. The Rust demo was safe only because it set ConditionalPutCommitHandler by hand. The fork now sets it for autumn:// on BOTH the open and create paths. (3) I had missed a THIRD connect entry point: connect, connect_namespace_client AND connect_namespace are all exported (python/src/connection.rs:872,944,1003); only the first two were covered. (4) build_store applies params.object_store_wrapper itself, so passing it to ObjectStore::new ran it twice — a mirroring wrapper would have mirrored twice; now None.
The acceptance gained a row it did not have: two Python processes appending concurrently must produce a CommitConflict. The old "并发 reader+writer" row has ONE writer and could never have seen finding (2).
Next steps: what is left needs a cluster, not a compiler. Build the wheel from the fork (maturin, needs a Linux target for io_uring), then the acceptance rows: both session paths, the removed-registration ablation expecting "No object store provider found for scheme: 'autumn'", the two-writer CommitConflict, and a Rust-side list of the scope. The Rust demo is itself the commit-handler ablation and needs no second writer process to be one: both writers sit at v3 and target v4, so an UnsafeCommitHandler silently drops one append and the 210-row assertion goes red — but that is an argument on paper until it runs.
NOT COVERED, recorded so it is not mistaken for done: the commit-handler selection lives only on ListingDatabase's create and open paths. Namespace-backed tables (connect_namespace*, manifest_enabled) and clone_table still fall to lance's UnsafeCommitHandler even though the scheme resolves for them. Deleting fuse_demo.py stays blocked on the acceptance rows AND on moving the linkat regression into crates/fuse, which still has no tests/ directory.
Date: 2026-09-21
TaskStatus: completed
Task scope: review.md R5 and V2 — make Recovery/EC apply and marker release attempt-safe across same-leader awaits, and make failed Recovery cleanup retry without failover.
Current summary: Recovery and EC now use one inflight commit path. The apply transaction compares the marker's exact etcd mod_revision plus bytes and the extent baseline, deletes the marker atomically with the extent update, and rechecks the same attempt before installing memory state. Byte-identical delete/recreate attempts are distinct. Recovery stale-layout cleanup keeps the in-memory marker when durable deletion fails; the standing-instruction tick retries it.
Verification: all 404 autumn-manager library tests passed. The ignored real-etcd apply_done_atomicity suite passed 3/3, including byte-identical marker recreation with a new revision. Four deterministic request/response barrier tests cover Recovery and EC successor preservation; the cleanup failpoint test proves retry without leader change.
Main gaps: R2 still needs Recovery attempt identity on the wire and EN-side dedup; R4 still needs Fence/Remove coordination with Recovery targets. R1 full dual-replica hard-restart acceptance and the remaining review backlog stay open in feature_list.md.
Next steps: implement R2 and R4 together so the Recovery nonce, target identity and Remove transaction share one contract.

Date: 2026-09-21 (2)
TaskStatus: completed
Task scope: review.md R2 and R4 — Recovery attempt identity from durable marker through EN dedup/completion, coordinated with Fence/Remove.
Current summary: wire 46 (client floor remains 43). Recovery carries its marker creation revision, pinned source eversion/slot/sealed length/EC shape/payload location, target UUID and disk identities. A new type-10 format-1 recoveryAttempt record is created/deleted atomically with the unchanged marker; replay checks matching revisions. Old markers without snapshots are cancelled and re-derived. EN validates instructions against the leader, serializes adoption/copy with other extent mutations, refuses a different running attempt, and closes EC staging for committed shard files. Lifecycle serialization covers acquire/apply/register/fence/maintenance/remove; apply additionally compares node/disk/override/tombstone and marker/snapshot in etcd. Fence cancels Recovery targets; failed cancellation remains a Remove blocker. Late dispatch replies cannot release a successor.
Verification: manager lib 411 passed; stream lib 204 passed; rpc lib 87 passed (one pre-existing ignored test). Recovery integration 8/8, node lifecycle 10/10, apply_done_atomicity 6/6 including real etcd, recovery_attempt 5/5 including real etcd and a terminated/joined target runtime restarted over its old directory. The new etcd test also removes the destination disk durably and proves apply cannot publish it. The adjusted corruption-recovery diagnostic passed. Related library/test targets compile; clippy completed with existing warnings (no new production-code warning). Nonce and lifecycle-lock ablations each fail their targeted assertion and were restored. Correction after local inspection: macFUSE libraries, headers and fuse.pc were already installed; the missing executable was pkg-config. Installing pkgconf resolves FUSE discovery. Linux-only sched_setaffinity benchmarks remain a separate all-target limitation; H200-1 access failed at jump-proxy authentication. No deployment performed.
Main gaps: R1 full dual-replica hard-restart acceptance, R6 placement/capacity precheck and the remaining review backlog stay open. No cross-host performance run was attempted for this control-plane fix.
Next steps: continue the remaining review backlog; deployment of wire 46 requires the normal stop-the-world manager/PS/EN rollout. Existing persisted formats need no conversion.

Date: 2026-09-21 (3)
TaskStatus: completed
Task scope: correct the local FUSE dependency diagnosis and complete the previously blocked verification.
Current summary: macFUSE was already installed, including /usr/local/lib/libfuse.dylib, headers, and fuse.pc/fuse3.pc. pkg-config was the missing executable. Installed Homebrew pkgconf 3.0.7; its default search path discovers FUSE 2.9.9 and FUSE3 3.18.2 without extra environment settings.
Verification: cargo check --workspace --lib --bins --tests --features autumn-manager/fuse-tests PASSED, including all eight FUSE-gated manager test targets; cargo test -p autumn-fuse --lib PASSED 63/63 with real libfuse linkage. Existing warnings remain. This does not claim a kernel mount test or Linux-only benchmark validation.
Main gaps: Linux-specific benchmark targets still require Linux. The previous FUSE-discovery blocker is resolved.
Next steps: remaining review backlog; macOS validation commands recorded in scripts/README.md.

Date: 2026-09-23
TaskStatus: completed
Task scope: diagnose the mm2 "direct-read fell back to PS proxy (extent_id=713)" WARN after restart, then fix the root cause client-side (layering untouched — no manager→PS push).
Current summary: ROOT CAUSE was a stale PS descriptor, not a startup race and not the delete/EC race the operator suspected. extent 713 was EC-converted (ops ledger `target=0->713 succeeded`); the WARN line came from the REPLICATED replica loop with replicas=3 — the serving PS (ps-0/ps-1, up 28h, across the flip) handed out a pre-flip replicated descriptor from its pull-invalidated `extent_info_cache`. Every EN refused the direct read fast (eversion gate / payload-not-here), the read took the proxy path, and that proxy read's own EversionStale refetch healed the PS cache — hence exactly one WARN, then 30+ GiB streamed clean. The suspected race (in-flight replicated reads × EC convert × .dat reclaim) is closed by design: eversion fence + CoW + reconcile's keeper-first ordering mean an old-layout read is always correct-bytes or a typed refusal, never wrong bytes. A 10-minute EC TTL before .dat reclaim was evaluated and rejected: ~12 GiB extra per converted extent for 10+ min plus a new age-tracking mechanism, to remove one self-healing slow read.
THE FIX (client-side only, crates/client/src/lib.rs): descriptor reads now classify their outcome. `DirectReadOutcome::{Value, StaleLayout, Failed}` replaces `Option<Bytes>` on `read_from_descriptor` / `read_redirect_replicas` / `read_ec_shards`. StaleLayout = every replica refused fast with an eversion-mismatch or payload-not-here text and no timeout. `read_descriptor_with_stale_heal` (all three call sites: get_direct, get_range_direct_into, apply_redirect_desc) then runs a 1-byte proxy probe — the PS's read path refetches ExtentInfo on EversionStale as a side effect — re-sends MSG_GET_REDIRECT once, and reads direct again. Any failure returns None and the proxy fallback runs exactly as before. One heal per read; once-flag INFO on first heal.
Verification: cargo check -p autumn-client --all-targets clean (2 pre-existing autumn-stream dead-code warnings, unrelated); cargo test -p autumn-client 63 passed incl. 2 new classifier tests (the two EN refusal texts match; timeouts/connect-refusals/checksum failures do not). Per operator instruction the fable review round was skipped for this change.
Main gaps: the heal path itself is not covered by an integration test (needs a cluster with an EC conversion mid-flight); the once-flag WARN text "ENs not client-reachable?" in read_redirect_replicas is still misleading for the stale case, now mostly routed to the heal path instead.
Next steps: watch for the WARN after the next mm2 restart on a post-conversion extent — with the fix it should appear as the one-line heal INFO instead, and the direct read should recover without the proxy fallback.
