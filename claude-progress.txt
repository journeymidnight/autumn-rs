Date: 2026-06-12
TaskStatus: completed
Task scope: /loop 生产视角急修 · 批次 3 = ENOSPC-1（写满盘行为定义+实测；
  顺带揪出并修复 ACK 短写静默损坏）。批次 1 = P0-D（2334304）、批次 2 =
  OBS-1（25d323d）已闭。

=== TWO LISTS REQUIRED BY THE LONG-TASK HARNESS ===

  Already implemented (this commit):
    - EN DiskHealth 三态机（Online/Full/Faulted）：ENOSPC/EDQUOT→Full
      （可自愈，2s sweep free≥5% 即恢复），其余→Faulted（历史永久语
      义）；所有写/persist 错误站点统一 mark_disk_error_for_extent
      （含此前零标记的 EC staging/commit 族）；choose_disk 只选
      allocatable。
    - **短写损坏修复**：build_append_future 裸 write_vectored_at →
      write_vectored_all_at（POSIX 短写曾被当成功 ACK → 读回零填充；
      enospc_chaos E4 实测抓获，1MB 值 3.5KB 完好+全零尾）。
    - manager 分配软避空间不足节点：node_max_free（df 内存态）+
      select_nodes spacious 层 + --min-alloc-free-bytes（默认 256MiB）。
    - metrics autumn_en_disk_full；cluster.sh
      AUTUMN_MGR_MIN_ALLOC_FREE_BYTES 接线。
    - E2E scripts/enospc_chaos.sh（512MB loopback fs；E1 分类/E2 写可
      用/E3 自愈/E4 零损）×2 PASS；seed=13 chaos 回归 ok。
    - 单测：DiskHealth 状态机+分类 ×2、select_nodes spacious ×1。
    - 文档：stream CLAUDE.md 25a、manager 37、README ENOSPC 节。
  Not implemented (recorded):
    - 生产清单后续项：BUG-LEASE-8（fuse 多 key 原子提交边界）；
      rolling upgrade（rkyv same-commit 约束）= 长线 track。
    - F227 recovery-stuck 家族（仍 OPEN）。
    - PS 端延迟直方图（OBS-1 遗留，需求明确再做）。

Current summary:
  coco 2P1+4P2+2P3 采纳 6 拒 2 并全部折入。P1-甲=第二个真 bug：低位
  lease 失败成 hole 后，高位完成仍给调用方 Ok，而 seal 按 contiguous
  commit 砍掉该已 ACK 区间——修复为 caller-ack 延迟到 contiguous 覆盖
  （failure_floor 以上回 Err）。P1-乙=多 shard 盘健康分裂——共享
  per-dir health cell。折入后全 gauntlet 绿：389 单测、enospc_chaos
  ×3、seed=13、transport tcp。

Main gaps:
  - 见 Not implemented。
Next steps:
  - /loop 下一项：BUG-LEASE-8（fuse 多 key 原子提交边界）。
