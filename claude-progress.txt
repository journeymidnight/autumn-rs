Date: 2026-07-10
TaskStatus: completed
Task scope: F-CHAOS-DECOMMISSION #1 — root-cause + fix the live-fenced-node
  decommission drain wedge (MSG_REMOVE_NODE never unblocks; kill-first "works").
RESOLUTION (supersedes the starvation theory below — the EN NEVER hung):
  Instrumentation (per-second EN liveness tick + stage markers + gdb + on-disk
  .dat/.meta evidence) proved the "hung" run_recovery_task COMPLETED in <10 ms;
  the runtime ticked at 1000 ms throughout; the 30s timeout "anomaly" = nothing
  left to time out (prior trace had no completion marker → success read as hang).
  REAL ROOT CAUSE, two layers:
  (L1 harness wiring) every EXT_MSG_DF in system_chaos was BLACK-HOLED:
    format registers control_address = advertise+1000; harness advertise = the
    TOXIPROXY port; toxiproxy fronted only the DATA port → node_health_loop
    (the ONLY drain of EN recovery_done → apply_recovery_done) dialed an
    unbound port every 2 s for every node, all run. No completion was EVER
    applied → replicates kept the fenced node → remove blocked forever.
  (L2 mechanism) lost-completion candidate POISONING: handle_df mem::take's
    recovery_done before delivery is known (at-most-once). Each 60 s
    stale-sweep re-dispatch consumed the next candidate (extent 16: node 3
    @:56, node 5 @+60s, node 9 @+120s — exact sweep cadence), each completed
    + lost; then ALL candidates refused "extent already exists" forever.
  The live-vs-SIGKILL contrast was incidental, not causal.
FIXES (working tree):
  1) system_chaos bootstrap_en: second toxiproxy proxy en-{i}-ctl
     (proxy_port+1000 → real_port+1000) + pick_proxy_port_pair; partition
     nemesis cuts both planes.
  2) extent_node.rs try_adopt_completed_recovery: require_recovery re-dispatch
     on an existing local extent ADOPTS (re-reports RecoveryTaskDone) iff the
     copy is verified complete vs the manager snapshot (same eversion + sealed
     + full length + !EC + !quarantined); else keeps the refusal. Red-before-
     green test: system_extent_recovery::
     lost_recovery_completion_redispatch_adopts_local_copy.
  3) Kept the 4 earlier latent-bug fixes (30s read bound / sealed-empty
     short-circuit / 120s recovery stale threshold / NOT_LEADER retry) —
     correct hardening, none were the cause; comments corrected to the proven
     narrative.
VALIDATION: exact repro (seed 1, 12-action set, DECOMMISSION=1) → drains in
  1 probe (~3 s), test PASSES in 80 s (was 270 s wedge-FAIL); seed 42 + KILL=1
  variant green; autumn-stream 90 unit + 3/3 system_extent_recovery green.
DEFERRED (documented in feature_list): at-least-once done-task handoff
  (DfReq.tasks acks drained ids) — adopt already self-heals a lost response
  within one sweep cycle.
NOT committed (user decides commit split).

------------- superseded in-flight notes (kept for history) -------------
Task scope (original): F-CHAOS-DECOMMISSION — "补齐缺少的场景" (add missing chaos scenarios:
  forcegc/fence/remove). Enable the dormant nemesis actions + add node-decommission
  ("remove") chaos coverage of the F211/F-FENCE-DRAIN fence→drain→remove pipeline.
Current summary: Scenarios ADDED (additive, default-OFF — base test + vphead
  UNAFFECTED, stay green). (1) Dormant-actions win: fence/killfence/ec/partition/
  latency are already in the harness (ALL_ACTIONS = harness default) but no script
  ran them; scripts/decommission_chaos.sh now runs the FULL 12-action set. (2)
  run_terminal_decommission + AUTUMN_CHAOS_DECOMMISSION=1: terminal one-shot that
  fences one EN, waits for full drain, MSG_REMOVE_NODE, then existing verify proves
  no loss. Compiles clean.
Main gaps: The decommission scenario FOUND A REAL BUG (why status = not_completed;
  passes:false in feature_list). **Decommissioning a LIVE fenced node STALLS** —
  fence drains open tails (F-FENCE-DRAIN) but recovery does NOT relocate the fenced
  node's healthy SEALED replicas while it's alive → MSG_REMOVE_NODE blocks 180s →
  FAIL. Confirmed by contrast: AUTUMN_CHAOS_DECOMMISSION_KILL=1 (kill the fenced
  node first) drains in ~3s. Contradicts docs/ops.md decommission runbook + the
  HDFS live-decommission model. Localized to recovery.rs::recovery_dispatch_loop
  503-572 (fenced dispatch at 568 fires but rebuild doesn't complete for a
  live-source extent). FINDING #2 (independent, found validating the full set with
  decommission OFF): the FULL nemesis set leaves GC reclaim STUCK on ~50% of seeds
  (seed 42 PASS, seed 1 FAIL on verify_gc_reclaim — flush+major-compact+force-GC
  deleted 0 extents; NO data loss). The dormant ec/fence churn can pin the GC
  replay floor (F-GC-FLOOR-OBS behavior). Retracts my "safe by construction" claim.
  Open Q: floor-protection-correct (verify_gc_reclaim too strict) vs real
  major-compact-doesn't-advance-floor bug. Full set == harness DEFAULT ALL_ACTIONS
  that no script ran to green → likely always latent.
FIX ATTEMPT (2026-07-10, user: "fix #1 and #2"):
  #2 FIXED + VALIDATED — verify_gc_reclaim now tolerates reclaim=0 when force-GC
  protected NOTHING (nothing-to-reclaim); fails only on protected+reclaim=0 (real
  stuck floor). Full-set seeds 1+42 both PASS.
  #1 NOT FIXED (deeper root cause found via EN instrumentation, but unresolved).
  The manager "stuck marker" is a SYMPTOM: the fenced-slot dispatch (recovery.rs
  568) DOES fire + acquires the Recovery marker, but the target EN's
  run_recovery_task HANGS in the copy path (EN logs: START→resolved→got permit→
  ensured→got nodes map→trying source node=9, then nothing). Blocking extent is
  often sealed_length==0 (F-FENCE-DRAIN-rolled empty tail) doing a pointless
  length==0 read-to-end that parks. DECISIVE: a compiled-in 30s read timeout NEVER
  fires while the task hangs 60s+ → the EN's single-threaded compio runtime is
  STARVED (no task incl. the timer polled; F206 failure class). FOUR correct
  latent-bug fixes landed but NONE resolve the hang: (i) read_bytes_chunk 30s
  timeout (bounds unbounded rpc_oneshot, F228/F229); (ii) sealed-empty short-circuit
  in run_recovery_task; (iii) recovery_stale_threshold_secs 120s (extent_inflight);
  (iv) NOT_LEADER retry in the decommission probe. Unit tests GREEN (stream 90,
  manager 182); base full-set regression LOSS-FREE (my recovery changes are safe).
  But 3-seed live-decommission STILL FAILS (all hang 270s).
Main gaps (UPDATED): #1 is an EN-runtime-starvation / acquire_recovery permit
  interaction under churn — a deep recovery-subsystem bug needing focused
  runtime-level investigation (compio task scheduling), NOT more inference in
  revert-prone code. Stopped per [[feedback_reproduce_before_fixing_mechanism_bugs]].
Next steps: (a) user decides KEEP (correct+safe latent fixes) vs REVERT the 4
  recovery changes for a clean follow-up. (b) #1 follow-up: instrument the EN
  compio runtime to find what starves the single thread during a fenced-node drain
  (candidate: an EC re_avali / recovery copy of a huge extent tying up the core,
  per F206) → fix → then live-decommission drains. Repro (STILL FAILS):
  AUTUMN_CHAOS_SEED=1 AUTUMN_CHAOS_DECOMMISSION=1 AUTUMN_CHAOS_NUM_ENS=6 \
  AUTUMN_MGR_INFLIGHT_STALE_THRESHOLD_SECS=60 cargo test -p autumn-manager --test \
  system_chaos chaos_real_kill_split_merge_ec_fence_no_data_loss -- --ignored --exact
NOT committed. #2 fix is green; #1 remains open (scenario gated default-off).

============================ prior task history ============================
Date: 2026-07-08
TaskStatus: completed
Task scope: autumn_vllm_loader — the REAL vLLM model loader (was "engine proven,
  plugin not packaged"). User: "装vllm就行我有gpu" → install vLLM + build the
  register_model_loader("autumn") plugin + verify end-to-end.
Current summary: DONE + vLLM-verified. Installed vLLM 0.24.0 in an ISOLATED venv
  (/root/vllm-venv, torch 2.11+cu130 — does NOT touch the shared system torch that
  Ray/sglang tenants use). New package python/autumn_vllm_loader:
  @register_model_loader("autumn") AutumnModelLoader(BaseModelLoader) — load_weights
  enumerates *.safetensors in an autumn dir, K reader threads (read_into releases
  the GIL) fill a bounded prefetch queue feeding model.load_weights((name,tensor));
  config/tokenizer stay on vLLM's local model= path (weights-from-backend split).
  entry point vllm.general_plugins for import-free discovery. E2E
  (tests/run_vllm_e2e.sh): local TCP cluster → upload gte-Qwen2-1.5B (7.1GB) to
  autumn → LLM(load_format="autumn", runner="pooling") → forward → embedding dim
  1536, finite, norm 1.0, **first5 BIT-IDENTICAL to the default local-disk loader
  = byte-exact weight load**. Offline unit tests 3/3. get_model_loader("autumn")
  returns AutumnModelLoader.
Main gaps: NONE for "is there a vLLM loader" — yes, real + verified. Optional
  future: H2D-overlap tuning of the vLLM-pull path; GPUDirect(GDR) direct-to-VRAM
  (box has no nvidia_peermem; not needed for bulk sequential weights).
Next steps: commit+push the package. Closed.

============================ prior task history ============================
Date: 2026-07-08
TaskStatus: not_completed
Task scope: F-REDIRECT-BATCH — batch the per-extent redirect so a large-file read
  makes ONE PS round-trip per partition instead of ~630, removing the single-PS
  serialization that capped F-MODEL-ZC-LOAD's RDMA loader at 63% of Model Streamer
  (peaked K=4 then declined on both direct + proxy). Follows the loader thread.
Current summary: DONE + measured. Wire: MSG_GET_REDIRECT_MANY (0x59) +
  GetRedirectItem/GetRedirectManyReq/GetRedirectManyResp, WIRE v16→17
  (fp 55411e9479326ff8, stop-the-world). PS handle_get_redirect_many loops
  get_value_inner + extent_read_descriptor per item. Client get_many_direct = 3
  phases (group ≥64KiB by partition → one redirect-many per partition → fan out EN
  reads on pre-resolved descriptors; per-item proxy fallback intact). Shared by
  fuse/kvcache/fsspec direct-read. Tests: rpc 44 / client 27 / PS 187 green (WIRE
  fp test pins v17). Local TCP byte-exact. **H200 UCX re-bench: 63% → 82% of Model
  Streamer** (EN-direct K=4 3.29→3.75 GB/s, byte-exact). coco (GPT-5.5): 2 findings
  FIXED — P0 authz (MSG_GET_REDIRECT_MANY added to authz_check keyed allowlist,
  per-item check_key), P1 batch-OOM (descriptor-lookup failure no longer inlines N
  large values → fails the batch → client per-item proxy). Re-tested green.
Main gaps: redeploy the coco-fixed ucx autumn-ps + quick byte-exact re-verify
  (fixes touch only authz-on + descriptor-failure paths, orthogonal to the measured
  82% path); docs/ops.md; commit+push. Residual K>4 decline = UCX rendezvous cliff
  (K×32 in-flight), not the PS — separate lever if the user wants past 82%.
Next steps: commit+push; redeploy fixed PS + re-verify; docs/ops.md; then closed
  unless the user wants to chase the UCX-cliff for >82%.

============================ prior task history ============================
Date: 2026-07-08
TaskStatus: not_completed
Task scope: F-MODEL-ZC-LOAD — zero-copy model load. Give `autumn.Fs` a pinned
  zero-copy read (`read_into`) so a thin vLLM streaming loader can hit ≈Run:ai
  Model-Streamer throughput (pinned host + overlapped H2D; NOT GDR). Built on
  F-DIRECT-MANY (batched EN direct-read, PS off the data path). User has a GPU
  (8× H200) so the loader benchmark is verifiable locally.
Current summary: M1 (the `Fs.read_into` seam) DONE + VERIFIED. `read::execute`
  was already dest-based → extracted `fill_region`, added `execute_into` (fills a
  caller buffer, pre-zeroed for sparse/short-extent semantics) + `read_into`
  (≤1 GiB/call, u32 GetReq cap; caller loops). PyO3 `Fs.read_into(ino, off,
  PyBuffer)` mirrors `get_into`'s buffer handling (writable/C-contiguous check;
  Send usize ptr crosses to the worker; the blocking call keeps the buffer alive).
  Honors `direct_read`. Verified: fuse lib 44/44; scratchpad/verify_read_into.sh
  all green — 20 MB multi-extent + ranged + EOF short-read, PS-proxy AND EN-direct
  paths, and 12 MB read straight into a pinned torch buffer → H200 → back
  byte-exact. Release wheel rebuilt + installed into system python (has torch).
Main gaps: M2 — the streaming vLLM loader (own package: autumn + torch only,
  register_model_loader("autumn"), pinned double-buffer + async H2D overlap) and
  the H200 A/B vs Run:ai Model Streamer (runai-model-streamer IS installed; vLLM
  is NOT importable here so the register_model_loader wiring is write-not-run).
Next steps: build the loader + run the H200 throughput A/B vs Model Streamer.
  Then coco review (confirm the F-DIRECT-MANY traecli --profile block is still
  present), docs/ops.md verify step, commit.

============================ prior task history ============================
Date: 2026-07-07
TaskStatus: not_completed
Task scope: F-DIRECT-MANY — batched client direct-read shared by fuse / kvcache /
  fsspec. Continues the "autumn-fuse 是否该默认 --direct-read" discussion. F259
  get_direct only bypasses the PS for a SINGLE whole-value read; the three
  frontends read in BATCHES via get_many_into (PS-proxied MSG_GET_ZC), so
  large-file (model) serving never took the PS off the data path.
Current summary:
  - PS: extended MSG_GET_REDIRECT (get_value_inner) to redirect VP SUB-RANGES
    whose clamped requested length >= 64 KiB (was whole-value-only; fuse
    whole-extent reads carry explicit length so never redirected). Returns
    value_offset = vp.offset + req.offset, value_len = clamped len. Single-key
    get_direct (0,0) unchanged. No wire-struct change → NO WIRE bump.
  - client: get_many_direct (dest-based, mirrors get_many_into) + shared
    read_redirect_replicas (factored out of get_direct) + get_range_direct_into
    (per-item core). Per item: >=64 KiB → redirect → EN direct read → copy into
    dest; inline → copy; ALL-replica-fail → proxy get_range_into. <64 KiB →
    proxy get_range + copy. Mixed-size batches route per item. One extra copy vs
    recv-into-dest is the price of failover-safety (pooled recv, not
    call_into_dest whose cancel-safety forbids timeout+failover).
  - frontends: opt-in flags, default OFF (topology-dependent, needs EN
    reachability): fuse --direct-read → FsState.direct_read → ReadPlan →
    execute; python BatchClient(direct=True); autumn.Fs.connect(direct_read=)
    (fsspec AutumnFileSystem(direct_read=True) threads here). Safe when ENs
    unreachable — each item falls back to PS proxy.
Verification: autumn-client 0 clippy warnings + 27 lib tests green;
  autumn-partition-server 187 --lib tests green; autumn-server/manager build
  clean; fuse `core` feature builds (fuse binary + python wheel are
  Linux/nightly build envs — main.rs/PyO3 changes are trivial flag threading).
Main gaps: coco findbugs DONE 2026-07-07 (GPT-5.5, deep, HEAD~1..HEAD) →
  未发现问题 (0 findings; traced sub-range clamp vs get_direct contract, FUSE
  explicit-range reads, BatchClient whole-value reads, EN short-read/failover
  → PS proxy fallback; overflow + small-dest-truncation weighed and cleared).
  Fixed coco-review.sh en route (traecli 0.200.16 repurposed headless -p to
  -p/--profile → switched to the `coco exec` subcommand, verified). STILL
  not_completed: fuse binary + python wheel are Linux/nightly build envs
  (unbuilt locally — trivial flag threading), and cross-host functional +
  PS-egress perf verification is DEFERRED to a real cluster run.
Next steps: on a real cluster, build+run the fuse binary with --direct-read
  (byte-identical read + PS read-bytes-flat check per docs/ops.md), build the
  python wheel, and measure the cross-host large-file PS-egress win.

============================ prior task history ============================
Date: 2026-07-05
TaskStatus: completed
Task scope: F-DF-OPENTAIL — user "amp 45.18x 放大45倍?": `autumn-op df` showed 45x
  amplification on a 3-replica cluster. Root cause = same open-tail asymmetry as
  F-OVERVIEW-OPENTAIL in the df path: physical_used counts open-tail bytes,
  logical_stored only summed SEALED extents. User corrected a key point: the
  open-tail LOG bytes are NOT "WAL debt" — large VALUES live in log_stream (SST
  holds only VP pointers), so they're LIVE logical data that belongs in the
  denominator. Chosen: #1 + #2 + (b).
Current summary:
  #1 (correctness) StreamClient::open_tail_committed_len returns the tail
     committed length ONLY when !sealed (sealed tail already in the manager
     sealed sum → double-count). PS open-tail probe uses it instead of
     commit_length. Fixes a double-count in the SHIPPED overview too: sealed-tail
     partitions (CoW split child / just-reopened) over-reported — part 45 was
     147.6 MB, true 94.1 MB.
  #2 (amp) ClusterDfResp.logical_open_tail (WIRE v16, fp 4c2dd28d4b3ff567) =
     Σ PS-reported open_tail_bytes across partitions (refs=1 private → simple sum,
     no CoW dedup); manager sums it per cluster-df tick from the policy window;
     autumn-op df + dashboard use logical_footprint = sealed + open_tail and
     amp = physical/footprint; df prints the sealed+open_tail=footprint breakdown.
  (b) did NOT add a "WAL debt" line — footprint−size_bytes is wrong for VP
     (size_bytes is SST-only, excludes live VP value bytes). Accurate dead-byte
     metric (needs open-tail discard tracking) deferred as F-DF-WALDEBT.
Verification: builds clean, 0 new clippy, rpc 44 wire tests green (v16).
  LIVE-VERIFIED (same-commit restart 5): df amp 45.18x → 3.00x (phys 5.32 GB /
  footprint 1.77 GB = sealed 117 MB + open_tail 1.66 GB); WRITABLE(est) 11 GB →
  155 GB; overview now == info --part to the byte for ALL partitions (part 45
  147.6 → 94.1 MB, #1 double-count gone).
Main gaps: NONE for this task. Deferred: F-DF-WALDEBT (accurate dead-value metric
  incl. open-tail discard).
Next steps: closed.

============================ prior task history ============================
Date: 2026-07-05
TaskStatus: completed
Task scope: F-PS-SIZE-BYTES-DEAD — user "fix this" on the dead size_bytes gauge
  found during F-OVERVIEW-OPENTAIL. `PartitionMetrics.size_bytes` had NO writer
  (always 0) → Prometheus `autumn_ps_partition_size_bytes` = 0 AND size-based
  split/merge policy (policy.rs split_size_*/merge_size_low) ran against 0.
Current summary: semantic = LSM-RESIDENT (Σ SST tables[].len + active + Σ imm
  memtables — logical data, the field's doc intent + the right split/merge signal;
  NOT total footprint incl. the transient log WAL tail). Fix:
  `PartitionData::lsm_resident_bytes()` (lib.rs, local/cheap, no RPC) stored into
  metrics.size_bytes on the SAME throttled 30 s maintenance tick as the
  F-OVERVIEW-OPENTAIL open-tail probe (synchronous, no detach). Stale field doc
  fixed. NO wire change (size_bytes already existed → PS-only restart).
  DISTINCT from overview live_size: part 17 = 21 MB size_bytes vs 1.5 GB overview.
  Policy-activation flagged: size-based split/merge now uses real values, but
  auto-actuates ONLY under an armed split/merge-enabling policy (aggressive
  preset); the cluster's armed policy is space-reclaim (GC+EC), unaffected.
Verification: PS builds clean, 0 new clippy. LIVE-VERIFIED after PS restart:
  `info --part 17 --detail` size_bytes 0 → 21242193 (~20 MB); overview still 1.5 GB.
Main gaps: NONE.
Next steps: closed.

============================ prior task history ============================
Date: 2026-07-05
TaskStatus: completed
Task scope: F-OVERVIEW-OPENTAIL — user hit live: `autumn-op info` showed part 17 as
  "0 B  3 ext" while `info --part 17` reported 1.5 GB ("0B，应该是显示不对").
Current summary: root cause = `compute_cluster_overview_resp` (manager) sums ONLY
  each extent's manager-authoritative `sealed_length`, and an OPEN extent's
  sealed_length is 0. A major-compacted / log-heavy partition whose data lives
  entirely in open tails renders 0 B. `info --part` probes the EN for open-tail
  live length; the overview deliberately did not (one-RPC / scalable).
  User decisions (AskUserQuestion): size = TOTAL EXTENT FOOTPRINT (matches
  info --part); mechanism = PS REPORTS IT (no per-EN probe in overview).
  Fix (3 crates, no per-EN probe in the overview):
   - rpc: `PartitionLoad.open_tail_bytes: u64` (WIRE v15, fp 4bbbbf8faeb05923,
     MIN=MAX=15 same-commit).
   - PS: `PartitionMetrics.open_tail_bytes` + `open_tail_probe_inflight`;
     `background_maintenance_loop` runs a throttled (30 s) NON-BLOCKING probe
     (detached task does `commit_length` on the 3 stream tails, stores the sum;
     kept at prior value on any error — detached because commit_length is up to
     15 s worst-case and must never stall GC/compaction). `report_load_loop`
     ships it.
   - manager: overview `live_size = Σ sealed_length + open_tail_of(pid)`
     (latest window bucket; 0 → sealed-sum-only fallback). Dashboard
     /api/overview inherits it (same builder).
  Also FOUND (not fixed — deferred as F-PS-SIZE-BYTES-DEAD): `size_bytes` gauge
  has no writer → always 0 → Prometheus gauge + size-based auto-split/merge
  policy see 0. Not fixed alongside because populating it would silently arm
  size-based auto-policy (behavioral change; needs its own semantic decision).
Verification: affected crates build clean; 0 new clippy (4 PS RefCell-across-await
  warns are pre-existing); rpc 44 wire tests green (v15 pinned).
  LIVE-VERIFIED on the running 5-EN cluster after same-commit `cluster.sh restart 5`
  (data preserved): part 17 0 B → 1.5 GB (== info --part 17 EN-probe); every
  partition size now accurate (part 45 1.6 GB → ~147 MB once open-tail bytes
  counted instead of stale sealed CoW extents). Idle part 17 matches info --part
  to the byte (combine correct); active part 45 shows expected window-staleness
  skew vs the live probe. User confirmed manual verification OK.
Main gaps: NONE for the display fix. Deferred: F-PS-SIZE-BYTES-DEAD (dead
  size_bytes gauge + broken size-based policy) — needs an explicit go-ahead.
Next steps: closed. Optional follow-up = revive size_bytes / size-based policy
  (F-PS-SIZE-BYTES-DEAD) if the user wants it.

============================ prior task history ============================
Task scope: F-SPLIT-STALECACHE-PROBE — user asked to close the "split-sealed tail
  cached-as-open → force-GC skips it" GC-promptness gap with a seal-time
  invalidate_extent_cache. Reproduce-first investigation.
Current summary: gap is UNREACHABLE for the split source (NOT a fix — no code
  change). The sealed tail is excluded from sealed_extents (last extent) until the
  source allocs a fresh tail, and that alloc routes through load_stream_tail which
  INSERTS the tail's fresh sealed state into extent_info_cache — so the cache is
  refreshed before the tail can become a force-GC candidate. Proven by a new guard
  test system_split_forcegc_stale_cache (split → source write → compact → force-GC
  → shared tail refs drops 2→<2, i.e. reclaimed; stable 2/2). A seal-time evict
  would be redundant dead code (repo rule: no defensive fixes for unreproducible
  bugs). Merge survivor has a brief frozen-window but self-heals on reopen.
  CLAUDE.md note clarified (backstop-covered → unreachable). Existing
  split/gc tests green.
Main gaps: NONE. Recommendation stands: don't add the redundant invalidate; the
  guard test documents + protects the invariant.
Next steps: closed unless the user wants the belt-and-suspenders invalidate anyway.

============================ prior task history ============================
Date: 2026-07-04 (coco + fixes + cluster validation closed 2026-07-05)
TaskStatus: completed
Task scope: F-GC-FLOOR-OBS — make the GC replay-floor protection legible to
  operators so a CORRECT "forcegc skipped, replay_floor_pos=0" isn't mistaken for
  a bug (user hit it live: forcegc 29/17 12 on CoW-shared extent 12). Four
  observability additions, zero behavior change to GC/recovery:
  #1 enrich the GC skip log (part_id + protected extents + floor_extent +
     pinned-by-SST + plain-language hint);
  #2 new PS RPC MSG_DIAG_PARTITION_VP (0x58, WIRE v14) → `autumn-op info --part`
     renders replay_floor + per-SST vp_heads (marks the SST pinning the floor);
  #3 forcegc returns a synchronous advisory (which requested extents are inside
     the replay window) instead of skip-only-in-PS-log;
  #4 dashboard bug: per-partition drawer showed OPEN extents as 0B — now probes
     the EN for live length (mirrors autumn-op info --part).
  #5 GC empty-sealed-log-extent reclaim (behavior, not obs): user "频繁 merge/split
     产生很多空 sealed log，gc 也要 punch". Live-confirmed part 17 extent 38
     starved (forcegc punched instantly = authoritatively sealed = PRIORITY
     STARVATION, not stale-cache). Fix: empties get a separate MAX_GC_EMPTY_ONCE=32
     budget + scan no longer breaks on the MAX_GC_ONCE=3 non-empty cap, so free-to-
     punch empties at the discard-sorted tail no longer starve behind big rewrite
     candidates. Latent follow-up (NOT done): split/merge don't evict the PS
     extent_info_cache, so some empties can be stale-open-skipped until refresh.
Current summary: implemented + builds clean (autumn-rpc/partition-server/manager/
  server); WIRE bumped 13→14 (registry recorded, wire test green); rpc +
  partition-server --lib tests green (lone f099i flake is pre-existing parallel-run
  noise, passes in isolation). Release binaries rebuilt.
Closeout 2026-07-05: coco review DONE (GPT-5.5) — 4 findings, ALL FIXED (commits
  9329960 + 6891fff): P1 MSG_DIAG_PARTITION_VP missing from extract_part_id →
  feature #2 (info --part floor) was silently DEAD; P2 forcegc advisory dropped by
  ClusterClient::maintenance's Ok(()) → feature #3 invisible; P2 dashboard rollup
  double-count via size==live coincidence; P3 dashboard probe first-replica-only →
  0B when replica[0] down. The stale-open "latent follow-up" (#5 note) was proven
  UNREACHABLE by F-SPLIT-STALECACHE-PROBE (top of file). USER VALIDATED on the
  running cluster: #5 empty-sealed auto-drain + the repaired info --part floor /
  forcegc advisory / dashboard open-extent size all render.
Main gaps: NONE — completed.
Next steps: closed.

============================ prior task history ============================
Date: 2026-07-04
TaskStatus: completed
Task scope: F-VPHEAD-CHAOS — multi-seed chaos validating the vp_head thread under
  real fault injection, ESPECIALLY split/merge/compact/forcegc (user ask), PLUS a
  POSITIVE reclamation check ("GC 后 extent 确定可以被删除"). Added Action::ForceGc +
  do_force_gc (force GC punches every partition's sealed log extents = maximal
  stress on the PS replay-floor guard) AND verify_gc_reclaim (final quiesce →
  compact → force-GC MUST physically DELETE extents, measured via manager etcd
  extents/ before/after; reclaim==0 = FAIL = floor-stuck) + re-verify per-key +
  accounting so the punch pass is loss/leak-free. Runner scripts/vphead_chaos.sh.
Current summary: reclaim check validated (single-seed: 4-9 extents deleted, 2-3 by
  force-GC, post-reclaim per-key 0/0). Fixed 2 self-inflicted test issues: (1) the
  write-liveness probe overwrites real keys -> moved it to run LAST (after all
  per-key checks); (2) seed-7 flake — the ~30s reclaim pass lets a background
  split/merge convergence move keys -> stale settle-topo mis-routes the
  post-reclaim VP-key reads (verify_per_key retries the SAME wrong partition, can't
  self-heal); re-run passed (transient, not a product loss). Fix = refresh_topology
  + retry-once before declaring loss.
  Hardened 6-seed sweep (1/7/42/137/2024/88888): ALL 6 PASS — zero loss + positive
  reclaim (physically DELETED 2-9 extents/seed, post-reclaim per-key 0/0, no leak).
  Seed 7 (the earlier flake) passed clean. vphead_chaos.sh RESULT: PASS.
Main gaps: NONE. The vp_head thread is proven both directions — no loss (data
  safety) AND positive reclaim (GC deletes extents, floor advances) — under
  multi-seed real fault injection incl. split/merge/compact/forcegc.
Extended soak: seed 7 (the flake candidate) x6 + fresh 313/4242 — ALL 8 PASS, zero
  loss, positive reclaim (3-8 extents/seed). The seed-7 flake did NOT recur (0/6);
  seed-7 record = 1 fail / 9 runs (~11%), timing-dependent, consistent with the
  merge-convergence mis-route (a real loss would be deterministic per seed). Only
  gap: retry_fired=0 across every run, so the refresh+retry mitigation stays
  UNEXERCISED (sound by construction — refresh_topology is the settle-time routine
  — but not watched healing a live flake).
Next steps: closed. Product side solid: dozens of chaos runs, zero data loss +
  positive reclaim every time; the single flake was test-side + non-recurring.
Main gaps: NONE. The vp_head correctness thread is now proven end to end:
  reproduce-first fixes (compaction-MAX, flush-tight, recovery-tail-seed) + coco
  + fable reviews + multi-seed chaos with split/merge/compact/forcegc = no loss.
Next steps: closed. (Optional: run on the remote 3-EN cluster host for a
  longer/cross-host soak if desired.)

============================ prior task history ============================
Date: 2026-07-04
TaskStatus: completed
Task scope: F-FLUSH-VPHEAD-b — recovery seeded p.vp to the replay-MIN (backward),
  so an idle-restarted partition's recovered active memtable rotated with a
  backward vp_head → GC floor couldn't advance without a fresh write (the user's
  original compact-then-forceg-stuck symptom). Fix: recover_partition tracks each
  replayed extent's committed end (at BOTH is_final AND the empty-extent early
  break — coco P2) and seeds p.vp to the committed log TAIL, kept separate from
  the replay start.
Current summary: SAFE (committed-clamped reads ⇒ tail ≥ every replayed record; and
  the recovered active always flushes INTO an SST holding its data). New guard
  system_recovery_vp_seed (restart w/ un-flushed data → flush recovered active →
  restart → nothing lost) green. 187 ps --lib + all flush/recovery/compact/gc/
  split system tests green; 0 new clippy. coco: P2 empty-tail-not-advancing FIXED;
  P2 drop(ps)≠crash KEPT (non-graceful REQUIRED — graceful flushes B, skips the
  path under test).
Main gaps: NONE for the vp_head correctness thread — it is now complete end to end:
  flush-tight (rotation boundary) + compaction-MAX + recovery-tail-seed all land on
  true content boundaries, so recovery never starts past an un-flushed write AND
  the GC floor advances (compact/gc reclaim works, including idle-restart).
Next steps: verify the whole vp_head stack on the real cluster (the user's original
  compact-then-forceg case) if desired; otherwise the thread is closed.

============================ prior task history ============================
Date: 2026-07-04
TaskStatus: completed
Task scope: F-FLUSH-VPHEAD — flush stamped the output SST vp_head from the LIVE
  cursor at flush-CLAIM time, not the position at rotate_active. The background
  flush lags the writer, so foreground writes between an imm's rotation and its
  claim push the cursor forward → the SST vp_head sits AHEAD of that imm's own
  content (past acked-but-un-flushed writes) → silent loss on crash. User named
  the premise ("必需是 rotate 瞬间的位置 / 如果这个前提坏了之前的修复都不对") — it
  is ALSO what F-COMPACT-VPHEAD's MAX(input vp_heads) rests on, so it was
  load-bearing for both.
Current summary: reproduce-first WITH a flush test sync-point (set_flush_test_pause
  + set_flush_mem_bytes). system_flush_race_vp_head FAILED pre-fix (a-15, an
  un-flushed tail key, lost = left:[]) and on a fix-reverted control, PASSES
  post-fix. Fix = rotate_active captures p.vp into PartitionData.imm_vp_heads
  (ptr-keyed map, same lifecycle as flushing_imm_ptrs); run_flush_async_phase_inner
  stamps the imm's captured vp for both the SST/meta vp_head AND the F178 sync
  barrier. Test now waits on flush_commit_count (durable-commit counter), not a
  sleep. 187 ps --lib + all flush/recovery/compact/gc/split system tests green; 0
  new clippy. coco findbugs: 5 findings ALL on the test (fix code clean) — fixed
  false-pass-sleep, pause-leak-on-panic (RAII guard), ignored setter return,
  imprecise comment; kept drop(ps1) (mirrors crash_mid_compact + proven by
  deterministic FAIL→PASS).
Main gaps (deferred, data-safe): recovery seeds p.vp = replay-MIN (backward), so
  an idle-restarted SINGLE-SST partition's recovered active rotates with a backward
  vp → GC floor can't advance without a fresh write (the user's original
  compact-then-forceg-stuck symptom for THAT specific case). Clean fix = seed p.vp
  = committed log tail at recovery (track the replay end in recover_partition).
  This is the LAST open item of the vp_head correctness thread.
Next steps: recovery-seed (p.vp = committed tail) as a separate reproduce-first
  task if idle-restart GC-reclaim matters; else the vp_head correctness story is
  complete (flush-tight + compaction-MAX both land on true content boundaries).

============================ prior task history ============================
Date: 2026-07-04
TaskStatus: completed
Task scope: F-COMPACT-VPHEAD — major compaction stamped the output SST vp_head
  (recovery replay-start) from the LIVE write cursor p.vp_*, which sits PAST
  acked-but-un-flushed writes in the active memtable. A major compaction wipes
  every older small-vp_head anchor and stamps all outputs at the live cursor, so
  recovery (latest checkpoint via decode_last_table_locations + live SST vp_heads
  — NO old-checkpoint masking) replays from PAST the un-flushed tail → silent
  data loss on crash. Found while investigating the user's "GC replay-floor stuck
  at 0 / compact 后 forceg 仍拒绝回收".
Current summary: reproduce-first. New regression system_compact_unflushed_vp_head
  FAILED pre-fix (un-flushed key-40 stranded past vp_head → NotFound = left:[]),
  PASSES post-fix. Fix = background::compaction_output_vp_head stamps the MAX over
  the INPUT SST vp_heads by stream position (newest input's content boundary) —
  ADVANCES the GC floor (so GC still reclaims post-split shared extents:
  system_gc_multiversion_same_extent stays green) while staying ≤ the live cursor
  = strictly safer than the pre-fix live-cursor stamp. coco findbugs P1
  (log_extent_ids unwrap_or_default → (0,0) → recovery no-replay branch = loss)
  FIXED by hard `?` (aborts before any row_stream append; caller logs+retries).
  187 ps --lib tests + system_crash_mid_compact / ps_recovery / crash_mid_flush /
  vp_after_split_gc / split_* / gc_chunked_read all green; 0 new clippy warnings.
Main gaps (deferred follow-ups, NOT done this task):
  (a) flush stamps the live cursor at CLAIM time, which can be AHEAD of its imm's
      content when flush races foreground writes → MAX can over-advance in that
      narrow window (coco P1 residual, documented, MAX ≤ live cursor so never
      worse than pre-fix). Clean fix = record each imm's content boundary at
      ROTATION and thread it through the flush (needs an ImmEntry-shaped change;
      SST vp_head SEMANTICS change ⇒ format version marker for stop-the-world
      migration).
  (b) recovery seeds p.vp = replay-min (backward), so an idle-restarted single-SST
      partition's floor still can't advance without a write+flush — the user's
      ORIGINAL GC-stuck symptom for that specific case is NOT closed by this fix.
Next steps: if the user wants full GC-reclaim on idle/restart AND closure of the
  flush-race residual, do the flush-tight (rotation-boundary) change as a separate
  reproduce-first task (bigger, touches recovery-critical flush + SST format).

============================ prior task history ============================
Date: 2026-07-04
TaskStatus: completed
Task scope: F-DASH-IN-MGR — fold the standalone Python web dashboard + auto-policy
  controller (python/dashboard/) into the autumn-manager binary. All-in-one, HTML
  embedded via include_str!, the auto-policy loop runs as a leader-fenced manager
  background task so it survives as long as the leader does. autoPolicy runs ONLY on
  the etcd leader. Full plan: docs/dashboard_in_manager_plan.md. Milestones M0-M4.

M0 DONE (pushed, 711ece0): embedded axum dashboard (cyper_axum::serve, gallery stack)
  in the manager; /api/overview read-only; flags --dashboard-*. 2 coco rounds.
M1 DONE (pushed, 9f5b3d0): full read-only data parity — /api/partition/<id> (full
  extChip contract + dedup), advisories[], node auto_state + full NodeStateEntry.
  auto_policy.rs ported pure fns. coco round (3 parity fixes). 177 tests.

M2 COMPLETE (2026-07-04, UNCOMMITTED): leader-fenced auto-policy controller + etcd config.
  · Wire (WIRE v12, fp c2ae65efd6e168f6): MSG_AUTOPOLICY_GET/SET (0x54/0x55) +
    MgrAutoPolicyConfig/Entry/Cooldowns + Get/Set Req/Resp (manager_rpc.rs).
  · auto_policy.rs: AutoPolicyMode (Off/DryRun/Armed) state machine, 5 presets,
    ported decide_actions/kinds_from_switches (+9 unit tests), AutoPolicyState +
    sanitize_entry (clamp interval/cooldown/max_actions).
  · Leader-fenced auto_policy_tick_loop (spawn_supervised; leader-only INVARIANT;
    DEFAULT-OFF; Armed needs --dashboard-allow-mutations else degrades to DryRun;
    interval + cooldown gating) + in-process actuate_candidate: split→auto_dispatch_split,
    merge→F185-freeze handle_merge_partitions, gc/compact→PS MSG_MAINTENANCE,
    ec→handle_force_ec_convert.
  · etcd persist (autoPolicy/config,cooldowns; F149-fenced; ETCD-FIRST compute→persist→
    apply + concurrent-update guard) + replay_from_etcd (fail-loud + sanitize).
  · autopolicy_set (SET_MODE/ACTIVE/UPSERT/DELETE, leader-gated); RPC handle_autopolicy_
    get/set + dispatch; dashboard /api/policies GET (leader-gated) + POST activate/upsert/
    delete (allow_mutations-gated); autumn-op auto-policy status|activate <name> [--arm]|
    deactivate + ClusterClient auto_policy_get/set; bin wires --dashboard-allow-mutations.
  · coco findbugs (GPT-5.5) on M2: 7 findings ALL fixed (etcd-first, overflow clamp,
    activate-mode, dashboard leader gate, max_actions truncation, client leader_rotations x2).

Verification (M2):
  · manager 182 + rpc 43 + client 27 lib tests; 0 clippy warnings; all bins build.
  · memory-mode e2e: status/activate/deactivate + dashboard /api/policies consistent;
    default-OFF; activate(no --arm)=dry_run, activate --arm=armed, deactivate=off.
  · ETCD-FAILOVER acceptance (b), REAL etcd: manager1 activate gc-only --arm → kill -9 →
    manager2 restarts same etcd → replays active=gc-only mode=armed (crash-safety proven).
  · Deferred (like M1): live ACTUATION vs a real cluster with partitions+load
    (multi-tenant box blocks local EN bring-up).

Main gaps / next steps:
  · M2 committed+pushed (ea9a7be). M3 committed next.
  · M3 DONE: POST /api/action STRUCTURED body {"action":"split","part_id":7} (no CLI
    cmd-string; user decision) → actuate_action typed dispatch to same in-process ops as
    the loop; gated by --dashboard-allow-mutations + leader-only. HTML rewired to send
    structured action objects. coco: 4 findings (leader gate + merge-same + strict
    extent_ids fixed; auth/CSRF = documented non-goal). 182 tests, 0 clippy.
  · M4: delete python/dashboard/ (5 files); DASHBOARD.md → docs/ops.md; update README,
    crates/manager/CLAUDE.md (F203 reconciliation note), crates/server/CLAUDE.md, deploy
    k8s/baremetal + cluster.sh env→flag threading (AUTUMN_DASHBOARD=1).
  · LIVE-VERIFIED on remote cluster host (2026-07-04): real 3-EN etcd cluster —
    dashboard real data; /api/action split → real 1→2 split; ARMED controller
    AUTO-fired compact (has-overlap) + gc (overwrites); kill -9 leader → new leader
    replays maintenance/Armed from etcd + keeps actuating. Deferred item CLEARED.
  · Corrected stale M0 startup WARN ("not wired until M2/M3").
