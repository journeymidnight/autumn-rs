Date: 2026-05-21
TaskStatus: not_completed
Task scope: F216-E — UCX end-to-end zero-copy READ data path for the
            kvcache (sglang HiCache L3). Relay topology (client <- PS <- EN,
            no partition-layer bypass). Read path first; write/send-registration
            is Phase 2. Plan: /root/.claude/plans/flickering-sauteeing-pearl.md.

Current summary:
  The client <- PS hop is DONE and verified end-to-end on a live 1-node
  cluster, both transports, byte-identical to the normal `get`:
    - TCP: large(256K, VP/log_stream path) / small(inline) / not-found.
    - UCX: same, with rc_mlx5 active in the PS log -> the registered
      zero-copy receive (memh) path is genuinely exercised over RDMA;
      the value CRC is verified over the registered dest.

  Primitives shipped (all unit-tested, build clean with AND without `ucx`):
    - RegPool (crates/transport/src/ucx/regpool.rs, 8464122): thread-local
      pool of UCX-registered buffers, pow2 classes, per-thread memlock cap,
      over-cap -> unregistered fallback.
    - recv_into seam (crates/transport/src/lib.rs, e6be8d2):
      ReadHalf::recv_into(buf, Option<&RegisteredMem>) — UCX+Some ->
      recv_registered(memh); UCX+None -> recv_unregistered; uniform across
      feature builds (uninhabited RegisteredMem stub on non-ucx).
    - memh wiring (crates/transport/src/ucx/endpoint.rs, a41b82f): THE
      pivotal fix — ucx_recv/ucx_recv_raw now set params.memh +
      UCP_OP_ATTR_FIELD_MEMH when memh is non-null. Pre-fix registration
      was a silent no-op (the "registration doesn't help" artifact).
    - call_into_dest + frame header-only decode (crates/rpc, bc4210b):
      Pending::IntoDest, RpcClient::call_into_dest(msg_type, payload, dest,
      cap, reg) -> DestMeta{code, value_len}; ZC framing
      [code:1][value_len:4 LE][crc32c:4 LE] (ZC_META_LEN=9) + value tail;
      read_loop dual-path (UCX recv-into-dest vs TCP try_decode +
      finish_into_dest_from_frame); CRC verified over dest. TCP loopback
      unit test passes.
    - PS handle_get_zc (MSG_GET_ZC=0x50, partition-server, b59e632):
      maps every GetOutcome (value/not-found/error) to a V0 ZC payload;
      ps-conn emits V0 frame for MSG_GET_ZC, V1 for MSG_GET.
    - ClusterClient::get_into (crates/client, 6041e7d): retry/epoch-stale
      shape of call_ps_for_key; reg=Some -> RDMA into the registered dest.
    - autumn-client `zc-get <KEY>` verification CLI (28b4c64): heads the
      key, registers a dest (ucx build), get_into, stdout.

Main gaps (NOT done — next session):
  1. PS <- EN hop (#9/#10) — the EN->PS value copy is still present. Design
     decided but DELIBERATELY not rushed: a naive call_into_dest into a
     caller-owned dest is NOT cancel-safe on this path because the EN read
     uses a 3 s timeout + replica failover. If the call times out / a
     replica is dropped, the multiplexed read_loop (separate task) can
     still write into a dest the caller has freed (use-after-free). The
     client <- PS hop avoids this (no timeout; the sglang page outlives the
     call). The SAFE design for PS <- EN: the read_loop OWNS a RegPool
     PooledBuf, recvs into it, and hands the PooledBuf back via the meta
     channel; on caller-cancel the read_loop drains into the pool buf and
     returns it to the pool (no UAF). Then EN gets a new additive
     MSG_READ_BYTES_ZC that emits a V0 frame [zc_meta:9][value] as 2 Bytes
     (also removes the EN-side ReadBytesResp.encode()+Frame::encode() double
     value copy); StreamClient gets a read-into-pooled fast path for the
     replicated, single-chunk, non-EC case (EC / chunked / failover-exhaust
     fall back to the untouched copy path); resolve_value returns the
     PooledBuf; handle_get_zc sends it to the client (no first-cut concat).
  2. python/src/lib.rs BatchClient (#12 python): register the sglang host
     pool ONCE at construction, replace copy_nonoverlapping with get_into.
     Needs a maturin rebuild + sglang env (deferred).
  3. PS->client first-cut concat copy in handle_get_zc -> vectored (Phase 2
     polish), once resolve_value returns a PooledBuf the value can be
     emitted as an aliasing Bytes (no concat).

Next steps:
  - Implement the read_loop-owns-PooledBuf handoff variant of call_into_dest
    (cancel-safe), then the EN MSG_READ_BYTES_ZC + StreamClient read-into-
    pooled fast path + resolve_value/handle_get_zc wiring; verify e2e + no
    regression (cargo test -p autumn-stream / -p autumn-partition-server,
    TCP perf-check stays green).
  - Then the python BatchClient get_into + maturin rebuild.

Verification this session:
  cargo test -p autumn-rpc (14 ok, incl call_into_dest_tcp), -p
  autumn-transport (all ok, incl --features ucx), loopback_ucx 5/5 ok over
  RoCE (AUTUMN_UCX_TEST_BIND=[fdbd:dc62:3:302::14]:0). e2e zc-get TCP+UCX.

Commits this session: 8464122 (RegPool), a41b82f (memh), 5412473 (bench
  consolidation), bc4210b (call_into_dest), b59e632 (PS MSG_GET_ZC),
  6041e7d (client get_into), 28b4c64 (zc-get CLI). Earlier in the arc:
  e6be8d2 (recv_into seam), 09cddc4/9832b5d/8e8b63b (ps-conn FU + spawn
  de-serialization).

Previous task (completed 2026-05-19): F216 autumn-kvcache MVP
  (Python-adapter-only sglang HiCache L3 backend, F216-A..D).
