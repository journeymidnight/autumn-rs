Date: 2026-06-10
TaskStatus: completed
Task scope: (a) F256 natural batching (landed earlier today, commit
  48cdf3c); (b) UCX "threads block" root-cause investigation (user
  question: autumn bug or UCX bug?); (c) F257 — accept errors must not
  kill manager/EN (the real autumn bug the investigation surfaced).

=== TWO LISTS REQUIRED BY THE LONG-TASK HARNESS ===

  Already implemented (this session):
    - F256 (commit 48cdf3c): partition_loop natural batching; TCP 4K
      write +24-31% A/B-verified.
    - F257 (this commit): manager serve() + EN accept_loop_on accept
      errors -> warn + backoff + continue (was `?` -> process death).
      Reproducer-verified: t1024 UCX storm + kill -9 leaves
      MGR/EN/PS alive + serving (pre-F257: manager died, PS fleet
      heartbeat-suicided).
    - UCX livelock investigation (no code change; findings below).

  Not implemented (deferred, by evidence):
    - UCX worker-creation throttle in autumn: NOT justified — the
      collapse is HOST-level (128 separate processes x 2 threads
      collapse identically to 1 process x 256 threads), so an
      in-process semaphore can't fix the general case; production
      shape (1 process, few workers, UCX_NET_DEVICES pinned) doesn't
      hit it.
    - UCX upstream fix: 1.20.1 self-compiled and tested — does NOT
      fix it. Would need an upstream patch to the libuct_ib
      DEVX-create spinlock path; out of scope.
    - coco P1 residual: failed-accept ep leak (no working close path
      under MODE_NONE) — accepted + documented, see F257 entry.

Current summary (UCX "thread 多 block" final verdict):
  - Mechanism: threads stuck INSIDE ucp_worker_create
    (uct_iface_open -> rc_mlx5/ib iface init -> devx_create_cq/qp),
    70% of sampled stacks on pthread_spin_lock (libuct_ib, UCX
    1.16.0). 10 mlx5 devices x N workers; spinlock wraps ioctl-slow
    paths. t16=8K ops/s, t64=462, t256=0, t1024=no output in 180s
    (709/1023 threads R-state burning ~190 cores, 0 ops; PS idle).
  - NOT autumn's progress loop / wireup / data path: UCX_TLS=tcp,self
    (no IB ifaces) t256 fully recovers (6.6K write / 149K read).
  - NOT process-local: 128 procs x 2 threads = same total-zero ->
    kernel/driver-level serialization on this 10-NIC host.
  - UCX 1.20.1 (self-built, client-side LD_LIBRARY_PATH swap): still
    0.00 at t256 — upstream has not fixed it.
  - UCX_NET_DEVICES=lo,mlx5_1:1 partial mitigation (t256 write 793
    vs 0; read 988K). Production cross-host config already pins one
    device (required anyway — auto-select hangs with 10 RoCE devs).
  - Practical guidance: UCX client OS threads <=16 per host for
    bench storms; scale via pipeline-depth/partitions; SDK
    single-runtime + fan_out shape is correct and unaffected.

Main gaps:
  - Failed-accept UCX ep leak (bounded; documented; no close path
    under MODE_NONE).
  - UCX worker-create host-level collapse is an upstream limitation;
    recorded in memory + feature_list, no autumn code can fix it.

Next steps:
  None for this task. Session-closure invariant satisfied.
