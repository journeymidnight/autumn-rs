Date: 2026-07-03
TaskStatus: completed
Task scope: 把 autumn-memory 的上层消费者全部 Rust 化,删除 Python glue。
  最终形态:autumn-memory (Rust core) + 两个 Rust example(codebase-memory、memory-browser),
  各自 Axum 网页 UI + --mcp stdio。Python 侧只保留 autumn 绑定的 Client/BatchClient(供 autumn_kvcache)。

本轮完成:
  · F-MEM-13 graph 核心(nodes/edges/adjacency/bfs + reconcile 图完整性):22/22 单测 + e2e 绿。
  · examples/codebase-memory(Rust):tree-sitter-rust 索引 autumn-rs 自身;网页搜索(词法/向量/混合)
    + 调用图;3D 图可视化(多 CDN 兜底 + settle-then-freeze physics);--mcp;--reset;
    key migration 已由用户跑完(gallery 那条无关)。真集群端到端验证过。
  · examples/memory-browser(Rust,新):通用 agent memory 浏览器 + MCP。4 tab:Search(remember+检索)、
    Facts(带 TTL)、Timeline(episodic)、Graph(联想图 3D)。--mcp 11 个 tool;--reset;端到端验证过。
  · gallery namespace 修复:所有 key 挂到 gallery/ 前缀,两处全键扫描收敛到 range("gallery/");
    migrate_ns.py 迁移脚本(安全白名单,用户已迁移 5563 key,16 个坏 thumbnail 是 extent 43 丢失的旧问题)。
  · 删除:python/autumn_memory(人体工学层+embedder.py+tests)、examples/hermes_memory_autumn、
    autumn.Memory PyO3 绑定(python/src/memory.rs + lib.rs 注册 + python/Cargo.toml 的 autumn-memory 依赖)、
    autumn_memory_langgraph、autumn_memory_mcp。
  · 文档:README(去掉 Python stack/Hermes,改指 Rust examples)、crates/autumn-memory/CLAUDE.md purpose、
    feature_list(F-MEM-2/6 标注 REMOVED;新增 F-MEM-14 codebase-memory、F-MEM-15 memory-browser)。

验证:
  · cargo build/clippy:gallery、codebase-memory、memory-browser 全绿。
  · autumn 绑定 clippy 绿(移除 Memory 后);cargo build 的 link 报错是 PyO3 extension-module 独立链接
    的固有现象(maturin 才有 -undefined dynamic_lookup),非真错误。
  · autumn-memory 22/22 lib 单测绿。
  · 两个 example 真集群端到端(remember/search/facts/episodic/graph/link/stats + --mcp)全通。

Main gaps / next steps:
  · 全部 UNCOMMITTED,等用户决定 commit 划分(建议:graph 核心 / gallery namespace / python glue 删除 /
    codebase-memory / memory-browser 分开)。
  · codebase-memory 需重新 build+restart 才能拿到 graph perf/CDN/MCP-footer 修复。
