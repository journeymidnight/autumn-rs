Date: 2026-05-12
TaskStatus: not_completed
Task scope: F190-F193 — multi-feature loop. F190, F191, F192 done;
            F193 still to go.

F190 (commit 19049a2): per-stream bad_nodes exclusion at alloc time.
F191 (commit 7981faa): ExtentNode control-plane port + manager
                       control_pool + DF timeout fix.

F192 done in this next commit:
  - MSG_REPORT_DISK_FAILURE wire (0x38) + ReportDiskFailureReq
    (node_id, extent_id, error_kind, reporter_part_id, ts_ms)
  - Manager handler: in-memory recent_failure_reports HashMap with
    60 s sliding window, quorum=3 distinct reporter_part_id →
    mark_node_disks_offline + clear entry. Env-tunable:
    AUTUMN_REPORT_DISK_FAILURE_WINDOW_SECS,
    AUTUMN_REPORT_DISK_FAILURE_QUORUM. Successful DF in
    disk_status_update_loop clears the entry.
  - StreamClient: bounded mpsc::channel(1024) + drainer task
    spawned in construct; apply_completion generic-Err path calls
    state.try_report_failure; NotFound deliberately skipped (stale
    cache, not health). reporter_part_id Cell + setter.
  - PartitionData: P-log and P-bulk StreamClients both call
    set_reporter_part_id(part_id) right after new_with_revision
  - 4 manager-side unit tests pass (62/62 manager lib)

Next steps (this loop):
  F193: independent. Pipelined EC encode (Option B, 2-chunk inflight),
        chunked recovery/copy_extent, jemallocator with dirty_decay_ms.

Pre-existing flaky tests carried unchanged from F190 baseline:
  - 2 partition-server timing tests (f099i_d1_fast_path_no_fu_allocation,
    f099i_fast_path_inactive_under_batch)
  - 7 manager integration tests (partition_server_put_get_and_split_flow,
    f029/f030/f031, f037 — flush/compact/split flow)
