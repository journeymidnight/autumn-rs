Date: 2026-06-11
TaskStatus: completed
Task scope: F261 Stage-1 — SST on-demand paging + bounded global block
  cache + streamed WAL replay. LAST item of the P15 ledger
  (F258/F259/F260 all completed earlier).

=== TWO LISTS REQUIRED BY THE LONG-TASK HARNESS ===

  Already implemented (this commit):
    - SstSource::{Resident,Paged}, into_paged, read_block_via (global
      bounded BlockCache; miss -> row_stream read w/ F258 rotation),
      open_paged_from_meta (meta-tail-only recovery open), materialize.
    - BlockCache: sampled-LRU, (extent_id, abs_off) keys,
      --sst-block-cache-bytes (default 512MB), hit/miss stats. No
      compaction invalidation NEEDED: extent ids never reused.
    - Async read paths: get/head/GC-liveness via lookup_in_sst_via
      (snapshot readers -> drop borrow -> await; F250 hop mirrored).
    - Sync iterator paths (range/do_compact/unique_user_keys):
      materialized_for_iteration (transient resident copies).
    - All 3 producer sites (flush/compact/recovery) emit Paged readers.
    - WAL replay STREAMED in 64MB chunked-carry (decode_records_chunk)
      — pre-existing whole-extent read was the real O(dataset) RSS
      source at restart. Chunks read via NEW
      StreamClient::read_committed_bytes_from_extent (committed-clamp:
      sealed_length / min-commit probe) — plain explicit-length reads
      walk past the seal into replica-local speculative bytes and trip
      StaleVpOffset, wedging partition open (e2e-hit, fixed, re-verified
      on the same dataset: 8/8 open, byte-compare pass).
    - coco fixes (2 rounds; 3 P0 + 2 P2 + 1 P3): lookup_in_sst_via
      error propagation (read error != miss; GC must not punch live
      VPs); GC paged-liveness await-window race — post-await snapshot
      VALIDATION loop (active/imm appearance or sst_readers ptr change
      -> redo lookup; no await between validation/verdict/seq-alloc).
      NOTE the first-cut "skip if key in active/imm at seq-alloc" was
      itself a data-loss P0 (mem branch IS the normal live-VP rewrite
      path) — replaced. Paged read_block_via bounds checks; get/head
      retry-once on readers-changed (compaction-truncate TOCTOU);
      range SST key-range pre-filter before materialize; CLI rejects
      invalid --sst-block-cache-bytes loudly.
  Not implemented (recorded for Stage-2):
    - Async TableIterator/MergeIterator (removes range/compact
      materialization cost).
    - Diag seq_opt/fullscan report miss on paged readers.
    - perf-check read-only mode for cleaner cache-bound measurements.

Current summary:
  ~14GB dataset, 8 partitions: idle post-recovery RSS 2,380MB
  (replay-window bound, dataset-size-INDEPENDENT; 18-28GB before).
  Byte-correct across multiple restarts (small/8M/range), 0 errors;
  cache delta under read storm +240MB < 512MB cap; perf in-band
  (4K w 27-64K / r 0.5-1.2M; 8M w 103.6 / r 500.4). After the 8M bench
  grew the set to ~22GB: post-fix restart 8/8 partitions open, idle RSS
  3,877MB, 4K w 64.1K (baseline 63.2K). 160+72 lib tests.

Main gaps:
  - Stage-2 items above; range over huge paged SST sets pays transient
    materialization (documented).
Next steps:
  P15 ledger complete. coco verdict folded before commit.
