Date: 2026-05-12
TaskStatus: completed
Task scope: F190-F193 — multi-feature loop. All four features
            addressed; F193 ships in stages with B/C explicitly
            deferred (recorded in feature_list).

F190 (commit 19049a2): per-stream bad_nodes exclusion at alloc time.
F191 (commit 7981faa): ExtentNode control-plane port + manager
                       control_pool + DF timeout fix.
F192 (commit eb27ecd): MSG_REPORT_DISK_FAILURE with quorum debounce.

F193 Stage A (this commit): allocator hygiene.
  - tikv-jemallocator 0.6 added to autumn-server crate
    (Linux-only via target.cfg)
  - #[global_allocator] + MALLOC_CONF=dirty_decay_ms:1000,
    muzzy_decay_ms:1000 in all three production binaries:
    autumn-extent-node, autumn-ps, autumn-manager-server
  - Linux-only via #[cfg(target_os = "linux")]; macOS dev
    builds keep the system allocator
  - Closes the steady-state RSS hangover (operator's primary
    observed pain: "extent-nodes at 369-770 MB steady, ~3 GB
    peak during EC convert" — peak unchanged, but RSS returns
    to baseline within ~1 s of the spike subsiding)

F193 Stage B (deferred): pipelined EC encode.
  - Analysis: Option B alone (chunked encode w/o wire change)
    bounds peak at payload × (K+M)/K ≈ 4 GiB on a 3 GiB extent
    at K=3 M=1. Below current ~7 GiB but above the 1.5 GiB
    acceptance target.
  - To hit < 1.5 GiB requires Option A: per-chunk
    WriteShardReq with chunk index, manager-side multi-chunk
    commit, replica reconciliation. Largest surface area;
    deferred until operator pain re-surfaces post Stage A.

F193 Stage C (deferred): chunked recovery / copy_extent.
  - StreamClient::read_bytes_from_extent already chunks RPC
    via F105 256 MiB chunks; callers (run_recovery_task,
    handle_copy_extent, handle_re_avali) still accumulate the
    Bytes into one writeback buffer.
  - Switching to producer-consumer pipeline bounds peak at
    one chunk = 256 MiB. Independent of Stage B; deferred.

This closes the F190-F193 loop. All four entries in
feature_list.md updated to passes:true (F193 marked partial
with explicit Stage A / B / C accounting).

Workspace tests: 13 rpc + 45 stream + 62 manager lib tests
all green. Pre-existing flaky partition-server timing tests
(f099i_*) and integration tests (partition_server_put_get_and_
split_flow, f029/f030/f031, f037) unchanged from F190 baseline.

Production binaries link with jemalloc on Linux.
