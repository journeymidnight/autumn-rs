Date: 2026-06-11
TaskStatus: completed
Task scope: F262 — F261 Stage-2: async SST iterators; delete the
  range/compact/split whole-SST materialization path.

=== TWO LISTS REQUIRED BY THE LONG-TASK HARNESS ===

  Already implemented (this commit):
    - AsyncTableIterator/AsyncMergeIterator (iterator.rs): seek/next/
      rewind are async + Result (block-read errors PROPAGATE — the sync
      iterator swallowed them into an unread field, which would silently
      truncate compaction output once reads became network-backed).
    - FetchMode::Cached (range: per-block via global BlockCache) vs
      FetchMode::Window(8MiB) (compact/split: sequential bulk windows,
      cache-bypassing = scan-resistant, RocksDB fill_cache=false
      analogue).
    - reader.rs: window_span (pure, unit-tested) + read_blocks_window
      (one RPC per window, coco-P2 bounds discipline) +
      decode_block_from_window. materialize() deleted.
    - do_compact: window-merge directly over paged inputs;
      materialized_for_iteration deleted.
    - handle_range: Cached async iteration; Stage-1 SST key-range
      pre-filter kept; merge.next() error -> Internal.
    - unique_user_keys: async over (mem_items, readers, sc) snapshots
      taken under ONE brief borrow (note 15); split call site updated.
  Not implemented (recorded):
    - Diag seq_opt/fullscan still report miss on paged readers
      (diagnostic-only surface).
    - perf-check read-only mode.
    - Compact read-side double-buffering (prefetch next window during
      merge) — only if compact wall-clock ever matters.

Current summary:
  e2e on ~60GB / 8 partitions / TCP 3disk: major compact over the
  window path OK (input=2 tables, kept=129,723, output 511MB; RSS
  plateau +2GB = output builder transient, read side no longer
  materializes); ls/range OK; merge(55,62) + split(13) exercised the
  async key scan (mid=!pc_0_9768); restart recovery 8/8, f262small +
  f262big (put-stream, get-stream byte-compare) exact; recovery RSS
  1,859MB (dataset-independent); fresh-cluster 4K write 70.4K
  (baseline 63.2K), reads 1.07M ops/s. 162+72 lib tests green.
  NOTE: CLI `get` returns the 29-byte stripe meta for put-stream keys
  BY DESIGN (F186 client-side striping; resolution is in get_stream) —
  byte-compares must use get-stream.

  coco round (2 P1 + 1 P2 + 1 P3, all fixed): window_span block-offset
  monotonicity check (corrupt meta could wrap win_len into a huge read);
  range whole-scan retry-once on readers-changed (compaction-truncate
  exposure is longer with on-demand reads than with materialization;
  scan body extracted to range_scan_sst_merge); split memtable sample
  moved AFTER the SST scan (sst_user_key_versions +
  finalize_unique_user_keys — writes landing during the scan count for
  the <2 check and mid selection, pre-F262 timing parity);
  .skillopt-sleep/ gitignored.

Main gaps:
  - Stage-2 leftovers above (diag, read-only perf mode, prefetch).
Next steps:
  - coco verdict folded before commit; P15 + Stage-2 complete.
