Date: 2026-09-20 (2)
TaskStatus: not_completed
Task scope: F-LANCEDB-PY-NATIVE — Python LanceDB reaches Autumn through an autumn:// ObjectStoreProvider instead of a FUSE mount. Code written; NOT verified, because nothing here can compile it. Status is not_completed per rule 5.
Why this exists: the adapter had two code paths, and only one was native. Rust hands LanceDB a store OBJECT through ObjectStoreParams.object_store — a field only a caller compiled against lance can reach. A prebuilt wheel never sees it; Python resolves a store from the URL SCHEME inside its own bundled Rust core. So Python's only route was a FUSE mount, which docs/lancedb_validation.md itself marks as the weaker path (nontransactional multi-key metadata, per-mount serialization, single-mount only).
WHAT WAS VERIFIED BY READING lance v13.0.0-beta.6 AND lancedb df5709ef, not inferred: ObjectStoreProvider (lance-io object_store/providers.rs:42) and ObjectStoreRegistry::insert (:418, takes &self) are public; Session::store_registry() (lance/src/session.rs:301) reaches a live session's registry; ObjectStore::new(Arc<DynObjectStore>, Url, ...) (object_store.rs:1800) is a public constructor. Therefore NO lance fork is needed — the patch is confined to lancedb-python. The ObjectStore struct's own fields are mostly private, so the struct-literal form the in-tree providers use is not available out of crate; the public constructor is the only door.
THE SECOND REGISTRATION SITE, which is the easy thing to get wrong: lancedb.connect(session=None) does NOT go through the Session class. python/src/connection.rs:926 only set a session when one was passed, and the builder's own fallback is Arc::new(Session::default()) (verified at database/listing.rs:464,569,644) carrying the stock registry. Patching Session alone would leave the default connect() unable to resolve the scheme. Both connect entry points now install the provider, and the demo takes --with-session precisely so both paths get exercised.
URL shape, and why the scope is NOT in it: the manager rides in the authority (autumn://host:port/path) so a dataset URI is self-contained. The scope is a storage option because new_store receives the TABLE url — autumn://mgr/objects/demo/vectors.lance gives no rule for where scope ends and object path begins, and a segment-counting guess writes to the wrong prefix silently when wrong. A missing scope is an error, not a default. The scope is also folded into calculate_object_store_prefix, which is the registry cache key (providers.rs:290) and build_store's metrics base. CORRECTED after review: it does NOT reach the ObjectStore struct's own store_prefix field, because ObjectStore::new resolves that through a PRIVATE static registry that has no autumn entry (object_store.rs:1795-1812) — so that field stays scope-less and every store creation logs a "Guessing that object store prefix is" warning. Saying it was "the metrics label" was half true.
Verification actually performed, and its limit: the provider's pure logic was EXECUTED — helpers extracted with a stubbed error type, 6 tests green (manager from authority, a portless manager refused rather than guessed, missing/blank scope refused, scope trimmed, two scopes get different prefixes, credential file parsed as <principal>\n<hex> with a non-hex second line refused). The lancedb patch applies cleanly to df5709ef and both patched files parse under rustfmt. The Python demo was run with a stubbed lancedb: its CLI works and it emits exactly autumn://10.0.0.5:9001/lancedb plus {autumn_scope, autumn_credential_file} — the shape the provider's tested parser accepts. build_patched_wheel.sh was driven end to end with a fake maturin: it applies the patch, restores the checkout via its EXIT trap, and BOTH guards were confirmed to exit 1 (dirty checkout, wrong revision).
NOT VERIFIED, and this is the honest gap: nothing was COMPILED. The provider needs lance-io; the wheel needs lance + DataFusion + pyo3. Neither builds on this macOS box. So the provider crate has never been type-checked against lance's real signatures, only written against them as read from source. No Cargo.lock exists for provider/ for the same reason. No wheel was built, no cluster was contacted, and every acceptance row in F-LANCEDB-PY-NATIVE is still open.
The lancedb checkout at ../lancedb was used and RESTORED: main @ df5709ef, zero dirty files. The patch lives in the repo, not in that tree.
DECISION (user, after review): drop the online-patch machinery and FORK lancedb. The patch form existed only to avoid touching upstream, and the commit-handler finding showed that constraint never held — the change is not confined to the Python bindings. patches/ and build_patched_wheel.sh are deleted; the work lives on branch autumn-native (570c0ae3) in the lancedb checkout, with main untouched.
WHAT THE REVIEW CAUGHT, all confirmed by re-reading the source myself, not taken on trust. (1) HIGH, and mine: autumn-op principal-create prints a LABELED pair (`principal:` / `credential:`, autumn_op/main.rs:559-560); my hand-written parser assumed two bare lines and would have failed every authenticated open on "not hex". Fixed by calling the client's existing read_credential_file, which autumnfs already uses — PROVEN here by a throwaway test against crates/client, since that crate does compile on this box: the labeled form parses. That also deleted decode_hex and with it a panic on a non-ASCII second line. (2) HIGH: lance picks a commit handler from a hard-coded scheme table, `_ => UnsafeCommitHandler` (lance-table commit.rs:1193-1261) — an unconditional manifest put. Python writers would have bypassed the adapter's CAS entirely. The Rust demo was safe only because it set ConditionalPutCommitHandler by hand. The fork now sets it for autumn:// on BOTH the open and create paths. (3) I had missed a THIRD connect entry point: connect, connect_namespace_client AND connect_namespace are all exported (python/src/connection.rs:872,944,1003); only the first two were covered. (4) build_store applies params.object_store_wrapper itself, so passing it to ObjectStore::new ran it twice — a mirroring wrapper would have mirrored twice; now None.
The acceptance gained a row it did not have: two Python processes appending concurrently must produce a CommitConflict. The old "并发 reader+writer" row has ONE writer and could never have seen finding (2).
Next steps: on Linux — cargo check the provider crate against lance (this is where signature errors will surface), generate its lock, run build_patched_wheel.sh for real, then the four acceptance rows: both session paths, the removed-registration ablation expecting "No object store provider found for scheme: 'autumn'", and a Rust-side list of the scope to confirm the bytes landed in Autumn. Only then does this become completed. Deleting fuse_demo.py stays blocked on that, AND on moving the linkat regression into crates/fuse, which has no tests/ directory at all today.

Date: 2026-09-20
TaskStatus: completed
Task scope: F-SCHEMA-HOMES — Scope 4 only: the per-extent payload-location byte gets three homes. The other four scopes (the 8 persisted manager types, exhaustive conversions, the MgrRegionInfo/MgrNamespace three-way split, the replacement guard) are untouched and the entry stays `passes: false`.
Current summary: One byte says WHICH FILE holds an extent's payload (`extent-{id}.dat` = 0, `extent-{id}.shard{i}` = 1). It has THREE carriers — the wire, the EN's `.meta` byte 41, the manager's `extentLayout/<id>` etcd value — and ONE lenient wire function governed all three: `PayloadLocation::from_byte` folded every unrecognised byte to `InDat`. Each carrier now parses its own byte, and none of them folds.
WHY THE OLD DOC WAS WRONG, both halves. It said an unknown byte "can only come from a peer that knows a location this build does not, and the safe reading is the pre-existing layout". (1) `InDat` is not the absence of an answer — it is a POSITIVE claim that `.dat` holds the payload, and `ExtentEntry::holds_payload` exists in this very file because serving `.dat` to a request for a shard "returns shard bytes as a whole value, the exact corruption the location field exists to rule out". The fold hands that corruption through the check built to stop it. (2) The old rule was not even applied consistently: byte 2 folded to `InDat` on the wire while two raw-byte comparisons read it as "not InDat" (extent_node.rs:3932) and "not InShardFile" (:5593) — one byte, three answers.
What the REVIEW corrected, and it was the sharpest finding: my replacement premise was ALSO incomplete. I wrote that a differently-versioned peer cannot exist because `cluster_peer_compat_check` demands exact equality. True for manager/PS/EN — and irrelevant to the carrier that matters most, because an embedded CLIENT sends `ReadBytesReq` straight to an extent node on the default-on direct read, and the EN has NO hello and NO admission gate at all (verified: zero matches for client_hello/admit_* in extent_node.rs). What actually holds is a chain: a client above the cluster's ceiling never obtains the redirect descriptor a direct read needs, because the manager and PS refuse it first. Both doc sites now say that, and name the case whoever adds a third location must re-check (a wire-45 client is in-window against a wire-43 cluster).
Shape: `from_byte` -> `from_wire_byte(b) -> Option`; `PayloadRef::for_extent` takes a RESOLVED location, not a byte; `ReadBytesReq.payload_location` is typed `PayloadLocation` (the message is hand-coded fixed-layout, so only encode/decode touch the wire byte) and `decode` refuses an unreadable one ONCE, which is what keeps `payload_ref()` infallible on the EN's hot path; `ExtentInfo::payload()` is the single reader of the published byte. `parse_meta`'s V2 branch refuses the whole record on a byte outside what `EXTMETA\x02` defines, which routes it through the EXISTING META-FAILCLOSED quarantine rather than inventing a mechanism. `decode_extent_layout_kvs` is fail-loud (unreadable byte / empty value / unparseable key all refuse leadership) and the manager's in-memory map now holds the RESOLVED location, so there is no second parse to re-answer the question. ABSENT is still `InDat` everywhere — that is the migration story, untouched.
BEHAVIOURALLY INERT TODAY, and that is stated rather than implied: only two locations exist, and no in-tree writer can produce a third byte (every store goes through `as_byte()`; the pre-field V2 writer zeroed byte 41). This changes what happens the day a third is added, and what happens on a rollback onto data a newer binary wrote. It is not a defensive fix in the rule-16 sense — it deletes a silent default, adds no timeout/retry/rollback, and every new branch is a refusal.
Verification: workspace check --all-targets clean, warnings identical to the stashed baseline; clippy rpc+stream+manager identical to baseline (one needless-borrow I introduced was fixed by HOISTING the resolve out of two per-slot read loops, which is also one resolve per read instead of per replica). Lib: rpc 87, stream 203, manager 381, PS 253, client 60, fuse 63, common 16. Integration: stream payload_location 13, ec_staging 3, placement_cleanup 10, extent_restart_recovery 3, shards 5, extent_delete 3, delete_recovery_race 1, sealed_truncation_guard 2; manager ec_integration 4, ec_failover 1, ec_participant_meta_seal 1, ec_staging_reclaim 1, ec_replay_selfheal 1, offline_reconcile 4, system_extent_recovery 8, system_corrupt_replica_rebuild 4, e2e_lifecycle 11, system_bulk_verification 4, integration 13, system_ps_failover 5, physical_deletion 4, scrub_isolates_rot 1, silent_corruption_rot 3; rpc client_surface_freeze 8, negotiation_freeze 3.
Four ablations, each confirmed RED then restored: removing the `parse_meta` guard (the EN test then reads CODE_OK — it SERVED the `.dat` bytes, which is the corruption itself); restoring the fold in `decode_extent_layout_kvs`; restoring the fold in `ReadBytesReq::decode`; and dropping an unparseable etcd key quietly. The EN test is a PAIR, not an assertion about state: the same fixture with byte 41 = 0 serves the bytes and with byte 41 = 2 is refused, CRC recomputed both times so nothing but the location byte can decide.
The review's second finding, fixed: `a_location_this_build_cannot_read_refuses_leadership` pinned the DECODER while the thing that matters (leadership refused) lived at the replay call site, so a later `.unwrap_or_default()` there would leave the test green and the mechanism dead — the shape this repo has been bitten by. The key parse moved INTO `decode_extent_layout_kvs`, so a test of that function now covers the whole decision replay makes and only a `?` separates them; the call site shrank to three lines.
NOT A REGRESSION, measured because it looked like one: `extent_pipeline::cq_flushes_fast_ops_while_slow_op_runs` failed 3/3 with the change and passed 3/3 on a stash. n=20 each says otherwise — baseline mean ratio 0.5070 / median 0.510 / 14 of 20 over the 0.5 threshold; with the change 0.5035 / 0.510 / 12 of 20. The baseline is marginally WORSE. The test straddles its own threshold on this 192-core multi-tenant box (see project_local_perf_baselines_stale_multitenant). Its threshold was deliberately NOT tuned — that would hide the noise rather than report it.
Main gaps: Scope 1/2/3/5 untouched, and the first Acceptance row (swap only the manager binary on a live cluster) has nothing to run against yet. The refusal is spelled out at seven call sites instead of one, which is forced by doing Scope 4 first — `ExtentInfo.payload_location` stays a `u8` because `ExtentInfo` is an rkyv struct on the client surface, so retyping it is a client-facing break; the stream guide says the fan-out is temporary and must not be "tidied" back into a shared default. The sibling `extentCorrupt/` replay still drops an unparseable key quietly (manager lib.rs), which this change's own argument now implicates — a dropped mark leaves the extent at RF-1 forever — but it is a different key and was left for its own change rather than scattered into this one. No live-cluster run: nothing here is reachable on a cluster that has only ever moved forward.
Next steps: F-SCHEMA-HOMES Scope 1+2 — the 8 persisted manager types into `crates/manager/src/persist/` with their own magic/version and exhaustive conversions. Sizing is done: 7 of the 8 are ALSO wire message fields, `crates/common/src/store.rs` holds the wire structs as the manager's in-memory state (so the split forces a decision about what `MetadataState` holds), MgrTenantAccount is the only one never on the wire, MgrAuditEntry is a 9th, and there are ~40 encode sites. An envelope (magic + version) must dual-read bare-rkyv legacy values or the third Acceptance row fails on day one.

Date: 2026-09-19 (8)
TaskStatus: completed
Task scope: F-LANCEDB-OBJECT-STORE in feat/lancedb-object-store worktree.
Current summary: Native object_store 0.14.1 adapter, immutable generation-qualified chunks, ranged/multipart IO, paged listing, offline vacuum; SDK/PS compare_put uses the partition write actor and existing WAL (wire 44). FUSE linkat support fixes the real Lance manifest EPERM failure. Native LanceDB lifecycle/concurrent append, FUSE lifecycle/concurrent reader-writer, and matched MinIO workload verified on H200-1.
Verification: Final Linux run at fcfc51f: native-final.exit=0 (210 final rows, 16 stored objects, vacuum 21 chunks), fuse-final.exit=0 (includes explicit link collision/source-unlink checks), contract-final.exit=0 (6 tests, 4.37 s). Final native demo verifies empty dedicated scope and table-only cleanup. Earlier local client 57 / partition-server 253 / RPC 69 library tests passed; adapter clippy has no adapter warnings. CAS ablation fails with two winners when the server comparison is bypassed, restored green.
Performance: debug/warm-cache smoke only, RF2 Autumn vs single-copy MinIO. 1 MiB range read 1828.4 vs 1416.6 MiB/s; put 320.5 vs 774.0 MiB/s. 1100-fragment list P99 29.07 vs 54.54 ms (100 samples). Final correctness-run list p50 26.185 ms, max 26.842 ms (20 samples). Full conditions in docs/lancedb_validation.md; not production A/B.
Isolation and cleanup: All remote work is under /data08/autumn-lancedb-20260919-01a0bb27 inside dongmao-autumn. Existing checkout, targets and services were untouched. Source sync uses git bundle into a separate checkout. Final cleanup verified mount=false and inactive task PIDs: test_cluster 3634637, FUSE 3726318, MinIO 3653969. Code, data and logs retained for inspection. No broad process-name kills or shared cluster.sh operations.
Access: goto H200-1 first runs ORTHRUS_REGION=I18N-OG orthrus-cli demand fdbd:dc62:3:302::14 -t24, then ssh. Repeating that authorized temporary-root step succeeded; a task-specific SSH ControlMaster avoided repeated jump authentication during final verification.
Limits: Payload GC is offline; FUSE namespace metadata is not a multi-key transaction; demo injects store per table, not an autumn:// connection provider. Initial performance data does not compare equivalent replication/durability configurations.
Next steps: ready for review/integration from the feature worktree. No deployment or main-branch merge performed. Final evidence: TASK_ROOT/logs/{native-final,fuse-final,contract-final}.log and corresponding .exit files, plus final-cleanup.json.

Date: 2026-09-19 (7)
TaskStatus: completed
Task scope: F-CLIENT-WIRE-COMPAT — close it out. `passes: false` -> `passes: true`, after an independent audit said do NOT flip it and named the row that was not met.
Current summary: Three things were missing and are now done: the batch path from an old client, a real python wheel, and the "合回单常量" ablation. The audit then found a fourth, and it was the one that mattered.
WHAT THE AUDIT CAUGHT, and it was right: acceptance row 3 ("绕过客户端自查的路径不再能写：模拟取 GetClusterIdResp 的 transport 失败，断言写入仍被服务端拒绝") was NOT satisfied. Nothing anywhere in the tree made that fetch fail. The two nearest tests both build a `ClusterClient` by hand, so `connect` never ran and there was no self-check to bypass — which proves something weaker than the row asks. New test `a_client_whose_self_check_was_skipped_is_still_refused_by_the_server` (`crates/client/src/connection_tests.rs`): a mock manager that CLOSES on `MSG_GET_CLUSTER_ID` but serves routing, so `connect` runs and its `if let Ok` genuinely skips. The witness for "it skipped" is a PAIR, not an assertion about state: the same fixture with that call SUCCEEDING and reporting a window this client is outside of refuses at connect, so one fixture with one difference gives opposite outcomes. (My first witness was `negotiated_cluster_wire == 0`, which is wrong — `say_hello` sets it too, so it read 44 and the test failed for the right reason.) Ablation: removing `say_hello` from `get_ps_client` reds it. An earlier ablation attempt — emptying `is_client_surface_ps_msg` — did NOT red it, and that is recorded in the test's own doc: the refusing peer is a MOCK, so this test pins the COMPOSITION (skip + per-connection hello + terminal client handling) and the real-server half stays with the partition server's `a_client_outside_the_window_is_refused_before_its_write_reaches_the_partition`.
The other two rows, done properly this round: BATCH byte-exactness comes only from the wheel — `perf-check --bulk` counts `Ok(Some(_))` and never compares bytes, so the 226k-op Rust run proves the opcodes are served, not that they are correct. The WHEEL is a real maturin build from `2568ed7`, installed in a venv; put/get at 2 KiB and 9 MB, `batch_put_from` + `batch_get_into` verified byte-exact across all 64 values, `batch_head`, `range`, `batch_delete`. It is demonstrably not a forged interval: with the floor at 44 the server answers "the client speaks 43", which is the number the wheel itself reported.
"合回单常量" needed both halves, and the halves hide each other: collapsing the CONSTANTS alone leaves two predicates, collapsing the PREDICATES alone leaves two constants; only together do they reconstruct the pre-split shape. Collapsing the constants is now a BUILD failure (the `const` guard), not a red test — I had written that down wrong. Collapsing the predicates reds three tests, and doing it properly exposed a test whose name outran its fixture: `a_stale_server_inside_the_client_window_is_still_refused` put the stale server at `WIRE_VERSION + 2`, OUTSIDE the window, so it survived the very ablation it exists for. It now uses the real window with the server at the FLOOR, and reds.
Preserved as a script rather than a session note: `scripts/client_window_verify.sh`. Rows 1 and 4 need TWO client builds at DIFFERENT wire versions, which no `cargo test` can produce, so a one-shot manual run proves nothing the next time somebody moves a number. It finds the newest commit at the window's floor by itself, builds a worktree, runs the data plane, then raises the floor and asserts the same binary is refused. Two of its own defects were found by running it: (1) `set -o pipefail` plus `client | grep -q` — a refused client exits non-zero, which sinks the pipeline even when grep matched, so a correct refusal read as "NOT refused"; the first recorded run was a false FAIL and the audit rightly refused to accept a script whose only preserved run was red. (2) It edits a TRACKED source file and kept its only backup inside the directory its own exit trap deletes — interrupt it between the ablation and the restore and the tree is left at floor 44 with two `const` guards commented out and no backup, on top of uncommitted work. The backup now lives outside that directory, is restored FIRST in the trap, and a stale one refuses the next run instead of being overwritten.
Written into the Status because it is the honest limit of what shipped: **today this gate can only refuse clients NEWER than the cluster.** The floor is pinned at exactly 43 by two `const` assertions and a silent connection is read as 43, so "below the floor" is unreachable in production. The mechanism is right and the direction the Trigger was about — an old wheel refused for a change it cannot see — will be served the day the floor actually rises.
Verification: workspace check --all-targets clean; clippy rpc+manager+partition-server 72 and client 11, both matching the stashed baselines. Lib: rpc 86, client 60 (+1), PS 253, manager 378. `client_wire_admission` 5. `scripts/client_window_verify.sh` green end to end, twice, and it leaves no backup file, no worktree, no process and the constants where it found them.
Main gaps: §7 (call sites serving two forms) is not a precondition for the window and stays open. `MSG_GET_REGIONS` outside the gate and the operator surface uncovered, both unchanged and both outside this entry's Scope and Acceptance. Every deployed PS and EN must move to 44 together, folded into the stop-the-world already pending at 35 -> 43.
Next steps: F-SCHEMA-HOMES, which is still 仅立账、未动工 — and carries the one real correctness debt in that area (`PayloadLocation::from_byte`'s "unknown -> InDat, never an error" is a persistence decision living on a wire type; a rollback would silently serve shard bytes as a value).

Date: 2026-09-19 (6)
TaskStatus: completed
Task scope: F-CLIENT-WIRE-COMPAT — OPEN THE CLIENT WINDOW. `WIRE_VERSION` 43 -> 44, `MIN_CLIENT_WIRE_VERSION` held at 43. Window `[43, 44]`. Verified on a live cluster with a client binary built from the older commit.
Current summary: The user asked for the ceiling to be raised. I first argued for lowering the floor to 42 instead, implemented it, got it green, and REVERTED it: the independent review proved it unsafe, and the user's instruction was right.
WHY LOWERING THE FLOOR IS UNSAFE, which is the finding of this session and is now a compile error: peer equality is enforced by each peer POLICING ITSELF at startup, so what decides whether a stale server joins is the predicate compiled into THAT server. Before wire 43 that was an interval OVERLAP (`wire_compat_check`, deleted in `f17f533`), and those binaries read `wire_version_min` as a PEER floor — because when they were written it WAS one. A wire-42 partition server computes `[42,42] n [43,43] = empty` and refuses itself today; against a reported `[42,43]` it computes `{42}` and JOINS. Nothing catches it afterwards: `RegisterPsReq`/`RegisterNodeReq` carry no version and sit outside the client-surface gate, and a silent connection is read as 43 regardless. That is a mixed-version cluster on the INTERNAL plane — the one thing stop-the-world exists to make impossible. New constant `FIRST_WIRE_VERSION_WITH_PEER_EQUALITY = 43` plus `const _: () = assert!(MIN_CLIENT_WIRE_VERSION >= ...)`, so the floor cannot go below 43 without a compile error (ablated: setting it to 42 fails the build). The second `const` keeps it from going ABOVE 43 by accident, since a floor above `WIRE_VERSION_WITH_CLIENT_HELLO` refuses every silent connection at once; that one says DELETE ME DELIBERATELY rather than describing a reason, after the review showed my stated reason (the extent node's startup check) was false — the EN uses a `ClusterClient` and does send a hello.
Raising the ceiling is safe in all four directions and that was checked, not assumed: a pre-43 peer's overlap misses a window starting at 43; a 43-or-later peer demands exact equality and never looks at the floor; a 43 client's `contains(43)` passes; a 42 client is correctly refused.
LIVE ACCEPTANCE RUN — the criterion this feature has carried unrunnable since it was written. `git worktree` at 2568ed7 (wire 43), built `autumn-client` there, ran it against a wire-44 cluster from the current tree (manager 19801 / EN 19901 / PS 20001, 1+0): put, get, head, 9 MiB bulk put, EN-direct read, range, delete — all OK, both 2 KB and 9 MB values byte-exact on the way back. CONTROL: floor moved to 44 (the two `const` guards bypassed for the run), same binary, refused with "this cluster speaks 44 and serves clients [44,44], the client speaks 43 — that client is older than the window this cluster still serves — rebuild its image from a commit inside that window". So acceptance criteria 1 ("an internal bump no longer reaches clients") and 4 ("an in-window old client actually works") are MET, and by a real binary rather than a forged version number. Environment notes for the next run: the EN needs `--cpuset` or it tries to bind 192 shard ports and fail-stops on the first collision; `autumn-client` KV commands need `--namespace`.
Also fixed from the review, all VERIFIED by it: `MSG_APPEND_CHAIN` was NOT "EN<->EN" — the wire-42 partition server sent it (`459520b:crates/stream/src/client.rs:2191`). The Trigger text keeps the wrong phrase (rule 8 forbids rewriting requirement text) and the Status paragraph corrects it explicitly. The three new `lib.rs` tests were rewritten: the old ones modelled OUR predicate where the deciding one is the stale peer's, so `the_floor_never_hands_a_pre_equality_peer_a_ticket` now runs the 42-era overlap verbatim against every pre-equality version. `the_client_window_is_open_and_the_floor_is_where_it_belongs` pins both literals WITH a message telling the next bumper to move 44 and leave 43 alone — a bare `assert_eq` on a literal is the pin-refresh reflex the freeze file warns about. `crates/rpc/CLAUDE.md` said `MIN` was 43 while `WIRE_VERSION` moved, and "while the two are equal the answer is always yes" survived the window opening. `client_hello.rs` claimed the swap is "invisible to every test in the tree", which the live manager test now sees.
`autumn-op cluster-version` gained the positive annotation (it only ever said "window shut"); both its forms and `upgrade-version`'s refusal were re-recorded in `docs/ops.md` from a real manager, and `crates/server/CLAUDE.md` now says what the command reports.
Verification: workspace check --all-targets clean; clippy rpc+manager+partition-server 72 and autumn-server 48, both matching the stashed baselines. Lib: rpc 86, client 59, PS 253, manager 378, stream 202, common 16, fuse 63. rpc integration 8 + 3 + 2 + 3. Manager integration: client_wire_admission 5, graceful_shutdown 4, e2e_lifecycle 11, system_bulk_verification 4, system_ps_failover 5. Ablations red-then-restored: floor to 42 (now a BUILD failure, which is the strongest form this could take); the live control run above.
Main gaps: every deployed PS and EN must now move to 44 together — but the stop-the-world was already pending at 35->43 (`BUG-WIRE36-UNDEPLOYED`), so this adds a number to that window rather than a new one. §7 (call sites serving two forms) remains, and is NOT a precondition for the window — it is what a CLIENT-FACING change will need. `MSG_GET_REGIONS` residue and the uncovered operator surface unchanged. The review's one open inference: no other server-side path was read for whether it would reject a stale peer after registration.
Next steps: the remaining acceptance rows are the ablation list and the two-form work. The window is now something that can be regression-tested rather than reasoned about. Nothing pushed since 2568ed7.

Date: 2026-09-19 (5)
TaskStatus: completed
Task scope: F-CLIENT-WIRE-COMPAT — make the open window (`lo < hi`) reachable, and consolidate the version pair the fleet actually depends on. Small production change plus tests and docs.
Current summary: `admit_client_within(lo, hi, v)` / `admit_connection_within(...)` added, with `admit_client` / `admit_connection` as thin wrappers supplying the constants; `reported_wire_versions()` maps the two constants onto `GetClusterIdResp`'s FROZEN field names in one place, and the manager's four hand-filled sites now go through it; `server_hello_resp()` likewise for the hello, replacing the same pair hand-spelled at both servers. Three new tests, one removed.
The correction that set the scope, and it reversed my own previous "next step": OPENING THE WINDOW DOES NOT NEED DESIGN §7. §7 is for a client-facing change that must keep old clients working. This feature's headline claim — an internal bump no longer forces every embedded image to be rebuilt — needs only that the next internal bump leaves `MIN_CLIENT_WIRE_VERSION` alone. That is a decision plus a proof, not machinery. What actually blocks it is that nothing has ever RUN with the two constants unequal, and the server predicate reached for them from inside while its mirror `client_compat_check` took them as arguments, so the open case was unreachable.
Two of my own claims died to my own ablations, which is the argument for running them: (1) "with the constants equal the two refusal arms are the same predicate" is FALSE — the branch is `v > hi`, on the input, not the bounds, and swapping the two pieces of advice reds the OLDER test too. What equality actually hides is narrower: the window's INTERIOR (no such value exists when lo == hi, and it is the only client the feature was built to keep serving) and any code reading one bound where it means the other — the refusal text printing `[{hi},{hi}]` renders identically today, and that one reds only the new test. (2) An assertion I wrote claiming to catch a swapped `server_hello_resp` does not: with the constants equal the swapped call emits identical bytes, and the ablation stayed green. That test was deleted rather than left standing as decoration.
What the review found that mattered most: I had hardened the WRONG CHANNEL. The hello's only production reader (`client/src/lib.rs`) keeps the first number and discards the second, so a swap there is a latent wrong value in `negotiated_cluster_wire` that nothing branches on — not the outage I had written into its doc comment. The "refuses everyone" failure is real but lives at the four hand-filled `wire_version_min`/`wire_version_max` sites in `manager/src/rpc_handlers.rs`, whose pair reaches every PS and EN startup check, every pre-hello client, `connect`, and autumn-op: swapped, once the window opens, `max` carries the floor and every server refuses to start while the reversed range refuses every client. Those four are what `reported_wire_versions` now serves, and its doc says why the mapping is not mechanical — `wire_version_min` carries a CLIENT floor, not a cluster minimum, so filling it from the field name alone gets it wrong.
Also from the review and fixed: my tripwire duplicated the existing one in `lib.rs` and gave the OPPOSITE instruction (add an assertion vs. delete the test), so whoever opened the window would have met two reds disagreeing. There is now ONE tripwire carrying the whole checklist of assertions that only become capable of failing once the constants differ — including a third equality I had missed, `WIRE_VERSION == WIRE_VERSION_WITH_CLIENT_HELLO == 43`: substituting `WIRE_VERSION` for the frozen introduction constant in the silent-connection default is invisible to all 12 tests today, which is exactly what that constant's own doc warns against. The refusal text also asserts `speaks 45` now, a gap the review measured. And design §4 described a `[code: u8]` prefix on the hello reply that the tree never shipped.
Verification: workspace check --all-targets clean; clippy across rpc + manager + partition-server identical to the stashed baseline (72 both ways). Lib: rpc 84, client 59, PS 253, manager 378. rpc integration 8 + 3 + 2 + 3; manager integration client_wire_admission 5, graceful_shutdown 4, e2e_lifecycle 11. Ablations red-then-restored: the refusal text printing one bound twice (only the new test reds — the discrimination is real); the cluster's own version printed as `lo`; and the two failed ones above, kept as findings rather than quietly dropped. The reviewer ran seven mutations of its own and reported which tests each reds; the table is in this session.
Main gaps: the window is still SHUT. Raising `WIRE_VERSION` while holding the floor is the act that proves the feature, and it changes what every embedded image in the field owes, so it is the user's call and was not taken. Unverified by anyone: the three benches (`ps_bench`, `ps_read_bench`, `append_bench`) connect with a raw `RpcClient` and send no hello, so they would be refused on data-plane msg_types once the floor rises above 43 — INFERRED by the reviewer, not read. `MSG_GET_REGIONS` residue and the uncovered operator surface unchanged.
Next steps: decide whether to raise `WIRE_VERSION` to 44 with `MIN_CLIENT_WIRE_VERSION` held at 43 — the first genuinely open window, which turns four of this feature's acceptance criteria from unrunnable into runnable. Everything in the tripwire's checklist becomes testable at that moment. Nothing pushed since 3a1fbad.

Date: 2026-09-19 (4)
TaskStatus: completed
Task scope: F-CLIENT-WIRE-COMPAT — make the design's §7 rule enforceable. Test and docs only; no production code changed.
Current summary: New `crates/rpc/tests/client_surface_freeze.rs` (8 tests) records the encoding of every request and response form behind a client-surface msg_type, and freezes the NUMBERING a client interprets (`StatusCode`, both `CODE_*` families, the payload selector, the lease kinds/modes/invalidation reasons). Docs in `crates/rpc/CLAUDE.md`, `docs/client_wire_compat_design.md` §7 and `docs/ops.md`.
Why this and not the rest of §7: the other four gaps (a retained form actually in the tree, §8's descriptor decline, raising the floor, letting a cluster peer identify itself) all have no content until a real client-facing change exists, so building them now is machinery for an imagined bug. This one is the opposite — it was leaking today. Steps 1 and 2 built a window mechanism whose entire value rests on a discipline that was one sentence in a design doc: nothing went red if someone edited `PutReq` in place, and "edit the struct, bump the version" is the cheap path. The freeze is what closes that, so the next client-facing change is FORCED down the two-form path instead of silently breaking every in-window wheel.
Why a byte freeze is legitimate here when the schema fingerprint was deleted, since that is the obvious objection: the fingerprint hashed SOURCE and covered the CLUSTER-INTERNAL schema, where an in-place edit IS the right answer (bump, stop the world) — so it fired on changes whose correct response was "yes, I know", and the false alarms taught the reflex of refreshing the recorded value, through which a real change later shipped. Both halves invert here. On the client surface an in-place edit is never the right answer, so a red is the rule speaking; and the record is of the ENCODING, so comments, doc edits and reordered `use` lines move nothing.
Two things a msg_type-keyed inventory misses, and both were found by looking past it: `CapClaims` is a field of no wire struct (it is rkyv-encoded into the opaque token) but the SDK decodes it out of its OWN minted token to check the namespace scope, so its layout is a client contract; and the extent-node direct read is on the client surface by the design's own §2 — `--direct-read` is on by default, so a client takes the descriptor from `GetRedirectResp` and reads value bytes straight from an EN. The review caught the second one after I had shipped a header claiming "every message an embedded client sends or decodes". §8 closing that edge from the PS side is an ADMISSION decision and says nothing about whose bytes those are; single-form makes it worse, not better, because an in-place edit is then the only change that can happen there. `ReadBytesReq` also turns out to be the tree's one message already serving two forms — discriminated by LENGTH, not opcode: `decode` reads 32 bytes as the form that predates the payload selector. Both widths recorded.
The review's other high finding was mine to have caught: coverage was per-OPCODE, so one form was enough to mark a msg_type covered, and deleting `HeadResp`'s fixture together with its recorded row left the whole suite green — `HeadReq` went on vouching for `MSG_HEAD`, and a response is exactly the half an old client decodes. Coverage is now per DIRECTION. What it still cannot check is whether a form's declared opcodes are the ones its handler serves; nothing ties those lists to the dispatchers, and that is stated rather than implied.
My own ablation then failed my own test, which is the whole reason rule 15 asks for them: the first `StatusCode` assertion was `from_u8(v) as u8 == v`, which is a tautology for any CONSISTENT renumbering — swapping `Unavailable = 5` and `AlreadyExists = 6` along with their `from_u8` arms left it green. Numbers are now pinned by NAME. A constant's value is as much a client contract as a field's offset and no encoding catches it, since the fixtures carry `code: 7` as a literal.
Also from the review, all fixed: three adjacent `DiskCapWire` bools were all `true`, so swapping two of them moved nothing — exactly the property the fixture rule exists to prevent; the decode-back test's stated rationale was incoherent (it is not there to catch a typo, test 1 does that first — it is there so a DECODER that drifted, a stricter bytecheck pass, cannot leave the encoder agreeing with the record while refusing bytes a deployed client sends); "renames leave it alone" was half-true (a rename forces a FIXTURE edit, never a recorded-value edit); "retyped field moves the bytes" is false for `u32`->`i32`; and I had narrowed the CLAUDE.md sentence about what §7 still needs without re-deriving it, so it is back to the broader wording.
The one shape that can still breed the refresh reflex is a MASS red — rkyv's archived format moving under all ~40 values through a dependency bump or a feature another crate turns on (Cargo unifies features and six crates here depend on rkyv independently). The two-form rule cannot express that; there is no per-message fix. The header now names the case and its answer outright (a MIN_CLIENT_WIRE_VERSION raise, an announced window, a rebuild of every embedded client, the table re-recorded in that same commit) rather than leaving it to be improvised under pressure, and the header's old absolute "the ONE legitimate write is the first recording" was self-contradictory against its own deletion path.
Verification: workspace check --all-targets clean; clippy on rpc byte-identical to baseline (3 lines, one pre-existing "impl can be derived"). Lib: rpc 82, client 59, PS 253, manager 378. rpc integration: client_surface_freeze 8 (new), negotiation_freeze 3, others 2 and 3. Both `docs/ops.md` commands run. Six ablations, each confirmed RED then restored: reordering two `HeadReq` fields; adding a field to `HeadResp` (compile error E0063 naming the field, not a hex diff); adding `MSG_ROLL_TAILS` to the PS surface set with no fixture; a recorded value that is not a valid encoding; deleting `HeadResp`'s fixture AND row (the review's regression — green before the per-direction fix, red after); reordering `ReadBytesReq`'s first two `u64`s (reds three tests). Plus the renumbering ablation above, red only after the by-name fix. Determinism was MEASURED rather than assumed — the same 8 tests pass in `--release`, so a flaky freeze (the one failure mode that would train the refresh reflex outright) is not sitting in here undetected.
Main gaps: unchanged from the previous entry, minus the enforcement one. `MIN_CLIENT_WIRE_VERSION` still cannot be raised — no retained form exists, so the connection version the servers record has nothing to choose between, and §8's descriptor decline is inert until the floor moves. `MSG_GET_REGIONS` still outside the gate (a PS sends it and cannot identify itself). The operator surface still deliberately uncovered. Not frozen anywhere, and the same shape: `crates/fuse/src/schema.rs` is stored metadata an older fuse binary reads — a real old-reads-new surface with no guard, out of scope here.
Second review round, on the batch written IN RESPONSE to the first review and committed without a pass of its own -- a gap worth recording, because that batch already had a proven defect (the tautological StatusCode assertion, caught by my own ablation and not by any reviewer, since it did not exist when the review ran). No HIGH. One MEDIUM: `assert_eq!(StatusCode::from_u8(9), Internal)` pinned the FOLD at the next free discriminant, so appending `Foo = 9` -- the one change that test's own comment calls legitimate -- would have gone red with a bare assert message. That is the false-red shape the file's header warns about, introduced two lines below the warning. Pinned at 255 instead. Three prose claims were false in detail and are corrected: "every fixture is a struct literal so an added field fails to compile" is untrue for the three HAND-CODED forms (`ReadBytesReq::new` and the two encoders -- a field added and filled inside them compiles, and `ReadBytesReq` is the one form that has already grown a trailing field), which is why those also carry offset-by-offset layout assertions; "both widths are recorded" was a claim, not a fact -- only the 40-byte row existed, so the 32-byte form now has its own row (no encoder emits it any more, which is exactly why it must be recorded rather than derived); and the extent-node CODE_* justification said "a client reads these on the direct path" when an embedded client interprets exactly one of them (CODE_OK) and the rest are pinned for the PS's StreamClient.
One finding I treated as a bug rather than a doc fix: the ERROR ENVELOPE (`[status_code][utf8]`, `RpcError::encode_status`) was frozen nowhere, though every embedded client decodes it on every failure -- and it is what carries THIS feature's own refusal, so a break there is a client that cannot read why it was refused. It belongs to no msg_type (it can answer any of them), which is the third thing a msg_type-keyed inventory missed after CapClaims and the EN read. Now recorded. Three more ablations, red then restored: appending a status stays GREEN (the MEDIUM fix, verified from both sides -- renumbering still reds); moving the status byte to the envelope's tail reds two tests. The review also re-derived all 41 direction tags against the actual senders and receivers and found none wrong, which was the risk of having assigned them by a name-suffix script.
Next steps: design §7's remaining half — call sites able to serve two forms — which is what the floor moving actually waits on. Nothing pushed; this is the 11th unpushed commit.

Date: 2026-09-19 (3)
TaskStatus: completed
Task scope: F-CLIENT-WIRE-COMPAT step 2 — `MSG_CLIENT_HELLO` plus server-side admission (design doc §4/§5 and the sending half of §6). Still deliberately inert; call sites serving two forms (§7) are not in this change.
Current summary: New `crates/rpc/src/client_hello.rs`: msg_type 0x5F, hand-coded fixed-layout binary (`[magic "AUH1"][client_wire_version]` answered with `[server_wire_version][min_client_wire_version]`), the two client-surface msg_type sets, and `admit_client` / `admit_connection`. The SDK sends it from `mgr_client()` and `get_ps_client()` — the two functions that OPEN connections, not `connect()`, because `rotate_manager` and the `mgr_call` error arm drop a manager connection and `mgr_client()` silently reopens it — and keeps the negotiated version in `negotiated_cluster_wire`, which the client used to discard. The manager gates in `handle_connection`'s decode loop SYNCHRONOUSLY, before the per-frame spawn; the PS gate is folded into `authz_gate` above its `!gate_active()` early return. Both constants still 43, and a silent connection is read as `WIRE_VERSION_WITH_CLIENT_HELLO` (43, frozen at the literal), so every client built before this and every internal peer is admitted exactly as before.
Two shape decisions worth keeping: (1) the PS's per-connection `principal` became a `ConnGateState` carrying the wire version beside it, rather than a second `&mut` parameter threaded through six sites — that is the shape this file has already shipped twice as a bug (`extract_part_id` and `authz_check` each landed a keyed opcode with no arm), and one struct makes the compiler enumerate every site. (2) A refusal is an ordinary error frame with `FailedPrecondition` plus text, not a code byte in the fixed layout, because a refusal that cannot say WHICH WAY ROUND the mismatch is (rebuild the client vs deploy the cluster) is not worth sending.
What the design did not anticipate: `MSG_GET_REGIONS` is on BOTH surfaces. An SDK routes with it and so does every PS's `sync_regions_once`, and a PS sends no hello — so gating it would refuse region sync fleet-wide the moment the floor rose, which is the outage the msg_type scoping exists to prevent, arriving through the set instead of through the connection. It is left un-gated, ablated, and pinned by two tests plus a note in every doc that lists the sets. The residue is a below-floor client able to fetch routing and nothing else; every data-plane message it then sends is refused. Closing it properly needs a way for a cluster peer to identify itself.
One thing I had written down wrongly and corrected before committing: "the PS and the EN are silent by construction" is true of the ONGOING internal traffic (both go through `autumn_stream::ConnPool`) but not of the extent node's STARTUP identity check, which is a `ClusterClient` (`verify_manager_cluster_id` / `register_with_manager`) and therefore now sends a hello. Harmless — an EN runs the cluster's `WIRE_VERSION` by construction, so it is always admitted, and both messages it sends are un-gated anyway — but the sweeping claim appeared in five places and is now precise in all of them.
Also fixed, pre-existing: `authz_gate`'s doc comment was welded onto `admin_ps_gate_and_strip`'s item (a function had been inserted between the doc and what it described — the same defect a review caught on `apply_df_disk_health`), so ten lines about the authz gate rendered as the admin-token helper's description.
Verification: workspace check --all-targets + `build --bins` clean; clippy on rpc/client/manager/partition-server byte-identical to the stashed baseline. Lib: rpc 82, client 57, PS 253, manager 378, stream 202, common 16, fuse 63. Integration (real binaries, so the real handshake runs): client_wire_admission 5 (new), graceful_shutdown 4, system_bulk_verification 4, system_ps_failover 5, e2e_lifecycle 11, system_namespace_layer_a 3, system_fuse_ns 3, system_status_connection_reuse 4. Six ablations, each confirmed RED then restored: the PS check moved BELOW `!gate_active()` (reds the PS test, which runs with authz OFF on purpose — that is the placement assertion); not recording a REFUSED client's version (reds "the verdict sticks for the rest of the connection"); adding `MSG_GET_REGIONS` to the client-surface set (reds the get-regions test); `connect` swallowing the refusal again (the message reverts to "cannot connect to any manager"); removing the `refresh_and_backoff` short-circuit (11 accepts and 28 s instead of 1 and 0.00 s); and never clearing the flag on a successful handshake (the client no longer recovers when its cluster catches up). The PS test drives a real `handle_ps_connection` over a real socket with a mock partition loop, so it can assert the refused write never REACHED the partition — the acceptance's own wording — rather than only that the reply was an error.
New freeze test `crates/rpc/tests/negotiation_freeze.rs` pins `GetClusterIdReq/Resp` and the hello to exact bytes, and its header prescribes the action on failure ("this is a client-facing break") and never "update the recorded bytes" — the reflex that let the deleted schema fingerprint wave a real change through. It also decodes the recorded string back into the struct, so a mistyped digit cannot be frozen in as the contract.
What the independent review found, and it was the whole value of the round: the mechanism was right but the CLIENT's handling of a refusal defeated the refusal's only purpose. (1) `connect` discarded `say_hello`'s error and reported "cannot connect to any manager" — the entry point every wheel, fuse mount, gateway and autumn-op comes through, and the one place an operator reads the message that says which way round to fix it. (2) The data path treated the refusal as transient and spent the full `MAX_PS_REFRESHES` budget: measured at 11 TCP accepts and 28 s for one `get`, then returned the refusal labelled `ConnectionError`. Both fixed with a NEW `AutumnError::WireVersionRefused`.
The fix is one choke point rather than seven, and finding that out cost a wrong first attempt worth recording: I first added a terminal arm to each retry loop's error match, which is the "scatter it everywhere" shape rule 16 warns about — and it was also incomplete, because the refusal arrives from OPENING the connection, not from the call, so it lands in `get_ps_client`'s error arm, which four of the seven loops classify with a bare `last_err = Some(e.to_string())`. My own new test caught the fifth loop (`get_range_pooled`) that both the review and I had missed. So: `ClusterClient.wire_refused`, set by `say_hello`, read by `refresh_and_backoff` (stop after the first attempt) and by a new `routing_exhausted` that REPLACED seven copy-pasted loop tails. The scattered arms were then deleted; one mechanism, not two.
Deliberately NOT a latch: a successful handshake clears the flag, and every call still makes its first attempt, so a client refused for running AHEAD of its cluster recovers by itself once the cluster is deployed. A fuse daemon is not something anyone restarts to clear a flag. Its own ablation.
Also from the review: the hello used `rpc_timeout` (30 s) where a hello on a fresh connection is by definition a first attempt, so it now uses `first_attempt_effective_timeout(0)` (5 s) — otherwise a peer that accepts and never answers stalls every new connection for 30 s before the data call with the short budget even starts. And the OPERATOR surface is deliberately uncovered by the client window (MSG_STATUS, stream/extent info, namespace_*, tenant_*, the op-ledger, autopolicy, MULTI_MODIFY_*): autumn-op ships WITH the cluster at the same commit, so a window buys it nothing — but those are rkyv structs, so a stale autumn-op still gets the cross-version decode this protects the data plane from. The trade is now written down in three places instead of being implied by an incomplete sentence ("routing and nothing else", which was false).
Main gaps: `MSG_GET_REGIONS` above. The operator surface above. `MIN_CLIENT_WIRE_VERSION` still cannot be raised — nothing serves two forms of a message yet (§7), so raising it today would refuse clients with nothing gained. The mock peer harness now answers the hello centrally (`crates/rpc/tests/support/status_peer.rs`), which keeps every existing mock's strict first-frame assertions working; one in-file mock in `crates/client/src/lib.rs` had to answer it by hand. No live-cluster run: the acceptance's "build a python wheel from an in-window commit" step needs a window that is open.
Next steps: design §7 — call sites able to serve two forms, which is the precondition for the floor ever moving. Nothing pushed; this is the 10th unpushed commit.

Date: 2026-09-19 (2)
TaskStatus: completed
Task scope: F-CLIENT-WIRE-COMPAT step 1 — split the wire constant and correct the two compat checks. Deliberately inert; the parts that open a window (a per-connection hello, server-side admission, call sites serving two forms) are not in this change.
Current summary: `WIRE_VERSION_MAX` -> `WIRE_VERSION`; `WIRE_VERSION_MIN` DELETED (with peers compared for equality, a floor pinned to the version says nothing, and a MIN/MAX pair whose MIN answers a different question from its MAX is what made the old spelling unreadable); `MIN_CLIENT_WIRE_VERSION` added, equal to it, so the window admits exactly one version. The single interval-overlap helper became two: `cluster_peer_compat_check(remote_max)` requires equality, `client_compat_check(min, max)` requires membership and refuses at BOTH ends. The PS and EN take the peer check, the SDK the client one. The manager fills the FROZEN `GetClusterIdResp.wire_version_min` field from `MIN_CLIENT_WIRE_VERSION` — field names now outlive the constants they carry, and both ends say so.
Why overlap was wrong for both, which is the whole point of the step: reporting a client floor in `wire_version_min` means an overlap test admits a stale PS or EN anywhere inside the CLIENT window, and this handshake is the only thing enforcing stop-the-world. In the other direction an in-window client computes no overlap against a cluster reporting only its own version and refuses ITSELF, so the window could never open.
Verification: workspace check + `build --bins` clean; clippy on rpc+client back to the measured 9-warning baseline. Lib: rpc 72, client 57, manager 378, PS 252, stream 202, common 16. Integration (these spawn real binaries, so they drive the startup handshake): graceful_shutdown 4, system_bulk_verification 4, system_ps_failover 5. Two ablations, each confirmed RED then restored: peer check relaxed to `>= WIRE_VERSION` reds both peer tests (including the one asserting a version a CLIENT may use is still refused for a SERVER); client check reduced to the floor alone reds the both-ends test.
What the review caught: the change itself is inert (it re-derived that the old predicate reduces to exactly the new client one, so no verdict moves for any deployable pair) but the documentation around it had gone false in several places. `GetClusterIdResp`'s own doc comment still described the overlap contract — the first thing anyone opening that field reads, and the design doc had promised a comment on both sides. The five wire-schema banners said "bump WIRE_VERSION (and set MIN = MAX)" after the mechanical rename, in the one place the bump discipline is supposed to be unmissable. One arm of the new client refusal blamed "not bootstrapped", which cannot produce it — that arm of `handle_get_cluster_id` fills both versions from the constants. Three crate CLAUDE.mds (manager, common) and docs/ops.md still named the deleted constant, and ops.md's manual steps printed output the command no longer produces. Also added: a `const _: () = assert!(MIN <= WIRE_VERSION)`, because the test pinning them EQUAL is meant to be deleted the day the window opens and nothing would then catch an inverted pair; and two dead conjuncts in the client check were dropped after the review showed `contains` already implies them.
Main gaps: nothing validates an incoming client yet — the check is client-side, skipped when the fetch fails, and no server inspects a client's version. That is the next step, not a regression. The `cluster-version --json` key names changed (`manager_wire_version_min/max` -> `cluster_wire_version` / `cluster_min_client_wire_version`); no in-repo consumer, but an external script reading them would break.
Next steps: `MSG_CLIENT_HELLO` and server-side admission (design doc §4 and §5). Nothing pushed.

Date: 2026-09-19
TaskStatus: completed
Task scope: User asked why a one-parameter change to one API still forces a stop-the-world upgrade, then asked to measure it. Analysis + two ledger entries; no code.
Current summary: Attributed all 44 wire-version intervals to the components whose source references what changed. Only 8/44 (18%) genuinely needed manager+PS+EN to move together; the EN — the expensive one to restart, since it re-scans every extent file — could have stayed up for 30/44 (68%). The two single-component buckets turned out to be different shapes: the 8 "PS only" are all client<->PS data plane (AUTH_HELLO, GetRedirectMany, the bulk batch families), which the client window already covers; the 9 "MGR only" are manager-internal, and several of them are not messages at all. `persist_extent` (crates/manager/src/lib.rs:4977) rkyv-encodes `MgrExtentInfo` straight into etcd, so manager_rpc.rs is two schemas under one version number, and the by-hand rule cannot tell a message from a persisted value.
Decision, with the option that was rejected and why: per-EDGE versioning is not worth building. The edge is not a property the code has — `ReadBytesReq` serves PS->EN, client->EN and EN->EN at once — so per-edge collapses into per-message, which is Kafka's per-API model, and 7 client-breaking changes in 45 bumps does not pay for it. It also only shortens the window rather than removing the outage, because restarting every PS is an outage regardless. The two cuts worth making are along boundaries the code really has: image lifecycle (the client window, F-CLIENT-WIRE-COMPAT) and durability domain (F-PERSIST-SCHEMA-OUT-OF-WIRE, new entry, independent of the first and parallelisable).
Corrected mid-measurement: I first told the user 17 of 20 `Mgr*` types were manager-internal, having grepped only PS and EN. Including the client crates and autumn-op, it is 6 manager-only plus 4 that only autumn-op reads (autumn-op ships with the cluster, so it constrains nothing) — a free set of 10 of 20, not 17. And it is not a mechanical move: `MgrRegionInfo` is both an etcd value and a field of `GetRegionsResp`, so dual-identity types are a design trade, not a file move. The new entry records the smaller number and leaves the dual ones alone.
Main gaps: the attribution is by source reference, a proxy, and it is blind to semantic-only changes — same caveats as the earlier measurement, so the magnitude is usable and individual rows are not. The new entry's Scope 2 is the part that needs thought rather than typing: "changing it bumps the wire version" is an ACCIDENTAL guard today, and moving these types out removes it, so the persistent side's own discipline has to be connected explicitly — and per project_rkyv_add_field_not_always_loud, fail-loud must be checked per struct shape rather than assumed.
Next steps: implement F-CLIENT-WIRE-COMPAT starting with the constant rename and the two corrected checks (design doc §1.1), which are self-contained and testable before any hello exists. Nothing pushed.

Date: 2026-09-18 (9)
TaskStatus: completed
Task scope: F-CLIENT-WIRE-COMPAT — write the concrete design before implementing. User asked for the design first. Doc only; no code.
Current summary: docs/client_wire_compat_design.md. Shape: one version integer with three constants (CLIENT_WIRE_MIN is the only one a client-breaking change moves, so it is the single thing review has to look at); a connection-level MSG_CLIENT_HELLO that is hand-coded fixed-layout binary rather than rkyv, because the negotiation message must not depend on its own shape to fail loudly; a silent connection treated as the introduction version, which makes the mechanism inert on arrival; and an extent-node edge closed from the PS side by declining the direct-read descriptor, so the EN acquires no version concept at all.
What the review caught, and both were decisive: (1) `wire_compat_check`'s interval-OVERLAP test makes the window unopenable and is wrong in the dangerous direction. A cluster at MAX=45 with the floor at 44 answers [45,45]; an in-window client at 44 computes lo=45 > hi=44 and REFUSES ITSELF. The only fix is for the frozen `GetClusterIdResp.wire_version_min` slot to carry CLIENT_WIRE_MIN, since already-deployed clients read that field with code we cannot change — but then a stale PS at 44 also overlaps [44,45] and is admitted, and the handshake was the only thing enforcing stop-the-world. So cluster peers must switch to exact `resp.wire_version_max == WIRE_VERSION_MAX` while clients use membership. (2) Refusal must be scoped by msg_type, never by connection: PS->manager and EN->manager dial through autumn_stream::ConnPool with no handshake of any kind, manager->PS drives split/maintenance/merge-freeze/roll-tails the same way, and nothing in a frame says which role sent it — so a connection-scoped refusal turns the first floor move into a cluster outage (register_ps, heartbeats, register_node, reconcile). Three more folded in: frame.rs is FROZEN rather than windowed, because a frame change fails CRC before the hello can be reached and a refusal cannot even be delivered; the PS check must sit above `authz_gate`'s `!gate_active()` early return or it never runs on the authz-off clusters that are most of them; and a new client's hello to an old PS returns NotFound, because `extract_part_id` answers `_ => 0` and partition ids start at 1, so it is indistinguishable from a real misroute unless the client reads it as "server predates the hello".
Main gaps: no code. The golden-bytes freeze catches a layout change but nothing catches the a857084 class (same bytes, changed meaning) — that stays a review obligation, written down as such. The msg_type boundary between client surface and operator surface cannot be seen by the linker (autumn-op's messages compile into the same autumn-client crate an embedded client links), so the design specifies a const set plus a test rather than a module split.
Next steps: implement, starting with the constant split and the two corrected checks (§1.1), which are self-contained and testable before any hello exists. Neither this nor the preceding four commits is pushed.

Date: 2026-09-18 (8)
TaskStatus: completed
Task scope: F-CLIENT-WIRE-COMPAT Scope 1 — enumerate the client-facing wire surface and MEASURE what freezing it would cost. Analysis only; no product code.
Current summary: The surface is enumerable: ~61 of 236 wire types. partition_rpc's data plane, manager_rpc's routing/lease/inode/authz subset (a mounted fuse daemon is an embedded client, and its statfs really does ask the manager for cluster-df, fuse/src/dispatch.rs:1100), frame.rs, cap_token.rs, plus extent_rpc's READ subset because --direct-read is default-on and the SDK reads straight from an extent node. EC conversion, recovery, WriteShard, df, reconcile, split/merge, the op-ledger and the dashboard are all outside it. Cost: classifying all 45 wire-version intervals three ways gave 16/46, 15/45 and 18/45; the stable finding is that ABOUT 60% of bumps changed nothing an embedded client decodes, and the exact count is not the load-bearing part.
What the review caught that I had wrong, and it changed the plan: (1) my detector is blind to semantic change — a857084's own commit message says "Wire 34 -> 35: no struct change, but a v34 client reads a declined item as a per-item error", a GetRedirectResp.code change on the SDK's direct-read hot path that I had filed as internal; 4a9b336 likewise deleted wire_fingerprint from GetClusterIdResp with WIRE_VERSION_MAX sitting at 36 on both sides, because the change landed in a non-bump commit. So every count is a LOWER bound. (2) My second conclusion was backwards. I had found 9 of the breaking bumps to be pure field additions and inferred a tagged encoding on the client subset would make them transparent. But 1de0005's added ec_data_shards is a DISCRIMINATOR — replica_addrs means something else when it is non-zero — so rkyv's layout shift is what fails loudly, and under prost an old client would decode "successfully" and then read EC shard bytes as a value, silently. Only 1 of the 9 was a clean win. (3) An rkyv 0.8.15 experiment on this tree's own AlignedVec<16> + checked from_bytes path shows the root sits at the END of the buffer, so an old decoder reads a longer struct's suffix: {u64,u64} reading {u64,u64,u64} returns Ok with the fields SHIFTED, and a u32 added into tail padding round-trips Ok in both directions with the new field reading 0. Only Vec/String shapes fail loudly. "rkyv fail-loud gives us the safety" is narrower than the memory records — that claim was about persistent-state replay, not cross-version RPC decode.
Conclusion, which reorders the entry's own Scope: the missing mechanism is not the encoding, it is that the client DISCARDS the negotiated version (crates/rpc/src/lib.rs:99 already says so). The residual breaks that no encoding can fix are behavioral — v26 key layout, v34's EC discriminator, v35's decline semantics, v28's frame reshape — and they need call sites able to branch on the negotiated version. Without that, no compat window is possible whatever the encoding. So Scope 2/3's precondition is to retain the negotiated version client-side first; the prost migration is explicitly NOT on this path.
Main gaps: no detector for semantic-only changes exists and none is proposed — the three known instances were found by reading commit messages. The count's exact value moves with the definition of the client surface (including all of stream/src/client.rs over-broadens it: AppendReq and WriteShardReq are the PS's writer path, not an SDK client's). Scripts are in the session scratchpad, not the repo.
Next steps: implement the handshake-retention step (keep the negotiated version on the client, make call sites able to gate on it) before touching the constant. Neither this nor the preceding three commits is pushed.

Date: 2026-09-18 (7)
TaskStatus: completed
Task scope: Ledger only. User challenged the upgrade story — "stop-the-world upgrade, and then every dependency needs a rebuild+upgrade, seems wrong" — and asked for it as a feature_list.md entry.
Current summary: New entry F-CLIENT-WIRE-COMPAT. The finding: one `WIRE_VERSION_MIN/MAX` constant (crates/rpc/src/lib.rs:104) welds together three things that should be separate — persistent state (genuinely stop-the-world, rkyv fail-loud, unrelated to clients), the cluster-internal wire (MIN=MAX costs nothing, those three binaries swap together anyway), and the CLIENT-facing wire, which is the one that should not be bound to the other two. Evidence, not reasoning: the 42->43 bump (2b76620) deleted MSG_APPEND_CHAIN, an EN<->EN message no client ever sends, yet every stale wheel now hard-fails at ClusterClient::connect (client/src/lib.rs:1527); and the stop-the-world window cannot cover client processes at all, since the vLLM / sglang / ComfyUI pods and users' fuse mounts are not ours to stop. The gate is also wrong in BOTH directions: client.rs:1515 is `if let Ok(...)`, so a transport failure fetching GetClusterIdResp SKIPS the check, while the server never checks a client's version at all (MSG_AUTH_HELLO carries no version and is optional; the wire_compat_check at partition-server/src/lib.rs:3482 is the PS self-checking as a CLIENT of the manager). That is the shape of the part_id = 0 incident the rpc crate already documents.
Main gaps: no code. Scope 1 of the entry (enumerate the client-facing message set and MEASURE what freezing it costs) is deliberately the precondition for splitting the constant — the client surface is wider than partition_rpc, because `--direct-read` is default-on and pulls extent_rpc's read subset onto it.
Next steps: do Scope 1 before deciding the shape of the split and the server-side enforcement. Neither this nor the preceding two commits is pushed.


Task scope: Remove chained replication (MSG_APPEND_CHAIN). Replication is star-only.
Current summary: -477/+39 across 11 files. Gone: the msg type and its chain-prefix codec, the `launch_append` chain branch, `IoDeadline::for_chain`, the EN's per-addr forwarder (`chain_fwd`, `ChainFwdJob`, `ChainFail`, `chain_forward_enqueue`, `CHAIN_FORWARD_TIMEOUT`) and its handler arm, `set_append_chain_min_bytes`, the `--append-chain-min-bytes` flag, and `AUTUMN_APPEND_CHAIN_MIN_BYTES` in cluster.sh, the docker entrypoint and the baremetal deployer. `WIRE_VERSION_MIN`/`MAX` 42 -> 43 (a msg type disappeared); msg type 16 keeps a tombstone comment, matching how 12 and 13 were retired.
Why, in the order the reasons actually weigh: the failure attribution was broken by construction. A chained ack carries ONE status for every hop, so `InflightResult.frames` had a single entry while `apply_completion` resolves the faulty replica by FRAME INDEX — every downstream failure resolved to index 0 and blamed the HEAD, calling `mark_bad_node` and filing a disk-failure report against a healthy EN while the node that actually failed stayed eligible. User reports it also measured mediocre. It was default-off from the day it landed (2026-06-10), had zero automated coverage, and its acceptance (cross-host >=1.5x, mid-chain-kill chaos, arch review) was never closed — the criteria survived only in commit e092d72's message, the ledger entry having been pruned.
What the review caught that I had wrong: I claimed an operator who had set the flag would now fail to start the PS. False — unknown args are `eprintln!`-and-continue (partition_server.rs:436), so it is two lines of noise. The review also found a remnant my greps missed: `deploy/baremetal/autumn-deploy` has no file extension, so `--include=*.sh` never looked at it; it still carried the env-var mapping. Third, my new CLAUDE.md section blamed the error-frame/AppendResp confusion on the chain shape. It is not chain-specific — `StatusCode::Unavailable` and `CODE_LOCKED_BY_OTHER` are both 5, and the EN still emits Unavailable on plain MSG_APPEND — so the section now says explicitly that the guard stays.
Verification: `cargo test --workspace --lib` 11/11 suites green (autumn-rpc 70 -> 69, autumn-stream 203 -> 202: the three deleted tests, less one added back); `cargo check --workspace --all-targets --features autumn-server/ucx` clean; e2e `e2e_lifecycle`, `system_bulk_verification` (incl. `bulk_write_crash_restart_all_data_intact`), `bug_flush_timeout_leak`, `prepared_append`, `stream_sqcq` green; clippy 58 -> 57 on the touched crates, no new warnings. Added: an `AppendReq` encode/decode round-trip asserting `encode_header` lays out the same 32 bytes as `encode` (the rpc crate had no unit coverage of that hot-path codec once the chain tests went), ablated red by swapping two fields in `encode_header` alone. Added a `debug_assert_eq!` that a completion carries one frame per replica — the attribution above depends on it and star replication is now its only producer.
Main gaps: no test asserts msg type 16 is refused, since MIN=MAX=43 makes a 42 peer fail at the handshake before any frame. The `IoDeadline` cap-after-multiply property went with `for_chain`; `for_len`'s own cap is still covered twice.
Next steps: neither this nor the preceding commit is pushed. The stream crate's "Replication is star-only" section states the precondition for ever reviving this: design the per-hop status in the ack FIRST, then the fast path.

Date: 2026-09-18 (5)
TaskStatus: completed
Task scope: One construction path for replica appends, and the CRC-sharing threshold it exposed. User asked why the star-replication send site needs two framing paths at all.
Current summary: `launch_append` gated on `size >= 64 KiB && replicas > 1` and built either a `PreparedPayload` or per-replica vectored parts. The wire bytes are identical either way, so the gate bought nothing AT THE SEND SITE — it is now one path, `PreparedPayload` + `send_prepared` for every star-replicated append, and the duplicate parts-rebuilding branch is gone. What the gate was really protecting moved into `PreparedPayload`, which now takes the frame count and picks a checksum strategy: share one scan and join each frame's header CRC with `crc32c_combine` at or above `COMBINE_MIN_BYTES` (1 MiB) with more than one frame, else re-scan per frame exactly as the vectored send did.
What the review caught that I had wrong: my first version deleted the gate outright, asserting in three CLAUDE.mds that `crc32c_combine` costs less than re-scanning 4 KiB. It is a GF(2) matrix ladder with a constant that dwarfs a hardware CRC pass, and I had asserted this without measuring. Measured per append, RF=3: 4 KiB 2.7 us re-scanning vs 62.8 us shared (a 23x regression on the WAL path, on the single-threaded per-stream worker); 64 KiB 32 vs 121 us; 1 MiB 463 vs 358 us. So the SHIPPED 64 KiB constant was itself ~8x too low and had been a pessimisation between 64 KiB and ~512 KiB since it landed. A single frame can never win the shared scan back at any size, which is why the replica-count term was load-bearing and is now an input to `PreparedPayload::new`.
Why 1 MiB and not the crossover: measuring the combine and scan costs separately puts the crossover near 460 KiB (RF=3) and 600 KiB (RF=2), but the in-tree sweep has ~10% spread and the loss curve is asymmetric — up to 23x wrong below, at most ~1.3x wrong above. Between ~600 KiB and 1 MiB at RF=3 the rule knowingly picks the more expensive arm by up to 34%; accepted, and written down at the constant.
Verification: autumn-rpc lib 70 green, autumn-stream lib 203 green, prepared_append + stream_sqcq green, the first two also under AUTUMN_TEST_ZEROCOPY=1 (which arms the zerocopy writer at 1 byte, the only newly reachable combination — with the knob on, small and single-replica appends are now zerocopy-eligible where they were not). `cargo build --workspace --bins` clean; no new clippy warnings. Ablations, each confirmed red then restored: swap the `crc32c_combine` argument order (mine); drop a part from the re-scan arm's `compute_ctrl_crc` (the reviewer's) — both arms are genuinely reached, at unit and e2e level.
Main gaps: the in-tree benchmark's two loops do not observe identical memory behaviour at multi-MiB sizes — its RF=1 rows there can report combining as cheaper than the single scan it contains, which is impossible. Caveated at the test; read the RF>1 rows. The band between ~600 KiB and 1 MiB is left on the safe side deliberately, not tuned.
Next steps: delete chained replication (MSG_APPEND_CHAIN). User reports it measured mediocre, and the error attribution is broken by construction: the chain path fills `InflightResult.frames` with ONE entry, so `apply_completion` resolves the bad replica at index 0 and blames the HEAD node for any hop's failure — `mark_bad_node` plus a disk-failure report against a healthy EN. It is default-off, has zero test coverage, and its acceptance was never closed (the criteria survive only in commit e092d72's message, the ledger entry having been pruned). client.rs:1996 already records one live PS wedge from a mid-chain EN death.

Date: 2026-09-18 (4)
TaskStatus: completed
Task scope: BUG-GC-ADVISORY-VS-SELECTION — close the two items the ledger still had open: re-verify the acceptance on a real cluster, and land the presentation half (extent view names the partitions sharing an extent).
Current summary: The predicate fix shipped 2026-09-12 (one `gc_selects` for both the advisory and selection, plus an absolute-bytes arm). No code was needed for the first open item; the second is a new `extent_sharers()` in autumn-op feeding both extent views plus the dashboard chip. Live: 3 EN + real etcd, ALL DEFAULTS including the 16 GiB extent seal size. extent 8 = 16.0 GB sealed / 2,576,980,376 B dead = ratio 0.15. The control (`gc --ratio 0.4 --stream-debt 1GiB`: halving on, NO floor) answered `no eligible extents to reclaim` and left `df` at 2.4 GB; the standing dispatch (`gc 13`, manager fills the floor from policy) gave `GC: punched extent 8, moved 3491 entries` (~14.6 GiB relocated, 7m22s) and `df` 2.4 GB -> 0 B. Unattended loop verified separately: armed gc-only advised at 08:36:22 (`gc_debt_bytes>1073741824 (1228 MiB) sustained 5m`), punched 08:38:30, gauge 0 B at 08:38:36 — the never-converging loop this bug is named for now converges by itself.
What the review caught that I had wrong: my FIRST live run used a 4 GiB extent with 1.2 GiB dead = ratio 0.30 and I called it a scale model of the production 0.195. It is not. A standing dispatch also fills `gc_stream_debt`, so crossing 1 GiB of stream debt HALVES the per-extent gate to 0.2 — 0.30 clears 0.2, so the RATIO arm selected it and the run was equally consistent with the absolute arm being dead code. Redone at 0.15 (under both 0.4 and the halved 0.2, over the 1 GiB floor), where only the absolute arm can select, with the no-floor control as the discriminator. Second catch, self-inflicted and worse: the dashboard chip COMPUTED the sharer line and never interpolated it into the template — the feature rendered nothing while every dashboard test passed. Both the fix and a render-level regression test are in (asserting the RENDERED string, not the helper). Third: the strings said "freed only once ALL of them GC it" for row/meta extents too, which are released by compaction's head truncate, not GC — now "drop it", with the debt clause gated on `role == "log"`.
Cost, stated rather than hoped: `run_partition_info` has no other partition's stream membership, so on `refs > 1` it spends one extra `MSG_STREAM_INFO` over all 3N streams, and the manager's reply clones every membered extent in the cluster — the full-cluster pull that view otherwise avoids. The review disproved my "the common case pays nothing": a split shares every pre-cut extent until BOTH sides collect, so `refs > 1` is common on a split-grown cluster. One pull per drawer open, not per refresh. Cheap needs a manager-side reverse lookup (a wire change); not done, written down.
Verification: autumn-server 8 test binaries green (35 unit incl. 4 new + the e2e); dashboard render_check + tabs_smoke green; `cargo check --workspace --all-targets` clean. Ablations, each confirmed red then restored: drop the dedup -> the two-streams-one-holder test; drop the ordered walk -> the 8-holder ordering test 10/10 (with only 2 holders it was red just 2/10, a hash order ascending by luck, so the test was rewritten); drop `${shared}` from the chip template -> 3 render assertions. Sharing display rendered against two REAL CoW splits.
Also fixed, PRE-EXISTING and unrelated: `crates/server/tests/op_bootstrap_e2e.rs` called `autumn-client put/get` without the now-mandatory `--namespace` and exited 2. Confirmed failing on HEAD with my change stashed.
Main gaps: no cross-crate "standing dispatch -> PS selects" test — deliberately, since the manager fill and the PS predicate (including the "under the halved gate but over the floor" assertions) each have unit tests and only a field copy sits between them; the untested link is the whole line, which is what the live run exercises. A shared extent whose holder count is below `refs` (orphaned stream / refcount leak) prints no sharer line in the scoped view; noted in ops.md.
Next steps: F-SPLIT-ADVICE-COUNTS-UNRECLAIMED's precondition is now cleared — its Scope 1 re-check (does `est_live` fall on a freshly emptied cluster) is newly runnable and is the one I would take next. 20 entries remain open.

Date: 2026-09-18 (3)
TaskStatus: completed
Task scope: BUG-FRAME-LEN-U32-WRAP — a frame past 4 GiB silently wrapped its u32 length, so the peer reported a CRC error on a frame that was never corrupt.
Current summary: The ledger asked for a bound check in the encoder. That alone would have been a NET REGRESSION, and it took four review rounds to see why: release is `panic = "abort"`, so an assert inside the shared encoder is a node kill, and FIVE producers are reachable from a remote request. The maintainer's call was "every place should have a bound; add them, then delete the assert". So the encoder's check is now `debug_assert!` (caught in `cargo test` and dev builds, absent in release) and each producer bounds itself and DEGRADES: extent-node reads refuse with `TooLargeForOneFrame`; the PS batch get answers CODE_PRECONDITION, which `get_many` already handles by falling back to per-key reads; the group-commit append splits the queue and launches the remainder next; `MSG_COPY_EXTENT` refuses; `MSG_GET_REDIRECT_MANY` declines items past the budget, which the client already proxies.
What the reviews found that I did not: (a) the PS batch get aggregates N values into one frame with no cap on N while the SDK's `get_many` sends every same-partition key in one request — hundreds of 8 MiB values is a model load, the target workload, and the codebase already names this hazard twenty lines away as the reason `get_redirect_many` refuses to inline; (b) three more producers (the group-commit append on the HOT WRITE PATH, `MSG_COPY_EXTENT`, `MSG_GET_REDIRECT_MANY`); (c) my own off-by-one — the read bound subtracted the frame's overhead but not the REPLY's ctrl, leaving sizes that passed the check and overflowed the encoder, and my boundary test pinned the panicking value as legal; (d) the same mistake AGAIN on the redirect-many bound, which counted values but not per-item rkyv framing (65,520 items x 65,535 bytes fits the values and overflows the frame); (e) `encoded_size()` undercounting every WAL record by 17 bytes; (f) three `should_panic` tests that would fail under `cargo test --release`.
Two things were already written and never connected: `WriteRequest::encoded_size()` carried `#[allow(dead_code)]`, and the `ps_inflight_cap` doc cites `MAX_WRITE_BATCH_BYTES = 30 MB` for its memory math — that constant did not exist in the code, so the queue was bounded by request COUNT (3072) while one Put may be 64 MiB. Both are now real; the false comment is fixed.
Verification: rpc 70 (debug and release), stream 203, PS 252, manager 378, client 57; integration system_bulk_verification / system_ps_failover / graceful_shutdown / system_extent_recovery / system_merge green; workspace check --all-targets clean. Ablations red-verified and restored: the encoder bound; the read bound reverted to its off-by-one (caught by a ceiling SWEEP that drives the real encoder — the single-value version is what let it through); the redirect-many per-item accounting. Reviewers: pi produced zero bytes in 12 minutes and was killed (second time, recorded in memory as unusable), then one opus round and three fable rounds.
Main gaps: the three PS call sites are not driven by a test (needs a live partition and gigabytes of values); a tighter memory-oriented batch cap — the 30 MB the old comment intended — is a separate decision with its own measurements; a BatchPut straddling the split is no longer durable in one append, which loses nothing ACKED because the response fires on the last op.
Next steps: 21 entries remain open in feature_list.md. BUG-GC-ADVISORY-VS-SELECTION (follow-up 1 of the real data-loss incident) is the one I would take next.

Date: 2026-09-18 (2)
TaskStatus: completed
Task scope: Close the residuals the two review rounds on the four handover features had left open, and stop the docs describing a mechanism that no longer exists.
Current summary: (1) The retired per-node EC commit phase still had 43 references across four files; several were not merely stale but false about current code (write_ec_prepared_marker carried three welded doc blocks from deleted functions, the first claiming it renames .ec.dat over .dat; remove_extent_files documented a file set it does not unlink; the stripe bounds guard and stripe-0 truncate justified themselves by what finish_ec_commit would publish; a comment claimed the old receiving side was RETAINED as upgrade repair code when all of it is deleted; the crash-consistency section documented EC-COMMIT-ATOMIC's intent marker and its startup replay as current). That section is rewritten around what the code does — staging durability is the per-stripe sync_data plus the parent-dir fsync, and there is no commit window because there is no commit phase; the manager's layout flip is the single commit point. (2) The EC content-corrupt release now isolates the coordinator's slot itself: `isolate_rotted_slot` was extracted from node_health_loop's scrub_rot loop (verified line-for-line identical after the documented renames) and is called from `release_corrupt_ec_attempt` in the window its own abandon opens. This closes a real race — isolation refuses while the extent has a stream-layer op in flight, and the node's df copy of the same finding is dropped exactly then; the df report is now the backstop for a leader change landing between the reply and the call. (3) Promotion forgets the previous term's df facts: the reset moved above replay_from_etcd's etcd early-return so a unit test drives the promotion path rather than the helper.
Decisions, not omissions: under `auto_disk` the zombie marker this work collects is deliberately KEPT — that gate dispatches on a failed probe, which is indistinguishable from a healthy slot, so releasing restarts a full-extent copy every tick; holding costs capacity that recovery-stats shows, releasing a live one costs availability that nothing shows. The probe-timeout hypothesis is closed as not-a-defect: the new faulted set is a strict subset of the old (which condemned on the error alone, with no probe), and a measured baseline on a real NVMe puts the 2 s budget three orders of magnitude above the worst observed sample (300 iterations of the exact syscall sequence: p50 0.14 ms, p99 0.43 ms, max 1.83 ms). A saturated-tail study is an enhancement, needs fio installed, and would disturb a shared box.
Third review, findings fixed: it caught me committing the very defect this session was cleaning — inserting `isolate_rotted_slot` BETWEEN `apply_df_disk_health`'s doc comment and its item, so three paragraphs about per-disk `online` semantics rendered as the rot helper's description; the doc is back on its function. Three docs claimed the `df` backstop "only has to cover a leader change" when the direct call also bails on a failed persist, a lost verify-at-apply race and any refusal — corrected in all of them, plus the manager guide's "would lose the finding exactly when it matters" (it delays by a tick, and loses only if another op takes the extent first). The shared helper's two operator-facing log lines said "scrub" whatever the caller was, so an operator would grep the EN for a scrub finding that never happened: both now carry a `source` field ("scrub" / "ec_content_check") and neutral wording. The EC path passed a freshly-read eversion, which made the predicate's eversion guard tautological; it now threads the DISPATCH-time eversion, which is the layout the evidence is about. The extracted body was left at loop-depth indentation and is now de-indented. One finding deliberately NOT acted on (reviewer agreed): verify-at-apply re-checks eversion but not the ledger after its persist await — pre-existing in the df path, benign for a Recovery marker, and per rule 16 it needs a repro before a guard. Recorded in feature_list.md instead.
Verification: manager lib 378, stream lib 199, client 57, rpc 66; scrub_isolates_rot / silent_corruption_rot / system_corrupt_replica_rebuild / system_manager_failover / leader_fence green; workspace check --all-targets clean. Two new ablations confirmed red then restored (drop the isolation call from the EC release; drop the reset from replay_from_etcd). Chaos `corrupt,ec` seed 603 green twice (after the isolation change and again after the eversion threading), with the same collision in the log and the new `source` label visible on the isolation ERROR. The doc-only commit changed no logic: one test renamed to what it pins plus two assert messages.
Main gaps: the saturated-tail measurement above; `auto_disk` keeps its markers by decision; claude-progress.txt was pruned to the four most recent sessions (older entries are one line each, full text in git).
Next steps: F-TYPED-ERRORS was dropped from the ledger at the user's request (2026-09-18). The survey that argued against doing it is in commit 7b40183 if it is ever wanted; the short version is that thiserror was already in use, the 489 anyhow uses carry context rather than define types, and half the string-sniffing classifiers judge a message that arrived over RPC, where the fix is a wire code and not a Rust type. 22 entries remain open; the two I would take first are BUG-FRAME-LEN-U32-WRAP (>=4 GiB frames silently encode a corrupt header, confirmed present) and BUG-GC-ADVISORY-VS-SELECTION (follow-up 1 of the real data-loss incident).

Date: 2026-09-18
TaskStatus: completed
Task scope: Finish the four items the 2026-09-17 handover WIP had in flight: disk fault self-check, obsolete recovery markers, EC corruption handoff, bulk refusal propagation.
Current summary: The WIP (459520b) carried working code for all four but no docs, no ablations and no independent review; this session closed those and fixed what the review found. (1) Disk self-check: a Media-class error now runs a bounded write+fsync probe into the failing extent's own hash directory before anything is condemned; the probe passing retains disk health while the caller's operation still fails. Acceptance's other half was untested, so the transient-EIO test now also asserts `df` keeps reporting the disk online (health the manager never hears about triggers no rebuild). (2) Marker release: `release_recovery_markers_for_healthy_slots` is level-triggered per tick with no TTL, and asks the DISPATCHER's question via `slot_verdict`, releasing only on Withhold. The 215-line `recovery_dispatch_loop` body was extracted verbatim (diff-checked identical) into `recovery_dispatch_tick` so a test could drive one real tick — the predicate had a unit test but nothing proved the loop REACHED it, which is the green-tests-dead-mechanism shape. (3) EC corruption: CODE_CONTENT_CORRUPT (wire 41->42) abandons the marker on the first content failure; `abandon_ec_marker` got an etcd value-CAS plus a post-await nonce recheck so a late reply cannot release a successor's marker, and now WARNs instead of failing silently when the CAS misses. (4) Bulk refusal: the bulk arm emits the typed error frame instead of flattening every get_extent error to CODE_ERROR "extent unavailable".
Review findings fixed (a fresh fable subagent on the commit, then a second fresh one on my response to it): (a) the release predicate would have dropped a rebuild that was still needed under the non-default `auto_disk` gate, where a slot dispatched because its PROBE failed reads healthy on every clause — now only a Withhold verdict releases; (b) between a leader promotion and the source node's first df, `faulted_disks` is empty while the persisted disk record replays online:true, so a genuinely faulted disk read healthy — `has_first_hand_df` now requires this leader to have applied a df from that node; (c) the EC pre-check's discovery of rot was being DISCARDED, so under the default gate nothing marked the slot and recovery would not rebuild it — the node now reports it on the same df channel the scrub uses, re-queued on each refusal because the manager drops findings for an extent with an op in flight and the refusal is what releases that op. Two review inferences were checked and NOT acted on: the probe-timeout-condemns-a-slow-disk hypothesis is not a regression (the new faulted set is a strict subset of the old, which condemned on the error alone with no probe) and wants an fio-saturation measurement before any change; and the claim that rpc/CLAUDE.md already said 42/42 at the parent was wrong (it said 41/41, matching lib.rs, and this change fixes both).
Second review, findings fixed: (d) `has_first_hand_df` was PROCESS-scoped, not leader-term-scoped — election is in-process and nothing ever cleared `node_max_free`/`faulted_disks`, so a re-elected manager answered for a node only an EARLIER term had heard from, and three of my own doc sentences asserted the property the code lacked; promotion now calls `forget_node_health_facts_of_the_previous_term`, which also restores what `faulted_disks` already documented about itself. (e) the tick test still read the process-wide gate env, so `recovery_dispatch_tick_under(gate)` now threads one gate through release and dispatch alike (one env read per tick instead of two). (f) two doc overstatements corrected: the release is STRICTER than the dispatcher (any override keeps the marker, not just Fenced), and the rot report isolates the slot while the REBUILD comes from a later tick's verdict — there is no force-dispatch on that path. The second review independently diff-verified the tick extraction as byte-identical and found no invariant violations.
Verification: unit stream 199, manager 376, client 57, rpc 66; stream `shards` 5; manager integration system_recovery_loop_drives / system_corrupt_replica_rebuild / system_extent_recovery / node_lifecycle / ec_integration / ec_failover green; workspace check --all-targets clean. Eight ablations, each confirmed RED and restored: probe removed; release made a no-op; the tick's call to it deleted; the verdict conjunct weakened; manager ignoring CODE_CONTENT_CORRUPT; EN not refusing after a ContentCorrupt attempt; the refusal-site rot re-queue removed; the bulk refusal re-flattened. Live chaos (subprocess ENs + real etcd): `corrupt,ec` seed 603 passed before AND after the fixes with the exact collision in the log (extent 20 rotted -> same extent chosen for EC -> recovery drove it to success, no pinned EC marker after quiesce); `fence` for 60 s ran 11 fence/unfence cycles with two `released recovery marker: source slot is healthy again` INFO lines, zero still-ACTIVE ops after quiesce, 400 keys byte-exact; a final combined `fence,ec,corrupt` run drove 9 recovery ops to terminal state with all invariants OK. Because promotion changed, the failover/replay suites (system_manager_failover, leader_fence, offline_reconcile) were re-run green.
Main gaps: the promotion-time reset has a unit test on the helper but nothing pins the CALL SITE in `replay_from_etcd` (it needs etcd, so it is out of reach of an in-crate test) — the same gap the four pre-existing clears beside it have; no real-hardware disk-fault injection (the probe's timeout arm is exercised by injection, not by a saturated device); `auto_disk` deliberately keeps the zombie marker this work collects, since releasing there restarts a full-extent copy every tick; the EC rot report can still be lost if a df lands between the refusal and the marker release, with the scrub as the backstop.
Next steps: F-TYPED-ERRORS (project-wide thiserror) is the queued follow-up the user asked for on 2026-09-17. Consider measuring probe timeout under fio saturation before tightening the timeout arm. claude-progress.txt is ~99 KB over 24 entries and is due a prune.

Date: 2026-09-18
TaskStatus: completed
Task scope: User asked to fix BUG-FUSE-WRITE-READ-DUAL-OPEN-EBUSY (ledger entry from the 2026-09-16 todo import): a mount refuses a read-open while it holds a write fd on the same inode, so ffmpeg MP4 faststart - hence every ComfyUI SaveVideo - fails with EBUSY.
Current summary: Implemented the confirmed design. FuseLease now counts per ROLE (writer_refs/reader_refs, + add_ref/drop_ref/total_refs); Open serves a READ open alongside this mount's own writer with NO manager RPC and upgrades readers->writer via acquire(WRITE) (Granted merges with entry(), never insert, or an upgrade forgets already-open read fds); RELEASE gets the fd's open flags, which ops.rs used to drop, and decrements that role; the last write fd closing with readers left flushes and downgrades (release + acquire(READ)) so a lingering tail -f stops holding the inode's single writer slot. check_write_allowed keys on writer_refs; compute_release_action gained downgrade_to_read and a last-writer must_flush; the dead "lease mode mismatch" EBUSY arm is gone; PyO3 Fs.acquire/release count per role. Distributed question the user raised: a remote read-only opener is its own lease client - it never blocks the local writer, still registers and receives invalidations, and gets WriterClosed when the local writer downgrades (new cluster test pins all three). Linear reads across mounts pick up appended data because the EOF recheck adopts a larger size AND drops the cached extent map (new system_fuse_read test).
Verification: fuse lib 63; manager fuse_lease_1 11, fuse_lease_2 6, bug_lease_3 4, system_fuse_read 5, system_fuse_release_best_effort 5, system_fuse_eof_clobber 4, system_fuse_flush_error_sticky 6, system_fuse_ns 4; manager 371, client 53 lib; python binding check; workspace check + fuse clippy clean. Real-mount e2e (release binaries, 3-EN cluster, fusermount3, two mountpoints): dual open; ffmpeg -movflags +faststart producing a probe-clean mp4; reader-outlives-writer append; second mount taking the writer slot after a downgrade with the daemon logging it; 3 MiB round trip byte-exact. Ablations: restoring slot.mode != req_mode reds two cluster tests and fails e2e T1/T2/T3 with 3 "lease mode mismatch" logs; removing the flush gate reds the new failed-flush test.
Independent review (fable subagent, after five pi runs produced nothing - pi burned its 16k output cap on ~11,700 thinking deltas; user then set the policy "complex changes -> fable, simple -> pi/opus"): found a SELF-INTRODUCED high-severity hole. The downgrade did not look at the flush result, and flush_inode zeroes wb.len BEFORE write_region, so a failed flush leaves dirty=true with meta.size still covering the lost bytes; handing the writer slot back then lets another mount take it, append, and have this mount's stale size land on top (stamped ANON, unfenced, because write_lease_for keyed on writer_refs), after which the next grow's clean_beyond_eof deletes the other mount's extents. Fixed: the downgrade now requires deferred_flush_err.is_none(), a failed flush KEEPS the write lease (pre-change behaviour, slot returns when the last read fd closes), write_lease_for keys on mode again ("do we still hold the write lease" is a different question from "is a write fd open"), and mode drops to READ the instant the release succeeds. Its other findings are all addressed: ops.md step 3 rewritten as an executable two-mount sequence (the old one ran the UPGRADE path and tail -f on a nonexistent file), e2e T5 added because T1-T3 could not distinguish the plumbed role from "always READ", the wrong "TTL revoke is the backstop" wording corrected, last_writer tightened to == 1, mode documented as load-bearing rather than a refcount restatement, and the binding's lack of a downgrade written down.
Last acceptance step: ComfyUI SaveVideo end-to-end on the comfyui-autumn pod cannot run from this box, and the user took it themselves and marked the ledger entry passes: true (2026-09-18). The equivalent ffmpeg faststart path was exercised here on a real mount; everything else in that acceptance list was verified in this session.
Rebase note: this landed on top of the connection-reuse work (a0d6384) and the handover WIP (459520b), which bumped WIRE_VERSION 41 -> 42. The stale target/debug/autumn-ps from Sep 16 then failed the compat check, so every test spawning it (system_fuse_release_best_effort) died with "autumn-ps never accepted" - a stale-binary symptom, not a regression; `cargo build --workspace --bins` fixed it. All fuse/lease suites re-run green after the rebase.
Next steps: F-LANCEDB-OBJECT-STORE's demo path is now unblocked (its writer has the same read-old-manifest-while-writing shape).
Date: 2026-09-17
TaskStatus: completed
Task scope: Keep healthy RPC connections after server status errors (F-EVICT-ON-STATUS-ERROR).
Current summary: Implementation and feature acceptance passed. RpcError::is_connection_error classifies before AutumnError mapping; local deadlines have their own Timeout variant. SDK plain/bulk/multi-value/pooled reads and manager calls, stream pool, and manager outbound pool retain Status connections. Authentication identity/token renewal still reconnect. Two necessary companions found by real verification: retired PS partition instances now close accepted connections through the shared shutdown signal (otherwise merge leaves old frozen connections alive); malformed large bulk prologues now follow read-loop teardown rather than synthesize Internal Status.
Verification: Linux ARM64 Docker, Rust 1.95 (H200-1 unavailable: jump-proxy gssapi authentication denied). Library totals rpc 66 passed/1 ignored, client 56, stream 196, manager 372, PS 242 passed/1 ignored; client_teardown 2, conn_pool_pin 1; graceful_shutdown, system_ps_failover, system_split_large_values and new system_status_connection_reuse passed. New real split/merge fixture: each phase 6 stale epoch refusals + 6 successes uses 1 TCP connection; restoring unconditional status eviction gives 7 connections in BOTH phases and fails. Independent ablations: removing PS shared connection shutdown fails bounded merge recovery; synthesizing Internal for corrupt 64 KiB bulk CRC fails transport classification. All ablations restored. Workspace/all-target check passed after final edits. Clippy completed once with existing warnings only; strict -D warnings blocked by existing PayloadLocation Default implementation; final repeat hit disk exhaustion/Docker I/O failure after tests and workspace check had passed. No UCX hardware validation.
Review: Final self-review traced every RpcError producer and all changed pool call sites; caught and ablated the corrupt-bulk-prologue synthetic status. No subagents invoked under the user's current no-delegation instruction. Feature removed from OPEN backlog after acceptance. No wire/storage format changes. No production deployment.
Environment cleanup: Docker Desktop recovered after restart. Removed only this task's container autumn-status-error-tests, volumes autumn-status-error-target/autumn-status-error-cargo and rust:1.95-bookworm image; docker ps responds normally. Existing untracked rollout.md belongs to user and remains untouched.

Date: 2026-09-16
TaskStatus: completed
Task scope: User asked to verify an RPC resource-lifecycle concern (after a put_bulk timeout the client only removes the RpcClient from its pool; do the background tasks exit, is the socket closed, are pending requests and queued buffers freed, do repeated timeout+reconnect cycles grow memory/FDs), then said to fix it by adding a Drop.
Current summary: Confirmed, and wider than reported: RpcClient had no Drop and both task handles were detach()ed, so a dropped client closed nothing. read_loop blocks in read until EOF, which holds even an IDLE healthy connection open; writer_task additionally blocks in write_all against a peer that stopped reading, pinning every queued frame. Probe (TCP loopback, 5 evictions of a never-reading peer, 16x1 MiB calls/cycle): 5 ESTABLISHED client sockets, 70 MiB of request values still referenced, peer never received a FIN; all released only when the PEER closed. A dropped idle client leaked identically - which is the common case, since ps_call_with_timeout evicts on ANY RpcError including a server status frame (stale region epoch), and token renewal calls ps_conns.clear(). Fix: the two JoinHandles are now FIELDS of RpcClient, so dropping the last Rc cancels both tasks, drops the socket halves and closes the connection (compio holds each in-flight op's buffer until its cancel CQE; no buffer is freed under the kernel). Follow-up found by review 1: ConnPool::send_vectored/send_prepared (replica appends) return only a receiver, so the pool map held the only Rc and an unrelated caller's eviction would now cancel an in-flight append; new conn_pool::PinnedRecv carries the Rc next to the receiver (call sites: stream client receivers vec, extent_node ChainFwdJob). Also corrected on review: the UCX cancel leak is a recv window PLUS a send buffer + iov array (the send is the larger), and the recv window is not fixed at 64 KiB.
Verification: crates/rpc/tests/client_teardown.rs (buffers freed, no ESTABLISHED socket outlives its client, peer sees EOF) - ablation: detaching the reader reds both cases, detaching the writer reds the queued-values case (14 MiB pinned at cycle 0). crates/stream/tests/conn_pool_pin.rs (an eviction must not kill another caller's in-flight request) - ablation: pin downgraded to Weak goes red with "slow request died with the eviction". Lib: rpc 66 / transport 13 / client 53 / stream 193 / partition-server 242 / fuse 57 / manager 371 / memory 33. Manager: integration 13, system_bulk_verification, system_putstream (+9 --ignored), system_ps_failover, system_extent_failover, graceful_shutdown, system_namespace_layer_a, system_split_large_values, system_fuse_read, system_ps_crash_takeover; stream prepared_append, extent_pipeline, stream_sqcq; seeded chaos (seed 583, 45 s) passed twice. clippy + workspace check clean. Two independent pi reviews (a second one because PinnedRecv was written after the first): no blocking finding; review 2 independently reproduced the eviction race and its ablation. Review 1's UCX SPIN_CAP finding was REJECTED after checking the code: on the cancel path await_pending's future is dropped and never resumes, so it cannot read the slot or return (UCS_OK, 0); on the normal path the guard drops only after WaitSlot resolved. Performance: no hot-path change - the handles are only dropped at teardown, plus one Rc clone per pipelined send.
Pre-existing, NOT from this change: system_merge --ignored split_merge_split_with_concurrent_writes and orchestrated_merge_zero_loss_concurrent_writes both hang on this host; verified hanging on the BASELINE with the change stashed (>10 min each, all threads parked in io_uring waits). The box has 3 GB RAM and other tenants, and stale fuse mounts from earlier sessions are still on it.
Next steps: F-EVICT-ON-STATUS-ERROR added to feature_list.md - a server status error (stale epoch after split/merge) should not evict a healthy connection; the leak is closed, the needless reconnect is not. UCX teardown is reasoned from code, never measured; a UCX-enabled run would confirm the leak bound.

Earlier sessions — one line each; full text is in git history
(`git log -p -- claude-progress.txt`). Unfinished work is NOT carried here:
it lives in feature_list.md, which is the requirements ledger, and the entry
that owns it is named below.

2026-09-15 completed     retire the rkyv MSG_GET (0x41); every production read goes through MSG_GET_BULK
2026-09-15 completed     close F-CORE-DATA-PATH-NEXT (remaining transport copy accepted); solve F-GENERIC-GET-COPIES
2026-09-15 completed     per-link receive-copy audit (Client->PS, PS->EN, read return; TCP kernel copies, UCX Stream unpack)
2026-09-15 not_completed controlled perf validation; what remains belongs to F-COMPIO-UPGRADE + F-UCX-LOCAL-PROTECTION
2026-09-14 completed     clean dated perf artifacts off H200-1
2026-09-14 not_completed execute F-COMPIO-UPGRADE + Linux validation; remainder in that ledger entry
2026-09-14 completed     record the compio upgrade plan in feature_list.md
2026-09-14 completed     single-partition CPU follow-up + compio upgrade assessment
2026-09-14 completed     core TCP/UCX + fs read-path performance work, measured on H200-1
2026-09-14 completed     fix the CLAUDE.md doc errors c4e4d64 left behind
2026-09-13 completed     fix two PRE-EXISTING fuse write-path defects the review turned up
2026-09-13 not_completed fuse write-side pipeline, depth never measured; owned by F-FUSE-WRITE-INFLIGHT-DEPTH
2026-09-12 not_completed make the GC advisory and GC selection judge on the same number; owned by BUG-GC-ADVISORY-VS-SELECTION
2026-09-12 not_completed "why is autumnfs read slow" — --direct-io disproved the diagnosis it was based on;
                         owned by F-FUSE-READ-LATENCY-NOT-BANDWIDTH
2026-09-11 completed     dashboard: the five changes the user asked for
2026-09-11 not_completed DATA-LOSS POSTMORTEM. Chain: under-seal (a preemptive roll sealing below what was
                         already acked, in the extreme at 0) -> extent sealed && sealed_length == 0 ->
                         the sealed-empty sweep legitimately punches and deletes it -> SSTs still hold VPs
                         pointing at it -> "extent N not found". manager/CLAUDE.md had written the warning
                         for exactly this ("an assertion that no under-seal bug exists, not a structural
                         guarantee"). Three model files were lost and re-downloaded. Write-path fixes shipped
                         in 947bae0. Its two follow-ups are owned by BUG-GC-ADVISORY-VS-SELECTION and
                         F-EC-STARVES-FOREGROUND-APPEND.
2026-09-11 completed     root cause of a partition that stopped answering: the preemptive roll did not quiesce
2026-09-11 completed     root cause of a permanently wedged partition: the per-stream worker's lifetime and its
                         init flag were not one lifetime
2026-09-11 completed     uploads wedged forever: put_bulk was the only data-plane call with no deadline
2026-09-10 completed     chaos gained multi-node / multi-disk
