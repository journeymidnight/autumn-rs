Date: 2026-07-14
TaskStatus: completed
Task scope: F-EN-DYNSHARD M0 — make the EN shard count dynamically changeable +
  k8s-ready by giving the EN a STABLE UUID identity decoupled from its network
  address (mirrors the PS ps_id-vs-address split). M0 = identity plumbing only
  (no routing/reshard change yet). Locked design (user 2026-07-14): #2→(a) keep
  extent_id%shard_count + shared dirs (reshard moves ZERO bytes); #4→全停全启;
  #3→mirror the PS registration model. Pivot: no back-compat, etcd reset OK →
  node_uuid rides IN-STRUCT on persisted MgrNodeInfo (same-commit stop-world).
  Authoritative design: docs/en_dynamic_shard_design.md.

Current summary:
  DONE — M0 code + tests + docs, all green, ready to commit.
    - WIRE v19 (MIN=MAX=19, fp ba8a88d46f0bbc47): RegisterNodeReq.node_uuid +
      MgrNodeInfo.node_uuid (in-struct, persisted → etcd-reset deploy).
    - manager handle_register_node REWRITTEN: identity resolved UUID-FIRST
      (uuid match → same node_id across addr/shard-port change; uuid-less legacy
      → address match + ADOPT uuid; fresh uuid on recycled IP → new node);
      zombie/decommission tombstone keyed by the matched node_id (uuid, not IP);
      updates address/shard_ports/control_address in place on change.
    - autumn-op format: mints a UUID v4 (reuses per-dir `node_uuid` sentinel if
      present), passes it on MSG_REGISTER_NODE, writes the sentinel to each dir.
    - 26 RegisterNodeReq construction sites across 15 files fixed (rkyv full
      literals, no Default) — incl. the 2 the bulk perl missed on
      `format!("...{i}...")` braces (stream_sqcq.rs, lib.rs:5095).
    - 5 acceptance tests `f_en_dynshard_*` in crates/manager/src/lib.rs:
      uuid-survives-address-change / legacy-adopts-uuid /
      identity-only-preserves-address / decommissioned-uuid-refused-at-new-addr /
      recycled-ip-under-fresh-uuid-accepted. ALL GREEN.
    - Tests: rpc lib 44, manager lib 187 (was 182 + 5), stream lib 96 — all
      green. Workspace + ALL test targets build clean.
    - Ledgers/docs: feature_list.md F-EN-DYNSHARD entry (M0 done, M1/M2/M3 open;
      unblocks F-EN-SHARD-AUTO); docs/ops.md "EN identity is a UUID" subsection.
    - coco deep review: RUNNING at handoff (background). Address any P0/P1 before
      or immediately after commit.

Main gaps:
  M1 (EN startup self-register + df-echo self-heal — the milestone that makes the
    shard COUNT actually dynamic), M2 (deploy: reshard runbook + k8s per-pod
    ClusterIP + Service port generation), M3 (chaos: reshard under load) — all OPEN.
  docs/en_dynamic_shard_design.md is still UNCOMMITTED (760-line design, by the
    fable subagent) — commit it alongside M0.

Next steps:
  1. Confirm coco findings clean (or fix P0/P1), then commit + push M0 to main
     (git push origin main — solo flow, no PR).
  2. Start M1: EN process re-reports address + live shard_ports[] at startup;
     manager shard_addr_for_extent computes from registered shard_ports; DfResp
     echo self-heal on node_health_loop. EN location stays etcd-persisted (manager
     DIALS the EN — in-memory would blackout on manager restart).
