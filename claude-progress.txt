Date: 2026-05-12
TaskStatus: not_completed
Task scope: F190-F193 — multi-feature loop. F190 implemented + tested
            and ready to commit; F191-F193 still to go.

F190 done in this commit:
  - StreamAllocExtentReq.exclude_node_ids field (rkyv schema extension)
  - StreamClient per-stream bad_nodes map (Rc<RefCell<>> shared between
    worker apply_completion path and public-API alloc_new_extent_once)
  - StreamTail.replica_node_ids parallel to replica_addrs
  - apply_completion marks failing replica node_id on Err (skips
    LockedByOther — control-plane fence event, not health signal)
  - 30 s TTL (env: AUTUMN_STREAM_BAD_NODES_TTL_SECS, clamped 1-600 s)
  - select_nodes + fallback walk filter by exclude_node_ids; fall back
    to unfiltered set if exclusion under-fills the pool
  - 5 new unit tests: 3 bad_nodes semantics + 2 wire-compat round trip
  - 45/45 stream lib + 58/58 manager lib + 13/13 rpc lib all green
  - 2 pre-existing flaky f099i_* partition-server timing tests remain
    failing pre-F190 (verified via git stash baseline)

Next steps (this loop):
  F191: ExtentNode bind 2nd listener at port+1000; --control-port flag.
        MgrNodeInfo.control_address rkyv schema extension.
        Manager carries control_pool: Rc<ConnPool>.
        disk_status_update_loop + recovery_collect_loop DF calls switch
        to control_pool. Replace conn_pool.call(EXT_MSG_DF) with
        control_pool.call_timeout(..., 5s) — longstanding P0 bug fix.
        cluster.sh + autumn-client info display both addresses.
  F192: depends on F191. New MSG_REPORT_DISK_FAILURE over control_pool;
        StreamClient fires fire-and-forget on apply_completion Err.
        Manager 60 s window, 3-distinct-reporter_part_id quorum →
        mark_node_disks_offline. disk_status_update_loop success
        clears the entry.
  F193: independent. Pipelined EC encode (Option B, 2-chunk inflight),
        chunked recovery/copy_extent, jemallocator with dirty_decay_ms.

Conversation context (carried from prior commit):
  Live cluster (4 extent-nodes + 1 PS at 127.0.0.1) showed
    1. disk-3 flap on extent-node 9103 (root cause: shared ConnPool
       backpressure under EC convert; F191 fixes via control_pool).
    2. forcegc 27 24 silent no-op (separate issue, deferred).
    3. extent-node RSS at 369-770 MB steady, ~3 GB peak during EC
       convert (F193 fixes).
