Date: 2026-07-14
TaskStatus: completed
Task scope: BUG-READ-TIMEOUT-STORM — generalize the write-only size-scaled
  append deadline into a shared IoDeadline primitive and apply it to the READ
  paths (the 8 MiB direct-read timeout storm: 566-646 "direct read TIMED OUT"
  under 32 concurrent 8 MiB direct reads on one congested EN).

Current summary (committed 954dc6a):
  IoDeadline (renamed from AppendDeadline; effective_io_timeout, IO_TIMEOUT_CAP,
    for_len/for_chain) is now shared by append + read. Reads use SEPARATE params
    read_base_timeout 3 s + read_floor_bytes_per_sec 4 MiB/s (looser than the
    8 MiB/s append floor — no RF=3 fsync barrier). 8 MiB read -> 5 s, 256 MiB
    chunk -> 67 s (< 600 s cap, never binds). Applied at all 5 size-varying read
    sites (read_extent_value_direct, read_value_into_pooled,
    read_shard_chunk_from_addr, ec_reconstruct_shard_subrange, ec_read_full).
  Retry damping on liveness timeout (is_liveness_timeout): read_redirect_replicas
    (client) + read_value_into_pooled (stream) stop walking replicas after 2
    timeouts, fall through to the authoritative proxy/copy path. Fast errors keep
    the full rotation.
  Append boundedness preserved verbatim (cap + for_chain-caps-after-multiply).
  Green: 103 stream lib (7 io_deadline_tests) + 27 client + 195 partition-server;
    0 clippy. coco (GPT-5.5 deep) 3 findings triaged (EC loser-timeout trade-off
    documented — EC off by default; public-config-field non-issue; test comment
    clarified). Stream CLAUDE.md note 29; feature_list F-EN-SHARD-HASH +
    F-READ-OPENTAIL-ROTATE (the underlying load-imbalance CAUSE, deferred).

Main gaps:
  - The load-imbalance CAUSE (shard-0 aliasing + open-tail replica[0] pinning)
    is recorded but NOT fixed — deadlines + damping only stop the STORM. See
    feature_list F-EN-SHARD-HASH / F-READ-OPENTAIL-ROTATE.
  - F-SEALED-EMPTY-SWEEP (BUG-FLUSH-TIMEOUT-LEAK backstop) still deferred.

Retest (2026-07-14, DONE — image 16ae27f rolled out to VKE, stop-the-world):
  8 MiB perf-check --direct-read, 32 threads, 32 partitions:
    write 920 MB/s (RF3 fsync-bound, p99 390 ms);
    DIRECT READ 3949 MB/s, p50 30 ms / p99 205 ms, 10368 ops, ZERO errors.
  Storm GONE: 0 "TIMED OUT" / fallback lines across all 3 PS + 5 EN pods AND
  the client perf pod (previously 566-646 direct-read timeouts + polluted
  throughput). Prior healthy baseline was 3182 MB/s → now 3949 MB/s, clean.
  Extents now spread across all 5 EN (70/53/63/49/56), not shard-0-aliased.

Next steps (optional, deferred backlog):
  - F-EN-SHARD-HASH / F-READ-OPENTAIL-ROTATE — fix the underlying load skew
    (the deadlines + damping only stop the STORM symptom).
  - F-SEALED-EMPTY-SWEEP — BUG-FLUSH-TIMEOUT-LEAK backstop.
