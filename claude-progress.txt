Date: 2026-07-04
TaskStatus: not_completed
Task scope: F-DASH-IN-MGR — fold the standalone Python web dashboard + auto-policy
  controller (python/dashboard/) into the autumn-manager binary. All-in-one, HTML
  embedded via include_str!, and (M2+) the auto-policy loop runs as a leader-fenced
  manager background task so it survives as long as the leader does (the Python
  webserver could be killed, stopping all auto-policy). User decision: autoPolicy
  runs ONLY on the etcd leader. Full plan: docs/dashboard_in_manager_plan.md.

Current summary:
  · Plan produced by a Fable planning sub-agent + verified against real symbols;
    4 design forks decided by the user (rollout=on-by-default bound to manager host;
    one flag arms both loop+UI; retire python/dashboard/ fully; include autumn-op
    auto-policy subcommands). Design doc: docs/dashboard_in_manager_plan.md.
  · M0 COMPLETE + verified. New crates/manager/src/dashboard.rs: axum served by the
    compio-native cyper_axum::serve over a compio TcpListener — SAME HTTP stack as
    examples/gallery (user decision mid-M0: first shipped hand-rolled, then switched
    for one web-serving pattern across the repo). send_wrapper::SendWrapper bridges
    axum's Send bound over the !Send manager (gallery idiom). Under spawn_supervised
    (F228); include_str! of the copied 29 KB dashboard_web.html. Endpoints: GET /
    (page), /healthz, /api/overview (read-only, in-process, byte-compatible with the
    Python build_overview contract), stubs for /api/partition/<id> (M1), /api/policies
    (M2), POST /api/action (M3). handle_cluster_df + handle_get_cluster_overview
    refactored to compute_*_resp pure builders (rpc_handlers.rs) reused directly by
    the dashboard (no self-RPC). Flags --dashboard-port/--dashboard-listen/
    --dashboard-allow-mutations wired in crates/server/src/bin/manager.rs
    (start_dashboard before serve()). Deps added to manager crate: axum, cyper-axum,
    send_wrapper, serde_json (dashboard-only; RPC stays rkyv).

Verification (M0):
  · cargo check + clippy -p autumn-manager + server bin: clean (0 warnings).
  · cargo test -p autumn-manager --lib: 172/172 (was 171; +1 overview_json contract
    test; the 4 hand-rolled-parser tests were removed with the parser).
  · e2e smoke: memory-mode manager on :19001 + dashboard :18799 — GET / serves the
    byte-exact 29680-byte page, /api/overview returns python-valid JSON with all
    contract keys, /healthz 200, unknown 404, POST /api/action 403 read-only; RPC
    plane concurrently live.
  · coco findbugs (GPT-5.5) ran on the FIRST (hand-rolled) M0 diff: P1 no read
    timeout (Slowloris) + P2 unchecked Content-Length arithmetic. The axum switch
    dissolves both (hyper rejects hostile Content-Length: u64::MAX → 431 no crash;
    DefaultBodyLimit caps bodies). Residual (documented, = gallery): no header-read
    timeout in cyper_axum::serve → active Slowloris cut-off not enforced; compensated
    by read-only-default + network-ACL guidance.

Main gaps / next steps:
  · UNCOMMITTED. M0 is a clean checkpoint; awaiting user decision on commit
    (solo-flow push-to-main) + coco review (rule 12).
  · M1: full data parity — /api/partition/<id> (extract handle_get_partition_detail
    to compute_*), advisories with desc/cmd/key (port describe_candidate/
    candidate_to_cmd), node auto_state from handle_list_node_states, side-by-side
    parity check vs the Python page on a live cluster.
  · M2: crates/manager/src/auto_policy.rs — ported decide_actions/candidate_to_cmd/
    cooldown_key + unit tests (from test_autumn_dashboard.py); MgrAutoPolicyConfig
    etcd keys (autoPolicy/config,cooldowns) + replay_from_etcd; spawn_supervised
    leader-gated loop capped at DryRun; MSG_AUTOPOLICY_GET/SET (wire bump) +
    autumn-op auto-policy status|activate|deactivate; /api/policies GET/upsert/
    activate/delete live. INVARIANT: leader-only tick.
  · M3: --dashboard-allow-mutations gates loop-Armed + /api/action; in-process
    actuation (auto_dispatch_split, extracted do_merge_partitions F185 path, PS
    MSG_MAINTENANCE gc/compact, force-ec inner fn); verb-whitelist validation.
  · M4: delete python/dashboard/ (5 files); DASHBOARD.md → docs/ops.md; update
    README, crates/manager/CLAUDE.md (F203 reconciliation note), crates/server/
    CLAUDE.md, deploy k8s/baremetal + cluster.sh env→flag threading.
