Date: 2026-05-10
TaskStatus: completed
Task scope: F189 — GC + compact Stage 3 (two-class admission control:
            fg priority + bg elastic, CockroachDB kvadmission pattern).
            Replaces F188's single-class IoTokenBucket; FG writes
            now also consult the controller (vs F188 where they
            bypassed entirely).

Live verification (4-node cluster, fg_rate=10 MiB/s, bg_rate=32 MiB/s,
saturation threshold=0.5):
  16-thread wbench at 8 KiB values: throttled to 9.98 MB/s (vs 10
  MiB/s ceiling — 0.2% accuracy). Latency p50=12.5ms (vs ~1ms
  unthrottled — expected queueing under admission cap).

Unit tests (f189_admission_tests, 7/7 passing):
  fg_unlimited_no_sleep
  bg_unlimited_no_sleep
  bg_respects_own_rate
  bg_yields_when_fg_saturated
  bg_does_not_yield_when_fg_idle
  bg_ignores_fg_when_fg_unlimited
  window_resets_after_1s

Files changed:
  crates/partition-server/src/lib.rs
    - IoTokenBucket renamed to AdmissionController; state now tracks
      fg_bytes + bg_bytes independently (single 1s wall-clock window)
    - account() split into:
        account_fg(bytes).await — sleeps only when fg_rate > 0 AND
                                  exceeded; default unlimited
        account_bg(bytes).await — sleeps until BOTH bg ceiling AND
                                  fg-aware yield (when fg observed
                                  rate > saturation_ratio * fg_rate)
    - new env vars: AUTUMN_PS_FG_RATE_BYTES_PER_SEC (default 0),
                    AUTUMN_PS_FG_SATURATED_THRESHOLD (default 0.8)
                    AUTUMN_PS_BG_RATE_BYTES_PER_SEC (default 256 MiB/s,
                    same as F188)
    - new test module f189_admission_tests (7 tests)
  crates/partition-server/src/background.rs
    - start_write_batch calls admission.account_fg(total_value_bytes)
      .await once per batch before Phase 2 launch (the FG hot path)
    - all BG sites: io_bucket.account(...) → io_bucket.account_bg(...)
  feature_list.md
    - F189 row + Open/Deferred entry (mechanism + verification + Stage 4
      future deferrals)
  crates/partition-server/CLAUDE.md
    - note 11 extended with F189 admission controller details
  claude-progress.txt
    - this file

Tests:
  cargo check -p autumn-partition-server          : clean
  cargo test -p autumn-partition-server --lib
                                  -- --test-threads=1 : 131/131
                                  (was 124, +7 F189)
  cargo test -p autumn-rpc                        : 13/13
  cargo test -p autumn-manager --lib              : 58/58
  Live cluster: fg ceiling honored to 0.2%

Cluster currently RUNNING with Stage 3 active. Default config in
production should be:
  AUTUMN_PS_FG_RATE_BYTES_PER_SEC=0           (unlimited; backwards-compat)
  AUTUMN_PS_BG_RATE_BYTES_PER_SEC=268435456   (256 MiB/s)
  AUTUMN_PS_FG_SATURATED_THRESHOLD=0.8

Operators who want to enforce a fg ceiling for predictable bg
behaviour can set AUTUMN_PS_FG_RATE_BYTES_PER_SEC > 0; the bg fg-aware
yield activates at that point.

Next steps:
  - Distributed-system race review (in progress in the conversation
    after this commit)
  - Stage 4 (deferred): per-op priority hints, per-extent-node
    bandwidth attribution, integrate with F183 advisory's req_per_sec
    instead of local 1s window.

Final commit: pending.
