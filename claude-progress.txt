Date: 2026-05-05
TaskStatus: completed
Task scope: F140 — Structural split-vs-{compact,GC} serialisation (dual-gate acquisition).

Root cause (confirmed):
  Two race subwindows between handle_split_part and background writers
  sharing the P-log compio runtime:
  (A) F140-A: background_compact_loop had a RowAppendReq awaiting its P-bulk
      oneshot when handle_split_part read commit_length(row_stream_id) and
      called multi_modify_split. The in-flight append landed past the seal
      point on replicas not yet receiving the eversion-bump push.
  (B) F140-B: background_gc_loop (run_gc) had a log_stream append via
      part_sc in-flight for the same reason — no gate prevented GC from
      writing past the newly sealed log_stream tail.

Fix (dual-gate pattern):
  1. PartitionData now holds compact_gate: Arc<CompactionGate> (PS-wide,
     clone of the existing gate used by background_compact_loop) and
     gc_gate: Arc<CompactionGate> (new per-partition gate).
  2. background_gc_loop receives gc_gate; acquires it around the
     "for eid in holes" block (covering all run_gc calls).
  3. handle_split_part acquires compact_gate then gc_gate (in that order)
     after the has_overlap guard and before flush_memtable_locked /
     commit_length / multi_modify_split. Both released RAII on return.

Files modified:
  - crates/partition-server/src/lib.rs (PartitionData two new fields,
    gc_gate creation + PartitionData init, background_gc_loop spawn,
    2 new F140 unit tests)
  - crates/partition-server/src/background.rs (background_gc_loop signature,
    gc_gate acquire/drop around holes loop)
  - crates/partition-server/src/rpc_handlers.rs (handle_split_part dual-gate
    acquisition)
  - crates/partition-server/CLAUDE.md (F140 dual-gate pattern documented)
  - feature_list.md (F140 passes:true + structural fix description)
  - README.md (F140 manual repro block)
  - claude-progress.txt (this file)

Verification:
  - cargo build --workspace: clean (only pre-existing warnings).
  - cargo test -p autumn-partition-server --lib: 105 pass / 2 pre-existing
    f099i parallel-test race failures (same as F139/F141/F142/F143 baselines).
  - cargo test -p autumn-stream --lib: 48/48 pass.
  - cargo test -p autumn-manager --lib: 24/24 pass.

Next steps:
  - Live cluster re-verification (cluster.sh reset 4 + wbench + concurrent
    compact/gc/split cycle) deferred per project policy on destructive commands.
  - F141: already completed (passes:true).
  - F145 or next unimplemented feature from feature_list.md.

Root cause (confirmed):
  Two reachable race subwindows between handle_delete_extent and run_recovery_task
  on the same extent_id:
  (a) Resurrection: ensure_extent auto-creates with create:true after delete
      unlinks the file → orphan on disk, no manager record.
  (b) Write-to-unlinked-inode: recovery holds Rc<ExtentEntry>; delete unlinks
      the path; recovery's pwrite succeeds on unlinked inode; data evaporates
      on fd drop. apply_recovery_done's None-branch silently Ok(())'d the stale
      task, masking the corruption.

Fix (symmetric in-flight-set pattern from F138):
  1. recovery.rs dispatch_recovery_task: skip dispatch when extent is in
     pending_extent_deletes — next 2 s tick naturally finds extent gone.
  2. rpc_handlers.rs handle_stream_punch_holes: return Err(Precondition) if
     any refs→0 extent is in recovery_tasks. PS GC retry backs off.
  3. rpc_handlers.rs handle_truncate: symmetric guard to (2).
  4. recovery.rs apply_recovery_done None-branch: enqueue PendingDelete for
     recovering node — immediate reap instead of 5-min orphan-reconcile wait.
  5. extent_node.rs handle_delete_extent: return CODE_PRECONDITION when
     recovery_inflight.contains_key(&extent_id). extent_delete_loop retries
     up to 60 × 2 s = 2 min budget.

Belt-and-braces: both manager and extent-node sides independently refuse,
covering leader failover where pending_extent_deletes is lost in-memory
but recovery_inflight survives on the extent-node process.

Files modified:
  - crates/manager/src/recovery.rs (dispatch pending-delete skip, apply None cleanup)
  - crates/manager/src/rpc_handlers.rs (punch_holes and truncate recovery guards)
  - crates/stream/src/extent_node.rs (handle_delete_extent precondition check,
    clone_recovery_inflight test helper)
  - crates/manager/src/lib.rs (4 new F139 unit tests)
  - crates/stream/tests/f139_delete_recovery_race.rs (new integration test)
  - crates/manager/CLAUDE.md (note 11 documenting F139 lock semantics)
  - crates/stream/CLAUDE.md (F109 section updated with F139 guard documentation)
  - feature_list.md (F139 passes:true + fix description)
  - README.md (F139 manual repro block to be added)
  - claude-progress.txt (this file)

Verification:
  - cargo build --workspace: clean (only pre-existing warnings).
  - cargo test -p autumn-manager --lib: 24/24 pass — 20 pre-existing + 4 new F139.
  - cargo test -p autumn-stream --lib: 48/48 pass.
  - cargo test -p autumn-stream --test f139_delete_recovery_race: 1/1 pass.
  - cargo test -p autumn-partition-server --lib: 103/105 pass — same 2
    pre-existing f099i parallel-test race failures as before.

Next steps:
  - README.md F139 manual repro block (concurrent recovery + GC scenario).
  - F140: Structural split-vs-compact serialisation (P-bulk drain barrier).
  - F141: GC log_stream rewrite storm — batch + must_sync=false + yield + rate-limit.
