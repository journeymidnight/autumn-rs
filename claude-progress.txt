Date: 2026-04-24
TaskStatus: completed
Task scope: F101-b root-cause the client-side UCX hang; fix under
thread-per-core; verify the full 2×2×2 matrix now produces end-to-end
numbers for both TCP and UCX.

Current summary:
  Used systematic-debugging skill with fail-stop experiment gates — no
  blind trials. Results:

  Exp 1 (p=32 × 256t × d=1) → 0 ops. Kills H1 (per-listener EP
    saturation) as primary cause — even at 8 EP/listener, 256 client
    threads hang.
  Exp 2 (fix p=32, sweep threads) → 64t=18 k, 128t=58 ops
    (collapse), 256t=0. Phase transition at 128 clients. Confirms
    problem is client-side scale, NOT per-listener.
  Exp 3 (UCX log + HCA caps):
    • uct_cm.c:97 DIAG "resolve callback failed with error:
      Destination is unreachable" — RDMA CM layer fail at connect.
    • PS log silent during hang — failure before server-side accept.
    • ibv_devinfo mlx5_0: max_qp=131 072, max_cq=16 M — HCA fine.
    • ulimit -l = 8 MB — present but doesn't change the diagnosis.
  H5 (connect storm) partial-confirm: connect-stagger 5 ms/tid on
    128t → 551 ops/s (10× over no-stagger), 256t → still 0. Stagger
    code reverted (dead code vs. the real fix).
  Decisive fix test: keep in-flight ≈ 256 but via 16 threads ×
    depth=16 instead of 256 threads × depth=1:
    • UCX p=32 × 16t × d=16 → 95 k write / 1.71 M read ops/s.
    • TCP p=32 × 16t × d=16 → 166 k write / 1.81 M read ops/s.
    Historical "1 M TCP reads" memory ratified.

  Root cause: perf_check.sh's hardcoded --threads 256 itself
  violates thread-per-core on the client side (192 logical CPUs
  available, but 256 OS threads each pinning a ucp_worker is
  *strictly worse* than fewer threads × deeper pipeline, and at
  100+ concurrent rdma_cm connects UCX saturates).

Fix (no server-side code change — thread-per-core preserved):
  ✓ perf_check.sh default: --threads 16 (overridable via --threads N).
  ✓ Matrix default still 2×2×2 (transport × partitions{1,8} ×
    pipeline-depth{1,8}); now all 8 combos produce non-zero
    end-to-end numbers.
  ✓ await_ports_clear deadline bumped 90 s → 180 s so TCP→UCX
    cluster transition doesn't die on TIME_WAIT from the prior
    TCP client run.
  ✓ Stagger code reverted.
  ✓ Eight fresh per-combo baselines written
    (perf_baseline_${transport}_p${parts}_d${depth}.json).

Default matrix measured this session (threads=16, disk, 4 KB):
  TCP p=1 × d=1 :  42 k w /  60 k r
  TCP p=1 × d=8 :  68 k w /  91 k r
  TCP p=8 × d=1 :  69 k w / 467 k r
  TCP p=8 × d=8 : 142 k w / 1.11 M r  ← TCP best
  UCX p=1 × d=1 :  33 k w /  70 k r
  UCX p=1 × d=8 :  49 k w / 104 k r
  UCX p=8 × d=1 :  61 k w / 277 k r
  UCX p=8 × d=8 : 129 k w / 764 k r   ← UCX best

Acceptance:
  (a) ✓ Default perf_check.sh completes all 8 combos non-zero.
  (b) ✓ TCP best read ≥ 1 M ops/s.
  (c) ✓ UCX best read ≥ 500 k ops/s (764 k actual).
  (d) ✓ No server-side code change (thread-per-core preserved).

Next steps:
  - None in this area. UCX + TCP both converge on the same
    scaling rule: total in-flight = threads × pipeline-depth; keep
    threads ≤ ~32 for cache-locality + avoid rdma_cm saturation.
  - Separate concern (not touched this session): cross-host UCX A/B
    (F100-UCX gate c) still requires a 2-host deploy.
