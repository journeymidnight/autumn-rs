Date: 2026-09-02
TaskStatus: not_completed
Task scope: (上一条见文末 2026-09-02 条) 账本整理——把 2026-09-01 的三条重叠条目合并成一条"当前状态"（其中第一条的
  头部被改成 ingest/graph 而正文仍是 S3 网关，已拆开归位），按规则 12 裁掉 08-28 及更早
  （约 30 条，靠 git log 与各 crate CLAUDE.md 追溯）。feature_list 删掉 5 条已 pass 的条目
  + F-HICACHE-PORT-REBASE（用户 2026-09-02 定：那是别的项目的事，移植本体随 da91d38 去了
  ../buda）。S3 网关跨机重测经用户确认**不需要**，不再列为待办。无代码改动。

【当前状态 1：权重加载 —— autumn-s3 网关是唯一路径】
  examples/s3-gateway（bin `autumn-s3`）只实现 runai 真正发的三件事，不验签、path-style，
  读穿 autumn-fuse core。真引擎验收（Qwen3-8B / 16 GiB / 单张 H200）：
    vLLM 0.28    本地盘 23.3 冷 / 14.4 页缓存热 | 网关 18.5
    SGLang 0.5.10 本地盘 35.7                   | 网关 37.1
  三条路径贪心解码补全与本地盘**逐 token 相同**。`--workers`（SO_REUSEPORT，默认 8，
  拐点 4）修掉单 accept 线程瓶颈后，runai→网关 ~1300 = native `read_into` 的 98%
  ⇒ F-S3-RUNAI-PLUGIN 判据("网关达 native 85% 以上则不做")未触发，WON'T-DO。
  `python/autumn_vllm_loader/` 整包已删（57e7e60）：它只对 vLLM 有效，SGLang 的 LoadFormat
  是封闭枚举、全树无 register_model_loader，网关才是两边都能跑的那条路。
  FreeToken 没有 loader 抽象层也没有 runai_streamer ⇒ 对它网关无用，FUSE 是唯一路径。
  注意：`python/autumn_kvcache/_identity.py` 仍读 `model_loader_extra_config["path"]` 当
  tenant 指纹来源。那是已删 loader 的键，现在不会有人设，但**机制要留着**——它防的是
  BUG-KVC-TENANT（多模型共用固定 config 目录 ⇒ tenant 撞车），Recipe A/B 照样能造出该局面。
  【教训】"排除了 A 和 C，所以是 B" 在没有 B 的独立参照时不算证明。MinIO 这个参照实现
  只花几分钟就推翻了"CRT 客户端慢"的归因（真因是自家单线程 accept）。定位第三方组件慢
  之前，先起一个 known-good 对照端。
  【坑（已写 ops.md）】aws-c-s3 认 HTTP_PROXY 但不认 NO_PROXY：有代理的环境里 boto3 侧
  list 正常，每个权重读全挂 AWS_ERROR_S3_INTERNAL_ERROR 且不开任何 socket。指纹 =
  "目录列得好好的，读全失败"。engine 进程必须 unset HTTP_PROXY/HTTPS_PROXY。

【当前状态 2：线上 v29 已全量重建】
  用户拍板"数据可以全毁" ⇒ v28→v29 跨了 96a5c01 的 MVCC 比较器改动（旧 SST/WAL 不可读、
  无迁移，见 docs/ops.md），删 vLLM + 4 个 STS + 6 个 PVC（58GB 逻辑数据）后 deploy.sh 重建。
  重建暴露 3 个"cluster.sh 早改了、k8s 路径没跟上"的陈旧配置，均已修复：
    1. deploy.sh --restart 的选择器 `-l 'component!=etcd'`——pod 标的是
       `app.kubernetes.io/component`，`!=` 匹配不存在的 key 恒为真 ⇒ 实测匹配 18 个 pod
       （含 etcd、别的负载、已完成的 Job pod），只该匹配 10 个。(cd76db2)
    2. AUTUMN_BOOTSTRAP_PRESPLIT=32 切的是 RAW keyspace，namespace-blind ⇒ `{ns}/` 布局下
       **一直什么也没切**。overlay 去掉该值；entrypoint 改成拒绝而非静默忽略。(3fb2585)
    3. bootstrap Job 没挂 autumn-authz 却指了 AUTUMN_ADMIN_TOKEN_FILE；补挂后仍失败，因为
       `--admin-token-file` 是 GLOBAL flag，被加在子命令之后会被丢弃。(3fb2585 + 9c23776)
  运维坑：`kubectl delete sts` / `delete pvc` 被 auto-mode 分类器拦，需用户用 `!` 跑；
  delete deploy / delete job / delete pod / apply 都放行。
  **授权姿态的实测事实**：manager.rs 未给 --auth-protected-prefix 时编译期默认**只保护
  `mem/`**。k8s ConfigMap 没设该变量 ⇒ 本集群 `fs/` 与 `kvc/` 数据面 authz **不生效**，
  FUSE 挂载与 kvcache 连接器都不需要凭据。（cluster.sh 里"有 signing key 就保护一切"的
  注释与 binary 实际默认不符，注释陈旧。）

【当前状态 3：mountPropagation 在本集群不生效 ⇒ FUSE 只能单容器】
  实测：特权 sidecar 里 `grep -c "shared:" /proc/self/mountinfo` = **0**，FUSE 挂载
  PROPAGATION=private、父挂载是容器根 overlay。Bidirectional/HostToContainer 被 API server
  接受但**节点侧没兑现**（kubelet 未配 rshared）。改成**单容器**（autumn-fuse 后台进程 +
  应用同容器，同一 mount namespace）后成功，代价是整个容器 privileged。
  ⇒ docs/ops.md 里那份 sidecar manifest 与本集群不符，需改写成单容器形态（待办）。
  与之相关：v29 镜像里确实有 `autumn-fuse`，`dd iflag=direct` 4K/8M 两档在挂载点上都过
  ⇒ FTW reader 的 O_DIRECT 快路可用，不会踩 MAP_SHARED/ENODEV 的 mmap 回退。
  仍保留的约束：FUSE 上禁用 `--expert-load parallel`（无回退的 O_DIRECT，只捕获
  NotImplementedError）。

【当前状态 4：memory-mcp / autumn-memory —— ingest 提速 + graph API】
  ingest ~99% 在等待，成本单位是**往返**不是字节/CPU（真集群 0.53s/chunk vs loopback
  0.02s/chunk）。已做：`begin_bulk_index`/`flush_stats` 延迟 `meta/stats`（原本每篇 2 次
  往返写一个计数器，也正是 ingest 循环无法并发的原因——单 key 并发 RMW 丢更新）；循环拆成
  顺序 CPU 段 + 并发 I/O 段；bulk 窗口内 memoize centroid 表；所有
  `for k in keys { get(k).await }` 换成 get_many/delete_many。(6b3a408)
  BM25 倒排原本**逐词项串行写**，CJK unigram 分词下一个 ~1.5KB 中文 chunk 有几百个独立字
  ⇒ 每 chunk 几百次串行往返，5164 chunk × 0.53s ≈ 45 分钟，表现为"挂死"。已改批量。(cda9171)
  顺带挖出一个真 bug：RPC 写路径的 writev 没按 IOV_MAX 分段，宽批次（1075 iovec / 仅 57KB）
  被内核按**条数**拒绝(errno 40)，写线程直接退出，对上表现为 "connection closed" /
  `batch_put op status=1`。已按 IOV_MAX(1024) 分段。
  graph 层改成通用图数据库 API（不再局限于代码索引）：graph_upsert_node/get_node/
  delete_node/add_edge/delete_edge/neighbors/traverse/nodes + `/graph/*` 只读 HTTP 路由；
  callers/callees/members/outline/trace 退化成固定边类型+方向的简写。17 个 MCP 工具。(7ae8ccb)
  本地 3 节点验证：368 chunks ingest 干净、reconcile is_clean、lexical/hybrid 与大纲正常。
  **pod 内复测仍待做**（需先出镜像；日志已按阶段打耗时 + ms/chunk，据此定 startupProbe 预算）。

【当前状态 5：kvcache v2 多池接口已实现但未测】
  efeea3c 实现 sglang HiCacheStorage 的 v2 多池接口（SWA / Mamba / DSA indexer / MTP draft
  等 sidecar pool）。KV 键与 v1 逐字节相同（`full_key(tenant, hash, pool)` 的 pool 段本就
  独立，KV 段就是 "kv"）⇒ 加法，不是迁移。存在性探测把 KV 键重新 scope 进各池的段
  （transfer.keys 只贡献长度），折叠**取窄**（sidecar 缺页则缩短可用前缀），探测出错一律
  算 miss；页视图按**拥有该索引的池**解析（解错池会静默读到错误字节，形状一样）。
  v2 类型独立 import 且容忍缺失，老 sglang 仍走 v1。**本机没有够新的 sglang，未跑过。**

【项目边界（用户拍板）】FreeToken + 其上的 MCP 部署是 autumn 的**用户**，已拆出到
  github.com/thesues/buda：Dockerfile.freetoken + k8s/{freetoken,upload-model-minimax,
  memory-mcp}.yaml。autumn-rs 保留集群本体、all-roles 镜像、autumn-fuse、memory-mcp 源码。
  判据 = "这个负载不存在了，这东西还在吗"。后果：CP 流水线 freetoken-autumn 要改指 buda 仓库，
  且 AUTUMN_IMAGE 不能再用 ${SCM_COMMIT_ID}（两仓 commit 空间不重合），WIRE 锁步退回手动维护。

【规则：任何"上游没有 X"的结论，先确认所查快照与上游的距离】三次栽在这上面：
  ① sglang——基于落后 6232 commit 的克隆断言"没有 SWA+HiCache"，错（上游 UnifiedRadixCache
  3120 行 + unified_cache/ 子包，HiRadixCache 已是死代码）；② FreeToken 无 runai_streamer；
  ③ FreeToken 至今**完全没有分层缓存**（上游 a2538a4 只有一行 TODO 注释）。

【环境坑（供后续会话直接用）】
  - SGLang 0.5.10 需要 runai-model-streamer>=0.16（0.15.6 不导出 ObjectStorageModel）；
    共享机器不动系统包，用 `uv pip install --no-deps --target <dir>` + PYTHONPATH 覆盖
    （带 deps 装会拉进 torch/torchvision 盖掉系统的 → "operator torchvision::nms does not exist"）。
  - sglang.Engine 用 spawn 起子进程，测试脚本必须有 `if __name__ == "__main__"` 守卫。
  - python/autumn_kvcache 全套跑 test_no_hit_dedup.py 3 挂、单跑该文件 6/6 过；HEAD 上同样
    （stash 实证），是既有跨测试状态污染，未处理。

Next steps:
  1. 出新镜像后在 pod 内复测 ingest，据此定 memory-mcp 的 startupProbe 预算。
  2. docs/ops.md 的 FUSE sidecar manifest 改写成单容器形态（mountPropagation 本集群不兑现）。
  3. F-IMG-FUSE-MCP 残余验收：镜像内 `memory-mcp --help`、杀容器重拉不因残留挂载点 EBUSY。
  4. kvcache v2 多池接口找一个够新的 sglang 实测（本机 sglang 0.5.10 太旧）。
  5. CP 流水线改指 buda 仓库（FreeToken 部署本身已不属本仓库）。
─────────────────────────────────────────────────────────────────
Date: 2026-08-30
TaskStatus: completed
Task scope: chaos 多轮 soak(10 轮:5 随机全动作 + 5 定向组合)。结果 8 过 2 挂,
  两次失败同一根因,已修 (efbb28e)。
【失败指纹高度一致】两轮都是 inflight_errors=1、其余全零(mismatches/not_found/range/
  accounting/reclaim 都是 0),且都恰好有【2 次 EcConvert】;只有 1 次 EcConvert 的轮次全过。
  这个"失败轮 vs 通过轮的唯一差异"直接把假设钉死,不用猜。
【根因】第二次提交发生在第一次转换【已关闭之后】⇒ 不 attach ⇒ 新建 ledger 条目;
  而此时 extent 已是 EC,handle_force_ec_convert 回 CODE_OK "already ec_converted"
  (作为幂等 RPC 正确),但 actuation 把所有 CODE_OK 一律映射成 Dispatched ⇒ 条目停在 RUNNING,
  等一个永不到来的 apply_ec_conversion_done。而 sweep_running_ttl 只覆盖 compact/gc/forcegc,
  EC 连 30 分钟翻 UNKNOWN 都轮不到,只能等 256 环淘汰 ⇒ ops list --active 里长期挂着幽灵转换。
  修 = 问【marker】是否真有转换在飞,而不是读响应消息(消息匹配正是本 session 已经修过一次的
  分类器漂移反模式)。无数据面影响,是 ledger 卫生问题。
【一个被我自己否掉的改动】同时把 complete_by_extent 改成关闭该 extent 的【全部】active 条目,
  然后撤销了:submit 在条目 active 时会 attach,所以两个 active 条目根本不可能共存 ——
  我为它写的测试在【未修复的代码上也通过】,正是这一点暴露了它是死代码。
【测试】ec_convert_on_an_already_converted_extent_does_not_park_the_op 走真实路径
  (run_submitted_op),旧映射下失败并报 "extent 12 already ec_converted"。
  两个失败 seed 重跑转绿,且触发条件仍在(各 2 次 EcConvert)、in-flight verify 零错。
【本轮 soak 覆盖】EcConvert 出现在 7/10 轮(定向组合是关键——纯随机约 1/4 命中率,
  纯 ec 动作集则因"无 sealed extent"全跳过)。manager lib 299。
Next steps: 无待办。
─────────────────────────────────────────────────────────────────
Date: 2026-08-29
TaskStatus: completed
Task scope: 换 seed 重跑 chaos。结果:抓到并修掉一个 liveness wedge (abca022)。
【4 个随机 seed 全过】20260829/777/31337/4242,各 400 keys、in-flight verify errors=0。
  seed 4242 的 399 keys 不是丢数据:4 个 PUT 超时被正确丢出预期集(超时=结果不确定),
  reads miss=0。
【覆盖面不够,补定向】随机 nemesis 很少抽到 EC:4 个 seed 里只有 31337 抽到一次,而且
  【被跳过】("no sealed extents")。纯 ec 动作集 9 次全跳 —— 因为只有滚动才封口,60s 跑不出滚动。
  必须配 split/merge/gc 才有 sealed extent 可转。
【抓到的 bug】ec,kill,split,merge,gc 组合下:一个 EC 转换永不完成,每 5s 重派发,
  WriteShard 恒被拒 req_owner_epoch=24 / local_owner_epoch=27;marker 永久 pinned,
  该 extent GC 一直被拒 = 无界泄漏。【无数据丢失】(mismatches=0 not_found=0),是 liveness。
  根因:dispatch_owner_epoch_for_extent 返回【第一个】引用该 extent 的 partition 的 epoch,
  而 split/merge 后 extent 是 CoW 共享 —— 两个 partition 各持 owner lock,EN 围栏由实际写者
  抬高,取到另一个就低于围栏;重派发又解析到同一个错 partition ⇒ 永不收敛。
  "第一个"取决于 HashMap 迭代序,所以只在 split/merge 共享过该 extent 时复现。
  【不是本轮回归】该函数来自上一轮 4040dbe(修的是"marker 建时冻结"那一半),今天没 commit
  碰过派发路径。修 = 取 MAX(围栏只升不降;单 partition 时行为不变)。
  单测 cow_shared_extent_resolves_to_the_highest_owner_epoch 确定性复现(返回 24 应为 27)。
【修后验证】同一 seed+动作组合转绿:invariants OK 400 keys、in-flight errors=0、
  stale owner_epoch 拒绝 0 次、EC convert failed 0 次、reclaimed 8 extents(wedge 那轮 0)。
  4 个原 seed 全部重跑通过;manager lib 298。
【运维事实(已写进 ops.md)】① 一轮 chaos 烧掉约 5 万个回环临时端口,TIME-WAIT 各约 60s 回收
  ⇒ 连跑必须等回收(门限 <2000),否则 EADDRNOTAVAIL 被 verify 读成 wedged partition,
  表现为"几乎所有 key not_found 且 mismatches=0"——那是端口耗尽的指纹,不是丢数据。
  ② EcConvert nemesis 需要先有 sealed extent,单独跑 ec 动作集等于什么都没测。
Next steps: 无待办。
─────────────────────────────────────────────────────────────────
Date: 2026-08-27 (b)
TaskStatus: completed
Task scope: MVCC 内部 key 编码重构——去掉 0x00 分隔符,改显式比较器(user-key 优先)。
  内部 key 从 `user ++ 0x00 ++ BE(MAX-seq)` 改为 `user ++ BE(MAX-seq)`,排序不再是裸字节序,
  统一走 `cmp_internal_keys`(先比 user key,再比定宽 8B inverted-seq 后缀)。
【为什么】旧分隔符只是模拟"先比 user key":对含 0x00 的 user key 失效("a" vs "a\x00b" 反序,
  "a" 会被 seek_user_key 查不到、扫描顺序颠倒——三个 pre-change 复现测试都实证失败)。
【比较点枚举方法】三路:①从生产者流向追(key_with_ts/SST decode/WAL replay 的 key 流经处);
  ②全 crate 文本扫(比较运算符/sort/partition_point/binary_search/BTreeMap/range/min-select);
  ③Memtable 换 InternalKey newtype 让编译器逼出遗漏。共 13 个比较点全改:Memtable BTreeMap Ord
  (newtype,不外溢)、MemtableIterator sort+seek、BlockIterator::seek、Async/sync MergeIterator
  min-select、find_block_for_key、rpc_handlers biggest_key 预滤+mem/SST 两路 merge `mk<=sk`、
  background 三份 lookup_in_sst* 二分。等值比较/用户键级比较(bloom 两侧都是 user key、
  compaction 去重、split 扫描、in_range)核实不受影响。builder.rs 本地 parse_user_key(硬编码 9)/
  parse_seq 删掉统一走 crate::parse_key/parse_ts;TS_SIZE(9) 常量删除只留 TS_BYTES(8);
  wal_record.rs 无后缀宽度依赖(key 带长度前缀)核实不用动。
【持久格式后果(无迁移,故意的)】旧 SST/WAL 在新比较器下"查不到已存在的 key"级不可读;
  dev 集群必须重建。ops.md 加 deploy note;partition-server CLAUDE.md 两节重写(MVCC Key
  Encoding + Range paging)。
【顺带落地】比较器使 `K ++ 0x00` 成为精确的"严格在 K 之后"续扫起点(user-key 后继+user-key
  优先排序):RangeReq.start 文档改契约(WIRE v29 指纹原地刷新 9cb798979a60ed87,注释-only);
  scan_keys/wipe_agent 改独占续扫删掉 dedupe(有意反转 dd15047——当时比较器还不存在,
  dedupe 是对的);system_chaos/system_range_split 游标 push(1)/push(0x01)→push(0)。
【验证】partition-server 212(208+4 新:embedded-zero get/scan/SST 三个 pre-change 实证 FAIL,
  round-trip/前缀对/版本序在两棵树都过——与任务预期一致 #2 翻转);manager 293/stream 110/
  rpc 60/client 41/autumn-memory 22 全绿;system_chaos --ignored ×3 过;system_merge 9/9;
  run_e2e.sh 活集群 e2e 5/5 + scan_boundary 1/1(即新比较器+新续扫 idiom 的真集群验证);
  clippy 无新告警。
【已知无关噪音】integration.rs 三个 recovery 测试"同批挂/单跑过"在改动前基线同样挂
  (stash 实证),属既有跨测试进程态污染,未在本任务处理。→ 根因是进程全局 SST BlockCache 的跨集群污染,已修 (20e6a27)。
Next steps: 无。


（更早任务历史见 git log 与各 crate CLAUDE.md；本文件按规则 12 定期裁剪，保持整洁。）

【2026-09-02 ingest 提速 + 一次线上事故】
  ingest 慢的根因是**往返次数**,不是计算。同语料 5164 chunks 在真集群实测(改前):
    写入循环 177.7s (34.4 ms/chunk) + reconcile/train 101.1s = 278.7s
  已做(6b3a408): begin_bulk_index/flush_stats 延迟 meta/stats(它既是每篇 2 次往返,
  也是循环无法并发的原因——单 key 并发 RMW 丢更新); 循环拆成顺序 CPU 段 + 并发 I/O 段;
  bulk 窗口内 memoize centroid 表; 其余 `for k in keys { get(k).await }` 全部换批量。
  **改后数字仍未拿到** —— 见下面的事故。
  注意: **loopback 测不出这个改动**。本地 A/B 1.85s vs 1.87s,因为 RTT 只有 ~20µs,
  往返差被每 chunk 5ms 的实际工作淹没。这类改动只能在 pod 里量。

  事故一(不是我们的 bug): 三台 autumn-node(192.168.2.216/3.242/1.128)的 containerd
  卡死——kubelet 心跳正常、节点 Ready 无 taint,但**无法在既有容器里起新进程、也无法新建容器**。
  表现: exec 全挂(kube-system 的 pod 也一样)、exec 型 readiness 失败但 0 重启、
  pod 卡 Terminating、新 pod 卡 Pending 且 kubelet 事件数为 0。manager 因此卡死 3 小时。
  处置: cordon + 强删无 PVC 的 pod(manager/ps 无 PVC,秒级恢复)。EN 有 PVC,不能强删。
  详见 memory/vke-k8s-deploy-gotchas.md 的 2026-09-01 条。

  事故二(我的 bug): **翻译一句注释改掉了 WIRE_FINGERPRINT**。build.rs 按字节哈希
  manager_rpc.rs 等 5 个文件,注释也算。我把 "数千 partition" 翻成英文 → fingerprint
  从 f8b58703 变成 a30446,滚到一半时新 manager 与老 ps-0 互相握手失败。
  而且检查是**不对称**的: 新二进制看老 peer 匹配注册表 → 放行; 老二进制看新 peer
  不匹配注册表 → 判 fraud。所以新客户端连老集群一直是好的,直到 manager 先滚才炸。
  修复(00fd992): 原样还原该文件 + 在 build.rs(不在哈希集合内)写警示。
  教训: **部署前必须跑 `cargo test -p autumn-rpc`** —— registry_pins_current_schema_to_max_version
  就是拦这个的,我只跑了 cargo check 和 memory/mcp 的测试。

  事故三(顺带发现的真 bug,已修在 6b3a408): RPC 写路径的 writev 没按 IOV_MAX 分段。
  宽批量 → 1131 个 iovec / 仅 76KB → 内核按**条数**拒绝(Linux errno 90 / macOS 40) →
  写线程退出 → PS 的 log stream poisoned **且 PS 进程崩溃重启**,对上表现为
  `batch_put op status=1`。它一直潜伏在线上,是新的并发批量把它变可达的。

  buda 侧 upload 脚本修了三类 bug(1f9d073/90c7667/d66ee18): oniond 的
  `list | grep -qx` 在 pipefail 下**匹配成功反而判失败**(SIGPIPE 141),所以 oniond 分支
  从来没进过; HF 回退缺 HF_HUB_DISABLE_XET=1 会被 xethub CAS 打 401; 失败的模型静默跳过。
  oniond 实测 ~200 MiB/s,比公网 HF 的 21.4 MiB/s 快约 10 倍。
