Date: 2026-05-22
TaskStatus: completed
Task scope: F222 + F223 + F224 — operator-driven recovery actually works end to end.
  F222: merge manager's two df-calling loops (recovery_collect_loop +
        disk_status_update_loop) into one node_health_loop (close the
        recovery-completion-discard race). User: "都是调用df,应当写成一个loop".
  F223: chunk copy_bytes_from_source so >2 GB extent recovery works.
  F224: reseed the F211-H rate limiter from the ledger each tick so caps are
        enforced + recovery-stats reports real numbers.

Current summary:
  ALL THREE DONE + unit-tested + e2e-verified on a live 5-node TCP cluster.

  E2E (2026-05-22, fresh `cluster.sh reset 5`, 4 GB wbench): fence node 7 (held
  sealed 3 GB extent 12 = [7,3,9]) →
    t+5s : recovery-stats `global: 1/64` (F224 — was always 0), inflight marker=1
    t+10s: extent 12 = [1,3,9] eversion 2→3 (F222 apply ran), marker released
           (NOT via the 10-min F208 sweep); the 3 GB copy succeeded (F223 — pre-fix
           it failed 10x/10 "no source replica available for copy").
  cargo build --workspace clean; cargo test -p autumn-manager --lib 108/0.

  Root-cause chain that started this (forensics on a live 4->5 node TCP cluster):
    - User fenced node 7; recovery "didn't seem to happen".
    - Found: recovery DID reach the EN and copied data (extent-33.dat written to
      the target node's disk after boot), but manager-side apply_recovery_done
      NEVER ran -> metadata not updated, recovered copy became an orphan, inflight
      marker stuck until F208 stale-sweep released it ~10 min later.
    - Bug 1 (df-drain race): EN handle_df does std::mem::take on recovery_done
      when req.tasks.is_empty(). disk_status_update_loop (10 s) sent empty tasks
      and DISCARDED the returned done_tasks (only updated disk status). Whenever
      its sweep won the race vs recovery_collect_loop (2 s), the completion was
      lost forever. crates/stream/src/extent_node.rs:3712-3739 (EN drain);
      crates/manager/src/recovery.rs (the two loops).

  F222 fix (this change):
    - crates/manager/src/recovery.rs: deleted recovery_collect_loop +
      disk_status_update_loop; added node_health_loop -- ONE df caller, 2 s, all
      nodes, empty tasks (EN drains full recovery_done), and ALWAYS applies every
      done_task AND updates disk status + node_states (Suspected/Online) +
      recent_failure_reports. One caller = no race.
    - crates/manager/src/lib.rs start_runtime_tasks: two spawns -> one.
    - crates/manager/src/rpc_handlers.rs: 2 stale message/comment refs updated.
    - Build OK. `cargo test -p autumn-manager --lib` = 108 passed / 0 failed.

Files changed:
  - crates/manager/src/recovery.rs: F222 node_health_loop (replaces the two
    loops); F224 limiter reseed at tick top + try_acquire gate in
    dispatch_recovery_task (release on RPC-fail, Ok-on-rate-limited).
  - crates/manager/src/lib.rs: F222 single spawn.
  - crates/manager/src/recovery_rate_limiter.rs: F224 reset_counts + seed_inflight.
  - crates/stream/src/extent_node.rs: F223 chunked copy_bytes_from_source +
    read_bytes_chunk helper; fetch_full_extent_from_sources passes sealed_length.
  - rpc_handlers.rs + 5 CLAUDE.md / source comments: doc sync (loop rename,
    cadence 10s->2s, F222 Programming Note 25).

Remaining / follow-ups (non-blocking, NOT started):
  - >4 GiB extent recovery: ReadBytesReq.offset is u32, so F223 covers up to
    4 GiB (guarded with an explicit error). Wider wire offset = separate change
    if max_extent_size ever exceeds 4 GiB.
  - Orphan reconcile: Bug-1-era orphans (recovered copies the manager never
    adopted) get LOADED on EN restart and make handle_require_recovery return
    PRECONDITION "extent already exists" (extent_node.rs:3776). F222 stops NEW
    orphans; existing ones are reaped by the F109/F113 reconcile sweep.

Next steps:
  1. Commit F222+F223+F224 (on a branch — currently on main).
  2. Optional: integration test for F223 chunking (small
     AUTUMN_STREAM_READ_CHUNK_BYTES) + F224 limiter reseed.
