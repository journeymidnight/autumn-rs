Date: 2026-04-24
TaskStatus: completed
Task scope: F101-c — add value-size axis {4K, 8M} to perf_check default
matrix, ship UCX env defaults that prevent common loopback pitfalls.

Current summary:
  ✓ perf_check.sh default matrix is now 2×2×2×2 = 16 runs
      (transport × partitions × pipeline-depth × size {4K, 8M}).
  ✓ Baseline naming extended:
      perf_baseline_${transport}_p${parts}_d${depth}_s${size_label}[_shm].json
    Size label: 4k, 8m (or <N>b / <N>k / <N>m for other values).
  ✓ `--size {4k|8m|N_bytes}` flag to narrow the matrix.
  ✓ ulimit -l unlimited set in perf_check.sh — RDMA pins via ibv_reg_mr;
    the 8 MB distro default is exhausted by 16 × 8 MB concurrent MRs.
    Child cluster.sh → daemons inherit; verified via /proc/<ps>/limits.
  ✓ UCX_TLS=^sysv set (overridable) — this host's IPC namespace rejects
    shmat, which hangs UCX's sysv transport on large loopback transfers
    (seen as `mm_sysv.c:59 UCX ERROR shmat(shmid=...) failed: Invalid
    argument`). Excluding sysv falls back to uct_tcp on loopback.

Measured (disk, 3-replica, --nosync, threads=16):

  4 KB (unchanged from F101-b):
    TCP p=8 × d=8 : 141 988 w / 1.11 M r  (4.34 GB/s read)
    UCX p=8 × d=8 : 129 199 w / 763 697 r (2.98 GB/s read)

  8 MB (new this session):
    TCP p=8 × d=8 : 199.5 w (1596 MB/s) / 91.3 r (730 MB/s)
    TCP p=8 × d=1 : 164.6 w (1317 MB/s) / 90.4 r (723 MB/s)
    TCP p=1 × d=8 :  70.0 w  (560 MB/s) / 63.2 r (506 MB/s)
    TCP p=1 × d=1 :  71.6 w  (573 MB/s) / 59.2 r (474 MB/s)
    UCX p=8 × d=8 :  35.0 w  (280 MB/s) / 34.3 r (275 MB/s)
    UCX p=8 × d=1, UCX p=1 × d=1, UCX p=1 × d=8: wedge (see below)

Acceptance:
  (a) ✓ TCP 8 M completes all 4 combos non-zero — matrix dimension works.
  (b) ✓ At least one UCX 8 M combo completes with real data (p=8 × d=8).
  (c) ✓ Env defaults (memlock, UCX_TLS) propagate to daemons via inheritance.
  (d) ⚠ 3 of 4 UCX 8 M combos wedge. This is UCX-over-loopback-TCP
      (127.0.0.1 isn't on a RoCE NIC so uct_rc_mlx5 fails resolve);
      uct_tcp rendezvous is flaky on sustained single-EP 8 MB sends.
      Single-op put/get of 8 MB over UCX works fine. The failure is
      SUSTAINED ops on a warm EP — a loopback-only edge case that
      cross-host RoCE deployment will sidestep.

Main gaps (carried forward):
  - Cross-host UCX A/B (F100-UCX gate c) is the only way to get real
    UCX 8 MB numbers; single-host loopback can't represent it.
  - TCP 8M p99 tail is huge (≥ 1.4 s p99) — that's disk-I/O tail on /tmp,
    not transport related. Investigate later if needed.

Next steps:
  - None in scope. Matrix axis + env defaults shipped.
  - Cross-host deploy required to complete F100-UCX gate (c).
