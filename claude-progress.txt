Date: 2026-06-01 (P0 #1 fsync-coalescer fix ATTEMPTED then REVERTED — not worth it)
TaskStatus: completed
Task scope: User asked to implement the "keep concurrent pwrite + add a
  completed-prefix watermark" fix for P0 #1 (the F178 coalescer crediting an ACK
  to bytes written AFTER the covering fsync, under out-of-order same-extent
  pwrite completion). Implemented it, ran 6 coco /findbugs rounds, then — with
  the user — DECIDED TO REVERT THE WHOLE FIX. Working tree is back at HEAD
  (180b52f): the honest "reproduced-as-accounting-bug, deferred" state with only
  the 2 repro-only unit tests. NO production code change ships from this.

=== WHAT WAS BUILT (and reverted) ===
  `Coalescer::record_completed` (contiguous gap-free prefix watermark via a
  `completed: Vec<(u64,u64)>` stash) replacing the plain `pending_fsync.store`,
  + `reset_to` + a `poisoned` flag + an `epoch` generation counter. The
  DATA-SAFETY CORE (the prefix watermark) was correct from round 1.

=== WHY REVERTED — the fix was worse than the bug ===
  coco found 6 REACHABLE bugs, all in the NEW waiter-lifecycle state machine the
  fix introduced (NOT the original P0), in the most revert-prone io_uring hot
  path:
   R1 coalescer busy-loop on gap-only waiters (no .await) — HUNG my own test.
   R2 failed low-offset write -> high waiter hangs forever.
   R3 poison() leaks the parked coalescer task + Rc<ExtentEntry>.
   R4 poison not cleared by recovery/re_avali (consistency; not append-reachable
      — those paths are sealed-only).
   R5 R3's "drop wake_tx" BROKE the single-loop invariant -> a 2nd racing loop
      could store a stale snapshot over a rewound last_synced (REGRESSION FROM
      MY OWN R3 FIX).
   R6 reset_to orphans `end > len` waiters -> park forever.
  6 rounds, each a reachable lifecycle bug, R5 a self-inflicted regression =
  the hand-written concurrency state machine was self-producing its own bug
  surface. Per project discipline (Items 2/4 lesson): do NOT maintain a
  low-severity fix that destabilizes the revert-prone stream concurrency area.

=== ACCURATE SEVERITY OF P0 #1 (why the bug itself doesn't justify the fix) ===
  - The bug IS real at the accounting layer: io_uring CQE completion order is
    UNORDERED by kernel design (man io_uring(7); code uses no IOSQE_IO_LINK /
    IO_DRAIN), so two same-extent pwrites (same stream worker, FuturesUnordered
    cap 32, ascending non-overlapping offsets via lease()) that straddle a
    read-burst boundary on the EN can complete out of order -> the coalescer can
    publish/regress the fsync high-water across a hole. The 2 kept unit tests
    DEMONSTRATE this accounting hole (by HAND-ORDERING the completions — they do
    NOT exercise real io_uring reordering).
  - But real DATA LOSS additionally needs: (a) the low-offset physical write to
    land AFTER the fsync that covered it, (b) a power-loss / kernel-panic in that
    µs window (a process kill does NOT drop un-fsynced page-cache — kernel writes
    it back, so NO process-kill chaos can ever surface it), AND (c) the same
    window to hit ENOUGH replicas (F227 all-replica-ACK + recovery re-replicate a
    single-replica loss). Never observed; structurally near-precluded.
  CONCLUSION: a never-manifested, power-loss-only, all-replica-masked bug does
  NOT justify a self-bug-producing hand-rolled state machine in the io_uring hot
  path. If we ever must defend against production power-loss single-replica loss,
  open a SEPARATE feature: per-extent SERIAL fsync accounting (RocksDB single-
  sequential-WAL-writer model — gap-free prefix by construction), a clean design,
  NOT state bolted onto the concurrent coalescer.

=== STATE ===
  Reverted to HEAD 180b52f. autumn-stream baseline green (2 repro tests pass;
  these stay as the honest accounting-hole record). P0 #1 remains DEFERRED with
  this accurate severity write-up. coco worktrees + temp cleaned.

--- prior ---

Date: 2026-05-31 (P0 #1 fsync-coalescer high-water — REPRODUCED deterministically)
TaskStatus: completed
Task scope: User: 研究复现 P0 #1 (coco's append fsync high-water finding).
  Applied reproduce-first discipline. OUTCOME: mechanism REPRODUCED with a
  deterministic in-crate test (no power-loss rig needed — it's an ACCOUNTING
  bug in the coalescer state machine). NO production code changed (per
  discipline: do not fix the revert-prone hot-path coalescer on a code-read;
  the repro test is the guard for a future fix).

=== THE BUG (confirmed by code + now by test) ===
  F178 per-extent fsync coalescer credits an ACK to bytes written AFTER the
  covering fsync, under OUT-OF-ORDER same-extent append completion.
  - build_append_future / handle_append do `pending_fsync.store(total_end)` —
    a PLAIN store, AFTER the pwrite .await (extent_node.rs ~1824 / ~4350).
  - extent.len is reserved synchronously at build (step 7 ~1789) so two
    same-extent futures in the inflight FU get ascending non-overlapping
    file_starts — BUT their pwrite CQEs can complete out of order.
  - RACE: F1=[0,100) F2=[100,150). F2 completes first → store(150) → coalescer
    fsync → last_synced=150 → F2 ACKs. F1 completes LATE → store(100)
    (REGRESSES pending, plain store not fetch_max) → register waiter(100) →
    coalescer `pending(100) <= synced(150)` branch (~624) satisfies it WITH NO
    FRESH FSYNC → F1 ACKs. But [0,100) was written AFTER the only fsync.
  - fetch_max ALONE does NOT fix it: even keeping pending=150, last_synced=150
    was set by a fsync that ran before [0,100) was written. Correct fix =
    CONTIGUOUS COMPLETED-PREFIX tracking (last_synced may only advance over a
    fully-written prefix). NOT applied (hot-path, revert-prone).

=== REPRODUCTION (committed, test-only) ===
  crates/stream/src/extent_node.rs new `#[cfg(test)] mod p0_fsync_highwater_tests`
  (had to be IN-CRATE — Coalescer / register_sync_waiter are pub(crate), the
  out-of-crate tests/ dir can't see them):
  - p0_coalescer_acks_write_that_completed_after_fsync: drives the high-then-low
    completion order on a real tmpfile ExtentEntry, asserts waiter(100) resolves
    Ok WITH last_synced UNCHANGED at 150 → proves the no-fsync ACK (the bug).
  - p0_in_order_completion_is_correctly_durable: control — in-order completion
    advances last_synced via a real covering fsync (isolates the bug to
    out-of-order completion → confirms narrow reachability).
  Both pass. autumn-stream lib 65→67 tests; clippy clean.

=== REACHABILITY / SEVERITY (why it's a repro, not an incident) ===
  - Needs 2+ concurrent SAME-extent append futures: the hot path COALESCES
    consecutive same-extent frames in one read burst into ONE pwritev; two
    futures only coexist when same-extent appends straddle read-burst
    boundaries → MINORITY case. Plus io_uring must complete them out of order.
  - ACK-before-fsync loss is observable ONLY under POWER LOSS / kernel panic /
    block-layer flush-fault injection. A PROCESS KILL does NOT drop un-fsynced
    page-cache writes (kernel writes them back after the process dies) → NO
    process-kill chaos can EVER surface this. Same harness-invisible class as
    the deferred extent-state CAS. This is why it has never been observed.
  - F227 all-replica-ACK + recovery: a user PUT ACKs only when ALL 3 replicas
    ACK; single-replica power loss in the µs window re-replicates from the 2
    healthy peers. User-visible loss needs the race + correlated multi-replica
    power loss in the same µs window → structurally near-precluded.

=== VERDICT ===
  Mechanism REAL and now DETERMINISTICALLY REPRODUCED (code-read elevated to a
  demonstrated test). Severity in practice: near-precluded (harness-invisible +
  all-replica-ACK + recovery). FIX DEFERRED on purpose: the only correct fix
  touches the F178 hot-path coalescer (contiguous completed-prefix watermark) —
  the most revert-prone area (Items 2/4 lesson). The repro tests are the guard
  to land WITH that fix when a demonstrated need or a quiet window arrives.
  coco /findbugs on this change: not run (test-only addition, no prod logic);
  run it before any actual coalescer fix.

NOTE: claude-progress.txt got binary-corrupted mid-session (87 garbage lines);
  restored from HEAD (commit c5b8aa4) and this entry re-added cleanly.

--- prior (same day) ---

Date: 2026-05-31 (F249 migration feature logged + coco arch verdicts verified)
TaskStatus: completed
Task scope: User: 处理 1/2/3 after the coco arch review of "主动跨-PS partition
  迁移". Done:
  (1) Logged F249 (proactive cross-PS partition migration / owner-transfer) in
      feature_list.md (Open/Deferred section) as passes:false / NOT BUILT, with
      the minimal owner-transfer form + coco's 4 pre-req fencing conditions.
      Verdict: 可选/不建议 (failover + auto-split/merge already cover
      correctness; F249 only fills operability gaps: planned decommission,
      partition回流到新PS, 长期倾斜). F249 = next free ledger number (max was F248).
  (2) Verified coco /arch (GPT-5.5) 4 既有-code findings against current HEAD —
      verdicts below. coco ran isolated-worktree (skill), 2 prior attempts
      OOM-killed (exit137) under load; 3rd run clean.
  (3) This progress entry + commit.

=== coco arch verdicts (verified against HEAD, NOT blindly accepted) ===
  P0 #1 — extent append fsync high-water may ACK an un-fsynced lower-offset
    write (extent_node.rs coalescer ~607-672 + build_append_future ~1789/1824).
    VERDICT: PLAUSIBLE, NOT already-fixed, NOT covered by db5d708 (that's the
    owner_revision fence, different concern). Mechanism: same-extent concurrent
    appends (supported by the SQ/CQ overlap model) can complete out of order;
    `pending_fsync.store(total_end)` is a plain store (can go BACKWARDS when a
    lower-offset append completes after a higher one), so the coalescer's
    `pending <= synced` branch satisfies the late low-offset waiter WITHOUT a
    fresh fsync → ACK on non-durable bytes. PER DISCIPLINE: reproduce-first
    before fixing (revert-prone stream area; same lesson as Items 2/4). NOT
    fixed this session — needs a crash/fault repro (kill mid concurrent
    same-extent append) to confirm reachability. Candidate fix if confirmed:
    fetch_max on pending_fsync + completed-ranges contiguity for last_synced.
  P1 #3 — drain_and_shutdown ACKs even when flush_one_imm errors
    (lib.rs ~4985-4995). VERDICT: AS DESCRIBED, but NOT current data loss —
    log_stream IS the WAL, so an un-checkpointed imm is replayed on reopen.
    It only becomes unsafe IF F249 migration reuses drain-ack as "safe to drop
    log_stream". Tracked as an F249 pre-req (drain ack must carry Result).
  P1 #4 — WAL recovery skips CRC-corrupt records and continues (F158,
    lib.rs decode_records* ~6076-6085). VERDICT: DELIBERATE (F158), debatable.
    log_stream is 3x-replicated + read-failover masks single-replica rot; a
    surviving-all-replicas CRC error is genuine corruption. Changing to
    fail-closed risks turning recoverable partitions un-openable. Non-
    reproduced theoretical in revert-prone area → DEFER (don't flip on a
    code-read).
  P1 #5 — .meta CRC/missing → fail-OPEN (owner_revision=0, sealed=false)
    (extent_node.rs ~2526-2596). VERDICT: KNOWN DOCUMENTED RESIDUAL from
    Item 1/db5d708 (".meta now atomic temp+rename+dir-fsync → torn .meta
    can't happen; only real bit-rot triggers fail-open; separate hardening if
    needed"). coco re-found it. Not new; left as documented residual.
  NET: 0 new must-fix shipped. P0 #1 is the only one worth a future
  reproduce-first investigation; the rest are as-designed/known/feature-gated.

--- prior (same day) ---

Date: 2026-05-31 (progress file compacted; multi-PS Q&A)
TaskStatus: completed
Task scope: User asked (1) clean up this progress file, (2) does the system now
  support multi-PS — PS death → partition opened by another PS → cross-PS
  migration. Compacted 2386-line history into this rolling summary (full detail
  remains in git). Answered the multi-PS question (see "MULTI-PS STATUS" below).
  No production code changed this step.

=========================================================================
CURRENT STATE (as of 2026-05-31)
=========================================================================
Last shipped work: uniform etcd CAS for the stream MEMBERSHIP class + the
  coco-prioritized must-fix durability batch. Build/tests green at HEAD
  (manager 114 lib, stream 65 lib; clippy clean). Working tree clean.

Recent commits (newest first):
  650164f docs: extent-state CAS reproduce-first → DEFER
  965d431 fix(manager): uniform etcd CAS — merge survivor-stream membership
  f37630a fix(manager): Item 3 value-CAS stream-membership etcd writes
  db5d708 fix(stream): P0-B durable owner_revision fence (must-fix Item 1/4)

=========================================================================
MULTI-PS STATUS (answer to the user's question)
=========================================================================
1. MULTI-PS REGISTRATION/DISCOVERY — SUPPORTED.
   - Manager keeps a PS registry: MetadataState.ps_nodes: HashMap<ps_id, addr>
     (crates/manager/src/rpc_handlers.rs), heartbeat liveness in
     ps_last_heartbeat. RPCs: register-PS, heartbeat-PS (unknown ps_id →
     CODE_NOT_FOUND → PS self-evicts). PS liveness loop evicts a PS that
     misses its heartbeat window, then rebalances.

2. PARTITION FAILOVER (PS dies → another PS opens the partition) — SUPPORTED.
   - Each partition has exactly ONE owner PS: MgrRegionInfo.ps_id.
   - On PS eviction, rebalance_regions() reassigns its partitions to the
     remaining least-loaded PS and persists to etcd.
   - PS polls the manager (sync_regions_once, ~2s); when a region's ps_id ==
     self it open_partition()s (replays log_stream from the VP checkpoint),
     and drops partitions no longer assigned to it.
   - Split-brain prevented by owner-lock revision fencing: the new owner
     acquires a higher revision; the old owner's appends/commits are refused
     (durable owner_revision fence, Item 1 / db5d708).
   - Covered by crates/manager/tests/system_ps_failover.rs (PS1 dies → keys
     reassigned + recovered on PS2).

3. CROSS-PS PARTITION MIGRATION (proactive move for load balance) —
   NOT SUPPORTED (by design). No migrate/move/transfer RPC exists.
   rebalance_regions() is REACTIVE only (fires on register / upsert / split /
   PS eviction) and PRESERVES existing assignments while the owner is healthy.
   Load rebalancing today is indirect: operator-driven split (relief valve) /
   merge shifts partition boundaries; the manager is mechanism, policy is
   external (F183/F185, see project_auto_split_before_merge memory).
   → A graceful "drain partition A from PS X to PS Y without killing X" path
     does not exist. If wanted, it is a NEW feature (proactive-migrate RPC:
     freeze-source → seal/handoff owner-lock → open-on-target → reroute
     clients). Relevant caution: the drain-before-reopen attempt in the
     reopen-overlap work DEADLOCKED on client port migration (see history),
     so a migration design must solve old↔new handoff carefully.

=========================================================================
OPEN / DEFERRED (all non-data-loss or non-reproduced; see git for detail)
=========================================================================
- Item 2 reopen-overlap (P0, low-exposure): drain-before-reopen deadlocks;
  reachable paths covered by F185 merge-freeze + in-place split. Correct fix =
  freeze-old-writes-before-open-new (generalize F185). To revisit on demand.
- Item 4 flush-barrier all-replica wedge (P1): never reproduced as a permanent
  wedge; current fail-closed (timeout→retry→self-heal) is acceptable. Fix
  attempts destabilized the stream-worker area; reverted.
- Extent-state RMW etcd-CAS class (split-seal / sync_vp_refs / ec_done /
  recovery_done): DEFER — not reproducible + structurally near-precluded;
  already guarded by verify-at-apply + F207 ledger + fence. helper +
  worked example kept (manager CLAUDE.md note 33) for a demonstrated need.
- Chaos write-wedge family (seed=13/15 open-tail) + seed=8/31 split-child
  (BUG#2) are RESOLVED (db5d708 + client.rs apply_reset_tail fix).

=========================================================================
WORKING RULES (from CLAUDE.md — keep applying)
=========================================================================
- 机制/并发 BUG: reproduce → root-cause → fix → coco review. Do NOT fix a
  non-reproduced code-read hypothesis in the revert-prone stream/manager
  concurrency area (Items 2 & 4 lesson).
- Every feature/bug reviewed with coco findbugs (rule 12).
- Keep this file as a ROLLING summary; let git hold the full history.
