Date: 2026-07-14
TaskStatus: completed
Task scope: F-EN-DYNSHARD — dynamically changeable EN shard count + k8s. M0 +
  M1a + M1b + M1c(list-nodes) + reshard runbook shipped. Design:
  docs/en_dynamic_shard_design.md.

Current summary (all pushed):
  M0 (e3477c6): node_uuid identity (WIRE v19, etcd-reset); coco+fable reviewed.
  M1a (3542970): EN --advertise self-registration (retry/fail-stop); cluster.sh
    wired. Manager unchanged (M0 uuid-match = reshard commit point).
  M1b (a802c4f): df identity echo (WIRE v20) → classify_df_echo -> Imposter
    (pod-IP reuse: fail df) / DriftWarn / Ok. coco P1/P2 → dropped the risky
    auto-heal write (M1a already authoritative); P3 doc fixed.
  M1c-partial + runbook (THIS session, ready to commit):
    - autumn-op list-nodes: UUID + SHARDS columns (NodeStateEntry += node_uuid /
      shard_ports, WIRE v21) — the reshard-verification tool. JSON += node_uuid/
      shard_count/shard_ports.
    - docs/ops.md: "Resharding an extent node" runbook (stop-the-world: stop EN
      → restart with new --cpuset + --advertise → list-nodes shows new SHARDS;
      zero data movement; per-EN independent). Updated the stale "shard count
      frozen" M0 paragraph.
    - rpc 44 (v21) / manager 193 green; all test targets build; 0 clippy.

Main gaps:
  - M1c REMAINING (deferred, LOW-PRIORITY cleanup, not functional): format ->
    identity-only registration; make EN --advertise REQUIRED; rewire entrypoint/
    autumn-deploy/k8s launchers. Rationale for deferral: M1a already makes the EN
    the authoritative location source (overwrites shard_ports every boot →
    reshard works via restart, NO format re-run) and F214-B already gives the
    "unbooted node stays Suspend" property, so this is cleanup that changes the
    operational contract with real risk NOT validatable without a live cluster.
  - M2 deploy: k8s per-pod ClusterIP + Service port generation (shard_count
    ports per pod). The reshard RUNBOOK (bare-metal) is DONE (ops.md); the k8s
    Service-port-gen part remains.
  - M3 chaos: reshard-under-load e2e (needs a LIVE cluster — likely not runnable
    in this sandbox per env constraints; deferred to a cluster run).

Next steps:
  1. Commit + push M1c list-nodes + reshard runbook.
  2. If continuing: M2 k8s Service-port generation (deploy/k8s), OR pause for a
     live-cluster run to do M3 reshard chaos + the deferred launcher rewiring.
