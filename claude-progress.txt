Date: 2026-06-12
TaskStatus: not_completed
Task scope: rolling upgrade 实施（docs/rolling_upgrade_design.md v1.1）。
  R0 滚动重启程序化已完成并 commit；R1 cluster_version 门进行中；
  R2（控制面 prost）/R3（数据面协商）待做，R2 范围与切换方式需用户拍板
  （设计 §9 决策点 1/2）。

=== TWO LISTS REQUIRED BY THE LONG-TASK HARNESS ===

  Already implemented (this session):
    - ROLL-R0: scripts/rolling_restart.sh（EN 逐个→PS→manager 滚动 +
      收敛门 + per-partition 区间内写活性探针 + seed 零丢失校验 +
      flock 互斥 + per-run 探针命名空间/清理）；cluster.sh 补
      start-manager/stop-manager/restart-manager（launch_manager 提取
      + compute_etcd_endpoints）。coco 1P1+3P2 全采纳（P1=空 start 分
      区探针假覆盖）。实测：3-EN/4 分区持续负载全序列 ×2 PASS，
      191/191 ACKed 零丢失。
  Not implemented (recorded):
    - R1: etcd cluster_version（ASCII decimal 持久值）+ manager
      GET/BUMP RPC（单调+1）+ rpc [WIRE_VERSION_MIN,MAX] 区间 +
      WIRE-1 检查放宽为区间交集（fingerprint 留诊断 + 防忘 bump 的
      per-version 指纹注册表单测）+ autumn-op upgrade-version。
    - R2: manager_rpc + 54 处 etcd 值 prost 化（需拍板范围/切换方式）。
    - R3: 数据面连接级 hello 协商 + FooReqV2 冻结纪律。
    - upgrade_chaos.sh 双版本滚动 harness（依赖 R1+）。

Current summary:
  R0 已 commit。设计文档状态行更新为 "R0 已实现"。
Main gaps:
  - R1 未动工时本文件状态为 not_completed；R1 完成后更新。
Next steps:
  - R1 实现（见上）；完成后停下来向用户汇报 R2 决策点（§9.1 范围、
    §9.2 切换方式）再继续。
