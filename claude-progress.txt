Date: 2026-05-12
TaskStatus: completed
Task scope: F196 — static cpuset pre-allocation for EN/PS + PS reject-only
            split gate + EN shards-from-cpuset + manager hot/cold advisory.

Final state:
  - autumn-common: cpu_pin gains set_cpuset / parse_cpuset / cpuset_len /
    cpuset_explicit. `pick_cpu_for_ord` honors the explicit cpuset and
    returns None past end (no wraparound).
  - autumn-extent-node: --cpuset SPEC (taskset syntax) is the sole
    sizing surface. shards = cpuset_len; --shards REMOVED (binary
    refuses to start if it's passed). cluster.sh translates SHARDS env
    into per-EN --cpuset ranges so existing scripts keep working.
  - autumn-ps: --cpuset SPEC + new partition_budget. cpuset_len/2 caps
    open_partition + handle_split_part. Split refuses with
    FailedPrecondition when budget exhausted; sync_regions_once skips
    new opens past budget with WARN. Pre-F196 deployments unaffected
    (gate off when --cpuset omitted).
  - autumn-manager policy.rs: compute_hot_cold_advisory(region_owners,
    now) — single WARN line per PS when max/min(req_per_sec) >= 10
    sustained AND hottest > SPLIT_QPS_HIGH/2, 5-min per-PS cooldown.
    Pure log; does NOT join advisory_cache.
  - cluster.sh: AUTUMN_PS_CPUSET + AUTUMN_EN${i}_CPUSET env passthrough
    to --cpuset; legacy --cpu-start path preserved when neither set.

Stage breakdown:
  Stage A: cpu_pin helper + CLI parsing on both binaries.
           7 new parse_cpuset_* unit tests pass.
  Stage B: PartitionBudget (Arc<AtomicUsize+max>), wired through
           PartitionServer + PartitionData + partition_thread_main.
           sync_regions_once inc/dec; handle_split_part gate.
  Stage C: EN auto-sizes shards from cpuset_len; --shards REMOVED.
  Stage D: PolicyEngine::compute_hot_cold_advisory dual-dimension
           (QPS + size_bytes), 5 unit tests, call site in
           policy_tick_loop.
  Stage D-recal: SPLIT_QPS_HIGH 50K→15K, MERGE_QPS_LOW 5K→1.5K.
                 Aligns F183 QPS thresholds with measured ~30K
                 single-partition ceiling. Pre-F196 50K was
                 unreachable.
  Stage D-r2:    PS report_load 5s→30s; POLICY_WINDOW_BUCKETS 30→10;
                 HOT_COLD_MIN_HOT_QPS decoupled to 10K; hot/cold
                 emits PolicyCandidate (kind=4) into advisory_cache
                 so `client info policy` shows it. Hot/cold list
                 simplified to "min/max-matched partition" so the
                 2-partition PS case works.
  Stage D-r3:    F189 fg admission default 0 → 2 GiB/s. Activates
                 the existing fg ceiling + bg fg-aware yield by
                 default. Derived from perf_check TCP peaks
                 (~1.74 GB/s for 8M values; 2 GiB/s leaves 15%
                 headroom). Operator can re-enable unlimited via
                 `--fg-rate-bytes-per-sec 0`.
  Stage D-r4:    fg admission grows IOPS dimension. account_fg now
                 takes (bytes, ops); EITHER cap reached → sleep.
                 bg-aware-yield triggers on either fg observed
                 rate. Default fg-iops 150K (≈ 36% headroom over
                 measured 110K perf_check 4K peak). bg stays
                 bytes-only (compact/GC are bulk). 9/9 admission
                 tests; new `--fg-iops-per-sec` CLI flag +
                 AUTUMN_PS_FG_IOPS_PER_SEC env passthrough.
  Stage E: cluster.sh env passthrough + even disjoint cpuset
           distribution (EN[i] + PS ranges); README updated; this
           progress file; feature_list F196 entry; commit.

Test pass:
  cargo test -p autumn-common cpu_pin          → 7/7
  cargo test -p autumn-manager --lib            → 65/65 (incl 3 new
                                                  hot_cold_advisory_*)
  cargo test -p autumn-partition-server --lib   → 131/131

Out of scope (deferred):
  - NeedsMerge wire response (user deferred; reject-only for now).
  - Auto-merge on advisory (advisory stays informational).
  - Dynamic cpuset resize without process restart.
  - Manager-side awareness of per-PS core budgets for routing.

Closes F196. Next-session starting point: feature_list.md F196 entry +
this file's "Final state" section together describe what shipped.
