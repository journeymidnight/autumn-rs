Date: 2026-06-10
TaskStatus: completed
Task scope: F260 — chained replication for large appends (third item of
  P15). Implementation landed, default OFF; acceptance gap = cross-host
  perf + chain-kill chaos + coco arch review.

=== TWO LISTS REQUIRED BY THE LONG-TASK HARNESS ===

  Already implemented:
    - MSG_APPEND_CHAIN(16) wire codec (+2 unit tests).
    - EN: per-downstream-addr forwarder tasks (unbounded ordered enqueue;
      v1 synchronous submit stalled handle_connection under 8M backlog —
      fixed), local write via build_append_future, ack aggregation,
      CHAIN_FORWARD_TIMEOUT=30s.
    - client: launch_append chain branch (single send to head, 1 ack =
      all replicas, timeout x3), --append-chain-min-bytes /
      AUTUMN_APPEND_CHAIN_MIN_BYTES (cluster.sh passthrough), DEFAULT 0
      = disabled.
    - Verified: 8M random value chain write -> readback byte-identical;
      chain ON t4xd4: 65.2 ops/s p99 667ms, 0 errors 0 timeouts;
      default-OFF regression: star 8M write 139 ops/s, 0 errors.
    - Same day: UCX_TLS positive-list rule (bc08aef) + scenario comment;
      TLS shm root-cause closed (54.9K/69K vs 8.3K).

  Not implemented (the acceptance gap — F260 stays not_completed):
    - Cross-host perf >= 1.5x via remote-autumn (::14 <-> ::15 RoCE);
      loopback shows no gain by design (store-and-forward overhead, no
      NIC bottleneck to relieve).
    - Chaos: kill mid-chain EN under sustained chained writes -> retry,
      zero loss.
    - coco arch review of chain ack / commit-truncation interaction.
    - Deep-queue latency stacking: chain currently UNSUITABLE at high
      in-flight depth even cross-host until per-hop budgets are
      re-derived (client x3 may need queue-aware scaling).

Current summary:
  Chain is in the tree but OFF; default paths regression-clean. The
  loopback wedge (timeout storm -> partition poisoning at t16xd8x8M) is
  understood: per-append store-and-forward hop latency stacking, not a
  deadlock (low-concurrency runs are error-free).

Cross-host acceptance DONE 2026-06-11: fair 3.8x, egress 3x->1x proven, chaos 174/174 zero loss after fixing the error-frame-as-LockedByOther bug (apply_completion is_error guard). Next steps:
  F261, then chaos + coco arch;
  flip default only if cross-host >= 1.5x holds AND deep-queue budgets
  are fixed. F261 (SSTable paging + block cache) follows.
