Date: 2026-04-26
TaskStatus: completed
Task scope: FOPS-01 — autumn-client `info` enhancements (shipped)
  + FOPS-02 queued (not started)

FOPS-01 details:
  GC discard signal + --json / --top N / --part PID flags added to
  `autumn-client info`. No manager state changes — discard data is
  read live from partition-server via new MSG_GET_DISCARDS RPC.

  1. New PS RPC `MSG_GET_DISCARDS` (0x48):
     - `GetDiscardsReq { part_id }` / `GetDiscardsResp { discards: Vec<(u64,i64)> }`
       in `crates/rpc/src/partition_rpc.rs`
     - `extract_part_id` extended with MSG_GET_DISCARDS arm
     - `handle_get_discards` in `crates/partition-server/src/rpc_handlers.rs`:
       snapshots sst_readers, calls `background::get_discards()`, filters
       via `background::valid_discard()` against live log_stream extent_ids,
       returns filtered (extent_id, bytes) pairs
     - Wired into `dispatch_partition_rpc` match

  2. `autumn-client info` enhancements
     (`crates/server/src/bin/autumn_client.rs`):
     - `Command::Info { json: bool, top: Option<usize>, part: Option<u64> }`
     - Parser: `--json`, `--top N`, `--part PID` (mutually exclusive for
       --top/--part)
     - Per-partition discard fetch: prefers `part_addrs[pid]` per F099-K,
       falls back to `ps_details[ps_id].address`
     - JSON output: `InfoSnapshot { nodes, extents, streams, partitions }`;
       each `InfoPartitionView` carries `discards: Vec<InfoDiscardEntry>`
     - Text output: log stream line gets `, discard: N ext / X pending`
       when non-empty; `--top` sorts partitions by live size; `--part`
       focuses the partitions section; `--top`/`--part` suppress
       nodes/extents/streams sections
     - `InfoNodeView`, `InfoDiskView`, `InfoExtentView`, `InfoStreamView`,
       `InfoPartitionView`, `InfoDiscardEntry`, `InfoSnapshot` — file-private
       Serialize types

  3. README.md updated with cluster info subsection + updated info table row

Files modified (FOPS-01):
  • crates/rpc/src/partition_rpc.rs
      - MSG_GET_DISCARDS = 0x48
      - GetDiscardsReq, GetDiscardsResp structs
      - extract_part_id MSG_GET_DISCARDS arm
  • crates/partition-server/src/rpc_handlers.rs
      - handle_get_discards (new)
      - dispatch_partition_rpc MSG_GET_DISCARDS arm
  • crates/server/src/bin/autumn_client.rs
      - Command::Info { json, top, part }
      - info flag parser
      - Info* JSON view types
      - rewritten info handler with discard fetch + JSON/text output
  • autumn-rs/README.md
      - info table entry updated
      - new "Cluster info" subsection

Verification:
  ✓ cargo build --workspace green
  ✓ cargo test -p autumn-partition-server -- --skip f099i: 83 passed
    (f099i_d1_fast_path_no_fu_allocation pre-existing failure unrelated
     to this work — confirmed by running against stashed baseline)
  ✓ cargo test -p autumn-manager (pre-existing register_node_duplicate_addr_rejected
    failure confirmed unrelated)
  ⚠ Manual end-to-end cluster test deferred — needs a running cluster
    with autumn-ps + manager + extent-nodes. User should verify:
      - `info` shows log stream discard line after GC
      - `info --json | jq '.partitions | length'` returns integer
      - `info --top 3` shows ≤3 entries sorted descending
      - `info --part 0` shows single partition with 3 stream sub-sections

Out of scope (not attempted):
  • New partition-server integration test for MSG_GET_DISCARDS
    (plan item; would need a running cluster fixture; deferred)
  • MgrStreamInfo punched_extents/punched_bytes (user clarification:
    dynamic data must NOT be stored in etcd; pivot to PS-side RPC)

---

Queued (not started yet, recorded in feature_list.md P3):

  FOPS-02 — cluster.sh auto EC (replicas≥3)
  ────────────────────────────────────────────────────────────────────────
    Decision matrix:
      N >= 4  →  log/row stream EC 3+1, meta stream replication 3+0
      N == 3  →  log/row stream EC 2+1, meta stream replication 3+0
      N <  3  →  all replication N+0 (same as now)
    Env overrides:
      AUTUMN_EC_LOG=off|K+M
      AUTUMN_EC_ROW=off|K+M
    Touch points:
      cluster.sh do_start (line 343-357 current --replication decision)
      crates/server/src/bin/autumn_client.rs Bootstrap branch
        (add --log-ec / --row-ec flags, pass to CreateStreamReq)
      manager handle_create_stream already supports EC fields — no change

Next steps when resuming:
  • FOPS-02: add --log-ec / --row-ec to `autumn-client bootstrap`,
    update cluster.sh auto-EC decision
  • Per CLAUDE.md long-task rules: print 已实现/未实现 list → code →
    test → README → commit
