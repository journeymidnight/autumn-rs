Date: 2026-04-23
TaskStatus: completed
Task scope: F100-UCX Phase 1 — autumn-transport crate scaffold + TCP impl
            + generic loopback suite (on f100-ucx feature branch).

Current summary: New crate at crates/transport/ with enum-dispatch surface
                 (Conn / Listener / ReadHalf / WriteHalf, with Tcp variant
                 today and Ucx variant gated to Phase 3 under feature="ucx").
                 AutumnTransport stays a dyn-safe trait so init() returns
                 &'static dyn AutumnTransport for runtime selection.
                 TcpTransport passes 4-test loopback suite (ping-pong, 2MB
                 large-payload, half-close, 1k concurrent connections) +
                 connect/bind round-trip + 5 probe::decide() env tests.
                 Full workspace test (excl manager+fuse): 187/0/0 — no
                 regressions vs baseline.

                 DESIGN PIVOT during P1.2: spec §3 originally had
                 Box<dyn AutumnConn> trait object; compio 0.18's
                 AsyncRead::read<B: IoBufMut> / AsyncWrite::write<T: IoBuf>
                 are generic methods, making the trait NOT dyn-compatible
                 (E0038). Switched to enum dispatch — strictly cheaper than
                 vtable, fully inlinable, runtime selection still works via
                 the dyn-safe AutumnTransport trait. Spec §3 + §12 Q2-rev
                 updated; plan top has DESIGN PIVOT NOTE.

Main gaps: P2 — migrate autumn-rpc / autumn-stream / autumn-manager call
           sites onto the enum surface (still TCP underneath); ~5 tasks.

Next steps: Phase 2. After P2 closeout, Phase 3 implements UcxTransport
            using in-tree bindgen sub-crate (per spec §13).

Branch: f100-ucx (off main; commits 14506a5..1198bfd).
