Date: 2026-06-12
TaskStatus: completed
Task scope: rolling upgrade 实施（docs/rolling_upgrade_design.md v1.2）。
  本次会话交付 R0（滚动重启程序化）+ R1（cluster_version 门 + wire
  区间握手），均已实测 + coco 评审 + commit。R2（控制面 prost，~1-2 周）
  / R3（数据面协商）按设计 §9 决策点 1/2 等用户拍板后另起任务。

=== TWO LISTS REQUIRED BY THE LONG-TASK HARNESS ===

  Already implemented (this session):
    - ROLL-R0 (020b02b): scripts/rolling_restart.sh（EN 逐个→PS→manager
      + 收敛门 + per-partition 区间内探针 + seed 零丢失校验 + flock +
      per-run 命名空间/清理）；cluster.sh start/stop/restart-manager。
      实测 3-EN/4 分区持续负载 ×2 PASS，191/191 ACKed 零丢失。
    - ROLL-R1: rpc [WIRE_VERSION_MIN,MAX] + wire_compat_check（区间交
      集）+ WIRE_VERSION_FINGERPRINTS 注册表（单测强制 bump 决策 +
      运行时 fraud 交叉校验）；manager etcd cluster_version（ASCII
      decimal、CAS-imprint、replay、bump=leader-only/+1/≤max/value-CAS、
      回滚 fail-closed）；GetClusterIdResp 携带区间+gate（结构冻结）；
      autumn-op cluster-version / upgrade-version。真混版本（旧 op vs
      新 manager）响亮拒绝实测。
    - 顺手修复：cluster.sh restart 选主竞态（leadership 等待门）、
      save_cluster_config pipefail 空匹配中止、rolling_restart.sh
      IFS=TAB 吞空 start 字段（探针错路由）。
  Not implemented (recorded):
    - R2: manager_rpc 全量 + 54 处 etcd 值 prost 化 —— 需拍板范围
      （§9.1: 全量 vs 仅 etcd 值）与切换方式（§9.2: reset vs 离线转换
      工具）。
    - R3: 数据面连接级 hello + FooReqV2 冻结纪律（按需触发）。
    - upgrade_chaos.sh 双版本滚动 harness（依赖 R2/R3 才有真实意义；
      当前版本窗口下混版本=启动拒绝，无可滚动的版本对）。

Current summary:
  R0+R1 已 commit+push。设计文档状态 v1.2（R0/R1 标注 ✅ + 实现注记）。
  单测全绿（rpc 25 / manager 152 / ps 162 / stream 77 / client 27）。
Main gaps:
  - R2/R3 等用户对 §9 决策点 1/2 拍板。
Next steps:
  - 用户拍板后：R2 推荐路线 = manager_rpc 全量 + etcd 值 prost 化 +
    cluster.sh reset 切换（开发期）；之后 R3 + upgrade_chaos.sh。
