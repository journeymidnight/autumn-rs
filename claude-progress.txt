Date: 2026-06-10
TaskStatus: completed
Task scope: F256 — partition_loop natural batching (remove the
  MIN_PIPELINE_BATCH launch gate that lock-stepped the 4K write
  pipeline whenever per-partition concurrency < 256).

=== TWO LISTS REQUIRED BY THE LONG-TASK HARNESS ===

  Already implemented (this session):
    - F256 (lib.rs partition_loop (B) + background.rs knob removal +
      partition_server.rs --min-pipeline-batch deprecated no-op +
      CLAUDE.md x2 + README.md + feature_list.md entry).
    - Verified (no change needed): append hot path does NO per-append
      commit_length RPC (stream CLAUDE.md note 9 — current_commit()
      is tail-init only), so the gate was the only lock-step source.

  Not implemented (out of scope, candidate follow-ups from the same
  4K-write investigation, NOT started):
    - put_many/MSG_BATCH_PUT adoption audit for kvcache writers
      (server batch path already exists).
    - client-side put() double-copy removal (key.to_vec +
      rkyv value.to_vec).
    - chain replication for >=64K writes / VP direct-read from EN /
      hedged+rotated replica reads (architecture discussion recorded
      in session, no design doc yet).

Current summary:
  F256 landed. Controlled same-environment A/B (fresh 3-replica
  3disk cluster per side, p8 x t16 x d8, EN base port 20000):
    TCP 4K write: F256 61,457 / 58,504 (two runs) vs baseline-code
      46,997 ops/s = +24-31%; p50 1.07ms vs 1.68ms.
    TCP 8M write: 165.12 vs 175.08 = -5.7% (single-run noise on the
      disk-bound leg; both sides tripped the same p99 warning from
      shared-tenant disk pressure). 8M read 623 vs 585 (+6.5%).
    UCX 4K write: 7,794 vs 6,994 = +11% (both ~7x below the 51K
      committed baseline — environment-wide UCX loopback write
      degradation that day; reads 99% of baseline on both sides).
  Tests: cargo test --workspace --exclude autumn-manager all pass
  (158 partition-server). coco /findbugs deep (GPT-5.5): no P0/P1;
  P2 (pub setter removal breaks out-of-repo callers) rejected as
  solo-workspace-theoretical; P3 (.skillopt-sleep) followed.

Main gaps:
  - Committed perf baselines (perf_baseline_*.json) were recorded on
    healthier disks: on 2026-06-10 even UNCHANGED code reached only
    74% (tcp 4k) of its own baseline. Baselines NOT re-recorded (the
    day's environment would bake transient tenant pressure into them).
  - Bench-environment traps hit during verification (no code change):
    (a) a Ray tenant now squats *:10101-10103 = EN ctl ports
        (data 9101 + 1000) -> TCP EN fail-stop on bind -> cluster
        start TIMEOUT. Workaround: AUTUMN_EXTENT_BASE_PORT=20000.
    (b) perf_check.sh:90 defaults UCX_TLS=^sysv,^posix (known-buggy
        spelling; '^posix' parses as a literal TL). Export
        UCX_TLS='^sysv,posix' when invoking.
    (c) UCX manager 9001 needs a few seconds of linger after kill
        before rebind (ucp_listener has no SO_REUSEADDR path).

Next steps:
  None for this task. Session-closure invariant satisfied: commit
  landed, progress + feature_list updated, workspace clean.
