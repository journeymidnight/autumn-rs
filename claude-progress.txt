Date: 2026-07-01
TaskStatus: not_completed
Task scope: 全库可读性 review(/loop 任务)——逐 crate 审查并**简化代码逻辑本身**
  (用户明确:不是加注释,是让逻辑更直白;不用 coco),直到 17 个 chunk 全覆盖。
  账本 = docs/readability_review_ledger.md(chunk 表 + findings log)。
  规则:行为保持不变、性能中性(热路径分配/锁/syscall 数不变)、
  每 chunk 全量 `cargo test -p <crate> --lib` 绿、逐 chunk commit+push main。
  wire-fingerprint 文件(rpc 的 manager/partition/extent_rpc、frame、cap_token)
  编辑即强制 wire-version registry 决策 → 低价值清理记入 findings log 延后。

Current summary:
  · Chunk 1 common(601 行)+ Chunk 2 etcd(1329 行):读毕,逻辑本已清晰,无改动(done)。
  · Chunk 3 rpc(6579 行)done,2 commit 均 pushed:
    - f833d1d client.rs:read_loop 四段近重复 ZC 分支(UCX/TCP × into-dest/into-pooled)
      合一(zc_fast_path + recv_value_ucx + ValueRecv);send_vectored 死 v1 分支删除
      (FLAG_CRC 恒真);await_response/with_timeout/register_and_submit 三组重复收敛。
      clippy 抓到我引入的 RefMut-across-await(match 临时量生命周期延长,F108 类)已修
      (先 let 绑定再 match)。43/43 测试绿(default + --features ucx),workspace check 绿。
    - f89802c ps_read_bench 三份复制的 driver 收敛为 drive() + 两个 read_loop。
  · 子代理审了 wire 文件:manager_rpc/build.rs 干净;cap_token/extent_rpc/partition_rpc
    三个低价值 finding 记入 ledger findings log(等下次真编辑时顺带)。
  · Chunk 4 transport(4140 行)done,2 commit pushed:
    - 0fe3e72 endpoint.rs 四处 Pending-completion 重复→await_pending()+ucx_recv 薄包装;
      lib.rs 双份 getifaddrs walk→all_ifaddrs();listener/sockaddr 双份 sockaddr 转换
      →from_ucx_storage;regpool clippy nit。
    - 63dcc34 regpool Drop 恒真死分支删除;transport_bench 3 份 client 协议+3 份 server
      drain 收敛(send_warm_timed/read_count/drain_n/drain_registered,删死参 _mode);
      tests/common round_trip_at 合并。
    验证:8/8 transport 测试套件绿(含 RoCE 上真 UCX loopback+cancel-safety 两测,
    正是重构的路径)+ bench 冒烟 TCP+UCX 全列有数;rpc 43/43 两 feature;workspace 绿。
  · Chunk 5 client(3966 行)done,commit c66d555 pushed:
    - NOT_LEADER 旋转循环 ×4(cluster_df/mint_token/tenant_create/tenant_delete)
      →mgr_call_leader(各调用点重试次数保持原值);get_many/batch_put 整组失败
      扇出循环 ×8→fail_slots;get_stream 双份 GetStream 构造合一;lease.rs 四个
      RPC 的 transport/decode 样板→lease_call;删两处 useless .into()。
    - put_zc_opts/get_range_into 各自的重试循环保留(调用原语不同:call_vectored/
      call_into_dest;refresh_and_backoff 已共享)——刻意不强行统一。
    验证:client 27/27 + memory 20/20 + fuse 44/44 + workspace 绿;client clippy 清。

Main gaps / risks:
  · UCX recv 路径无本地测试(需 RoCE 集群);read_loop 重构按构造保持行为,
    TCP 路径有测试覆盖。若跑真集群 chaos/perf 出现 ZC 读异常,先查 f833d1d。
  · 大 chunk(stream 18k、partition-server 22k、manager 40k)尚未开始。

Next steps:
  1. Chunk 6/7 stream(18k 行,分 server 侧 extent_node 与 client 侧 StreamClient 两迭代;
     autumn-stream 有 22 条 clippy 警告可顺带清)。
  2. 之后按 ledger 顺序 8-17;dead code(partition-server decode_records_with_offsets/
     lookup_in_sst 等)在对应 chunk 处理。
