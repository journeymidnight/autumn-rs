Date: 2026-05-09
TaskStatus: completed
Task scope: F183 + F184 — Partition merge primitive + auto-split/auto-merge
            policy engine end-to-end. Auto-trigger and full closed-loop
            integration tests landed across iters 1-8 of an autonomous
            /loop session. PS-side dual-gate handle_merge_part still
            deferred (Stage 1's CLI orchestration + region_sync_loop
            reload-on-rg-change is the live path).

F183 (initial Stage 1 commits, 16 commits, e59e6dd..4e9182c):

  Wire types (autumn-rpc):
    - MSG_MULTI_MODIFY_MERGE 0x34, MultiModifyMergeReq/Resp
    - MSG_GET_POLICY_CANDIDATES 0x35, GetPolicyCandidatesReq/Resp,
      PolicyCandidate (POLICY_KIND_SPLIT=0, POLICY_KIND_MERGE=1)
    - MSG_REPORT_PARTITION_LOAD 0x36, ReportPartitionLoadReq, PartitionLoad
    - MSG_MERGE_PART 0x4D + MergePartReq/Resp (reserved; no handler)

  Manager pure-fns (lib.rs):
    - compute_merge_streams (log_stream splice with [L]+[V]+[E_new])
    - splice_streams_without_new_tail (row + meta splice)
    - merged_partition_vp_refs (per-extent sum)
    - apply_merge_mutations (in-memory applier)

  Manager state + handlers:
    - last_op_at HashMap + partitionLastOp/<id> etcd sidecar
    - PolicyEngine (policy.rs)
    - handle_multi_modify_merge (4 phases, F124+F138/F145/F146+F149)
    - handle_get_policy_candidates / handle_report_partition_load
    - policy_tick_loop spawned in start_runtime_tasks

  Partition-server + client + CLI:
    - Arc<PartitionMetrics> (req_count, imm_full_count, size_bytes)
    - report_load_loop (5 s cadence)
    - ClusterClient.merge_partitions / policy_candidates
    - autumn-client merge / policy-candidates

F184 follow-on (8 commits, e516789..527d0a4):

  F184-A: --auto-split / --auto-merge manager flags + auto-dispatch
          loops in policy_tick_loop (rate-limited 1/tick)
  F184-B: PartitionHandle.opened_with snapshot + sync_regions_once
          reloads partitions when (rg, stream_ids) changed
  F184-C: read_all_table_locations walks every meta_stream extent and
          unions LAST records — fixes survivor recovering victim's
          tables post-merge
  F184-D: integration tests merge_split_round_trip_keys_intact +
          merge_then_split_again_round_trip
  F184-E: 3 more merge handler unit tests (recovery_inflight,
          pending_delete, last_op_at)
  F184-F: public force_auto_split / force_auto_merge test helpers +
          auto_dispatch_merge_orchestrates_full_flow integration test
  F184-G: auto_dispatch_split_dispatches_msg_split_part integration test
  F184-H: PolicyConfig (runtime-configurable thresholds) +
          auto_merge_fires_via_policy_tick_loop_fast_mode — exercises
          the FULL closed loop:
            MSG_REPORT_PARTITION_LOAD → metrics window →
            compute_candidates → auto_dispatch_merge → multi_modify_merge
          end-to-end in ~6 s
  F184-I: auto_split_fires_via_policy_tick_loop_fast_mode — mirror of
          F184-H for the SPLIT closed loop, ~5.5 s e2e

Test status (post-F184):
  cargo test -p autumn-rpc                                      : clean
  cargo test -p autumn-manager --lib                            : 51/51
  cargo test -p autumn-manager --test system_merge -- --ignored : 8/8 (~57 s)
  cargo test -p autumn-partition-server --lib                   : 130/130
  cargo build --workspace --exclude autumn-fuse                 : clean

system_merge breakdown (8 integration tests):
  refusal-path:
    merge_refuses_self_merge
    merge_refuses_non_adjacent_partitions
  happy-path:
    merge_split_round_trip_keys_intact
    merge_then_split_again_round_trip
  manual auto-trigger:
    auto_dispatch_merge_orchestrates_full_flow (force_auto_merge path)
    auto_dispatch_split_dispatches_msg_split_part (force_auto_split path)
  full closed-loop (policy_tick_loop drives):
    auto_merge_fires_via_policy_tick_loop_fast_mode
    auto_split_fires_via_policy_tick_loop_fast_mode

Manager unit tests (51 total) breakdown:
  Pre-existing                                  : 30
  F183 merge pure-fn (4)                        : compute_merge_streams,
                                                  splice_streams_without_new_tail,
                                                  merged_partition_vp_refs,
                                                  apply_merge_mutations
  F183 merge handler refusal (3)                : non-adjacent, self-merge,
                                                  ec_inflight
  F184-E merge handler refusal (3)              : recovery_inflight,
                                                  pending_delete, last_op_at
  F183 policy engine (11)                       : 5 split + 6 merge

Out of scope (deferred follow-ups, separate features):
  - PS-side handle_merge_part with proper dual-gate + freeze-drain
    (Stage 1 uses CLI orchestration + region_sync_loop reload; the
    sub-2 s window between FLUSH and merge can lose writes if writes
    arrive during the window).
  - Cross-PS merge (requires partition migration primitive; advisory
    correctly marks cross-PS pairs same_ps=false / feas=no).
  - p99 latency reading on PS (PartitionLoad.p99_us = 0 always).
  - VP+compact 5-byte offset bug (pre-existing, exposed by
    merge_preserves_value_pointer_resolution sanity check; reproduces
    without merge code path; tracked separately).
  - Per-cluster threshold tuning via CLI flags on autumn-manager-server
    (F184-H added runtime API set_policy_config; the binary doesn't
    yet expose it as flags).

Stage 2/3 production rollout:
  Stage 2 (auto-split default-on) — gate: ≥30 days advisory burn-in,
    ≥50 advisory→manual splits/merges with 0 issues.
  Stage 3 (auto-merge default-on) — gate: ≥60 days Stage-2 stable,
    0 false-merge events.
  Order rationale: TPC means merge concentrates load onto one core;
    auto-split is the relief valve. Recorded in
    feedback_auto_split_before_merge.md.

Final commit: 527d0a4 (F184-I).
