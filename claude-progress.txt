Date: 2026-06-12
TaskStatus: completed
Task scope: /loop 生产视角急修 · 批次 1 = P0-D（EN durability：清零被吞的
  `.meta` persist 失败）。

=== TWO LISTS REQUIRED BY THE LONG-TASK HARNESS ===

  Already implemented (this commit):
    - extent_node.rs 最后 3 处 `let _ = save_meta` 全部 fail-closed
      （mark_disk_offline + 报错）：run_recovery_task 末尾 persist /
      convert_to_ec prepare 路 seal / convert_to_ec post-convert
      eversion-seal。grep 确认仓库已无被吞的 save_meta。
    - coco P1 ×2 折入：F119-D 幂等跳过路径 ensure save_meta（内存原子
      满足≠sidecar 持久）；recovery persist 失败后 remove partial
      entry（防复用下线盘 + 防 "already exists" 卡死重派）。
    - 文档：stream CLAUDE.md note 25（不变量：RPC 返回 OK 即断言
      sidecar 持久）；feature_list P0-D 条目。
  Not implemented (recorded):
    - 生产清单后续项（按用户拍板顺序）：observability /metrics 薄层 →
      ENOSPC 行为定义+测试 → BUG-LEASE-8（fuse 多 key 原子提交边界）。
      rolling upgrade（rkyv same-commit 约束）= 独立长线 track。
    - F227 recovery-stuck 家族（仍 OPEN）；note 24 fsync coalescer
      （结构性不可达，defer 维持）。

Current summary:
  P0-D 验证全绿：autumn-stream --lib 72 单测；seed=13/60s 隔离
  system_chaos ×2（P1 折入后）全 ok 零丢失；transport_chaos tcp PASS。

Main gaps:
  - 见 Not implemented。
Next steps:
  - /loop 下一项：observability /metrics 薄层（目前零 metrics 端点，
    生产无法观测 QPS/延迟/盘健康/分区分布）。
