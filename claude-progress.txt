Date: 2026-07-15
TaskStatus: completed
Task scope: F-EN-DYNSHARD M1c — the deferred launcher-rewiring cleanup.
  Make `autumn-op format` IDENTITY-ONLY (mint node_uuid+disk_uuid, register
  with EMPTY location); make EN `--advertise` REQUIRED when `--manager` is
  given (the EN is now the sole location source — no format stamp to fall
  back on); rewire ALL launchers to pass `--advertise` to the EN binary.
  Design decision (user, referencing Kafka/Ceph/HDFS): mkfs/format mints
  identity only; the daemon self-registers its network location every boot.

Current summary (ready to commit):
  - args.rs: Command::Format reduced to { dirs }; --listen/--advertise/
    --shard-ports now hard migration-error stubs (exit 2). Dead
    derive_control_address fn + 3 tests removed (logic moved to the EN's
    build_register_req in M1a).
  - main.rs cmd_format: registers RegisterNodeReq with addr=""/shard_ports=[]/
    control_address="" (identity only). JSON adds node_uuid.
  - extent_node.rs: --advertise REQUIRED with --manager; registered shard_ports
    + control base derived from the ADVERTISE port (peer-reachable), not the
    bind --port — supports NAT/proxy (warn-not-fail on port mismatch, the
    toxiproxy case). Both multi-shard + run_single_shard paths.
  - Launchers wired: cluster.sh, deploy/docker/entrypoint.sh, deploy/baremetal/
    autumn-deploy, deploy/k8s/extent-node.yaml (comment), + 7 e2e/chaos scripts
    (run_authz/run_e2e/run_vllm/run_fs/run_fs_lease/run_memory + crosshost_chaos).
  - coco (deep) 2 P2 fixed: (1) missed e2e/chaos tests using old format syntax +
    EN missing --advertise (f213_op_bootstrap_e2e.rs + system_chaos.rs); (2)
    identity-only re-register called on_heartbeat_ok, prematurely flipping a
    not-yet-booted node Online with a stale location — now gated on !addr.empty().
  - Green: EN bin 2, manager lib 193, f_en_dynshard 6, df_echo 5, rpc lib 48;
    workspace + all test targets build clean. deploy/validate.sh k8s OK.
  - VALIDATED LIVE: reshard_chaos.sh 2→4→1 PASSES on the new identity-only flow
    (format registers empty → list-nodes shard count comes entirely from the
    EN's self-register), zero data movement, all 150 keys byte-exact.

Main gaps (deferred, unchanged from prior backlog):
  - M2 k8s Service-port generation (multi-shard needs shard_count data+control
    ports per pod; kustomize can't loop → a generator/template); needs a live
    k8s to validate. Only remaining F-EN-DYNSHARD item.
  - F-SEALED-EMPTY-SWEEP (BUG-FLUSH-TIMEOUT-LEAK backstop) still deferred.
  - Remote host can't build the workspace (no crates.io egress;
    project_remote_host_no_cratesio) → validate on the local box + scp binaries.

Next steps:
  - Commit + push M1c to main (solo-flow). Feature_list F-EN-DYNSHARD M1c → DONE.
