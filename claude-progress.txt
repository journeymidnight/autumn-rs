Date: 2026-06-12
TaskStatus: completed
Task scope: /loop 生产视角急修 · 批次 4 = LEASE8-LITE（fuse truncate 崩溃
  一致性 + 读路径脏缓冲 bug）。批次 1-3：P0-D（2334304）、OBS-1
  （25d323d）、ENOSPC-1（4b10922）已闭。

=== TWO LISTS REQUIRED BY THE LONG-TASK HARNESS ===

  Already implemented (this commit):
    - fuse truncate-shrink 改 meta-first 提交序（关掉"durable size=旧值
      但尾部已毁→文件内读零"的伪造数据窗口）。
    - coco 2P1 折入：clean_beyond_eof 挂全部 grow 冷路径（残留 extent
      复活旧数据）；提交后清理失败 WARN 不报错（防 same-size 重试
      no-op）。
    - 第二个真 bug（T2 钓出，partition-server 读路径）：0 长 clamp 的
      子区间 GET 走 pooled 快路径返回回收脏缓冲 = 逐次变化的垃圾 →
      read_len==0 显式短路。
    - fuse_chaos.sh：T1（truncate+kill+remount 前缀级校验）+ T2
      （shrink→grow 零值）+ coco 3 项 + 两个 harness 根修（fusermount3
      缺失致历史 umount 全 no-op → unmount_all；裸目录写入竞态 →
      fs-type 守卫 + T 阶段前停负载）。
    - 文档：fuse CLAUDE.md "Crash-consistency contract" 全节。
  Not implemented (recorded):
    - BUG-LEASE-8 完整 generation manifest（含 crash-mid-unlink 空间泄
      漏 GC）——维持 ARCHITECTURAL deferred。
    - rolling upgrade（rkyv same-commit 约束）= 长线 track，需用户定
      线协议兼容策略。
    - F227 recovery-stuck 家族（仍 OPEN）；note 24 fsync coalescer
      （结构性不可达）。

Current summary:
  coco 终轮 1P0+2P1+1P2+1P3 全采纳折入（P0=write buffer size 提前抬升
  抹掉 pre-grow EOF → write() 入口 sweep）。LEASE8-LITE 已 commit+push
  （75ff306）。生产急修战役四项全闭环：P0-D（2334304）→ OBS-1
  （25d323d）→ ENOSPC-1（4b10922）→ LEASE8-LITE（75ff306），合计修复
  6 个真实 acked-data/读损坏级 bug。

Main gaps:
  - 见 Not implemented。
Next steps:
  - 无（loop 结束）。剩余生产项均需用户决策：rolling upgrade 线协议
    兼容策略 / F227 recovery-stuck（用户持有）/ BUG-LEASE-8 完整
    generation manifest。
