Date: 2026-07-03
TaskStatus: completed
Task scope: all-in-one AI 存储扩展 —— (1) fsspec 让 Python dataset 直接从
  autumn load 数据【F-FSSPEC-1 完成】;(2) vLLM 快速加载研究【交付
  docs/model_loading.md】;(3) F-FS-UNIFY 设计(用户批准 Q1/Q2/Q5)+ M0
  分配器【完成】;(4) chaos 测 fuse + fsspec 后 commit & push【全 PASS,
  已推 main】;(5) 用户"开工"M1【完成】;(6) M2 PyO3 `autumn.Fs`
  绑定【完成,已推 main 2a91e51】;(7) M3 fsspec facade 重写到共享 inode
  布局【完成,见下】。M4(lease 接线 + 围栏 + 跨面测试)为剩余里程碑,
  codebase-memory 已 re-index。

F-FS-UNIFY M3 完成(2026-07-03,用户"M3 start"):
  · `autumn_fsspec` 重写成建在 `autumn.Fs`(共享 inode 布局)之上的薄
    facade —— fsspec 与 fuse mount 现在看见**同一批文件**(同 [0x01]/[0x02]/
    [0x03] 键),F-FSSPEC-1 的私有 `fs/` chunk+manifest 布局(不同文件、
    互不可见)退役。
  · `filesystem.py`:info/ls→resolve+readdir(真 POSIX dirent,不再 keys-only
    range + manifest multi-get);cat_file→read;写/pipe_file→自动 mkdir 父链
    + create/resolve + 流式 write + flush(覆盖先 truncate 保精确尺寸);
    mkdir/rm(递归)/mv→mkdir/unlink+rmdir/rename(原子 dirent 交换)。
    完全同步(去掉 asyncio 桥 —— Fs 本就同步)。
  · 退役:删 `_layout.py`、`_bridge.py`、`tests/fake_kv.py`。offline 用纯
    Python inode 树 `tests/fake_fs.py`(镜像 Fs sync API),让 offline 走与
    live **同一份 facade 代码**。
  · 验收:offline 28(test_fs_offline 尺寸边界往返/ranged/负偏移/seek、
    ls/info/dirs、makedirs-不覆盖文件、rm/递归 rm、覆盖收缩精确、独占创建、
    跨块 append、mv、root-bucket 隔离;test_datasets_offline save/load +
    load_dataset('json');test_vllm_loader_offline;test_models_offline
    upload→materialize SHA 校验走递归 put/get)+ **live 9**(run_fsspec_e2e.sh
    自建隔离集群:0 B–5 MiB 往返 + 跨 extent ranged、ls/find/rm、覆盖收缩、
    跨块 append、独占创建、**datasets 往返**)。
  · 跨面写写围栏(per-inode lease)+ 一致性测试 = M4。
  · models.py/vllm_loader.py 只用公共面 → 不改;chaos_workload.py 公共面 →
    仅更注释。README/docs/ops.md/design M3 行/feature_list 同步。
  · coco findbugs(M3 diff)见文末 coco-M3 段。

F-FS-UNIFY M2 完成(2026-07-03,用户明确 M2 范围 = 把 M1 解耦的 core 经
PyO3 暴露给 Python,连接 M1↔M3 的枢纽):
  · 新 `python/src/fs.rs` 的 `autumn.Fs` 类(与 Client/BatchClient/Memory
    并列)。sync-blocking façade(照搬 Memory,非 Client 的 async ——
    因 M3 fsspec AbstractFileSystem 是同步 API):专属 compio worker 独占
    `!Send` FsState,Python 同步方法 ship job 并阻塞(GIL 释放)取结果。
    job 用 HRTB `for<'a> FnOnce(&'a mut FsState) -> LocalBoxFuture<'a,()>`
    让 core op 在 await 间持有 `&mut`(worker 串行,一次一借用,安全;
    已用 scratchpad 探针先验证该 HRTB 形状可编译再动手)。
  · API 面 = 设计 §4 全量:resolve/getattr(→dict)/readdir/lookup/mkdir/
    create/unlink/rmdir/rename/read/write/flush/truncate + lease
    acquire/heartbeat/release(M2 薄 wrapper;facade lease-on-open 是 M4)。
  · **反 drift(关键)**:绑定调用的是 fuse 二进制跑的同一份 Rust core,
    不在 Python 重写。为此把原内联在 dispatch 的文件 create/unlink 抽成
    core 函数,dispatch 改调(M1 同款):
      - dir::create / dir::unlink(文件)/ dir::resolve(path→ino,kv_get_opt
        屏障,`..` 拒绝)
      - meta::ensure_root(建根 fail-loud,不本地 seed → 全部 inode 走
        M0 manager 发号,对并发写者安全);dispatch::init_root 改调它 +
        仅全新 FS 叠加 fuse 专属 pre-manager 本地 seed(fuse 行为不变)
      - FsState::new_with_host(显式 host,不读 env,[[feedback_no_env_in_rs]])
  · wheel 依赖 `autumn-fuse { default-features=false, features=["core"] }`
    (无 fuser/kernel-mount 机器)。
  · 验收:headless py e2e `python/tests/run_fs_e2e.sh`(隔离 memory-mode
    集群,无 etcd,自建 wheel+manager+EN+PS 再拆):create/write/flush/read
    byte-exact(inline + 10 MiB 多 extent + 跨 8 MiB 边界 ranged + EOF 裁剪)、
    getattr/resolve/readdir/lookup、mkdir/rename/truncate/unlink、lease
    acquire→heartbeat→release smoke、**CROSS-INSTANCE byte-exact**(一个 Fs
    写、另一个 Fs 读回 + 提交的 shrink 跨 client 可见)—— exit 0。
  · 行为保真(dispatch Create/Unlink/init_root 重构):fuse lib 44、
    f_fuse_lease_1 6/6(含 fuse_create_acquires_writer_lease + 双 mount
    init_root)+ f_fuse_lease_2 2/2 + system_fuse_read 全绿 --ignored;
    core-only 构建 fuser 计数=0;workspace 构建;新码 clippy 干净
    (fs.rs 无警告;存量 doc-comment 警告在未触碰行,不做纯注释 pass)。
  · coco findbugs(M2 diff)已跑 —— 见文末 coco 段。

F-FS-UNIFY M1 完成(2026-07-03,用户指令"开工"):
  · crates/fuse 拆两层 feature:`core`(fuser-free 文件系统核心 ——
    meta/dir/extent/read/write/state + key/schema;唯一额外依赖 libc,
    getuid/getgid + S_IF* mode 位)/`fuse` = ["core", fuser…](default
    不变,二进制 required-features 不变)。
  · 新 fuse-gated `attr.rs` = **唯一** core→fuser 转换点:inode_to_attr/
    dt_to_filetype 从 meta/dir 上移;dir::lookup/mkdir 改返回
    (ino, InodeMeta),dispatch 在 reply 边界转 FileAttr;ReaddirEntry
    移入 schema(kind=DT_* byte),bridge 再导出保路径兼容,ops 在
    reply.add 处转 FileType;S_IF* 常量升 pub(持久 mode 格式位,
    M2 PyO3 绑定要用)。
  · 验收:core-only 构建 0 警告且 **cargo tree 中 fuser 计数=0**;
    default 构建 0 警告;fuse lib 44 + core-only 12;manager fuse e2e
    (lease 6/6+2/2、system_fuse_read)全绿含 --ignored;workspace 构建;
    **fuse_chaos PASS(65 文件)** = 行为零变化金标准。
  · 纯类型/可见性重构,无行为、无磁盘格式改动;clippy 新码干净
    (存量 doc-comment 风格警告在未触碰行,不做纯注释 pass)。
  · 下一步 M2:PyO3 `autumn.Fs` 绑定(绑 core + autumn_client::lease)。

Chaos 结果(2026-07-03,全部真 kill -9 注入):
  · fuse_chaos.sh PASS ×2(64/63 文件 byte-exact;F1 杀 PS→迁移、F2 杀
    manager→respawn、F3 杀 fuse daemon→remount + T1-T3 truncate/grow/
    unlink;第二遍带全部 coco 修复重跑)—— **M0 分配器在环**:每个 create
    走 manager 取号,杀 manager 后创建恢复 = M0 实战验证。
  · fuse_rmw_chaos.sh PASS(EIO + 文件原样 = RMW get-swallow 守卫正确)。
  · fsspec_chaos.sh PASS(新 harness):573 轮 0 MISMATCH、529 落账文件
    fresh-process 全 byte-exact + find/info 一致、写活性 OK、两次 failover
    0 条瞬时错误上浮(SDK 重试全吸收)。超时=UNCERTAIN 规则内建。
  · 全量测试:fsspec 36/36(27 offline + 9 live)、manager lib 171、
    fs_alloc e2e(memory+etcd 含 --ignored)、fuse lib 44、rpc 43、client 27。

coco M0-diff 轮(GPT-5.5)6 条全修:P2 makedirs 覆盖同 key 文件 manifest →
  NotADirectoryError/FileExistsError 语义;P2 subtree_prefixes("") 建
  `fs/m//` 匹配不到 → 裸命名空间特判;P2 fuse legacy cursor 非单调回写 →
  read-compare-write 单调守卫 + 文档降级为 advisory-only;P3 autocommit
  假承诺 → NotImplementedError;P3 CAS 无退避 → 3ms×attempt 线性退避;
  P3 model_loading.md fuse 命令缺参 → 修正。

F-FS-UNIFY 决策已定 + M0 已完成(2026-07-03):
  · 用户拍板:Q1=只写租约(读走 poll_invalidations/cache_is_stale epoch 一致,
    零读 RTT);Q2=方案 B manager AllocInodes RPC,计数器落 etcd 用 CAS;
    Q5=精简 POSIX 面(file/dir/size/mtime/read/write/rename/unlink)。
  · **M0 done**:MSG_ALLOC_INODES(0x53,WIRE v11=8a586475b2930f0f,
    MIN=MAX=11 同 commit 部署)+ crates/manager/src/fs_alloc.rs
    (leader-fenced etcd value-CAS on autumn-rs/fs/next_inode;严格 BE u64
    fail-loud;首建走 create_revision==0;memory 模式 Cell;与 alloc_ids
    分离)+ ClusterClient::alloc_inodes(count, floor)(mgr_call_leader)
    + fuse meta::alloc_inode 改为 manager 批量取号,旧 [0x04]next_inode
    值作迁移 floor(永不低于/永不回退)+ 每批 best-effort 回写旧 key
    (advisory,灾备重建新鲜度)。
  · 验证:memory e2e(16 路并发批次不相交、floor 单调、count=0 拒绝)+
    etcd e2e(8 路 CAS 批次不相交、etcd watermark 精确、follower
    NOT_LEADER 拒发 = 脑裂守卫)+ fuse 回归(lib 44、f_fuse_lease_1 6/6、
    f_fuse_lease_2 2/2、system_fuse_read,全含 --ignored)+ rpc 43/
    manager 171/client 27 lib 全绿 + workspace build + 新码 clippy 干净。
  · 文档同步:crates/manager/CLAUDE.md note 42、crates/fuse/CLAUDE.md
    分配器段、design doc M0 行、feature_list F-FS-UNIFY。
  · coco findbugs(M0 diff)后台进行中。
  · M1(解耦 fuse core)→M2(PyO3 Fs)→M3(fsspec facade)→M4(lease 接线)
    未动 —— 下一步 M1。

F-FS-UNIFY 设计文档(docs/fs_unify_design.md,已审批;原始提案记录):
  · 目标:fuse(POSIX mount)与 fsspec(Python)是同一文件系统的两个前端 ——
    同一 inode 布局、同一 per-inode lease 围栏,而非当前 F-FSSPEC-1 的独立
    `fs/` 命名空间(不同文件、互不围栏)。
  · 架构:一个共享 Rust FS core —— 把 crates/fuse 的 meta/dir/extent/read/write
    从 `fuser` feature 解耦(返回裸 InodeMeta,→FileAttr 转换上移进 dispatch.rs),
    fuse 二进制与新 PyO3 `autumn.Fs` 绑定共用;autumn_fsspec 重写成 shared inode
    布局之上的薄 facade(path↔inode、真 dirent、extent 读写)+ open/close 时
    autumn_client::lease acquire/heartbeat/release。
  · 关键既有支撑:lease 已是可复用库(autumn_client::lease);namespace core 已
    ~纯粹(ClusterClient + key/schema,仅 return 边界薄薄耦合 fuser)。
  · **前置 bug**:inode 分配器(meta.rs:alloc_inode)对 next_inode 计数器是
    非-CAS RMW → 并发分配者(fuse+fsspec 或双 mount)会重号 → 损坏。须改成
    crash-safe 多写(manager AllocInodes RPC 或 SDK CAS)。此项独立可先发,
    也顺带修当前多-mount fuse 的潜在隐患。
  · 分期 M0(分配器)→M1(解耦 core)→M2(PyO3 Fs)→M3(fsspec facade,退役
    `fs/` 布局)→M4(lease+围栏+跨面测试)。
  · 待用户定的开放问题:Q1 只写租约 vs 恒租约;Q2 分配器 manager-RPC vs SDK-CAS;
    Q5 Python 面 POSIX 语义广度。

Current summary:
  · 新增 python/autumn_fsspec/ —— `autumn://` fsspec 文件系统
    (AutumnFileSystem, AbstractFileSystem)。纯客户端适配器,建在既有 `autumn`
    PyO3 KV SDK 上,无新 server wire、不绕 partition 数据面
    ([[feedback_no_parallel_data_plane]] / [[feedback_client_side_complexity_first]])。
    与 autumn-fuse(原生 mount)互补,作程序化文件面。
  · On-KV 布局(_layout.py):manifest key `\x01+path` → 小 JSON
    {t,s,cs,n,m};数据块 `\x02+path+\x00+u64(idx)` → 每块 8 MiB
    (对齐 autumn-fuse MAX_EXTENT、ZC ≥64K、低于 64 MiB inline-put 上限
    → 任意大小文件可存,尽管 SDK 只暴露 inline put)。隐式 s3fs 目录 +
    显式空目录标记。读:多块 batch_get_into 进精确尺寸 buffer(块尺寸由
    manifest 已知)。
  · **关键坑(已修)**:autumn `range` 是 KEYS-ONLY 扫描
    (rpc_handlers.rs range_scan_sst_merge 恒 `value: vec![]`),值永远空。
    ls 原本从 range 值读 manifest → 真集群 JSONDecodeError。改为:ls 用
    keys-only range 拿子键,再对直接子节点 manifest 做 pipelined 多 get;
    info/cat_file 用 get 读 manifest。offline FakeKV 同步改成 range 返回空值,
    离线用例遂走生产同一路径。
  · 模型辅助(models.py):upload / materialize(拉到本地供 `vllm serve`)/
    load_safetensors(bytes 逃生门)。streaming loader 原型(vllm_loader.py):
    safetensors header 解析 + 逐 tensor ranged fetch(离线已测);vLLM
    register_model_loader 接线为骨架(**GPU/vLLM 未验证** —— 无 GPU)。
  · 研究交付:docs/model_loading.md —— fsspec 只喂 HF datasets,不喂
    transformers/vLLM/SGLang 权重加载(后者要本地路径或专用 streaming loader)。
    三条实用路径排序:A materialize-to-local(通用/零引擎码)、B FUSE mount +
    强制 `eager`(躲 mmap-over-FUSE 30-50× 陷阱)、C 自定义 streaming loader
    走 autumn ZC get_into(吞吐最高,原型)。含 vLLM --load-format 全景 + 来源引用。

Verification(真集群 1-node TCP,manager 127.0.0.1:9001):
  · 构建坑:装着的 `autumn` wheel 陈旧(wire [8,8] vs 集群 [10,10])→ maturin
    重建 + pip 强装。EN 控制口 10101 撞 Ray → AUTUMN_EXTENT_BASE_PORT=20000
    ([[project_local_perf_baselines_stale_multitenant]])。
  · offline 19/19(FakeKV 镜像 keys-only range;含 HF datasets
    save_to_disk/load_from_disk + load_dataset('json') 往返、safetensors
    header planner)。
  · live 9/9(0 B–5 MiB 分块往返含跨块 ranged read、ls/find/rm、覆盖写 reap
    (3→1 chunk 真集群校验)、跨块 append、xb 独占创建、
    **datasets 往返打真集群**)。递归 upload/materialize SHA 校验、
    load_safetensors/iter_tensors 打真 safetensors blob —— smoke 全绿。
  · 全量 33 passed(offline 24 + live 9)。

coco findbugs(GPT-5.5)两条 P2 全修 + 补测(离线+真集群):
  (1) xb/ab 写模式原被当普通覆盖 → ab 静默截断、xb 不查存在;改为 xb 抛
      FileExistsError、ab 从现有 manifest 续写(回读末尾 partial chunk 保
      uniform-chunk 不变式)。
  (2) 覆盖写较小内容会遗留旧尾块(manifest 不再引用)→ 加
      _reap_chunks(path, keep_n, old_n) 定点删 [keep_n, old_n)(pipe_file +
      buffered writer 两路);coco 建议的 generation 前缀+原子切换更重方案
      延后(KV put 单键原子;写中读对任何对象存储都是 last-writer-wins)。

用户复审提问("和 fuse 能访问相同文件吗?")→ 发现并修 latent bug:
  · fsspec 与 fuse 是同一集群的两扇独立门,NOT 同一批文件 —— fuse 按 inode
    键控([0x03][ino][off]),fsspec 按 path 键控;互不可见(故意:纯客户端
    不复刻 fuse 的 inode 分配 + lease 协调)。
  · **但**初版 fsspec 用了 \x01/\x02 前缀,与 fuse 的 inode(0x01)/dirent(0x02)
    **撞命名空间** —— 共享集群时 fsspec 根 ls(range \x01)会扫进所有 fuse
    inode。改到保留 `fs/` 命名空间(fs/m/ manifest、fs/d/ chunk;比照 kvcache
    `kvc/`、memory `doc/`)。真集群验证:注入 fuse 式 0x01/0x02/0x03 键,fsspec
    ls('') 完全无视之,且 fuse 键原样存活 → 二者可安全共享一集群。
  · 全量仍 33 passed(含 layout 前缀断言 + fuse 非碰撞断言更新)。

Docs/账本已更新:docs/model_loading.md(新)、python/autumn_fsspec/README.md、
  docs/ops.md(fsspec 验证段 + wire-mismatch/keys-only-range 提示)、README.md
  (fsspec 面 + highlights)、feature_list.md(F-FSSPEC-1 passes:true)。

Main gaps / risks(follow-up,无阻塞):
  1. vLLM 自定义 streaming loader(Recipe C)是原型 —— torch/GPU/vLLM 接线未验证;
     需 GPU 机验证后才能算 feature 完成。
  2. 大文件整段 cat_file 会把该 range 全部载入 RAM(调用方决定范围);
     buffered file 流式读按块,内存有界。极致吞吐走 Recipe C。
  3. S3-compatible 网关(可解锁零改码的 runai_streamer)未做 —— 未来选项。

Next steps:
  · M2 已完成推送(2a91e51)。M3 已完成全绿(offline 28 + live 9);coco
    findbugs(M3)结果见 coco-M3 段,如有 P0/P1 已 triage+修+复测。
  · commit M3(facade 重写 + 退役 fs/ 布局 + FakeFs + 测试 + 文档)。
  · M4(剩余里程碑):lease 接线(open 拿写租约、心跳续期、release)+
    围栏/一致性测试 + 跨面互通(写 via mount / 读 via fsspec byte-identical);
    并接 M2 fs.rs 标注的 stale-cache 边界(poll_invalidations/cache_is_stale)。
  可选(F-FSSPEC-1 遗留):GPU 机验证 Recipe C streaming loader;补 S3 网关。

── coco findbugs(M2 diff,GPT-5.5)4 条,triage + 修 + 复测 ─────────────
  · P1#1 stale cache(长生命周期 Fs 缓存了 inode 后,别的 client 改+flush →
    读到 stale size/extent):REAL 但 = **M4 边界**(设计 Q1:写租约 + 读经
    poll_invalidations/cache_is_stale 一致)。M2 明确只做绑定+headless 正确性。
    → **不在 M2 修**,在 fs.rs 模块 doc 显式标注该限制 + M4 计划(单 client
    读自己写、任何 fresh Fs 都一致 —— 正是 e2e cross-instance 覆盖的)。
  · P1#2 负 offset(write::write 无 read::prepare 那样的负 offset 检查,
    `offset as u64` 回绕成巨偏移污染 extent key):REAL 且在 M2 范围。
    → **修**:core write::write 顶部加 `offset<0 → Err`(与 read 对称,护住
    所有前端);e2e 加 `fs.write(ino,-1,..)` 必抛断言。
  · P2#3 lookup 把所有 Err→None(吞掉 decode/get_inode/routing 硬错,伪装成
    不存在):REAL。→ **修**:新增 core `dir::lookup_opt`(kv_get_opt 屏障,
    absent=Ok(None)/硬错=Err,不碰 lookup_count),`dir::lookup` 改调它(dedup;
    对 fuse 行为中性 —— ops.rs:73 本就无条件把 lookup 错映射成 ENOENT);绑定
    lookup 改用 lookup_opt → 硬错抛异常、仅真缺失返回 None。
  · P2#4 lease refcount 不对称(acquire refcount++ 但 release 无条件释放 +
    删 entry,acquire×2/release×1 会过早释放丢 fence):REAL。→ **修**:
    Fs.release 改 refcount-aware(refcount>1 只递减;==1 才 manager release +
    删 entry;借用在 await 前释放,无 await_holding_refcell_ref);e2e 加
    acquire×2→release×1 后 heartbeat 仍 True 的断言。
  复测:fuse lib 44、fuse e2e 6/6+2/2+1/1(--ignored)、M2 headless e2e
  (含新负 offset + refcount 断言)exit 0、core-only fuser=0、新码 clippy 干净。

── coco findbugs(M3 diff,GPT-5.5)6 条,5 修 + 1 定为 M4 边界 ──────────
  · P1#1 写路径无租约/围栏(pipe_file/buffered write 直接 truncate/write/flush,
    两个 fsspec 或 fsspec↔fuse 并发写同 inode 会丢写/混内容):REAL 但 =
    **M4 边界**(设计分期 M4=lease 接线 + 围栏;M3=共享可见性 + 读写)。
    coco 亦承认"M3 暂不支持则应…"。→ **不在 M3 修**,在 _ensure_file_ino
    加显式 M4-边界注释(单写者=datasets/upload 今日安全)。
  · P1#2 cat_file 把整个 e-s 传给 Fs.read(size 是 u32)→ >4 GiB 读(大模型)
    溢出回归:REAL 在范围。→ **修**:cat_file 改按 min(block_size,1 GiB)
    分步循环拼接(块内存有界,>4 GiB 可读)。
  · P2#3 _mkdirs/create 非原子 lookup→mkdir 无 EEXIST 重查(并发建同父目录
    竞争):→ **修**:_mkdirs + _ensure_file_ino 对 mkdir/create 的 EEXIST
    幂等(catch→重 lookup→校验类型)。
  · P2#4 root= 设了但 bucket 目录尚未写创建时 ls("")/info("") 抛
    FileNotFoundError(API 回归;旧版 fs 根恒存在):→ **修**:虚拟根 ——
    info("")恒 directory、ls("")在 bucket 不存在时返回 [](不泄露集群 ROOT)。
  · P3#5 run_fsspec_e2e.sh pip 失败被 tail 掩盖:→ **修**:pip install 落
    日志 + `||` 守卫(maturin/pytest RC 本就已 `||`/PIPESTATUS 捕获)。
  · P3#6 mkdir 忽略 create_parents=False:→ **修**:False 时父目录须已存在
    (缺→FileNotFoundError),只建 leaf。
  复测:offline 30(+虚拟根 +create_parents=False 两用例)、live 9(fixture
  1 MiB block + 5 MiB 读已实测 cat_file 循环)全绿。
