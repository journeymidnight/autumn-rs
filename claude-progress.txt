Date: 2026-04-23
TaskStatus: completed
Task scope: F100-UCX Phase 2 — migrate autumn-rpc / autumn-stream /
            autumn-manager call sites to AutumnTransport (still TCP
            underneath); wire init() into all 4 binaries.

Current summary: Client-side TCP connect paths migrated to use
                 autumn_transport::current_or_init().connect — covers
                 RpcClient::connect (autumn-rpc), rpc_oneshot in
                 extent_node, manager's internal RpcConn::connect.
                 RpcClient::from_stream renamed to from_conn taking a
                 Conn enum. TCP_NODELAY conditional on Conn::as_tcp().
                 All 4 binaries call autumn_transport::init() at
                 startup; tracing log "autumn-transport: init kind=Tcp"
                 confirms wiring.

                 SCOPE NOTE: server-side accept paths intentionally
                 NOT migrated — rpc/server.rs uses std::net::TcpListener
                 on a dedicated OS thread (compio's TcpListener is
                 !Send), and stream/extent_node.rs + manager/rpc_handlers
                 use compio TcpListener directly. UCX accept needs a
                 fundamentally different per-worker ucp_listener
                 pattern; that work belongs in Phase 3+ when UCX impl
                 lands. Today's TCP server-side path remains identical
                 to pre-F100 behavior.

                 Test results (workspace, serial, includes live etcd):
                 224 passed / 0 failed / 9 ignored
                 = Phase A baseline (213) + 11 new transport tests.
                 No regressions.

Main gaps: P3 — UcxTransport implementation under feature = "ucx".
           Includes: in-tree ucx-sys-mini bindgen sub-crate, per-thread
           UcxThreadCtx + progress task on compio::fs::AsyncFd,
           UcxConn read/write via ucp_stream_*_nbx, UcxListener via
           ucp_listener_create + on_conn handler, UCX loopback test
           suite mirroring loopback_tcp.rs.

Next steps: Phase 3 (Tasks 13–19 in plan). First sub-task: scaffold
            crates/transport/ucx-sys-mini with bindgen build.rs over
            <ucp/api/ucp.h> producing the symbol whitelist from
            spec §13.

Branch: f100-ucx (commits 14506a5..1283c4a).
