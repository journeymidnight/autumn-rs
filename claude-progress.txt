Date: 2026-06-11
TaskStatus: completed
Task scope: BUG-LEASE-2 Phase 2 + F263 fencing naming convergence — fence-floor persistence (WAL +
  checkpoint), MSG_PUT_ZC fencing, fuse/ioring lease-epoch stamping.

=== TWO LISTS REQUIRED BY THE LONG-TASK HARNESS ===

  Already implemented (this commit):
    - OP_FENCE_BUMP WAL record (0x08): queued BEFORE the admitted write
      in the same group-commit when check_and_bump_fence RAISES the
      floor; never enters memtable; replay max-merges (idempotent, no
      ts-dedup needed); GC VP scan skips it.
    - TableLocations.fence_floors checkpoint snapshot (3 publish sites,
      captured under the same borrow — F148-A preserved); recovery
      seeds via max-merge over ALL meta records (merge-union correct)
      then replay applies later bumps.
    - PUT_ZC_HEADER_LEN 28 -> 44 (inode_hint + lease_epoch);
      enqueue_put_zc + enqueue_batch_put (per-op fields, CODE_FENCED
      in statuses slot) run the same fence check; value-too-large
      before fence ordering kept (coco R2-P1 #5).
    - Client: WriteLease + AutumnError::Fenced + put_fenced /
      put_zc_fenced / put_many_fenced; anonymous paths unchanged.
    - fuse: FsState::write_lease_for(ino) -> write_region (appends via
      put_many_fenced + RMW), truncate straddle put, put_inode.
    - ioring: write_lease_of(opened) -> execute_step puts,
      maybe_persist_size_growth, daemon dirty-meta deferred flush
      (map value now (InodeMeta, lease_version)) + close-time flush.
    - coco round folded: range-before-fence admission ordering in all
      4 enqueues (mis-routed writes can't poison/persist the floor);
      DELETE fencing (DeleteReq fields + enqueue_delete + client
      delete_fenced + fuse truncate/unlink stamping); daemon drops
      stale dirty-meta on terminal Fenced instead of requeue-forever;
      e2e wait loops bounded 60s. snapshot-of-undurable-floor accepted
      with rationale (floor=E proves all <E revoked; fail-closed).
      Naming converged: client-side fencing epoch = `lease_epoch`
      everywhere (FuseLease/OpenedExtents renamed); manager wire keeps
      `version`.
    - MSG_STREAM_PUT removed outright (user decision): zero callers
      since F186, and its handler bypassed both the inline cap and the
      fence check. 0x46 reserved.
  Not implemented (recorded, BUG-LEASE-8 family):
    - multi-key atomic commit boundary (unchanged).

Current summary:
  e2e bug_lease_2_phase2_persistence x2 PASS (subprocess PS kill -9 +
  restart): WAL-replay path (no flush) and checkpoint path (flush moves
  vp past the bump records) both keep the floor; stale epoch fenced
  post-restart on MSG_PUT and MSG_PUT_ZC; live epoch unaffected.
  Phase 1 e2e x2 regression PASS. Units 162+72+27+68+19 green.
  4K write A/B: A 52.5-56.4K vs B(pre-change) 49.5-62.8K overlapping
  = environment noise, no regression.

Main gaps:
  - BUG-LEASE-8 (atomic multi-key commit boundary) still deferred.
Next steps:
  - coco verdict folded before commit.
