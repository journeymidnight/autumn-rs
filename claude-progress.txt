Date: 2026-06-17
TaskStatus: completed
Task scope: 修复分区 merge 泄漏 extent refs 的 bug(MERGE-REFS-LEAK),并提供现网审计工具。
  起因:用户排查 "EC extent 10 删不掉且 autumn-op info 里看不到"。定位为 merge 路径
  refs 记账不对称导致的不可见、不可回收孤儿 extent。
Current summary:
  - 根因:`compute_merge_streams` / `splice_streams_without_new_tail`(manager/src/lib.rs)
    对每个 victim extent 无条件 `refs += 1`,而 `apply_merge_mutations` 删除 victim 的 3 个
    stream 却无对应 `refs -= 1` → 每次 merge 每个 victim extent 泄漏 +1;CoW 共享 extent 还会
    在 survivor extent_ids 里被重复列举。多轮 split→merge→GC 后 membership 归 0、refs 卡正数
    → extent_can_delete(refs==0 && vp_table_refs==0)永不成立 → 永久泄漏 + info 不可见
    (info/handle_stream_info 只渲染 stream 成员 extent)。
  - 修复:merge 对 victim extent 的 refs 改为成员关系中性 —— 共享则 refs-=1 且 extent_ids
    去重不重列;独有则 refs 不变(转移)。splice 顺序与 vp_head replay 不变量保持不变。
  - 测试:修正 2 个把 bug 当预期的旧单测(f181 merge/splice:refs 应为 1,原断言 2);
    新增 2 个 CoW 共享回归(f_merge_refs_leak_*)。manager lib 165 全绿;cargo build --workspace 绿。
  - 审计工具 python/audit_extent_refs.py:etcdctl + autumn-op --json info 交叉核对,报告孤儿/
    refs 不匹配/重复列举(现网准确复现:孤儿 10 与 47、refs+1 的 12/20/33/46、stream 29 重复列 20)。
现网处置(已执行):
  - major-compact partition 15/31/42:vp_table_refs(10) 5→3(part 31 的引用被压实丢弃);并暴露出更多
    历史泄漏 —— compaction 把 +1 泄漏的 extent(12/20/26/46)从 stream 截走后,因 refs 卡正数没被回收,
    转成孤儿(印证 bug 危害:截出 stream 却减不到 0)。
  - 按 vp_table_refs 分类 6 个孤儿:extent 10 = LIVE(part 15&42 的活 key 值就在里面,vp_table_refs=3,
    EC 4 盘,必须保留);extent 12/20/26/46/47 = DEAD(0 stream + 0 vp 引用,3 副本)。
  - 回收 5 个 DEAD 孤儿:删 etcd extents/{12,20,26,46,47} + unlink 30 个文件(.dat+.meta,3 盘各份)。
    etcd extent 数 22→17;审计复跑确认 5 个已消失。
  - 可观测性修复 + 部署:`handle_stream_info` 全量 dump 现包含非成员 extent;`autumn-op info` 增
    `refs=N (streams=M)` / `vp_table_refs` / `(ORPHAN…)` / `(refs-leak)` 标签。cargo build --release -p
    autumn-server → `cluster.sh restart-manager`(replay 收敛 stale 内存)。验证:`info` 现在能看到
    extent 10(标记 ORPHAN, retained by vp_table_refs — live data, refs=2 streams=0 vp_table_refs=3);
    manager lib 165 单测仍全绿。
  - 残留 refs 泄漏修复:`python/patch_extent_refs.py`(etcd v3 REST gateway,base64 二进制安全;按
    root=len-88、refs@root+32 定位 + 校验 + 备份 + readback)。停 manager → patch extent 10 refs 2→0、
    extent 33 refs 2→1 → 起 manager。审计增强:用 info JSON 的 vp_table_refs 把孤儿分为 DEAD(可回收泄漏)
    与 RETAINED(活数据,正常)。
  - 终态:审计 exit=0,"every extent's refs matches its live stream membership";extent 10 = retained
    orphan(streams=0, vp_table_refs=3, refs=0 准确),info 可见并标注 live data;manager 内存已随重启收敛。
Main gaps:
  - 无功能/一致性缺口。extent 10 仍是孤儿但属正确的"活数据保留"(vp_table_refs=3);其 key 将来失效 +
    compaction 后 vp_table_refs→0 会变 DEAD,届时用 python/reclaim 流程回收(无自动触发器,因它不在任何 stream)。
设计文档:docs/value_stream_design.md(对齐 WAS:log_stream 退化纯 WAL + 大 value 进独立
  value_stream,单一生命周期 vp_table_refs,回收折进 compaction + vp 侧删除触发,退役独立 GC loop;
  分 Phase 0 止血 / 1 写路径 / 2 回收折叠 / 3 迁移)。feature_list.md 加 VALUE-STREAM 条目(DESIGN ONLY)。
Commits: 已提交 + push 到 main(solo-flow):merge refs 修复 + 单测 + 可观测性(handle_stream_info / autumn-op info)
  + python 审计/修复工具 + value_stream 设计文档 + 文档更新。
Next steps:
  - 排期 VALUE-STREAM:先落 Phase 0(vp 侧删除触发 + 兜底 sweep,止血 + 终态删除原语)。
  - 可选:对 merge 修复补跑 coco / code-review。
