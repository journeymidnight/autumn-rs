Date: 2026-08-26 (p)
TaskStatus: not_completed
Task scope: 自主循环 —— 【修掉本 session 唯一一个已确诊未修的 bug】:get 的 GC-pin false-NotFound。
  【为什么可以直接修,不违反 reproduce-first】这不是从现象推断出来的机制假设,
     而是代码里明写着 `return Ok(GetOutcome::NotFound)`,与【同一个文件】里
     sst_lookup_paged_retry 的注释 "a real read error ... NEVER a false NotFound" 直接冲突。
     确定性问题,读得出来,不需要复现。
  【修法与依据】GC 持 writer pin 只说明 punch 即将发生,【不说明这个值没了】——
     relocate-then-punch 保证 live 值先被重定位,所以 live key 的字节要么还在原处、要么已重写到别处。
     原注释自称 "the client falls back / retries",但 NotFound 对客户端是【终态】,做不到它宣称的事。
     改为 StatusCode::Unavailable → 客户端映射成 AutumnError::ConnectionError = 【可重试】;
     pin 只在 punch 期间持有,重试要么读到原字节、要么读到重定位后的 VP。
  【测试】新增单测钉住让该分支可达的前提:writer pin 持有时 reader 被拒、释放后恢复。
     若这个前提哪天不成立,Unavailable 分支就成了死代码,被它替换掉的 false-NotFound 可能悄悄回来。
  门:ps lib 208(新增 1)、system_gc_multiversion_same_extent、system_split_forcegc_stale_cache、
     system_gc_inflight_wal_put、system_chaos(--ignored)全绿。
Next steps(未变,按价值):
  1) F1+F4(split 右孩子 replay 起点,2/25):读 (a) split flush 与 commit_length 捕获之间 vp 能否前进;
     (b) duplicate_stream 对 meta_stream 的 seal 是否让右孩子读到旧 checkpoint;
     (c) CoW 共享 extent 下 recover_partition 的 chosen_pos/first_pos_by_eid 是否正确。
  2) F3 单独查 EC+VP 读链(仅 VP、读不到、持久)。
  3) F2 改 EC marker 断言(带 coordinator 状态 + age 门限)。
  4) 评审剩余 P1 批次。
─────────────────────────────────────────────────────────────
Date: 2026-08-26 (o)
TaskStatus: not_completed
Task scope: 自主循环 —— 【F1 与 F4 统一为同一根因方向】,搜索空间大幅缩小。产品代码未动。
  【统一的依据(不是"都静默"这种弱依据)】
    - chaos key = mem/b{000..199} + mem/q{000..199},共 400 个;split 用 at_key: None ⇒ PS 取【median】;
      排序后 b* 全在 q* 之前,中位数正好落在两者交界 ⇒ 【q* 全部进右孩子】。
    - F4 丢的 key 【全部是 q*】(w1 的 b* 一个没碰)⇒ 丢失发生在【split 右孩子】。
    - F1 的 wedge 发生在 part 22,也是【split 右孩子】。
    ⇒ 两者是【同一位置 + 同一机制维度】的连续谱,而不只是"都静默":
        replay 起点超出 extent 末尾 → 读越界 → wedge(F1)
        replay 起点仍在 extent 内但太靠后 → 跳过中间一段 → 丢最后的写(F4)
      F4 的 got_seq 比 expected 小 32–432,正是"从太靠后的位置开始 replay"的表现。
  【与 F3 的区别仍然保持】F3 仅 VP(kid 全为 8 的倍数)、读不到、持久 —— 不并入本条。
    合并 F1/F4 的依据是同一位置+同一维度;F3 缺这个依据,别再重蹈之前把不同失败混为一谈的覆辙。
  【方向反转】此前一直在问"seal 为何短于 vp";现在应反过来问【右孩子继承的 checkpoint vp 为何会超过 seal】。
    右孩子的 checkpoint 来自 duplicate 出来的 meta_stream(即源分区的 checkpoint)。
    需要查的三点:
      (a) split 的 flush_memtable_locked 与 commit_length 捕获之间,vp 是否还能前进;
      (b) duplicate_stream 对 meta_stream 的 seal 是否可能切掉【最新】checkpoint,让右孩子读到旧的;
      (c) 右孩子 open 时 recover_partition 选 chosen_pos 的逻辑(first_pos_by_eid)在 CoW 共享 extent 下是否正确。
  【统计】19+6=25 次运行:F1×1 F2×2 F3×1 F4×1 reclaim×1,其余 ok。单模式 ~1/25 ⇒ 不再靠 hunt。
Next steps:
  1) 按上面 (a)(b)(c) 读代码;这条不需要复现就能推进,且 F1+F4 合起来占 2/25,是最值得修的。
  2) 修 get 的 GC-pin false-NotFound(独立、明确、低风险)。
  3) F3 单独查 EC+VP 读链;F2 改 EC marker 断言;评审剩余 P1。
─────────────────────────────────────────────────────────────
Date: 2026-08-26 (n)
TaskStatus: not_completed
Task scope: 自主循环 —— 新标签跑 6 次;F3 未命中(累计 1/19),但抓到【第四种失败 F4:acked 写的 lost update】。产品代码未动。
  【F4(新,最严重的一类)】f3run2:mismatches=22,读到的是【旧版本】而不是读不到。
     证据(scratchpad/f3_2.log):
       mem/q000185 expected seq=4797 got seq=4653;q000007 4859→4515;q000131 4775→4343 …
       got seq 【永远小于】expected seq(差 32–432)。
     【两个强模式】
       (a) 全部是 `q` 前缀 —— 只有 writer w2 的 key,w1 的 `b*` 【一个都没有】⇒ 按 writer/分区聚集,不是随机。
       (b) kid 【不是】8 的倍数为主(185/7/158/100/131/117/125/88/166/52 混合)
           ⇒ 与 F3 不同,【不是 VP 专属】,普通小值也丢。
     序列:LatencySpike → FenceUnfence → Split → SIGKILL+restart node 1。
     chaos 只把 CODE_OK 记进 expected ⇒ 这些是【确认过的写】被丢回旧版本 = acked-write loss。
     且 "no fail-loud markers in any EN log" —— 又是静默。
  【F3 vs F4 的区别要保持清楚,别再合并】
     F3:仅 VP(kid 全为 8 的倍数)、读不到、持久。
     F4:混合大小值、读到旧值、按 writer 聚集。
     两者共同点只有"静默 + acked 丢失",机制很可能不同。
  【运行统计(19 次累计)】F1 stale_vp 1 次;F2 EC marker 2 次;F3 VP not_found 1 次;
     F4 mismatch 1 次;reclaim_errors 1 次;其余 ok。
     ⇒ 单个失败模式都 ~1/19,靠 hunt 撞见效率很低;应转向【代码可读】的路径。
Next steps:
  1) F4 优先级最高(acked 写读回旧值 = 最直接的正确性破坏)。入手:全部是同一 writer 的 key,
     且序列含 Split + SIGKILL ⇒ 查 split 后新分区的 WAL replay 是否丢了最后一段
     (与 F1 的 vp/seal 方向可能同源)。b* 与 q* 落在 split 的不同侧是关键线索:
     先确认 split 的 mid_key 是否恰好把 q* 分到新分区。
  2) 修已定位的 get GC-pin false-NotFound(rpc_handlers.rs get_value_inner)——独立、明确、低风险。
  3) F1 转查 vp 侧;F2 改 EC marker 断言;评审剩余 P1。
─────────────────────────────────────────────────────────────
Date: 2026-08-26 (m)
TaskStatus: not_completed
Task scope: 自主循环 —— 发现 chaos 的 not_found 是【折叠过的】,已修诊断(commit ed74e12)。产品代码未动。
  【为什么这条很重要:它让上一轮的结论站不住】
     verify_per_key 的重试循环里:`Ok(r) if r.code == CODE_OK => 成功` / `Ok(_) => {}`
     —— 任何【非 OK 的 code 被静默丢弃】继续重试,10 次耗尽后一律 push 进 not_found。
     所以 chaos 报的 "not_found" 同时包含:
       (a) 真的 CODE_NOT_FOUND(key 不存在)
       (b) 读【失败】(Internal / FailedPrecondition / eversion mismatch / decode 失败……)
     ⇒ F3 的 24 个 key 未必是"条目从 LSM 里消失",同样可能是"条目好好的,值读不回来"。
     这两者机制完全不同,而 harness 把区别抹掉了 —— 上一轮据此推断"VP 条目消失"是【不成立的】。
  【已修】记录最后一次非 OK 的 (code, message),输出时区分:
     真 NOT_FOUND 只打 key;其它打 "key [read failed: code=N msg]";一次响应都没有的打 "[no response — wedged/timeout]"。
  【模式复现第三次】本 session 已修的同型诊断缺口:
     ① code_description 把未命名 code 折叠成 "error"(EC fence 被误读成瞬时错误)
     ② truncate_to_commit 静默截短 extent(唯一让长度回退的路径,零日志)
     ③ 本条:chaos 把所有非 OK 读结果折叠成 not_found
     教训一致:【拒绝/失败到达运维时若不带原因,每次都要重查一遍】。
  【进行中】改后连跑 6 次 chaos 收集数据,期望命中 F3 时能直接看到真实 code。结果未出。
Next steps:
  1) 看 6 次结果;若命中 F3,not_found 行会带 "[read failed: code=N ...]" 或确认是真 NOT_FOUND。
     - 若是读失败 ⇒ 查 EC-converted extent 的 VP 读链(ec_subrange_read 解码 / shard 缺失)。
     - 若是真 NOT_FOUND ⇒ 才轮到"条目消失"方向(compaction 丢 VP 条目 / replay dedup)。
  2) 修 get 的 GC-pin false-NotFound(rpc_handlers.rs get_value_inner):应返回可重试错误而非 NotFound。
  3) F1 转查 vp 侧;F2 改 EC marker 断言;评审剩余 P1 批次。
─────────────────────────────────────────────────────────────
Date: 2026-08-26 (l)
TaskStatus: not_completed
Task scope: 自主循环 —— F3(静默丢 acked 写)缩小到 VP 路径;顺带发现一处 fail-open。未改产品代码。
  【F3 决定性线索:丢的全是大值(VP)】丢的 24 个 key 的 kid【全部能被 8 整除】,
     而 chaos 的 make_value 里:`big = kid.is_multiple_of(8)` → 8192 字节(> 4KiB VALUE_THROTTLE)
     ⇒ 这些 key 全部走 ValuePointer 路径(值在 log_stream,VP 在 SSTable)。小值一个没丢。
     ⇒ F3 不是随机丢失,是【VP 路径专属】。序列:EcConvert extent 12(log extent)→ SIGKILL node 1。
     即:EC 转换一个存有 live VP 的 log extent 时杀节点 ⇒ 大 value 读不到,且【持久】
     (post_reclaim 仍是同样 24 个 key,不是瞬时窗口)。
  【顺带发现,已记未修:get 的 VP 路径有一处 fail-open】
     rpc_handlers.rs get_value_inner:VP 值在 `acquire_reader_pin` 拿不到 pin 时
     (GC 持 writer pin)直接 `return Ok(GetOutcome::NotFound)`。
     【同一个文件里 sst_lookup_paged_retry 的注释明写 "NEVER a false NotFound"】——
     这里却让一个 live key 在 GC 窗口期读起来像"不存在",而 NotFound 对客户端是终态、不会重试。
     注释自称 "the client falls back / retries",但 GetResp 的 NotFound 不触发重试路径。
     【判断】不是 F3 的成因(F3 持久,pin 窗口瞬时),但它本身违反了本文件已明确的原则,值得单独修。
Next steps:
  1) 【F3】查 EC 转换后的 VP 读路径:extent_read_descriptor 拒绝 EC'd extent(shard bytes ≠ value),
     VP 读要走 ec_subrange_read 解码。查这条链上有没有把"shard 缺失/解码失败"吞成 NotFound 的地方
     ——F3 的特征(持久 + 静默 + 仅 VP)与"EC 后读不回来但不报错"高度吻合。
     复现:chaos 约 1/13;命中标志是 not_found 的 kid 全是 8 的倍数。
  2) 修上面那处 GC-pin fail-open:应返回可重试的错误(Unavailable/FailedPrecondition)而非 NotFound,
     或在 pin 等待后重试。注意 relocate-then-punch 保证 live 值会先被重定位,
     所以"GC 要删它"不足以推出"它不存在"。
  3) F1 转查 vp 侧;F2 改断言;评审剩余 P1 批次。
─────────────────────────────────────────────────────────────
Date: 2026-08-26 (k)
TaskStatus: not_completed
Task scope: 自主循环 —— 13 次 chaos 统计,把"间歇 flaky"分解成【三种不同的失败】。未改产品代码。
  【累计 13 次运行的分类】(日志在 scratchpad/{chaos_freq_,chaos_tr_,hunt_}*.log)
    F1 stale_vp → split 右孩子 wedge → 200/400 key 丢 —— 1/13
       序列:FenceUnfence node1 → Split。extent=12 offset=8946242 sealed_length=5372990。
    F2 EC marker 钉住(43s)→ 数据完好 not_found=0 —— 2/13
       序列:EcConvert → LatencySpike on coordinator's node。疑似断言太严(EC dispatch 本就跳过
       Suspected/Fenced coordinator),非产品 bug。
    F3【最严重】静默丢 24 个【已 ack】的 key —— 1/13
       序列:LatencySpike → FenceUnfence → EcConvert extent 12 → SIGKILL+restart node 1。
       `writes acked=10456 failed=0`(零写失败!)、无 wedge、无 EC marker、
       【no fail-loud markers in any EN log】——每一层都以为自己没事。
  【负面证据:上轮的 truncate 假设被削弱】加了 truncate warn 之后跑了 12 次,
     【trunc_warn = 0/12】。若 F1 依赖 truncate(truncate 是该链条的必要条件),
     truncate 的频率应 >= stale_vp 的频率;实测 truncate 更罕见 ⇒ 削弱"truncate→last_synced 降低→seal 短"这条解释。
     【严格说】F1 那次(run1)是在加日志【之前】跑的,所以它没有 trunc_warn 只能说明日志不存在,
     不能直接证明那次没发生 truncate。这是统计论证不是证明,但足以把它降级为次要假设。
  【矛盾仍未解决】flush 屏障保证所有副本 last_synced >= vp;commit_length 返回 last_synced;
     append 在 EN fsync 之后才返回 ⇒ min last_synced >= vp。那 seal 为何会短于 vp,目前【无解释】。
     若 truncate 不是原因,则问题在 vp 侧:checkpoint 记了一个不该记的 vp。
Next steps(按严重性重排):
  1) 【F3 优先】acked 写丢失 + 完全静默,比 F1 更严重(F1 至少 fail-loud 了)。
     入手点:EcConvert + 杀 coordinator/participant 的交互。先在 EC 的 apply/读路径找"静默"的位置
     ——本项目反复出现的模式是【破坏性操作没留痕】(已修过 EC fence 静默、truncate 静默)。
  2) F1:改查 vp 侧。给 checkpoint 写入(save_table_locs_raw)加一条记录 vp 的日志,
     命中时可与 seal 长度对照,判断是 vp 记错还是 seal 记错。
  3) F2:改 verify_no_ec_markers_pinned 断言(带 coordinator 状态 + age 门限),把它从失败降级为信息。
  4) 评审剩余未处理项:EC P1-3/P1-4、errors F3/F4/F5、delete F3/F4/F5、recovery F3/F5/F6。
─────────────────────────────────────────────────────────────
Date: 2026-08-26 (j)
TaskStatus: not_completed
Task scope: 自主循环 —— split 右孩子 wedge(A)的【机制链已完整打通,只差"谁发的小 commit"】。未改产品代码。
  【完整链条(逐环读代码确认,非推断)】
    1. flush 屏障 await_extent_synced_to 要求【ALL replicas】last_synced >= vp_offset 才写 checkpoint;
       replica_addrs_for_extent 不做健康筛选 ⇒ 副本集合不是变量。
    2. handle_commit_length 对【open extent 返回 last_synced】(不是 entry.len)——
       这是刻意的:entry.len 在 pwrite 前就被预留(build_append_future 第 7 步),
       返回它会让 manager seal 到未 fsync 的位置。
    3. truncate_to_commit 里:`len.store(commit)` 之后【也把 last_synced.store(commit)】
       (注释写明:那些字节已不在盘上,而 last_synced 正是 MSG_COMMIT_LENGTH 返回的东西)。
    4. ⇒ 某个 writer 发出偏小的 header.commit → truncate 把 last_synced 降到该值
       → 之后的 seal probe(compute_commit_seal = min over members)读到降低后的值
       → seal 短于已 checkpoint 的 vp → replay 起点越界 → 右孩子 wedge + 丢 key。
    降 last_synced 本身【是对的】(字节确实没了)。缺口在【谁发出了低于已 ack 位置的 commit】。
  【新增可疑点,未验证】seal_and_roll_tail 用 `alloc_new_extent(stream_id, None, 0)` 即【probe 模式】。
     而 stream CLAUDE.md note 20 明确写 `None` 是 "genuine new-owner takeover ONLY",
     并警告 probe 会捕获 speculative 字节。但 drain_fenced_open_tails 会在【活跃 writer 正在写】时
     经 MSG_ROLL_TAILS 调它 —— 与 note 20 的适用条件不符。run1 的 nemesis 序列正是
     FenceUnfence(→ 触发 drain_fenced_open_tails → ROLL_TAILS)紧接着 Split(第二次 seal)。
     【下一步要验证的正是这条】:probe seal 与并发写 + truncate 的交互。
  【捕获器已就位】truncate_to_commit 的 warn(prev_len/commit/dropped)在上一轮加好并验证了低噪声。
  【本轮 hunt 进度】7 次批量运行中,已完成的都是 ok / stale_vp=0 / trunc_warn=0(命中率 ~1/6,尚未命中)。
Next steps:
  1) 继续 hunt 直到命中;命中时看 truncate warn 的 prev_len→commit,反推是哪个 writer。
     【反向判据】若命中 stale_vp 却无 truncate warn,则第 3-4 环不成立,问题在 vp 侧(checkpoint 记了不该记的 vp)。
  2) 独立于 hunt,可先审 seal_and_roll_tail 的 probe 用法是否违反 note 20 的适用条件
     ——这条不需要复现就能读出结论,若确实违反,再判断它能否产生"seal 短于 vp"。
  3) 改 verify_no_ec_markers_pinned 断言(带 coordinator 状态 + age 门限)。
─────────────────────────────────────────────────────────────
Date: 2026-08-26 (i)
TaskStatus: not_completed
Task scope: 自主循环 —— 追 split 右孩子 wedge(A)。【把矛盾收窄到一个具体机制,并架好捕获器;未抓到根因,未修】。
  【推理链收窄】症状是 checkpoint 的 vp_offset(8946242)落在 split 给 log tail 的 seal(5372990)之外。
     逐条排除:
       - flush 屏障 await_extent_synced_to 要求【ALL replicas】的 last_synced >= vp_offset,
         且 replica_addrs_for_extent 不做健康筛选(不会因为 fence/suspected 缩小集合)⇒ 副本集合不是变量;
       - split 用 part_sc.commit_length()(all-replica min),而 last_synced <= len ⇒
         正常情况下 commit_length >= vp_offset。
     ⇒ 结论:【只有 `len` 回退过】才能解释 commit < 已 checkpoint 的 vp。
     而 len 回退的唯一来源是 commit-based truncation(append 前 `local len > header.commit → truncate`),
     即某个 writer 发出了偏小的 header.commit。
  【发现的诊断缺口 + 已修】truncate_to_commit 是【唯一把 extent 变短的路径】,而它【完全没有日志】。
     beyond-commit 的字节按定义是未 ack 的,丢掉是对的;但如果 writer 给出的 commit 低于已 ack 的量,
     acked 数据就在这里消失,而没有任何记录。已加 warn(extent_id/prev_len/commit/dropped)。
  【验证:低噪声高信号】加日志后连跑 3 次 chaos:全通过,stale_vp 0 次,
     【truncate warn 也是 0 次】——说明这条路径在健康运行里根本不触发,
     所以它一旦出现就是异常信号,不会淹没在噪声里。
  【频率】stale_vp 6 次运行命中 1 次(~1/6),比上轮估的 1/3 低。
Next steps:
  1) 继续跑 chaos 直到 stale_vp 命中(~1/6),这次日志里会有 truncate warn 说明是谁把 len 截短的
     (extent_id + prev_len → commit + dropped 字节数)。有了那一行就能反推是哪个 writer 发的小 commit。
     若命中 stale_vp 却【没有】truncate warn,则上面的推理链有漏 —— 那本身是更重要的发现,
     说明 len 没回退,问题在 vp 侧(checkpoint 记了一个不该记的 vp)。
  2) 改 verify_no_ec_markers_pinned 的断言:带 coordinator 状态与 age 门限,区分"卡住"和"合理等待 flapping coordinator"。
  3) 评审剩余未处理项:EC P1-3/P1-4、errors F3/F4/F5、delete F3/F4/F5、recovery F3/F5/F6。
─────────────────────────────────────────────────────────────
Date: 2026-08-26 (h)
TaskStatus: not_completed
Task scope: 自主循环 —— 给上轮记的"chaos 间歇失败"建立频率并分类。【本轮未改产品代码】,只跑测试+记录。
  3 次运行 2 次失败,但【是两个完全不同的失败】,之前把它们混为一谈了:
  【A. 严重 — 真问题,未修】run1:split 右孩子打不开 → 200/400 key 读不到
     证据(日志留在 scratchpad/chaos_freq_1.log,seed=1787741844459):
       nemesis: FenceUnfence OK — fence+unfence node 1
       nemesis: Split OK — split part 9001
       → partition thread failed: recover_partition: failed to read extent 12 after 10 attempts:
         stale_vp_offset_past_sealed_length: extent=12 offset=8946242 length=67108864 sealed_length=5372990
       → verify: ROUTE FAILED key=... part_id=22 — no part_addr, marking partition wedged
       → per-key verify: total=400 mismatches=0 not_found=200
     part 22 是 split 出的【右孩子】。右孩子继承源分区 duplicate 出来的 meta_stream checkpoint,
     那个 checkpoint 的 vp_offset=8946242;而 split 把 log tail seal 在 commit_length=5372990。
     ⇒ replay 起点【超过】seal 长度 ⇒ 右孩子永远打不开。
     与 memory 里 project_split_underseal_vp_past_sealed 【同型但不同】:那条的根因是
     apply_reset_tail 清零 commit 导致 seal(0),已修;这次 sealed_length 非 0,是 seal 短于 checkpoint 的 vp。
     待验证的假设:FenceUnfence 之后该副本仍算 committed member 但数据落后,split 捕获 commit_length
     取 min 时被它拉低 ⇒ seal 短于已 flush 并 checkpoint 的 vp。
     【注意】按理 all-replica-ACK + flush 的 await_log_synced_to 屏障应保证 checkpoint 的 vp 在所有副本 durable,
     所以 commit_length 不该低于它 —— 这个矛盾正是下一步要查的点。
  【B. 轻微 — 疑似测试太严,非产品 bug】run3:EC marker 钉 43s,数据完好(not_found=0)
       inflight: op ... kind=7 target=0/12 still ACTIVE attempts=0 last_error=""
       nemesis 序列:EcConvert OK — ec submit extent 12 → LatencySpike en-0 (node 1) +500ms
     attempts=0 + last_error="" ⇒ dispatch 从没失败过,也没完成。EC dispatch 会【跳过】状态为
     Suspected/Fenced/Maintenance/Suspend 的 coordinator(设计如此,避免 flap 期刷日志),
     而 latency spike 正打在 node 1 上会让它 df 超时转 Suspected ⇒ 合理地不派发。
     但 verify_no_ec_markers_pinned 是"只要有 marker 就报错",不看 age、不看 coordinator 状态
     ⇒ 一个正在健康等待的 convert 也会被判失败。
Next steps:
  1) 查 A(严重):先确认矛盾点 —— checkpoint 的 vp 明明有 await_log_synced_to 屏障保证 all-replica durable,
     为什么 split 捕获的 commit_length 会低于它?看 handle_multi_modify_split 的 commit_length 捕获
     与 compute_commit_seal 的成员筛选(recovering_nodes_for_extent 是否把刚 unfence 的节点排除)。
     复现脚本:直接重跑 system_chaos --ignored,约 1/3 命中;命中标志是日志里出现 stale_vp_offset。
  2) 改 B 的断言:让 verify_no_ec_markers_pinned 带上 coordinator 状态与 age 门限,
     区分"卡住"和"合理等待一个 flapping coordinator"。
  3) 评审剩余未处理项:EC P1-3/P1-4、errors F3/F4/F5、delete F3/F4/F5、recovery F3/F5/F6。
─────────────────────────────────────────────────────────────
Date: 2026-08-26 (g)
TaskStatus: not_completed
Task scope: 自主循环 —— 清掉台账 next steps 的 #1 和 #2,并发现一个新的间歇失败。
  #1【CLOSED,非 bug】system_merge 的 split_merge_split_with_concurrent_writes 57.8% "丢失":
     它跑的是【未编排】的裸 MSG_MULTI_MODIFY_MERGE(flush→commit→drop handles→reload),
     损失窗口 = manager 耗时 × 写速率,【没有任何东西给它设界】。20% 是当初那台机器的经验值,
     忙一点的机器上就漂到 58%。
     【怎么确认的,不是推断】把 helper 指向生产路径 MSG_MERGE_PARTITIONS 后同一个测试立刻通过;
     而隔壁 orchestrated_merge_zero_loss_concurrent_writes 早就在测同场景走 freeze-drain 的【零丢失】。
     保证已经有人守了。所以把断言【方向反转】:这个测试的职责是【对照组】——断言拓扑存活 +
     无保护路径确实有损失。给有损的那一臂设上界是在测机器不是测代码。
     顺带删掉说 freeze-drain "Stage 2/3, deferred" 的过时注释(早已上线)。
     helper 没动:整体换路径会打破 merge_refuses_non_adjacent_partitions(它测裸 handler 自己的校验)。
     commit 4952c86,system_merge 9/9。
  #2【CLOSED,非 bug】chaos 里稳定出现的两类写失败,都是集群【正确拒绝】+ workload 正确记为失败:
     - "key is out of range"(42-59 次):workload 用 region_epoch: 0(跳过检查)+ 自维护路由表,
       【故意绕开】SDK 的 epoch-mismatch→refresh→retry 自愈。split 后它的表短暂陈旧,PS 拒绝错路由的 key。
       看到它反而说明 admission ordering(epoch → in_range)在工作。
     - "code=7" = CODE_UNAVAILABLE(2-8 次):merge freeze 期间 PS 停写。这是 freeze-drain 在干活;
       merge 期间【没有】它才是问题。
     已把这两条结论固化进 chaos 输出的标注(commit 3125085),免得每次重新调查一小时。
  【新发现,未查】chaos 间歇失败:4 次运行里至少 2 次 FAILED,症状 inflight_errors=2 +
     gc-reclaim 只回收 1 个 extent(正常 5-7)。与第一轮修的 EC frozen owner_epoch(4040dbe)【同症状】,
     但那条已修且这次 op ledger 是 0 active。可能还有别的路径让 EC marker 钉住。
     【不是本轮引入的】——本轮只动了测试断言和输出标注。
Next steps:
  1) 查上面那个间歇失败:inflight_errors=2 的具体内容(EC marker 钉住?),
     先多跑几次统计频率,再看 EN 日志里 WriteShard 的拒绝原因(现在 code 会带名字了)。
  2) 评审剩余未处理项:EC P1-3/P1-4、errors F3/F4/F5、delete F3/F4/F5、recovery F3/F5/F6。
  3) 可选:让 examples/dashboard 真正消费 ops history。
─────────────────────────────────────────────────────────────
Date: 2026-08-26 (f)
TaskStatus: completed
Task scope: /loop ①corrupt replica ②op 可观测性 —— 【两项全部完成】。
  ②-3【DONE:dashboard 走 RPC 向 leader 要】(commit f50d901)
     新开 MSG_OP_HISTORY(0x5E)+ OpHistoryReq/Resp,【没有】给 OpQueryReq 加 include_history 布尔:
     "现在在跑什么"(leader 本地内存 ring)和"历史上跑成什么样"(etcd)是两个问题、两个数据源,
     混一个 RPC 会让 leader-gating 和分页语义都变含糊。
     读取利用 key 布局:opLog key 定宽零填充 ⇒ prefix 扫描天然按时间有序,"最近 N 条"就是尾部切片,
     不需要按解码字段排序,也不依赖 etcd 的返回顺序。解不开的行【跳过并 warn】而不是让整个查询失败——
     历史是诊断用的,一行坏了不能连累其余。
     CLI:`autumn-op ops history [--kind K] [--since UNIX] [--limit N]`,
     渲染走【与 ops list 同一个 formatter】(提取成 render_ops)⇒ 运维看到的格式不因记录是活的还是历史的而变。
     顺带把历史写入改成【每次 drain 一个 txn 批量写】:drain 挂在 PS 心跳上,一批完成如果逐条 etcd 往返,
     就等于把 N 次串行往返压在维持 fleet liveness 的路径上——那正是最不能加延迟的地方。
     wire 指纹就地刷新(v29 未部署)。
  【chaos flaky 说明】期间遇到一次 chaos 失败(303s,平时 77-80s),但随后连跑两次 78.34s / 79.86s 均通过,
     且失败那次的现象与本轮改动无关。已改批量写降低心跳路径延迟。若再见 300s+ 的 chaos,先怀疑环境
     (机器负载 / etcd 端口)而非 op-log。
  门:rpc 60 / manager 293 / ps 207 / client 41 + system_corrupt_replica_rebuild + chaos ×2 全绿。
Next steps(本轮任务已完;以下是仍开着的旧项,按价值排序):
  1) system_merge 的 split_merge_split_with_concurrent_writes 57.7% merge-window 丢失(与本轮无关,
     领先假设=merge freeze 不停 PS 自己的 GC/timer-armed compaction)。要先复现再动刀。
  2) chaos 里稳定出现的两类写失败:59 次 "key is out of range"(split 后 SDK 路由没刷新,本该自愈)、
     8 次 "PS rejected: code=7"。都被 workload 容忍,但都不该稳定出现。
  3) 评审剩余未处理项:EC P1-3/P1-4、errors F3/F4/F5、delete F3/F4/F5、recovery F3/F5/F6。
  4) 可选:让 examples/dashboard 真正消费 ops history(加一个视图)。
─────────────────────────────────────────────────────────────
Date: 2026-08-26 (e)
TaskStatus: not_completed
Task scope: /loop ②op 可观测性【第一二层已完成;第三层未做】。
  ②-2【DONE:终态入 etcd + rotate】(commit 199ee35)
     另开 `opLog/<ts_ns>_<seq>` 前缀,【值就是 OpRecord 本身】——读回来直接解码成
     `ops status` 已经在渲染的形状,不用维护第二套 schema。
     【为什么不扩 audit】audit 答的是"谁要求做什么"(每个 admin RPC 都写、留 90 天、
     只存 result_code 0/1,error 全文在调用点就丢);op 历史答的是"这次跑成什么样",必须带原因。
     混在一个前缀里等于把两种保留策略和两类消费者搅在一起。
     【解耦手法】终态转换同步入队(queue_terminal,五个终态路径都经 finish/reconcile_outcome/
     complete_by_extent 三个函数),async 调用方排空(flush_op_log)⇒ etcd 写永远不在 borrow_mut 里,
     ledger 保持纯同步结构。排空点:PS 心跳里【outcome 循环之后】(本次心跳的终态立刻落盘,
     不必等下一拍)+ policy tick 兜底(覆盖没有 PS 上报的 recovery/ec-convert)。
     【rotate 按条数不按时间】op 量由集群活动驱动而非时钟:按时间窗两头都不准——安静一周几乎不留,
     compaction 风暴一小时写的比人翻得完的还多。按条数(OP_LOG_CAP=2000)最坏情况可预测;
     每 200 次写摊销一次扫描(rotate 要读一次 prefix,不该每写一次都做)。
     写历史是 best-effort(同 audit):因为历史写不进去就让 op 失败,等于把可观测性缺口变成故障。
     门:manager lib 293(新增 4 个单测)+ system_corrupt_replica_rebuild +
     system_recovery_loop_drives + system_chaos(--ignored)全绿。
  ②-3【TODO:dashboard 走 RPC 向 leader 要】
     dashboard 是 standalone、只 shell 到 autumn-op,拿不到 leader 的文件 ⇒ 必须走 RPC。
     建议【新开 MSG_OP_HISTORY】而不是给 OpQueryReq 加 include_history 布尔:
     "现在在跑什么"和"历史上跑成什么样"是两个问题,数据源也不同(内存 ledger vs etcd opLog),
     混进一个 RPC 会让 leader-gating 和分页语义都变含糊。
     要做:wire(OpHistoryReq{kind/since/limit} + Resp{Vec<OpRecord>})+ manager handler
     (读 opLog prefix、解码、过滤、按 key 倒序)+ dispatch 注册 + autumn-op 子命令 + 指纹刷新。
Next steps:
  1) 做 ②-3(见上)。注意 opLog 的 key 是定宽零填充的,所以 range 扫描天然按时间有序,
     倒序取最近 N 条只需从尾部截。
  2) 三层做完后可考虑:让 dashboard 真正消费它(examples/dashboard 加一个视图)。
─────────────────────────────────────────────────────────────
Date: 2026-08-26 (d)
TaskStatus: not_completed
Task scope: /loop ②op 可观测性【第一层 live 进度已完成 commit d2bfe8b;第二三层未做】。
  ②-1【DONE:live 状态带进度】
     数据本来就在(run_gc 每 chunk 有 cur vs sealed_length),只是从没离开过 partition。
     链路:PS 在自己的循环里发样本 → 搭 PartitionLoad 心跳(与终态 ring 并列)→ manager 折进 ledger →
     autumn-op ops 渲染。
     【关键设计决定】用【原始计数】不是百分比,遵循 cluster-df 既有规则(wire 传事实,消费端算比例):
     光给 "50%" 分不清"2 张表"还是"50 GB",而运维要判断等不等得起,需要量级。
     CLI 按 kind 用对单位(gc/forcegc 是字节、compact 是表数)——给字节印 "3/8" 或给表数印 "3.0 MB" 都误导。
     【progress 是样本不是累加器】,由此决定实现:每 chunk(64MiB)一把短锁而非每条记录;
     单槽而非 ring(过期样本无价值 ⇒ 丢一次心跳零代价、无需重传);update_progress 只碰 RUNNING
     (PS 每次心跳重发 outcome ring,样本可能【晚于】自己 op 的终态到达,不能重开已关闭的 op
     或复活被 cap 淘汰的);同一心跳里先应用样本再应用终态;record_maint_outcome(所有终态出口的
     唯一必经处)清槽,避免已完成的 op 永远显示在飞。
     wire 指纹就地刷新(v29 未部署)。门:rpc 60 / manager 289 / ps 207 + 4 个集成 + chaos 全绿。
  ②-2【TODO:终态日志入 etcd + rotate】
     现状缺口:ledger 是 cap256 内存环 + leader 换即失;audit log 虽在 etcd(90 天)但只存
     `result_code: 0/1`,error 全文在调用点就丢(rpc_handlers.rs 的 append_audit 处)。
     要做:终态记录(含 error 全文 + progress 终值)写 etcd,按条数/时间 rotate。
  ②-3【TODO:dashboard 走 RPC 向 leader 要】
     dashboard 是 standalone、只 shell 到 autumn-op,拿不到 leader 日志文件 ⇒ 必须走 RPC。
     要么扩 MSG_OP_QUERY,要么新增 op-log 查询 RPC(leader-gated)。
Next steps:
  1) 做 ②-2:先定 etcd key 形状(参考 mgr_audit_log/<ts_ns>_<seq> 的既有模式)与 rotate 策略,
     再决定是扩 audit entry 还是另开 opLog/ 前缀。倾向另开——audit 是"谁做了什么"的操作审计,
     op 终态是"这次跑成什么样",两者保留期与消费者都不同。
  2) 再做 ②-3。
─────────────────────────────────────────────────────────────
Date: 2026-08-26 (c)
TaskStatus: not_completed
Task scope: /loop ①corrupt replica【已修复并验证】 ②op 可观测性【未开始】。
  ①-f【前置基建:让 recovery loop 在集成测试里可驱动】(commit ed4c652)
     上一轮"loop 完全不动"的根因是两条叠加,都不是 bug:
       (主) EN 没配 manager endpoint —— handle_require_recovery 第一步就校验它,未配则拒绝,
            于是每个候选都是"看起来合格但拒绝一切"⇒ manager 报 "all recovery candidates rejected"。
            support::start_extent_node 从来没设过;silent_corruption_rot.rs 自己那份 helper
            却带 mgr 参数 —— 说明这事曾被知道但没推广。新增 start_extent_node_with_manager。
       (次) 需要非成员节点作为重建目标;RF=2 + 2 节点时人人都是成员,无处可去。
     两个测试钉住,第二个让第一个可信:有 spare 时 fence 成员会驱动重建完成;
     没有时 loop 惰性 ⇒ 今后任何"什么都没发生"的断言必须自带 spare,否则真空。
     【诊断靠的是上一轮加的 leader 日志事件】——那条 "all recovery candidates rejected"
     正是此前无处可查的原因,自己救了自己。
  ①-g【真 bug 复现 + 修复】(本轮)
     真正的缺陷不是"隔离被撤销"(那条链因 gate 不可达),而是【隔离了但没有修复路径】:
     corrupt 上报只清 avali bit、不 fence 任何东西,默认 fenced_only gate 在读 avali【之前】
     就 continue 掉该 slot ⇒ extent 永久停在 RF-1,静默、无告警、无修复,除非人工 fence。
     复现:3EN+RF2+spare,上报 corrupt 后断言该副本最终被重建 —— 30s 超时失败(非真空,
     同配置在 drives 测试里能重建)。修后 4.3s 完成。
     修法(照 extentLayout 的 sibling-key 模式,不动 rkyv 布局):
       新模块 extent_corrupt.rs + `extentCorrupt/<id>` → u32 位图 + 内存影子 + replay 挂载;
       上报时在 avali 持久化【之后】记录被熄灭的 slot(失败则返回错误让 PS 重试);
       recovery_dispatch_loop:标记过的 slot 【无视 gate】直接 dispatch_and_record;
       apply_recovery_done 清位(重建后字节已从健康 peer 拷贝);extent 删除时随 extentLayout 一起丢弃。
     门:manager lib 288、system_corrupt_replica_rebuild、system_recovery_loop_drives 2/2、
     ec_failover、re_avali_no_destroy 2/2、system_recovery_vp_seed、system_chaos(--ignored)全绿。
  ②op 可观测性:未开始。设计见 memory project_op_observability_redesign(三层:live 带进度% /
     终态入 etcd+rotate / dashboard 走 RPC 向 leader 要)。
Next steps:
  1) 做 ②。起点建议:先做"live 状态带百分比",因为数据都在手边只是没上报
     (GC 有 cur/sealed_length、compact 有 input/output tables、recovery 有已拷字节、EC 有 stripe 序号)。
  2) 然后终态入 etcd + rotate(现 audit 只存 result_code 0/1,error 全文在调用点就丢)。
  3) 最后 dashboard 侧的读取 RPC。
─────────────────────────────────────────────────────────────
Date: 2026-08-26 (b)
TaskStatus: not_completed
Task scope: /loop ①corrupt replica ②op 可观测性。本轮做 ① 的复现,结论:【原报告链路不可达,且我的第一版测试是真空的】,未修。
  ①-d【推断链缺一环,原报告部分错误】上一轮记的四环链条里,第二环不成立:
     recovery_dispatch_loop 在 `if (ex.avali & bit) == 0` 之【前】有
     `if gate_mode == FencedOnly && !is_fenced { continue; }`,而 AUTUMN_MGR_RECOVERY_GATE
     默认就是 fenced_only,corrupt 上报又不会把节点标成 Fenced ⇒ 那段 RE_AVALI 代码
     在默认配置下【根本执行不到】。改 auto_disk 再测,同样不触发。
  ①-e【真正的教训:第一版测试真空通过】按"隔离应保持"写的断言 15s 内一直绿,
     avali 稳定在 0b0010。但补上非真空探针(fence 掉 victim,断言 recovery 循环必须有动作)后
     【探针失败】——连 FENCED 节点都没触发任何重建 ⇒ 循环在该测试环境里对这个 extent 完全不动作,
     所以"隔离保持住了"这个绿是【什么都没证明】。差点把真空绿当成"没问题"结案。
     与上一轮 GC P0-2 同型:harness 不到位时,断言无意义;区别是这次非真空探针当场抓住了。
     附带观察(未追):fence 后 extent_info 的 replicates 顺序从 [3,1] 变成 [1,3],而 avali 未随之调整。
  ①【结论】按 house rule 复现不出来就不修:corrupt 隔离相关代码本轮【一行未改】,
     未跑通的测试已删除不留半成品。上一轮那个 `let _` 吞错误修复(0115b14)独立有效,保留。
  ②op 可观测性:未开始。
Next steps:
  1) 先解决前置能力:【让 recovery dispatch loop 在集成测试里真正动起来】。
     这是可复用基建(任何 recovery/re_avali/隔离相关断言都依赖它),且必须自带非真空探针。
     查清为什么 fence 掉节点后循环仍无动作(memory-mode? 扫描前置条件? 需要更久?)。
  2) 有了它再回头判定 corrupt 隔离:默认 fenced_only 下真正的问题可能不是"隔离被撤销",
     而是"隔离后【没有修复路径】"——没人会重建这个 corrupt 副本,extent 长期少一个可用副本。
  3) 然后做 ②(设计见 memory project_op_observability_redesign)。
─────────────────────────────────────────────────────────────
Date: 2026-08-26
TaskStatus: not_completed
Task scope: /loop ①修 corrupt replica 隔离失效 ②op 可观测性设计+实现。本轮只完成 ① 的定性与一处吞错误修复。
  ①-a【链路已坐实,四环全在】corrupt 隔离活不过一个 recovery tick:
     handle_report_corrupt_replica(rpc_handlers.rs:1066) 的隔离手段【只有】清 avali bit,无其它持久标记
     → recovery_dispatch_loop(recovery.rs:997)看到 `(ex.avali & bit) == 0` 就发 EXT_MSG_RE_AVALI
     → EN handle_re_avali 的判据是 `local_len >= sealed_length` 就 CODE_OK,而 bit-rot 副本【长度是满的】
     → manager `mark_extent_available` 把 bit 重新置上 ⇒ 隔离被自动撤销。
     【根因】`avali == 0` 承载了两个不同语义:"副本落后"(RE_AVALI 能修)和"副本内容损坏"
     (RE_AVALI 修不了,它只看长度)。与 memory 里 re_avali 那条"avali==0 不代表 lagging"同型。
  ①-b【已修】上面第四步的 `let _ = self.mark_extent_available(...)` 吞错误(违反 no-let-underscore 规则):
     吞掉后 bit 仍是清的而循环以为已治愈 ⇒ 下一 tick 无限重发 RE_AVALI。改为 warn + 下轮重试。
  ①-c【设计定稿,未实现】用本仓已有的 sibling-key 模式(照 extentLayout/<id>):
     新增 `extentCorrupt/<extent_id>` → u32 位图,记录哪些 slot 被报告为内容损坏。
     - handle_report_corrupt_replica:清 avali bit 的同时,在【同一个 leader-fenced txn】里写该 key
     - recovery_dispatch_loop:slot 命中 corrupt 位 ⇒ 【跳过 RE_AVALI 快捷路径】,直接 dispatch_and_record
       走 recovery 重建(EN recovery 从 peer 拉完整数据覆盖本地,这才是 bit-rot 真正需要的)
     - apply_recovery_done:重建成功后清掉该 slot 的 corrupt 位
     选 sibling key 而非给 MgrExtentInfo 加字段:后者会改 rkyv 布局使所有已存 extents/<id> 失效。
  ②op 可观测性:未开始。设计见 memory `project_op_observability_redesign`(用户 2026-08-26 定的三层)。
Next steps:
  1) 实现 ①-c,并按 house rule【先复现】:构造 sealed extent + 上报 corrupt → 断言隔离在
     N 个 recovery tick 后仍然成立(当前会在 1 tick 内失效)。killable PS harness 已可用。
  2) 再做 ②:live 状态带百分比(GC 有 cur/sealed_length、compact 有 input/output tables、
     recovery 有已拷字节、EC 有 stripe 序号 —— 数据都在,没上报)+ 终态入 etcd+rotate + dashboard 走 RPC。
─────────────────────────────────────────────────────────────
Date: 2026-08-25 (b)
TaskStatus: completed
Task scope: /loop 第 N 轮——把 fable 评审挖出的两个 P0 收口，并让 chaos 真正跑绿（commit b5a4dff、4040dbe）。
  ① compaction truncate（P0，先复现后修）：pickup_tables 的 head-extent 策略里，
     `chosen = 该 extent 的表 .take(COMPACT_N)`，但返回 truncate_id 等于向 manager 断言
     "这个 extent 已经空了"——handle_truncate 删的是 extent_ids[..pos]，即目标之前的全部。
     head extent 的表数超过 COMPACT_N 时，压掉最老的 5 张、其余仍活在这个即将被删的 extent 里：
     这些 key 不可服务，且 checkpoint 指向已删 extent 里的 SST ⇒ 分区打不开。
     复现：pickup_tables 是纯函数，8 张小 head 表 + 1 张大表 ⇒ 选 5 张却照样返回 truncate_id（断言失败）。
     修：谓词改成"chosen 覆盖 head extent 的全部表"。压缩照常进行，只是不再声称 extent 空了。
     配套第二个测试钉住 truncate 存在的理由（一次压完的 head 仍要被 truncate），杜绝"永不 truncate"式假修。
  ② EC 转换被自己的 fence 永久卡死（P0，chaos 复现）：WriteShard 上盖的 owner_epoch 在 marker 创建时
     取一次、之后每轮重发都原样复用。但该 epoch 在每次 open_partition 都会重新获取并抬高——
     marker 建好后任何一次常规 PS 重开（重启/rebalance/LockedByOther 自逐）都会把 EN 的 per-extent
     floor 抬到冻结值之上，此后每个 participant 都回 CODE_LOCKED_BY_OTHER：转换永远完不成 ⇒
     marker 永不释放 ⇒ 该 extent 的 GC 永远被拒（"has in-flight EC conversion"）＝一次普通重启换来无界空间泄漏。
     证据：coordinator 每 5s 重发一次、每次都停在 WriteShard shard 1 @ 0，在已静默的集群上把 extent 12
     的 marker 攥了整轮，force-GC 只回收到 1 个 extent。
     修：dispatch 时用 dispatch_owner_epoch_for_extent 现取；submit 侧只是用同一个 resolver 播种。
     【为什么这样仍然安全】fence 的作用是挡被 fence 掉的旧 coordinator 的幽灵写，而幽灵手里仍是它当初
     捕获的旧 epoch，照样低于 floor；抬高的只有活着的那次 dispatch。marker 的其余字段（targets/disks/
     eversion）必须继续钉死——重新推导 ASSIGNMENT 会把 layout 写到根本没有 shard 的节点上。
  ③ 这次为什么查了几个小时而不是看一行日志（两处诊断，顺手修掉）：
     - code_description 把所有没列名字的 code 一律渲染成光秃秃的 "error"，于是 CODE_LOCKED_BY_OTHER(5)
       ——一个精确、永久、可行动的判决——传到运维眼里是 "code=error"，读起来就像个正在重试的普通瞬时错误。
       现在给该 code 和 PAYLOAD_NOT_HERE 起了名字，且调用方一律把数字打在名字旁边。WIRE 指纹就地刷新（v29 未部署）。
     - owner_epoch fence 是静默 return，而紧挨着的 attempt-nonce 守卫是 WARN：于是拒绝方的日志一片空白，
       只剩 coordinator 的日志，而它只能报一个 code。现在也 warn，并带上两侧 epoch。
  门：system_chaos 现在【通过】（同一个复现它的 harness）——op ledger 0 活跃、0 个 EC marker 被钉住、
  in-flight errors 0、quiesce 后回收 5 个 extent（修前 1 个）、400 keys 0 mismatch 0 not_found。
  单测 rpc 60 / stream 110 / manager 286 / ps 207 全绿；ec_failover、ec_integration、ec_staging、
  payload_location、placement_cleanup、re_avali_no_destroy、system_compact_unflushed_vp_head、
  system_crash_mid_compact(含 --ignored)、system_split_forcegc_stale_cache、system_gc_multiversion_same_extent 全绿。
Next steps（按价值排序，都不是本轮引入的）：
  1) system_merge 的 split_merge_split_with_concurrent_writes 挂在 57.7% merge-window 丢失——
     已确认【与本轮改动无关】（改前改后同样失败、同样的 missing keys）。领先假设＝评审提的
     merge-freeze 项：freeze 并不停 PS 自己的 GC / timer-armed compaction，它们的写落在 merge 已捕获的
     commit 之后，被 seal 甩掉。要先复现再动刀。
  2) chaos 的失败原因现在看得见了，剩下两类值得追：41 次 "key is out of range"（split 后 SDK 路由没刷新，
     本该自愈）、4 次 "PS rejected: code=7"。都被 workload 容忍，但都不该稳定出现。
  3) 评审剩余未处理项：EC P1-3/P1-4、errors F3/F4/F5、delete F3/F4/F5、recovery F3/F5/F6。
─────────────────────────────────────────────────────────────
Date: 2026-08-25
TaskStatus: completed
Task scope: 文档去迁移化 + 跑 chaos + 给 chaos 加失败原因诊断（用户："如果不考虑升级，更新设计文档和
  ops.md，然后跑 chaos 测试，需要增加检查失败原因的"）。commit de90e9c。
  ① 文档：设计文档 §7 从 "Persistence, migration, and legacy" 改写成 "Persistence and wire"——
     没有历史数据就没有迁移约束；保留"sibling key 仍是更好的设计"的独立理由（marker 身份是 etcd 已知的
     事实，复制进 value 只会让两者不一致），并单独记一句【真要升级时需要什么】(0 哨兵语义)以免以后重新发现。
     crash matrix 里两行 pre-upgrade 场景删掉。ops.md：删掉 EC 相关的"升级前置条件"runbook，
     新增【EC CoW 运维视图】：一次转换的 5 个阶段各自对应哪条日志证据、卡住时看哪里、盘上应该长什么样
     （每节点一个 sealed_length/K 的 shard + .meta，coordinator 多一个 16B 的 .ec.prepared 属正常）。
     另修正一处会误导人的旧步骤：让运维去 grep "phase 2 (commit) complete"——那条日志已经不存在了。
  ② chaos 跑出真问题（而且不是 EC 的）：system_chaos 从 2026-07-18 Layer-A 命名空间强制落地(f5336db)起，
     【五周来一直在"写 0 个 key"的情况下报 all invariants OK】。因为 expected 集合为空时，逐 key 检查
     全部真空成立 ⇒ 0 mismatches / 0 not_found 和"干净跑完"完全无法区分；只有 write-liveness 探针有异议，
     而它读起来像"分区卡住"，不像"这次测试什么都没验"。
     根因：workload 写的是裸 key（b000021），Layer-A 要求 {已注册ns}/...，全部被 NamespaceUnknown 拒。
     【为什么只有它中招】：chaos workload 天然要容忍写失败（有 nemesis 在注入故障），
     而这份"正确的容忍"恰恰让 100% 失败看起来像正常的故障压力。普通测试不受影响——
     support::ps_put 重试 30 次后 panic，被拒的写会响亮失败。
  ③ 按用户要求加的"失败原因"三件套（这也正是把两小时的谜题变成一行答案的东西）：
     - workload 记录【为什么】失败并按原因汇总（原来 5 个失败点只加计数、把原因全丢了）；
     - acked==0 直接判定失败并明说"下面所有不变量都是真空的"——不能对空输入永远通过的检查不叫检查；
     - 结论先说 WHY：扫 EN 子进程日志里的 fail-loud 标记（WAL-FAILSTOP/META-FAILCLOSED/quarantine/
       stale VP/被拒的 EC 完成/superseded attempt/disk OFFLINE/supervised loop panic）。
       【标记不存在本身是更尖锐的发现】：不变量崩了而每一层都以为自己没事。
       故意排除噪声项：nemesis 下 LOCKED_BY_OTHER 正是 fencing 应有的样子，df 失败是杀节点的目的。
     修法本身只有一个常量：key 改成 mem/{b|q}{kid}（mem 是内建命名空间，无需注册），分区范围跟着改。
  门：system_chaos 现在【真的】过了——400 keys、0 mismatch、0 not_found、quiesce 后回收 2 个 extent、
  无 fail-loud 标记；system_ps_failover_chaos 2/2、chaos_inode_lease_replay_failclosed 2/2；workspace 0 error。
Next steps: 其余 chaos 脚本（scripts/*_chaos.sh 共 13 个）尚未在本轮验证；若要继续，建议先跑
  transport/manager_ha/selfheal 这几个覆盖面最大的，并注意同类"真空通过"风险——凡是容忍写失败的 harness
  都要检查它是否真的写进去了。
─────────────────────────────────────────────────────────────────
Date: 2026-08-24 (c)
TaskStatus: completed
Task scope: EC CoW 第 6 步 —— 删掉 commit phase（commit 4362b07）。用户定调：
  【现在是 active 开发阶段，相当于全新集群，不考虑任何历史数据/迁移】。所以原本 gate 住第 6 步的
  "soak + 升级前置条件"整个不成立——它保护的是不存在的数据。直接删。
  删掉：MSG_COMMIT_EC_SHARD(12，按本仓库惯例退成保留号+墓碑注释) + CommitEcShardReq/Resp、
  handle_commit_ec_shard、commit_shard_local、finish_ec_commit、ec.commit intent marker 及其
  load_extents 三态重放、以及整套 .ec.dat 机制（staging path / scanner / reconcile 的 ec-staging
  orphan 上报）。转换只写 additive 的 extent-{id}.shard{i}，manager 的 layout flip 是唯一提交点，
  上面这些永远不可能再被驱动。
  §7 那个 --refuse-legacy-ec-state 检查【也一起删了】（用户："这个什么破玩意就不要了"）——它是几小时前
  为"删除前证明全集群干净"加的，没有迁移要 gate 时纯属仪式。ops.md 的对应 runbook 一并删除。
  【ec.prepared 保留】，与设计文档 §5 表格的说法相反：nonce 没有取代它，nonce【住进了】它。
  nonce 走在请求里，磁盘上的 shard 文件本身不带 nonce ⇒ 没有这个 marker，coordinator 无法判断
  本地 staging 是不是本次 attempt 的，只能每次 re-dispatch 都重新编码。§4.3 预见到了形状
  （"命名保护的是 serving，不是 staging confirmation"）但对结论判断错了。
  测试连带：两个 suite 之前拿 WriteShard+CommitEcShard 当【fixture】造"sealed 到 eversion N 的 extent"，
  现在改成走具名的 test_seal_local——本来就更诚实，它们从来测的不是 commit 路径，只是借它的副作用。
  （踩坑：test_seal_local 只写元数据，而老 fixture 的 rename 顺带把 shard payload 变成了 .dat 内容，
  所以还要先 append 真实字节。）ec5_commit_window_tests 测的是 rename<->save_meta 崩溃窗口，
  窗口没了就一起删；ec_2pc_crash_safety 改名 ec_staging（已经没有 2PC 了）。
  净效果：-1107 / +77 行。
  门：rpc 60、stream 110 lib + ec_staging/eversion_zero_skip/append_semantics/payload_location/
  placement_cleanup/restart/delete/multi_disk、manager 279 lib + ec_failover/ec_integration/
  update_stream_ec/system_extent_recovery/offline_reconcile/re_avali_no_destroy 全绿；
  live 3EN 实证：转换成功、marker drain、.dat 全回收、8 个值在全集群无 .dat 下逐字节一致。
─────────────────────────────────────────────────────────────────
Date: 2026-08-25 (b)
TaskStatus: completed
Task scope: fable 对抗审查 5 个面(ec/recovery/gc+compact/自动删除/错误报告+自动重试) → 修 → 更新 system_chaos。
  【5 个 P0/HIGH，其中 3 个是本会话自己写的代码，2 个是长期潜伏的】：
  1. a78376a P0 apply_placements 删掉正在 staging 的 shard。placement 是 manager 在【回答时刻】算的、
     节点【稍后】才应用；这中间转换可能开始，而【participant 上没有任何本地标记】(ec_convert_inflight
     只在 coordinator 上)。于是过期的"payload 在 .dat"判决删掉了 coordinator 正在写的 shard；下一个
     stripe 发现文件没了就重建并按自己的 offset pwrite ⇒ 前面 stripe 的位置全是【零洞】。flip 照样发布
     (报告来自同一 attempt，nonce/reporter 全过) ⇒ 读到零 + CODE_OK。三重修：op-lock 串行化、
     拒绝"说 InDat 但本地已 staging"的矛盾判决、offset>0 而文件消失时【响亮失败】而不是静默重建。
     (我第一版守卫用"本节点是否曾 staging"做判据 → 会永远跳过清理、.dat 永不回收；靠"真 staging 而非
      摆文件"的测试抓到。)
  2. a78376a HIGH marker 有五种死法，只有两个成功钩子会关 ledger。EC 既不在 TTL 扫描内，
     submit 又对任何 ACTIVE 目标 attach-dedup 且【attach 后直接返回不 actuate】⇒ 一条泄漏的 RUNNING
     记录让该 extent 之后所有 force-ec-convert 变成静默 no-op，除非重启 leader。改成在【唯一的 drain 漏斗】
     里关闭，覆盖现在和将来所有路径；顺带把 3 处 `let _ =` 换成真处理。
  3. 12ec612 HIGH apply_recovery_done 不校验完成报告是否属于活着的 marker。释放路径的注释明说
     "如果执行者还在跑、之后完成了，它的完成会被拒绝"——【这个拒绝根本不存在】。df 抖一下就会让节点
     Suspected→marker 释放；若此时 extent 被 EC 转换，老执行者迟到的【转换前完整副本】报告会把槽位
     换到持有 .dat 的节点上、而 layout 说的是 shard 文件，被换掉的节点变成非成员后【真正的 shard 被
     reconcile 回收】。这是 EC nonce 那个洞的 recovery 孪生体，用同样方式关闭。
     注意顺序：门要放在 EC-inflight defer【之后】(ledger 每 extent 只允许一个 op，所以转换期间到达的
     recovery 完成永远没有 marker，放前面会吞掉更具体的判决)。
  4. be1bb9b P0(GC) 【任何活过两代 GC 的大值会被静默丢失】。GC 自己搬迁写的 WAL 记录 op=1 不带
     OP_VALUE_POINTER(同一次搬迁的 memtable entry 却带)，recovery 靠长度兜底照样能重建，
     但【GC 自己的扫描没有兜底】⇒ 该记录在它刚被搬进去的那个 extent 下一次 GC 时被当成 chunk blob 跳过、
     不搬迁、然后连同活着的 VP 一起 punch。现有 GC 测试全是单代，看不见。
     真正的缺陷是【扫描和重放对"什么是 VP 记录"的判据不一致】——改成两边共用同一个谓词。
  5. 85e426b P0(EC) 一个"rename 后崩溃"的残留分支能【什么都没 stage 就报告转换完成】。CoW 下 rename
     根本不存在，但它的【触发条件】还在：seal-over-reachable 合法地封在落后成员长度之上，于是一个落后的
     coordinator 只要 local_len 恰好等于 ceil(S/K) 就跳过整个 prepare 并返回 Ok。
     nonce/reporter 校验的是【这次 attempt】、不是【干了什么活】，所以报告是真的、flip 提交到一个
     没有任何节点持有 shard 的 layout ⇒ 永久不可读、0/K 无法重建、没有反向转换工具。
  6. 8f2c83a P0(EC/recovery) 多 shard EN 上完成报告【被困在兄弟 shard 里】。manager 每个 node 只发一次 df
     (故意的：handle_df 会 take 队列，第二个调用者会 drain-and-discard)且拨的是 shard 0 的控制口，
     而队列是 per-ExtentNode 的、由【拥有该 extent 的 shard】写入 ⇒ 生产形态(shard 数=核数)下
     (N-1)/N 的 extent 转换永远无法提交(marker 永久钉住、GC 被阻)、重建好的副本永远不被应用。
     修法是【共享队列】而不是加第二个 df 调用者，形状照抄 shared_disk_health；
     但必须【按 node(data dirs) 而不是按进程】键控——测试套件在一个进程里跑多个逻辑 EN，
     进程级共享会让一个 node 的 df 抽走另一个的完成报告，而 manager 会拒绝非 coordinator 上报的 ec_done，
     恰好会让"证明转换能提交"的多 EN 测试失败。两个方向都断言了。
  system_chaos 更新：①加"静默后不得有任何在飞"检查(ledger active + EC marker)——这个类别数据检查
  【结构上看不见】：泄漏的 RUNNING 不丢数据但让后续转换变静默 no-op，钉住的 marker 无限期阻塞该 extent 的 GC。
  ②检查同时报告 ledger【总记录数】，因为"0 active"在 ledger 从没被用过时是真空成立的——这立刻发现
  nemesis 全程只留 1 条记录(它直接调 handler，不走 submit)。③把 EC nemesis 改走 MSG_OP_SUBMIT
  (即 autumn-op 的路径)⇒ 每轮 5 条记录且都到达终态。
  门：stream 110 lib + payload_location 9/placement_cleanup 8/ec_staging 3/restart/multi_disk/delete_race、
  manager 283 lib + ec_failover/ec_integration/system_extent_recovery/silent_corruption_rot/
  re_avali_no_destroy/offline_reconcile/system_gc_*(含 --ignored)、partition-server 205 lib、rpc 60；
  system_chaos 真过(400 keys/0 mismatch/8 extent 回收/0 在飞)；live 3EN EC smoke PASS。
  每个修复的守卫都做了【非空验证】(禁用后精确地只有对应测试失败)。
  坑：改 wire schema 后 system_chaos 会在 setup 就挂——它 shell 出去调【磁盘上的】autumn-op/
  autumn-extent-node 二进制，必须先 cargo build -p autumn-server(同 cluster.sh 那个老坑)。
Next steps: 审查里剩下的 P1/P2/P3 尚未处理，按价值排：
  ① EC P1-3 已 flip 的 shard 文件可被"被释放的僵尸 coordinator"截断/覆写(nonce floor 在 placement 落地时
     被清掉、重启也会清)——建议：layout 已是 InShardFile 时拒绝 nonce 不匹配的 WriteShard。
  ② EC P1-4 coordinator 自己的本地 staging【不登记 ec_stage_nonce】⇒ 修复①的 coordinator 形状漏洞仍在。
  ③ GC P0-2/P0-3(评审给的是 P0，我尚未独立验证)：relocation 跨 rotation 的 seq 顺序问题；
     pickup_tables 的 take(COMPACT_N) 可能让 truncate 删掉仍被引用的 SST。【下一轮优先验证这两个】。
  ④ errors F3/F4/F5、delete F3/F4/F5、recovery F3/F5/F6。
─────────────────────────────────────────────────────────────────
Date: 2026-08-24 (b)
TaskStatus: superseded (§7 的检查已于同日随第 6 步一并删除，见上)
Task scope: §7 升级前置检查（第 6 步的解锁手段）。commit b66af8d。
  本 build 已经不会再产生 extent-{id}.ec.dat / .ec.commit（转换只写 .shard{i}，manager 的 layout flip
  是唯一提交点）。所以这两个文件只可能来自跑老二进制的节点，而它们正是【保留的 commit-phase 修复代码】
  存在的理由。删那批代码的前提 = 全集群没有任何节点还持有这种状态，而在此之前唯一的验证手段是
  登录每台机器去 find。
  实现：EN 启动时扫描（放在 load_extents 【之后】，这样 marker replay 能解决的已经解决、解决不了的已
  quarantine，剩下的才是真·未决残骸），ERROR 日志列出 extent id。
  【默认只报告不拒绝，这是刻意的】：保留的修复路径今天能正确处理这些状态，默认拒启动会打断一个本来
  能work的升级。--refuse-legacy-ec-state 才变成拒绝 —— 这才是"建立前置条件"的手段：全集群带 flag 起一遍，
  全起来了，第 6 步就从"赌"变成"查过了"。
  最关键的测试是【反向】那个：只有当前格式文件的节点（含 .ec.prepared —— 它是当前方案的，不是 legacy）
  必须在 flag 下【干净启动】。误报和它要找的状态无法区分，而且会卡住它本来要解锁的那次升级。
  顺带删掉被重构孤立的 parse_ec_staging_extent_id。
  门：stream 115 lib + legacy_ec_state 4 + placement_cleanup/payload_location/ec_2pc/extent_restart/
  multi_disk、manager 279 lib + ec_failover；clippy 19 不变（注意：git stash 不带 -u 会漏掉未跟踪的
  测试文件，导致基线编译不同、warning 计数假性暴涨，比对时要 stash -u）。
  ops.md 写了操作步骤。
─────────────────────────────────────────────────────────────────
Date: 2026-08-24
TaskStatus: completed
Task scope: v29 EC CoW 跨机验证（用户选 A）。脚本入库 scripts/ec_crosshost_verify.sh。
  动机：v29 攒了 5 步改动(nonce/payload location/shard-holder/方案切换/file-granular reconcile)，
  全部只在单机 loopback 验过；本项目反复吃过 loopback 藏 bug 的亏(SO_RCVBUF 窗口冻结 6× 写吞吐，单机看不见)。
  形状：manager + PS + EN0 在 ::14，EN1/EN2 在 ::15，EC 2+1 ⇒ 分片跨网扇出、3 个持有者里 2 个在远端。
  实测这轮的 coordinator 落在【远端】en1，它把一个 shard 推回 ::14——真正的跨机路径。
  结果 PASS：
    转换成功 → 8×64KiB 跨机读回逐字节一致 → 重启全部 EN 触发 reconcile →
    两台机上 pre-conversion .dat 【全部回收】(日志实证 "reclaimed the pre-conversion .dat;
    this node now serves its shard" 在 ::14 和 ::15 都出现) → 再读一遍仍逐字节一致、全集群无 .dat。
  ── 三个坑都是【测试脚本自己的】，产品侧零改动 ──
   1. EN/PS 的 --listen 默认 0.0.0.0（IPv4 通配），却 advertise IPv6 地址 ⇒ manager 拨 v6 被拒、
      节点全 Suspected。两端都要显式 --listen <v6>。（顺带：list-nodes 在 df 从没成功过时
      HB_AGO/SUSP_AGE 显示成几百秒的假值，看着像"陈旧状态"，实际是没有基线——排查时别被带偏。）
   2. 远端 data dir 残留上一轮的 cluster_id ⇒ format 拒绝加入不同集群。这是 cluster_id 守卫【正确工作】，
      不是 bug；脚本每轮要先 rm -rf 远端 dir。
   3. 重启 EN 那步我写了 kill "${PIDS[-1]}" 且注释成 "PS stays"——PIDS[-1] 恰恰【就是 PS】。
      于是 8 个读全挂在 "connect PS ... failed"，看起来像"清理后跨机读坏了"。
      教训：读失败先看是不是【服务端还活着】，再怀疑数据面；把 stderr 丢进 /dev/null 会让这种事多花两轮。
  另：管道 `| tail -N` 会缓冲到进程结束，后台跑长脚本时没法增量观察——直接重定向到文件。
     pkill -f 会匹配到自己的命令行(exit 144)，用 pgrep+逐个 kill。
Next steps: 跨机已过 ⇒ 第 6 步的前置从"没跑过真集群"降级为"没部署过"。真要做仍按 §7：
  要么确认全集群无老格式状态，要么加 EN 启动 fail-loud 前置检查(推荐，便宜且把"赌"变成"检查")。
  auto-policy 的 ec arm 现在功能上具备条件(空间窗口已关、跨机验过)，但从未在自动驱动下跑过，要 arm 建议先小范围观察。
─────────────────────────────────────────────────────────────────
Date: 2026-08-22 (b)
TaskStatus: completed
Task scope: §9 handle_re_avali truncate-in-place 审计（设计文档最后一个未做项）。commit 28520d7。
  设计文档问的是"re_avali 的目标会不会是 manager 仍然计数的副本(avali 位还置着)"。字面答案是【不会】：
  唯一的下发点(recovery.rs)只在 `(ex.avali & bit) == 0` 时才发 RE_AVALI。
  但【这个问题问错了】——avali==0 不等于"落后"：manager CLAUDE.md 的 seal-over-reachable 写着
  "封存时不可达的成员，它的 avali 位就是不置"。也就是说一个仅仅【当时不可达】的成员会带着 avali=0，
  而它手上可能是全集群【最长】的那份副本；而 stream_extent_from_sources 选源时遍历成员列表，
  【根本不看 avali】——所以这份文件恰恰就是别的节点做 recovery 时会去拉的源。
  不被计数让它【更】暴露而不是更安全：没有任何人在看着它。
  先复现（house rule）：crates/manager/tests/re_avali_no_destroy.rs —— extent 封在高于目标持有量的长度、
  唯一的 peer 被杀 → 修复不可能成功 → 实测把 4096 字节干成了 0。
  修：re_avali 改走 peer_copy_full_extent_to_dat（先写 temp、拿到完整 sealed_length 才 atomic rename），
  修不成就是 no-op。它【没有】reconcile-down 是这条路径上的有意为之：拿更短的 peer 副本盖掉更长的本地副本，
  正是 re_avali 绝不能做的交易。recovery 继续用 stream_extent_from_sources——它的目标要么是全新的、
  要么是被证明不完整的残骸，没有东西可失去。【选哪个 helper 的判据是"目标有没有东西可失去"，不是 avali 位】。
  两个测试互相独立验证过：把修复还原成旧代码 → destroy 测试挂、repair 测试仍过（所以"只会拒绝"的假修复
  不会被误认成修复）。repair 测试也是 re_avali 有史以来第一份覆盖。
  门：stream 115 lib、manager 279 lib + re_avali_no_destroy 2 + ec_failover/ec_integration/offline_reconcile/
  system_extent_recovery/system_recovery_vp_seed/silent_corruption_rot、stream payload_location/
  placement_cleanup/ec_2pc/extent_restart 全绿。
Next steps: EC CoW 只剩第 6 步，且【被设计本身 gate 住】：删 commit phase / ec.commit / ec.prepared 要等
  "soak + 一个 release 边界"。它删的是【升级时崩在 rename 中途】的节点的修复代码，现在删等于赌没有那种状态。
  真要做时按 §7：要么确认全集群无活的老格式状态(无 .ec.dat/.ec.commit/无老 marker)，要么加 EN 启动 fail-loud 前置检查。
  ec.prepared 另有前提：nonce 完全接管它的角色之后才能删。
─────────────────────────────────────────────────────────────────
Date: 2026-08-22
TaskStatus: completed
Task scope: EC CoW 第 5 步——file-granular reconcile + cleanup（用户："start"）。commit 2d4a5fd。
  reconcile 的回答从 extent 粒度改成【文件粒度】：garbage（不是成员→全删）+ placements（每个 extent 带
  payload_location + 这个节点自己的 slot）。节点据此推导出"我该持有的那【一个】payload 文件"，其余全是残骸。
  转换后清 .dat、被放弃 attempt 的 shard 回滚、非成员垃圾——三件事一条规则搞定，不需要第二套机制，
  也不需要 intent marker（清理中途崩溃靠启动 discovery 重新推导磁盘现状收敛）。第 4 步开的空间窗口就此关闭
  （现在由 sweep 周期 5min 界定，EN 重启则立即）。没有单独写 driver：reconcile 一旦会发 placement，它本身就是 driver。
  删 payload 文件是破坏性的，三道门：①要留的那个文件必须【已经在本地】（.dat 只在确实持有被点名的 shard 之后
  才删——否则 placement 早于 staging 到达就会删掉本节点唯一的副本）②该 extent 无在飞 op ③只由 manager 决定。
  manager 侧对【任何有在飞 ledger op 的 extent 一律不给判决】——participant 正在 stage 的 shard 是当前 layout
  没点名的文件，而节点侧的 ec_convert_inflight 只看得见本节点自己 coordinate 的转换。
  ── live 测试删掉了一个活的 extent（比我在测的东西更严重，且根因不在第 5 步）──
  EN 不知道自己的 node_id，一直报 0。R4 的成员判据下，报 0 = 不是任何 extent 的成员 = 整块盘都是垃圾；
  又因为宽限计数器 key 是 (node, extent)，三个节点全报 0 ⇒ 共用同一个计数器 ⇒ 每人报一次就把三轮宽限烧完，
  第三个来问的节点被告知删除一个已转换的 extent，它就删了。R4 之前判据是"manager 还认不认识这个 extent"，
  【根本不依赖上报者】，所以 node_id=0 当时确实无害——发送侧那行注释"manager 只拿它记日志"写下时是真的。
  修：请求带上 EN 真正拥有的 node_uuid，manager 解析 node_id→uuid，解析不出来就【什么判决都不给】(fail closed)。
  三个 R4 单测当初伪造了一个 manager 从不知道的 node_id——正是这个虚构掩盖了 bug；现在它们先注册节点，
  另加两个新测钉死 fail-closed 与 uuid 解析。教训写进设计文档：判据从"对象的属性"变成"对象与提问者的关系"时，
  所有标识提问者的输入都变成 load-bearing，包括此前被注释为"仅诊断用"的那些。
  顺带修掉第 3 步的一个重复计数：shard-only entry 把 shard 字节同时放进 len 和 shard_files，
  导致每个已清理的 extent 在 df 里翻倍（len 语义 = .dat 长度，没有 .dat 就是 0）。
  门：rpc 60、stream 115 lib + placement_cleanup 6（新）+ payload_location 8 + 其余全绿、
  manager 279 lib + ec_failover/ec_integration/offline_reconcile/system_extent_recovery/node_lifecycle/
  silent_corruption_rot/e2e_lifecycle；clippy 数不变(60)；破坏性守卫都做了非空验证。
  live 3EN 实证：转换后重启 EN → 三个节点各剩【一个】262316B 的 shard，【所有 pre-conversion .dat 全部回收】，
  8 个 64KiB 值在【全集群没有任何 .dat】的情况下仍逐个字节一致。
Next steps: 第 6 步（soak + 一个 release 边界后）：删 commit phase(MSG_COMMIT_EC_SHARD/commit_shard_local/
  finish_ec_commit)、ec.commit marker 及其 load_extents 三态重放；ec.prepared 只有在 nonce 完全接管它的角色
  之后才能删。注意 §7：这些是【升级中途崩在 rename 里】的节点的修复代码，删之前要么确认没有活的老格式状态，
  要么加 EN 启动 fail-loud 前置检查。§9 handle_re_avali truncate-in-place 审计仍未做。
  可选：cleanup backlog 的 admission gate（§8）——目前窗口已由 sweep 界定，backlog 堆积仅在 reconcile 卡住时发生。
─────────────────────────────────────────────────────────────────
Date: 2026-08-20
TaskStatus: completed
Task scope: EC CoW 第 4 步——方案切换（用户："go"）。§10 里唯一"两套方案同时在线"的那个 commit（0e8f4b6）。
  转换从此【只加文件，不替换文件】：WriteShard 写 extent-{id}.shard{i} 就结束，不 rename、无 per-node
  commit；manager 的 layout flip 是唯一提交点，一个 CAS 过的 txn 里同时落 membership/eversion/
  payload_location=InShardFile。
  它消掉的是"没人能分类的中间态"：per-node rename 会让集群停在"有的改名了有的没有"，coordinator 死在
  里面时没有任何一方有权决定 roll-forward 还是 roll-back——这正是 marker 卡死无法释放的原因，也是
  "放弃一次 attempt 可能毁掉最后的完整副本"的原因。现在放弃 = 删掉几个没有任何 reader 指向的文件，
  所以 EC marker 的 coordinator 没了就能像 recovery marker 一样释放，后继者可以挑完全不同的分配。
  三个只有代码（和一个集成测试）才知道的点：
   1. EC shard recovery 把重建出来的 shard 写进了 .dat。在"shard 就是 .dat"的年代是对的；在本设计下
      会让该节点把 shard 字节喂给任何还在要整值的读者，同时 layout 指向的那个 shard 文件根本不存在。
      改成写 layout 点名的那个文件。
   2. read_plan 用 extent 的长度而不是【被读文件】的长度做界。shard 是 sealed_length/K，而旁边等着被
      清理的 .dat 还装着整个 extent ⇒ 一个 to-end shard 读会要到好几倍于 shard 的长度。界改成按文件取。
   3. "跑不动了" ≠ "不是 Online"。给 EC 打开 marker 释放时用 is_online() 做判据，会把 coordinator 还处于
      Suspend（新注册节点在第一次 df 之前的状态）的 attempt 每个 tick 放弃一次 ⇒ 任何跨过一个 tick 的
      转换永远完不成。判据改成"已不在集群 或 Suspected"。是 ec_failover 抓到的，单测抓不到。
  连带：WriteShard + CommitEcShard 不再能组合（prepare 已经不写 .ec.dat 了），所以那些拿这对 RPC 当
  【fixture】制造"EC 转换到 eversion N"的测试改成直接摆一个 .ec.dat ——那正是保留的修复代码存在的场景，
  覆盖比原来更真实。retained repair code（handle_commit_ec_shard/commit_shard_local/finish_ec_commit/
  ec.commit replay）全部保留，只是不再被驱动；本 build 不再有任何路径会创建 .ec.dat。
  门：stream 115 lib + 11 个 test 二进制、manager 277 lib + ec_failover/ec_integration/update_stream_ec/
  system_extent_recovery/silent_corruption_rot/offline_reconcile、rpc 60。
  live 3EN 实证：转换成功、marker drain、【8 个 64KiB 值 flip 之后逐个读回字节一致】(RS 解码，全栈)，
  磁盘上 coordinator 仍持有【未被触碰的】524632B .dat + 262316B shard0（= /K）——CoW 不变量成立。
  运维：force-ec-convert 现在每次都打 WARN——cleanup driver 未落地前，转换后两种形态会同时占盘且
  【没有任何东西回收】。§8 要求 policy 驱动的 EC 保持关闭直到第 5 步；auto-policy 默认 Off，但
  balanced/aggressive 预设里含 ec，谁要 arm 需自行知晓这个空间窗口。
Next steps: 第 5 步 file-granular reconcile + cleanup driver（reconcile 现在只能表达"这些 extent 你不该
  有"，表达不了"留着 extent、删掉 .dat"；member 节点的清理还要 repoint entry + persist meta，只有
  非成员才是纯删）。之后第 6 步：soak + 一个 release 边界后删 commit phase / ec.commit / ec.prepared
  （ec.prepared 只有在 nonce 完全接管它的角色后才能删）。§9 handle_re_avali truncate-in-place 审计仍未做。
─────────────────────────────────────────────────────────────────
Date: 2026-08-19 (c)
TaskStatus: completed
Task scope: EC CoW 第 2、3 步（用户："on disk layout 可以改，continue"）。§10 六步里的 2 和 3，都是
  可单独上线、可回滚的 inert 步；第 4 步（两套方案同时在线的那一个 commit）未开工。
  第 2 步 = 读请求【点名】要读哪个 payload 文件（575f352）。
    动机：CoW 下 shard 是加在 .dat 旁边的新文件，两种形态同时存在，必须有人说哪个是权威的；
    推断没有安全答案——把 .dat 喂给想读 shard 的请求 = 返回整值的头部，调用方还会拿去 RS 解码。
    ReadBytesReq 加 (payload_location, shard_index)（32→40B，32B 老请求仍解成 InDat）；EN 服务被点名的
    那个文件，否则答 CODE_PAYLOAD_NOT_HERE，绝不回落到另一个。位置存 sibling key extentLayout/<id>
    （absent=InDat），不进 MgrExtentInfo：那是持久化的 extents/<id>，加字段会让老集群 replay 校验失败
    →当不上 leader；absent=InDat 正好就是所有存量 extent 的真实形态（老方案把 shard 改名盖到 .dat）。
    两个只有读代码才看得出、各自都能单独读错文件的点：
      (a) EN 的读批处理只按 extent_id 聚合，一个 batch 解析【一个】fd 服务所有 slot ⇒ 点名不同文件的
          两个请求会被同一个文件应答。聚合键必须扩成文件身份（两处 grouping 都改）。
      (b) InDat 必须【不论 slot 都是同一个身份】：复制读带着自己的 slot，若 slot 混进文件身份，
          同一 extent 从不同 slot 的读会看起来是不同文件而丢失批处理。PayloadRef::for_extent 对 InDat
          把 index 归零。
    EC shard recovery 现在【按名字】问每个 peer 要它自己的 shard——原来是问"这个 extent"，靠该 peer 的
    .dat 恰好就是它的 shard（只有老方案改名才成立）。read_plan 的 None 改成具名 ReadRefusal（两种拒绝
    对应两个码，各自自愈方式不同）。SDK direct 路径不用改：extent_read_descriptor 本来就拒 ec_converted，
    而 InShardFile 只由 flip 发布，flip 必设 ec_converted——现在把这条不变量显式检查而不是默默依赖。
  第 3 步 = EN 作为 shard holder 的生命周期（daea6a4）。
    EN 的 extent 生命周期整个钉在 .dat 上：没人扫的文件会躲过所有清理、然后在下次重启时从系统里消失；
    而"只有 shard 的 extent"在 manager 眼里像不存在——这正是重建副本变成 blocking orphan 的路径。
    所以先让 discovery/删除/计账认识这个形态，再让任何东西去写它。
    entry 记录真实持有：has_dat + shard_files(index→length)。同时两个 index 是合法瞬态（两次 attempt，
    或重新分配后同时占 parity 槽和 data 槽）；权威由 extent 发布的 layout 定，不由这张表定。长度放表里
    而不是另设计数器，避免"有哪些文件"和"占多少字节"漂移。
    shard-only entry 不带 fd 且【绝不能创建 .dat】（extent_file 开文件不带 create 就是为这个）；它的 len
    是 shard 长度，不是 extent 的 sealed_length；真 payload 文件旁边没有可解析的 .meta = META-FAILCLOSED
    →quarantine，不允许 fail-open 到 owner_epoch 0。df 的 extent_bytes 要加 shard_bytes（转换中的节点
    两者都持有，只算一个会低报 cluster-df 和 --min-alloc-free-bytes 门限读的那个数）。shard fd 不进 FdLru
    （那个 cache 按"每 extent 一个 fd"计账）。reconcile 不用改就能看见 shard-only extent（它上报
    self.extents，discovery 已把它们放进去）；还表达不了"留着 extent、删掉 .dat"——那是第 5 步。
  门：rpc 60、stream 115 lib + payload_location 7 个新测（手工摆放文件，producer 之前就可测）+
    ec_2pc/restart/delete/delete-race/multi-disk/cluster-df 全绿；manager 277 lib +
    ec_failover/ec_integration/system_extent_recovery/silent_corruption_rot；clippy 警告数前后不变
    （stream 20、manager 49）；每个新行为都做了非空验证（禁用该行为后精确地只有对应测试失败）。
    live ec_smoke.sh 两步各跑一次都 PASS。
    线材 v29 就地刷新两次（仍未部署）→ 最终 92b10b22701423fa。坑复记：指纹哈希整个 schema 文件，
    【给 extent_rpc.rs 加测试也会变指纹】——所有 schema 改动做完再刷新一次，别中途刷。
Next steps: 第 4 步（唯一不可回滚的那个 commit，两套方案同时在线）：staging 改写 .shard{i}（§5 列的守卫
  一个都不能丢：per-extent op lock、post-lock owner_epoch 复读、stripe-0 truncate、corrupt_meta 拒绝、
  mark_disk_error 接线）、不再发 commit phase、apply_ec_conversion_done 写 InShardFile + value-CAS(§4.5)、
  EC 打开"coordinator 离线即释放 marker"。然后第 5 步 file-granular reconcile + cleanup driver（在它落地前
  空间窗口无界，policy 驱动的 EC 要保持关闭），第 6 步 soak 后删 commit phase / ec.commit / ec.prepared。
  另：§9 handle_re_avali truncate-in-place 审计仍未做。
─────────────────────────────────────────────────────────────────
Date: 2026-08-19 (b)
TaskStatus: completed
Task scope: EC CoW 改造第 1 步——per-attempt nonce（用户："其他的呢？"= 接着做 recovery 之外的部分）。
  docs/ec_cow_conversion_design.md §10 六步里的第 1 步，它"独立修掉今天的暴露面"，且后面每步都继承它。
  真洞：ec_done 今天只验两件事——上报者是 marker 的 coordinator + new_eversion 对得上。两者都分不开
  "两次 attempt"：被放弃的 attempt 从没 bump 过 extent，所以重发的 attempt 拿到同一个 new_eversion，
  而重发很可能挑中同一个 coordinator。于是上一轮的迟到 report 两关全过 → 把 layout 翻到这一轮那批
  "什么都没 stage" 的目标上 → 随后的 cleanup 删掉最后的完整副本 = 不可恢复丢数据。
  实现（63c258e）：nonce = 创建 marker 那个 etcd txn 的 revision（txn_fenced_revision 从 txn 自身响应取，
  不能事后 GET——并发同键写会让两次创建拿到同一个身份），promotion 时从 key 的 mod_revision 重建。
  故意【不】加进 MgrEcDispatchInflight：它以 Option 嵌在持久化的 MgrExtentInflightRecord 里，加字段会移动
  archived 布局 → 所有活 marker（含 recovery/delete）replay 校验失败 → 升级即当不上 leader。etcd 本来就存
  mod_revision，所以连 sibling key 都不用（设计文档原本的备选）。
  revision 还自带【单调】，换来随机 nonce 给不了的守卫：participant 拒收 nonce 低于当前正在 stage 的那轮的
  stripe。owner_epoch fence 盖不住这个——它只在 ex-coord 被 FENCE 时才抬升，而"仅仅被释放"的 coord（离线/
  重新派生的常规路径）epoch 不变，会把 stripe 混进后继者的 staging 文件。该守卫是内存态（仲裁两个活写者），
  重启后回落到 stripe-0 truncate + epoch fence。
  线材 v29 就地刷新（仍未部署）fp a6979f12→d0b7673fa49fa121：ConvertToEcReq/ExtConvertToEcReq/EcConvertDone
  加 attempt_nonce，WriteShardReq 二进制头 44→52。0 = 无身份（旧 marker/旧 peer）：0 只能配 0，升级能收敛，
  但 0 的 report 永远完成不了有身份的 attempt。
  顺带可读性：apply 侧三个检查收进纯函数 classify_ec_done（三者互不冗余，各自都不足）；coordinator 的
  prepare-skip 现在按 attempt 判定（原注释早就说它需要这个，只是当时只有 new_eversion 可用）。
  门：stream 115 lib + ec_2pc 6（新增 superseded-stripe 拒收）、manager 274 lib（新增 6 个 classify_ec_done
  + 1 个"重发 attempt 拿到新 nonce"）、ec_failover/ec_integration/update_stream_ec/apply_done_atomicity/
  system_extent_recovery/offline_reconcile/node_lifecycle 全绿；clippy 警告数前后都是 49（未新增）。
  两个新测都做了非空验证（把守卫改成 if false 后精确地只有对应测试失败）。
  live e2e ec_smoke.sh（3EN RF1 + log-ec 2+1）PASS：force-ec-convert --wait → op succeeded、marker 已 drain。
Next steps: §10 还剩 2~6 步未做（2 持久化+wire inert / 3 EN shard-holder inert / 4 scheme switch 单 commit /
  5 file-granular reconcile + cleanup driver / 6 soak 后删 commit phase），以及 §9 的 handle_re_avali
  truncate-in-place 审计。第 4 步是唯一两套方案同时在线的 commit，前面三步都是可单独上线、可回滚的。
─────────────────────────────────────────────────────────────────
Date: 2026-08-19
TaskStatus: completed
Task scope: recovery 侧按 marker 模型逐步落地（用户："/loop 先从 recovery 开始，逐步实现"）。
  设计=docs/ec_cow_conversion_design.md 定的统一模型：marker 是【常驻指令】——leader 反复重发、
  无 TTL、按执行者存活释放、执行者必须幂等；残骸按【成员判据】回收。4 步各自编译+测试绿+推 main。
  R1(8f1a0fd) EN 幂等化：try_adopt_completed_recovery 返回三态 LocalCopyVerdict（Complete 收养并
     补报 done / Incomplete 丢弃重建 / Unknown 拒绝重试）。真 bug=崩在半路的 recovery 留下部分 .dat，
     重载成普通 open extent，旧代码永久回"already exists"→该 (node,extent) 永久毒化，而 orphan
     reconcile 也看不见它（extent 本身活着）。三态是关键：Incomplete 的动作是破坏性的，"看不出来"
     绝不能塌进"不完整"（manager 不可达 / EC / 未封 / quarantine 一律 Unknown）。
  R2(96df88a) marker=常驻指令：EN 对"正在跑同一个 recovery"改答 CODE_OK（幂等接受，与
     handle_convert_to_ec 同契约；答 PRECONDITION 会让 leader 把健康在飞任务的 marker 抽掉另找目标）；
     manager 加 redispatch_pinned_recovery（5s 超时、非 Online 直接跳过免得给尸体发心跳吃掉整轮、
     恒返回 Ok、绝不因 RPC 失败 drain marker）。EN 重启即自愈：它丢了内存态，下一次重发再跑一遍。
  R3(1964eaf) 释放改事件驱动 + 删 TTL：release_recovery_markers_for_dead_executors（level-triggered，
     每轮扫 pinned 执行者是否还在 s.nodes 且 Online）；删掉 sweep_stale_inflight_once 的 Recovery 分支
     与 recovery_stale_threshold_secs。理由=超时与"慢但在推进"不可区分，按时间释放会和还在写的执行者
     对撞。现在只剩 Delete 走 wall-clock。
  R4(e563472) 残骸按成员判据回收：handle_reconcile_extents 的 garbage 从"manager 已遗忘的 extent"
     改成"你不在 replicates++parity 里的 extent"（前者永远看不到崩溃残骸——extent 还活着）；
     加 NON_MEMBER_ROUNDS_BEFORE_GC=3 宽限（成员视图在 apply_recovery_done 换槽/leader 刚稳定时会短暂
     不准，误删真数据远比多留几分钟残骸严重；计数器 leader-local 且按上报裁剪，换 leader 只会推迟删除）
     + extent_inflight 在飞守卫（recovery 目标按定义就是非成员，正在造那份将使它成为成员的副本；
     EN 侧 reconcile unlink 同样跳过 recovery_inflight/ec_convert_inflight，与 handle_delete_extent 对齐）。
  门：stream lib 115、manager lib 267、5 个 recovery 集成测（含 2 个新增：不完整副本被丢弃重建而非
     永久拒绝、重复下发是幂等接受）。live e2e scratchpad/recovery_smoke.sh（4EN RF3，kill+fence 一台）
     PASS：3 个 recovery 在 ledger 里 running→succeeded，attempts=1。
  过程教训：e2e 一度报 PARTIAL 而它自己打印的 ops list 明明全 succeeded —— 是断言缺陷不是产品问题：
     "所有 recovery op 都 succeeded"在活集群永远收敛不了（被 fence 的节点持续产生新 recovery，
     日志里 rolled open tail(s) off fenced node(s)）。改成正确的静默条件：无 active + 至少一个 succeeded。
  文档：crates/stream/CLAUDE.md（require_recovery 三态幂等 + orphan reconcile 成员判据/宽限/在飞守卫）、
     crates/manager/CLAUDE.md（Recovery 段加 marker 模型与两个释放点、stale sweep 段订正"只有 Delete
     走 TTL"、删掉与实现矛盾的 seed_ec_replay 遗留行）。
Next steps: recovery 这条线收口。剩 EC 的 CoW 改造（docs/ec_cow_conversion_design.md §10 六步，
  从 per-attempt nonce 起）—— 量级另一档，建议单独起一轮；未开工。
  另延后：handle_re_avali 的 truncate-in-place 审计（CoW 文档 §9）、handle_fence_extent 的 fence-on-absent。
─────────────────────────────────────────────────────────────────
Date: 2026-08-15
TaskStatus: completed
Task scope: op 命令统一异步 + 可查状态（用户："所有 op 都应异步保持一致，只要能查当前状态"）。fable
  agent 统一规划(scratchpad/ops_async_design.md)→用户定案:in-memory ledger+audit 持久、build 全 7 op。
  5 阶段全完，每阶段编译+测试绿+推 main；live e2e 10/10。
  ① 线材(c9e0a94,28→29 一次 bump,fp b65437d410e5f2c6):MSG_OP_SUBMIT/QUERY+OpSubmit/Query/Record+
     MaintenanceOutcome+OP_KIND_*/OP_STATE_*;PartitionLoad.maintenance_outcomes;MaintenanceReq.op_id;
     is_admin_mgr_msg+=SUBMIT;AUDIT_OP_* 7..12。
  ② manager(7da6b3f):op_ledger.rs(VecDeque cap256,状态机 Pending→Running→Succeeded|Failed+合成 Unknown,
     op_id=(ms<<16)|seq16 非零,attach-dedup,finish 幂等,sweep_running_ttl 30min);handle_op_submit(leader+admin
     门,spawn catch_unwind actuation 复用 auto_dispatch_split(加 explicit at_key)/handle_merge/rebalance/
     force_ec_convert/send_maintenance)+handle_op_query;EC 钩 apply_ec_conversion_done→complete_ec;TTL 挂 policy tick。
     split/merge/rebalance 就地终态,compact/gc/forcegc+ec 留 Running。
  ③ PS(cce6dcf):MaintenanceReq.op_id 穿 compact_tx(CompactTask)/GcTask{op_id};PartitionMetrics.
     maintenance_outcomes 环(cap8);background_maintenance_loop 各终态点(compact Ok/Err/skip/freeze-defer;
     GC 早退+holes 聚合)record_maint_outcome;report_load_loop 抄环上心跳;manager handle_report_partition_load
     reconcile_outcome(按 op_id 一次)+audit。3 recovery 集测 in-batch 挂=既存 BlockCache 污染(对基线验过)。
  ④ CLI/SDK/docs(491898a):ClusterClient.submit_op/op_query;autumn-op 7 触发改 submit+印 op_id、新 ops
     status/list、全局 --wait/--timeout(位置无关,预过滤);presplit 留低阶 split_at。docs 全刷。
  ⑤ e2e(fd187f0):--wait 位置无关修 + GC 两早退补 outcome(空分区 gc 曾卡 Running)。scratchpad/ops_smoke.sh
     10/10:compact/gc/rebalance/split --wait 终态、split 分区+1、异步 submit→poll、ops list、unknown、admin 门。
  残留/延后:seed_ec_replay(promotion 列上代 leader 在飞 EC)未接;GC 候选阶段极少数 infra 早退(916 类)未全补→
     TTL 兜底。手工 op 现全异步+可查错;老低阶 SDK 阻塞法保留给测试/内部。
─────────────────────────────────────────────────────────────────
Date: 2026-08-14
TaskStatus: completed
Task scope: dashboard 大规模可用性改版（用户："partition 非常多时应点 ps 才看这台机的 partitions" → "这套可以" → build+rollout）。
  信息架构从"扁平上千行"改为 partition-server-first 三层下钻，色调一律不变（保留 :root dark+gold token）。
  改动全在 examples/dashboard/static/index.html（include_str! 静态串，不影响 Rust 编译）+ README 导航说明。
  - keyspace ribbon：每 partition 一段、按属主 PS 上色；点某 PS 段 → 整条只高亮该 PS、其余调暗，并 scope 列表。
  - Partition servers 选择器（主入口）：PS 卡片（地址/分区数/容量/iops）→ 列表只列该机 partitions；
    "All servers" 回全量虚拟滚动。filter header 仅在 scope 变化时重建（非每键/每轮询）→ 输入保持焦点跨 5s 刷新。
  - vital-signs masthead；detail 抽屉+per-extent 不变；EN 存储节点降到底部小条。
  验证：node --check JS 语法过；字段访问器与旧可用页 1:1（仅新增 a.kind，回退到 advisory action verb）；
  提交 3cd8bfc 推 main → CP 构建 autumn-rs:3cd8bfc（dongmao-lerobot pipeline，run 54f67abb Succeeded）→
  kubectl set image deployment/autumn-dashboard（ns autumn）滚动完成，pod READY，新镜像。
  live 验证（port-forward）：served HTML 带 PS-first signature；/api/overview part_count=21 ps_count=3
  nodes=5 errors=[]，ps_roll=[(1,7),(2,8),(3,6)]，partition 字段齐全 → 三层下钻可正确渲染。
  同带上未推的 3b84692（run_op 30s 超时）。核心 manager/PS 未动（仅 index.html 变，无 wire 变更）。
  仍待用户确认（未动）：kvc 多 split（需先把 auto-policy aggressive→balanced 停自动 merge，再定分区数）。

─────────────────────────────────────────────────────────────────
Date: 2026-08-14
TaskStatus: completed
Task scope: 管理面清理（用户："复制/TLS 不急，管理很丑"）。①CSS ②CLI ③dashboard 抽离 —— 全部完成并推 main。
  ① CSS（5ddebb0）：dashboard :root token 对齐 examples/gallery（暖色+金 accent），换掉硬编码蓝。
     现由 standalone app 的 static/index.html 承载同一 restyle。
  ② CLI 一致性（e01b686，"consistency+aliases 不破坏"）：修 --bulk 真 bug（deprecated arm 遮蔽 live
     parse→批量路径从不跑）；--size 认字节后缀(perf-check+ycsb)；namespace-create 认 --tenant；
     server CLAUDE.md 订正签名+加 CLI 约定注。
  ③ dashboard 抽离 —— 从 manager 拿掉、搬进 examples、token-gated（4 步全完）：
     1. autumn-op `overview`（a9082a9）：4 RPC→新纯函数 dashboard_compose::build_overview_json，页面
        JSON 契约一字不差；partition-detail 复用 `info --part --detail --json`。
     2. examples/dashboard 新 app（1f19928）：cyper-axum+include_str!(index.html)，spawn_blocking 跑
        autumn-op；token-gated；/api/{overview,partition,action(结构化→allow-list argv),policies,activate}。
     3. 删 manager dashboard（e2d2c39）：删 dashboard.rs+html+死 actuate_action；去 --dashboard-* flags+
        start_dashboard；controller 改纯 per-policy（seed=Armed 自行 actuate，无进程级 gate）。247 lib 测绿。
     4. deploy/docs（a494e18）：Dockerfile 出 autumn-dashboard 二进制；k8s vke 换 standalone Deployment+
        ClusterIP（挂 autumn-authz admin.token）；entrypoint/autumn-deploy/cluster.sh 去死 flag；ops.md+
        manager CLAUDE.md+旧 plan doc(标 SUPERSEDED)+UI 去 server read-only gate（token-gated 后只留 Danger 开关）。
  ③ 收尾：autumn-op `auto-policy upsert/delete` 子命令已加（args.rs+main.rs：--switches csv/--interval/
     --cooldown/--max/--desc → MgrAutoPolicyEntry → AUTOPOLICY_OP_UPSERT/DELETE），app 去掉 501-stub 真接线。
     全端到端 live 验证（scratchpad/dash_smoke.sh：1-EN RF1+dashboard，非 authz+decoupled admin token）：
     /api/overview、/api/policies(reshape 对)、/api/partition(merge metrics+extents 对)、
     custom upsert(gc+compact,builtin=false)→readback→delete 全过。dashboard 无 stub，功能完整。
  有意保留残留：manager_rpc.rs AutoPolicyGetResp.allow_mutations 字段+注释仍提 --dashboard-allow-mutations
     —— 字段 wire-stable 且仍被 autumn-op status 消费（现恒 true）；改注释会动 WIRE 指纹→强行 bump wire 版本，
     一句注释不值当，故留。

─────────────────────────────────────────────────────────────────
Date: 2026-08-11
TaskStatus: completed
Task scope: 修复 autumn_kvcache 的 KV-load bug —— 线上 7B 对 hermes 固定长 prefix 输出 garbage。
End-to-end 验证（2026-08-11）: 修复已合入 main（024cb00）并全集群停机重部到 024cb00。
  vllm 加载权重（direct_read=false 绕过 024cb00 vllm 镜像 EN-direct read 的构建 wire 坏点）、
  输出连贯、KV key 变 qwen7b_<fp>_0_1。换 Qwen2.5-Coder-7B-Instruct-AWQ（同架构同 tokenizer、
  仅权重不同）后拿到独立 key qwen-coder7b_<fp>_0_1（171 vs 29 keys 两 tenant 共存不串读），
  coder 输出连贯且代码风格 → 原 7B/32B 串读 bug 确认修复。hermes mem round-trip OK。
遗留（另立项）: 024cb00 vllm-autumn 镜像 native 客户端 EN-direct read（direct_read=true）
  wire 坏（构建不可重现，非 python 改动；89a429a 是好的）—— 现用 direct_read=false 绕过，
  只影响权重加载速度。下次重建 vllm 时验证 EN-direct 是否恢复；根因疑似 rkyv/依赖未 pin 的构建漂移。
Current summary: 根因两条，均已改代码 + 单测（47 passed，本地 uv run pytest）:
  1) BUG-KVC-LOAD-ATOMIC（fail-open external KV load）: vllm_connector.start_load_kv 改
     all-or-nothing（len(oks)==len(layers) and all(oks) 才注入，否则一层不注入）+ 记录失败
     请求 block_ids；新增 override get_block_ids_with_load_errors() 上报让 vLLM 重算回退；
     _ReqMeta 带 block_ids。单测 tests/test_load_atomic.py。
  2) tenant key 简洁+有效: _identity.tenant_cfg_from_vllm 用 autumn weights-path basename
     (qwen7b) 当 model 段，替代恒定无效的 /model-cfg → key 从 model-cfg_<fp>_... 变
     qwen7b_<fp>_...；tests/test_tenant_identity.py 同步（44 passed）。
  feature_list BUG-KVC-LOAD-ATOMIC passes:true。
Main gaps: 仅代码+单测；线上端到端未验证（须 rebuild vllm-autumn 镜像 + 重部署 vllm + hermes
  复测长 prefix 不再 garbage）。清 kvc 缓存治标（已做过一次，hermes 仍 garbage=活 bug 确认）。
Next steps: 提交（分支）→ CP 重建 vllm-autumn → 重部署 vllm 到 .95 → hermes 发长 prefix 验证。

── 更早 ──
Date: 2026-08-08
TaskStatus: not_completed
Task scope: EN 写路径 owner-model 重写（已收官）+ fable 评审跟修 + GC lost-update 窗口调查（进行中）。
  设计草案 scratchpad/en_actor_design.md (v2, fable 评审)；基线 scratchpad/en_bench_baseline.md。

── fable review 跟修：owner mailbox 跨连接合批的 fence 旁路（design-breaking，已修）──
  fable 复审 owner 重写 diff 发现：owner mailbox 是新的跨连接聚合点，一次 drain 可能把
  僵尸写者(owner_epoch E)+接管新主(E+1)（或换连接重放的 retry）合进一个 burst；而
  append_burst_frames 只按【首 slot】校验 owner_epoch+commit（这在"一批=单连接连续帧"时成立）。
  → [new,zombie] 会把僵尸写在累加 offset 上 ACK 过 fence（acked-data loss）；
    [zombie,new] 会因首 slot 陈旧把整批（含合法新主）拒成 LockedByOther。
  修：owner_loop 在 group-commit 前把 drain 切成同质 run（owner_epoch 变化 OR
    commit 不连续 `commit != prev.commit+prev.payload.len()` 即开新 run），每 run 首 slot
    走完整 prologue、run 间串行。单写者流水线=单 epoch+连续 ⇒ 一个 run ⇒ 热路径零成本。
  回归测试 owner_burst_splitter_tests（两 drain 序都验：僵尸恒被 fence、合法主恒 ACK）；
  临时禁 splitter 证明两测都失败（zombie ACK=0 / 新主拒=5）再复原。
  顺带清理：删死 OwnerState(write_cursor)、去 ExtentMsg 的 WIP allow、订正 DIRECTION 陈旧注释、
  同步 crates/stream/CLAUDE.md（coalescer→owner 模型，note 4/24 + 新增 "Per-extent owner" 小节）。
  门：stream lib 114/114（+2 新）。
  fable 其余项 DEFERRED（非 active bug）：P2-1 handle_append 是 wire-dead 的 owner-bypass 写者
  （MSG_APPEND 在 dispatch 前被拦；~12 测直接调它）；P2-2 删掉的 p0 accounting 测无替代守卫；
  P2-3 跨 burst 流水线消失（A/B 已验无回归）；P3-5 Coalescer 名不副实（剩两水位原子）。

── GC lost-update 窗口：复现 + 修（用户质疑守卫时序图，已解）──
  真因：GC 搬迁 value 的两守卫（active/imm 复检 + VP-identity）只查 memtable+SST 快照，
  但 Put 若只在 WAL 写入流水线里（Phase1 已 seq-assign+编码、Phase2 durable、Phase3
  memtable-insert 未做）对两守卫都不可见；而 Phase1 已 bump seq_number ⇒ GC 随后分配的 seq 更高
  ⇒ 把旧值搬迁盖过用户新 Put（静默 lost update / Delete 则复活）。
  确定性复现 crates/manager/tests/system_gc_inflight_wal_put.rs：两 test-only sync 点
  （set_gc_verdict_pause 把 GC 停在 SST-lookup 后/裁决前；set_write_phase3_pause 把 Put 停在
  Phase2 后/Phase3 前）。坑=Phase3 hold 会 stall partition_loop→force_gc RPC 走 partition_loop
  死锁→改成【先触发 force_gc(GC 停在裁决)→再 hold Phase3+发 Put→放 GC→放 Phase3】；
  另坑=force GC 要 X 权威 sealed（split 封 X 后加一次 roll 写evict PS cache）+ 要 durable_ckpt_vp
  过 X（roll 后再 flush，否则 replay-floor guard 把 X 剔出 holes）。实证 pre-fix got=v_old（'A'）失败。
  修（in-flight write keys 守卫）：PartitionData.inflight_write_keys（refcount 的 64-bit
  FNV(user_key) 集合）；start_write_batch Phase1 seq-assign 同借用内 add、finish_write_batch
  Phase3 memtable-insert 同借用内 release（insert↔release 间无 await ⇒ 每个 key 恒在 memtable
  或集合其一）；Phase2 失败臂 + Phase1b encode-panic 臂也 release 防泄漏；process_gc_chunk
  在 VP-identity 命中后、seq 分配前若 key 在集合 ⇒ Err 中止本 extent 轮（不搬不 punch，下轮重试）。
  中止而非跳过=在飞写可能不提交、被扫值仍可能是活的、不能 punch。hash 碰撞只多一次无害中止。
  门：repro pre-fix 失败(v_old)/post-fix 过(v_new)；PS lib 204/204；GC 集成测试待跑确认无误伤。
  热路径代价=每 key 约几个 HashMap op + 一次 FNV（~1% 写 CPU）——已在 CLAUDE.md 记。
  用户定案 2026-08-08：就用集合方案，接受这点热路径成本（评估过 reserved-marker=在 autumn
  是陷阱、Titan-merge=条件搬迁破坏 relocate-then-punch 即时回收、watermark=翻转"memtable
  只装已提交"不变量面最大，都比集合重）。GC 修完结。
  fable 其余项仍 DEFERRED（见上）。

── 以下为 owner 重写原始记录 ──
Date: 2026-08-05
Task scope: EN 写路径 owner-model 重写（用户拍板 big-bang 新写、只重写 EN、fable 设计+评审过）。
  分多个 gated 步逐步推进；本 loop「按节奏逐步完成」。

── Step 1 done + committed（owner 路由 appends）──
  一个 per-extent owner task（懒启停、mailbox=queue+running 镜像 coalescer 无 lost-wake 生命
  周期、排空即退出不 pin extent）成为 append 的唯一串行写者。两个 append 入口（批处理帧路径 +
  chain 本地 append）改发 Append 消息给 owner；owner 排空 burst 跑迁移来的 append_burst_frames
  （原 build_append_future 改名），逐 slot 帧经 oneshot 路由回；chain forward 仍留 conn。
  无 wire 改动。coalescer 本步仍作 fsync 机制；per-await seal/fence 复检 + offset 预留也留
  （seal/truncate/fence 还并发，下一步搬进 owner 才删）。
  门：stream lib 114/114；append_bench errs=0 depth-scaling 保持（depth4/8/64 更高）；
  G2 130/130 durable + recovery 干净；system_putstream 7/7 真集群 append e2e。

── Step 2 done + committed（删 coalescer，内联 fsync）──
  owner 已把 append 串成 burst，一个 burst 一次 sync_data 覆盖它 → 跨 burst 的 coalescer 冗余。
  append 路径改内联 sync_data（pending_fsync 前、last_synced 后，保 fd_evictable）；删 coalescer_loop
  + register_sync_waiter + CoalescerInner + 2 个 coalescer 回归测试 + mark_disk_offline（净 -410 行）。
  Coalescer 结构剩两水位原子（last_synced/pending_fsync）。门：lib 112/112；append_bench 背靠背 A/B
  无回归（inline 略好）；G2 130/130 durable；system_putstream 7/7。

── 结论：re-check / op-lock / meta_write_lock 删除 = WON'T-DO（load-bearing，用户认同聚焦热路径）──
  深挖确认：fence 复检守 eager owner_epoch fetch_max（G1 僵尸写者需它同步/快）——单线程下 eager 抬升
  会落在 append 的 await 里，复检正是抓它；seal 复检守并发冷 EC op 的 seal（用户拍板 EC convert 等与
  热路径无关、不搬进 owner，那要重构 ~1000 行 EC 代码换删 ~30 行便宜检查，风险/收益极差）。所以复检
  整体 load-bearing、便宜、钉着 shipped-bug → 保留；offset 预留也留。
  owner 重写净结果 = append 经干净的 per-extent owner 串行 + 内联 fsync，删掉 build_append_future 两阶段
  并发机器 + coalescer；结构改善 + 净 -400 行，安全检查全保留。**EN owner 重写到此收官。**

─────────────────────────────────────────────────────────────────
Date: 2026-08-05
TaskStatus: completed
Task scope: close 两个"几乎完工"feature（用户"先把几乎完工的 close 了，不需要 coco review"）。
  重跑验收（树在 G1/G5/G2 后动过 wire/stream/PS）确认仍绿，再按规则从 feature_list 删除：
  - F-WIRE-CRC-UNIFY：实现+验收早已达成，唯一 blocker 是 coco 评审 → 用户免除 → close。
    重验：rpc --lib 56 / client 41 / stream 114 / PS 204 全绿；registry_pins_current_schema_
    to_max_version（v28 指纹 pin）+ CRC 翻转具名测试 ok。
  - F-DIRECT-MANY：代码完成、coco 早已 0 findings、跨机功能线上跑过（VKE 39.56GiB 字节正确）。
    重验：fuse 二进制构建绿、client clippy 0 warning。唯一残留=`--direct-read` on/off 的
    PS-egress A/B 量测（perf 特征化数字，非功能门），作为可选后续，不阻塞 close。
  feature_list 85 行；两条已删（git 是记录）。

─────────────────────────────────────────────────────────────────
Date: 2026-08-04
TaskStatus: completed
Task scope: G2 断电/崩溃一致性测试 —— 单机 lazyfs（用户"G2可以测试吧？做loop设备跑
  dm-log-writes,单机就行"）。dm-log-writes 内核 target 容器里没有且 modprobe 不了
  （无 /lib/modules）；insmod 精确匹配的 Debian .ko 属宿主级操作被安全策略拦；用户
  改选 lazyfs（userspace FUSE，不碰内核）。

交付:
  - scripts/g2_crash_consistency.sh（已入库）
  - docs/ops.md "G2 — power-loss crash-consistency test" 手动步骤
  - lazyfs 装到 /opt/lazyfs（脚本默认自动发现；scratchpad 是会话临时的）
  - cluster.sh line 1066 set -u unbound bug 顺带修（裸 `start N` 不设
    AUTUMN_BOOTSTRAP_PRESPLIT 就崩 → ${AUTUMN_BOOTSTRAP_PRESPLIT:-} 兜底）

机制: 单机 RF1 集群，data 面（AUTUMN_DATA_ROOT）在 lazyfs 挂载；etcd bind-mount 出
  lazyfs（只测数据面持久化，控制面设为独立 durable quorum）。起集群 → cache-checkpoint
  固化 baseline（sentinels+bootstrap extents，否则 format 未 fsync 的 sentinel 被 clear-cache
  误清=假失败）→ must_sync 写 N 键记 key+SHA → kill -9 autumn（丢 memtable）+
  lazyfs::clear-cache（丢未 fsync 磁盘=断电）→ cluster.sh start 1 重启恢复 → 逐键 get 比 SHA。
  verdict 要 0 LOST + 0 CORRUPT + survived==acked + 0 fail-loud。默认 70×2MiB 大值
  （140MiB>MAX_WAL_GAP 128MiB）强制 rotate+flush → 恢复也走 checkpoint 路径。

Result（实证，多轮 40~240 键）:
  - autumn 是同步持久化: quiesced 与 --immediate（最后 ACK 后瞬间断电）都 0 丢 0 腐。
    即 client put 返回 ⟹ 已 fsync（kill 在 clear-cache 前、coalescer 无法补刷仍零丢）。
  - checkpoint-reload 与 WAL-replay 两条恢复子路径都覆盖（open_partition: ready
    tables=1 sst_readers=1 max_seq=N），max_seq 全数恢复，0 fail-loud。
  - io_uring 写路径在 FUSE 后端可用（无 O_DIRECT → buffered io_uring 走 FUSE OK）。
  - lazyfs 机制有牙齿（冒烟: fsync 的存、未 fsync 的被 clear-cache 清）。

Main gaps（非阻塞，后续可选）:
  - G2 只测数据面（etcd 设为 durable）；etcd+EN 同时断电是更大场景，未测。
  - 定点 torn-write（lazyfs torn-op / crash::timing=before::op=write）未接 —— 可复现
    WAL-FAILSTOP Corrupt 路径，本轮未做。
  - 路线图剩 G10（读线性一致 Elle checker，较大方法论）。

Next steps: 无待办，G2 收口。

（更早任务历史见 git log；本文件按规则 12 定期裁剪，保持整洁。）
