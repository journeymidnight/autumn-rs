Date: 2026-05-14
TaskStatus: completed
Task scope: F199 — per-extent GC failure cooldown (stop hammering
            broken EC layouts on macOS test cluster).

User-reported (2026-05-14, follow-on to F198):
  - Test cluster with one node's disk offline (`disk 8: online=false`).
  - Partition opens fine after F198, but local `background_gc_loop`
    re-tries `run_gc` on the same 2.8 GB EC(3+1) extent every 5-7 s.
  - Each failure path: `ec_subrange_read` → one shard read fails (disk
    offline) → fall back to `ec_read_full_and_slice` → 4 × 933 MB
    parallel reads with 5 s timeout → "0/3 shards available for EC
    decode" → log error → next tick same thing.
  - macOS CPU/IO saturated.

Fix:
  - `background_gc_loop` carries a `HashMap<u64, Instant>` of recent
    `run_gc` failures.
  - `GC_FAILURE_COOLDOWN` = 5 min. Auto tasks filter holes against
    this map; Force tasks bypass.
  - Success → remove from map. Failure → stamp `Instant::now()`.
  - Stale entries evicted lazily on each pickup pass.

Files touched:
  - crates/partition-server/src/background.rs: +30 lines.
  - feature_list.md: F199 entry above F198.
  - claude-progress.txt: this file.

Tests:
  - cargo build -p autumn-partition-server --lib: clean.
  - cargo test -p autumn-partition-server --lib: 138/139 passed.
    The 1 fail (`f099i_d1_fast_path_no_fu_allocation`) is pre-existing
    on main (poisoned mutex on global counter), unrelated to F199.

Deferred (F200+):
  - Sub-range RS reconstruction in `ec_subrange_read` so a missing
    data shard doesn't trigger full-extent fall-back. The right
    long-term fix; F199 only stops the retry storm.
  - Configurable cooldown duration via env var.

Prior F198 status (still: completed):
  - F198 rich `ecConversionInflight/` marker prevents EC dispatch
    stuck-state on restart. Two commits: 187dab3 + c3d07b3 already
    pushed to main.

Next session: feature_list.md F198 + F199 entries describe both
shipped fixes.
