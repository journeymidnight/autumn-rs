Date: 2026-06-10
TaskStatus: completed
Task scope: F258 — replicated-read replica rotation + optional hedged
  read (first of the P15 ledger F258→F259→F260→F261, user-ordered
  3-2-1-4). Earlier same-day: F256 (48cdf3c), F257 (0653af3), P15
  ledger (f703d95), UCX livelock investigation (memory + ledger).

=== TWO LISTS REQUIRED BY THE LONG-TASK HARNESS ===

  Already implemented (this commit):
    - rotated_replica_start (SplitMix64, pure fn, 3 unit tests) in
      crates/stream/src/client.rs.
    - read_replicated_with_failover: sealed extents start at the
      rotated replica; failover walks remaining replicas in rotated
      order; open-tail keeps replica[0]-first.
    - read_hedged_pair: timer-raced hedge to the second replica,
      first Ok wins; eversion-stale fails fast on every arm; loser
      future drop is cancel-safe (rpc layer owns buffers).
    - Config: autumn_stream::set_read_hedge_ms (OnceLock, default 0 =
      hedge off, rotation always on), autumn-ps --read-hedge-ms,
      cluster.sh AUTUMN_READ_HEDGE_MS passthrough.
    - Docs: stream CLAUDE.md read_bytes_from_extent row updated;
      feature_list F258 entry -> completed with measured numbers.

  Not implemented (next in P15 order):
    - F259 VP large-value client direct-read from EN (next up).
    - F260 chained replication for >=64K writes.
    - F261 SSTable on-demand paging + bounded block cache.
    - F258 chaos coverage rides the next full chaos cycle (read path
      unchanged semantically: same RPC, same failover, same eversion
      fail-fast).

Current summary:
  Verification (TCP 3disk p8 t16 d8, EN base port 20000):
    default (rotation only): 8M read 433.8 ops/s (113% of committed
      384 baseline), 4K write 64.8K (103%), 4K read 1.18M — no
      regression.
    hedge=50ms: 8M read 570.2 ops/s (+31% vs rotation-only, p50
      353ms -> 151ms) — hedge repairs the slow-replica tail under
      shared-tenant disk pressure, exactly its design target.
  stream lib tests 70 passed (3 new), partition-server 158 passed.

Main gaps:
  - Hedge default-off; enabling in production is an operator call
    (cluster.sh AUTUMN_READ_HEDGE_MS).
  - EC reads and open-tail reads intentionally unchanged.

Next steps:
  F259 per the P15 ledger (PS returns VP descriptor for >=64K reads;
  client pulls straight from EN; eversion/GC-pin semantics must hold).
