Date: 2026-07-19
TaskStatus: completed
Task scope: 4-task batch (用户 2026-07-18) — ALL DONE, committed + pushed to main。

1. autumnfs 前缀 fs/{tenant}/{volume}/ → fs/{tenant}/ (去 {volume} 层)   — DONE (c7ad5a2)
2. 默认打开 authz (部署层默认开, dev/测试仍关)                            — DONE (35c4e98/066c552/b3bf127)
3. fix F-AUTUMNFS-SLOW                                                     — DONE (615fed9)
4. push F-KVC-LAYOUT-FP + BUG-MGR-RETRY-CLASS + F-REGION-REBALANCE PhaseB  — DONE (4718714/7f8ca9e)

commit 顺序:
  c7ad5a2 Task1 去 {volume}(fail-loud 迁移 guard + tenant 隔离测试)
  615fed9 Task3 F-AUTUMNFS-SLOW(流式分窗 put_many + 分窗 get_many_into; GET 33→686MiB/s 20×)
  4718714 Task4 三 feature 推 done(BUG-MGR-RETRY-CLASS 修真 P1 回归)
  7f8ca9e Task4 coco 复审(retry 不 rotate 瞬态 precondition + rebalance raw-field 校验)
  35c4e98 Task2A 客户端 --credential-file(fuse/autumnfs/autumn-client)+ autumnfs orphan 清理
  066c552 Task2A 修凭据文件=HEX(read_credential_file 共享 helper, 4 单测)
  b3bf127 Task2B 部署层默认开 authz(autumn-deploy + k8s + entrypoint + docs)

Task2 authz e2e(memory-mode authz-ON cluster, protect fs/): 无凭据 put→DENIED;
  带 --credential-file(真 tenant-create 凭据)→ small+20MiB put/get 字节精确。
Task2B coco 5 findings 全修: P1 凭据 skip 前 mint-token 验证(防 etcd wipe 后 stale
  cred); P2 disable 分支 unset authz env; P2 deploy.sh disable 语义澄清(configmap 权威);
  P2 k8s doc --admin-token-file; P2 deploy.sh --from-file+umask 临时文件(secret 不进 argv)。

关键测试全绿: fuse 45, client 35+4(credential parser), stream 114, PS 202, common 13,
  manager 229+1, kvc 27; autumnfs bin 2; system_fuse_ns 3 集成; deploy validate.sh OK。

cluster.sh/chaos/测试从不 set AUTUMN_AUTH_* → 天然 authz-OFF(未动)。

── 关键工程记录(供后续会话)──
- BUG-MGR-RETRY-CLASS 首版把 CODE_PRECONDITION 一刀切 fail-fast → 误杀 alloc_new_extent
  依赖的瞬态并发冲突重试(manager 显式回 "retry with [a] fresh snapshot"/"defer…until
  it completes")。修=is_transient_conflict() 按 marker 区分;瞬态 retry 但**不 rotate**
  (leader-side self-heal);fence + 业务规则 precondition 仍 fail-fast。
- 凭据文件 = HEX(不是 raw bytes);read_credential_file 镜像 Python;native 客户端 +
  autumn-op mint-token 都读 hex。default.cred 存 BARE hex(mint-token verify 要 bare hex)。
- 部署 authz 逃生门 AUTUMN_AUTH_DISABLE=1: baremetal=unset env; k8s=configmap flag(entrypoint
  读 pod env, 非 deploy.sh env; deploy.sh flag 只 skip Secret gen)。
