Date: 2026-04-24
TaskStatus: completed
Task scope: F100-UCX Phase 4 — probe_ucx() body + env-driven init()
            dispatch + UCX bind-IP validator + server-side UCX accept.

Current summary:
  ✓ probe_ucx(): captures ucp_context_print_info via open_memstream;
    greps for rc_mlx5/rc_verbs/dc_mlx5/ud_mlx5/ud_verbs. Pure-TCP UCX
    intentionally treated as "unavailable" (no RDMA → no gain over
    native TCP).
  ✓ decide() wired into init(): AUTUMN_TRANSPORT={auto,tcp,ucx}
    selects at runtime. UCX requested but no RDMA → explicit panic.
  ✓ check_listen_addr(addr, kind) — pure function preflight that
    rejects non-RoCE-attached IPs under UCX (walks getifaddrs,
    checks /sys/class/net/<dev>/device/infiniband, lists candidates
    on error). Opt-in, not auto-enforced by init() because tests
    legitimately use 127.0.0.1 (UCX falls back to TCP, correctness
    still works).
  ✓ rpc::RpcServer::serve() now branches on transport kind:
    - TCP: unchanged, std::net listener + dedicated OS thread +
      Dispatcher (multi-core scaling).
    - UCX: accept on calling compio runtime, spawn handler as task
      on same runtime. No Dispatcher (UCX EPs are thread-bound).
      Single-thread server — sufficient for path 2 correctness;
      multi-core UCX scaling via per-worker listeners is deferred.

Validated under AUTUMN_TRANSPORT=ucx (wireup + data path both work
end-to-end over UCX wire protocol; for 127.0.0.1 the underlying
transport falls back to UCX's tcp — same wire as TCP, just through
UCX's framing layer):
  - 7/7 rpc round_trip integration tests pass individually
  - writer_task_handles_2048_concurrent_vectored passes
  - 4/4 UCX loopback tests still green (from P3)
  - probe tests: 4 ucx + 5 no-ucx, all green
  - listen_validator: 4 tests (tcp ok + ucx-loopback-reject + wildcard
    + roce-ip-accept) all green

Default workspace: 192/0/0 (= P3 188 + 4 new listen_validator tests).
No regressions.

Main gaps:
  - Full-suite run under AUTUMN_TRANSPORT=ucx with --test-threads=1 on
    the SAME binary still hangs due to cross-test UCX worker-state
    bleeding across compio runtimes. Individual-test runs are clean.
    This is a test harness limitation, not a transport bug. Production
    binaries are one process = one runtime, so this doesn't affect
    real deployment.
  - Multi-core UCX server scaling: current serve_ucx is single-thread;
    for a production PS or ExtentNode expecting thousands of concurrent
    connections we'd need per-worker listeners on different ports +
    manager-side discovery. Spec §10 tracks as P5+ follow-up.

Next steps: Phase 5 — perf A/B (ps_bench --transport tcp|ucx,
            scripts/perf_ucx_baseline.sh), capture
            perf_baseline_ucx.json, README UCX walkthrough,
            mark F100-UCX passes: true in feature_list.md.

Branch: f100-ucx (commits 14506a5..94ad4bb..6cee8bc..validator).
