Date: 2026-07-18
TaskStatus: not_completed
Task scope: 4-task batch (用户 2026-07-18):
  1. autumnfs 前缀 fs/{tenant}/{volume}/ → fs/{tenant}/ (去 {volume} 层)  — DONE
  2. 默认打开 authz (部署层默认开, dev/测试仍关)                          — PENDING
  3. fix F-AUTUMNFS-SLOW (autumnfs CLI upload/read 慢)                    — PENDING
  4. push F-KVC-LAYOUT-FP + BUG-MGR-RETRY-CLASS (+F-REGION-REBALANCE PhaseB)
     等 "代码已做待评审" 的推到 done                                      — PENDING

── Task 1 (F-KEY-NS-NOVOLUME) — completed 2026-07-18 ──
改动:
  - crates/fuse/src/state.rs: FsState::new(manager, tenant) / new_with_host 去
    volume 形参; 删 vol 字段/is_valid_volume/wire(); 8 个 kv_* 直传相对 key;
    kv_range_keys 直接 collect entries.key (无 wire/strip)。
  - crates/fuse/src/read.rs / extent.rs: batch 数据路径去 wire()（直接 extent_key）。
  - crates/fuse/src/meta.rs ensure_schema_version: None 分支加 FAIL-LOUD 迁移 guard
    —— range-scan 相对 start=\x05（旧 {volume} key 首字节≥0x2d，新类型字节 0x01-04，
    fresh FS 空）；发现旧 volume 数据 → 响亮拒绝 mount（防静默当空盘 + 影子数据集）。
  - crates/client/src/lib.rs: 删 connect_volume/scoped_volume（无外部调用者）。
  - crates/fuse/src/main.rs / python/src/fs.rs / server/bin/autumnfs.rs: 删 --volume /
    volume= 参数（零调用者，clean removal; PyO3 signature=(manager,host,tenant,direct_read)）。
  - AllocInodesReq.volume 字段休眠保留（wire 不动）。
  - 文档: fuse/CLAUDE.md, server/CLAUDE.md, schema.rs, meta.rs, key.rs, docs/ops.md
    去 {volume}; docs/key_namespace_split_design.md 顶部加 REVERSION banner;
    feature_list.md 加 F-KEY-NS-NOVOLUME 条目 (passes: true)。
Review: coco 4 findings → P1 fail-loud schema guard 采纳(+测试); P2 Python/Rust
  compat shim 因零调用者 WON'T-FIX(clean removal 是用户意图); P3 tenant 隔离测试补齐。
测试(全绿): fuse lib 45, client 35, manager lib 228, fs_alloc 2; 新增/更新
  system_fuse_ns.rs 三个 #[ignore] 集成测试全过:
    cold_remount_reads_back_written_extents / two_tenants_isolate_same_inode /
    stale_volume_data_refuses_mount。workspace build clean。
Next: commit Task 1 → 进入 Task 2 (authz 部署层默认开)。

── 最近已完成(上一批, 已 commit) ──
  36489b2 BUG-KVC-NO-HIT (vLLM connector 异步 save + dedup, no-hit +6.5/7ms)
  0c7dd21 sglang connector batch_exists 128 RPC → 1 早退
