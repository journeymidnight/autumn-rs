Date: 2026-07-14
TaskStatus: completed
Task scope: F-EN-DYNSHARD — dynamically changeable EN shard count + k8s via a
  stable UUID identity decoupled from the network address. M0 (identity
  plumbing) DONE + pushed (commit e3477c6). M1a (EN startup self-registration)
  DONE this session. Design: docs/en_dynamic_shard_design.md.

Current summary:
  DONE — M0 (committed e3477c6): node_uuid in-struct on MgrNodeInfo +
    MgrNodeOverride (WIRE v19, etcd-reset deploy); handle_register_node
    UUID-first resolution (uuid match keeps node_id + updates location in
    place; legacy adopt; duplicate-address refused; identity-only preserves
    location); uuid-keyed tombstone survives remove_node; format mints/atomic-
    persists a node_uuid sentinel. Reviewed by coco (deep) + fable subagent (7+5
    findings all fixed). 6 f_en_dynshard_* tests.
  DONE — M1a (EN startup self-registration, this session, ready to commit):
    - crates/server/src/bin/extent_node.rs: `--advertise HOST:PORT` (OPTIONAL,
      backward-compatible — unset = keep format-stamped location); validated
      IP:port with port==--port. `read_node_identity` reads node_uuid +
      per-dir disk_uuid sentinels (fail-loud). Pure `build_register_req`
      (transport-conditional control_address, unit-tested). `register_with_
      manager` (retry 30x1s through mid-election manager, fail-stop on hard
      refusal/exhaustion) called from shard 0 + run_single_shard, after
      verify_manager_cluster_id, before serving.
    - Manager side needs NO change — M0 uuid-match already updates location in
      place (the reshard commit point).
    - cluster.sh EN launch passes --advertise "${BIND_HOST}:$port" (idempotent
      with format's advertise → exercises the path e2e in dev/chaos harness).
    - 2 EN-binary unit tests (build_register_req TCP/UCX); workspace builds
      clean; manager 188 / rpc 44 unchanged; 0 clippy warnings in extent_node.rs;
      cluster.sh syntax OK.

Main gaps (M1b + beyond, all OPEN):
  - M1b: DfResp echo fields (node_uuid/advertise_addr/shard_ports) → WIRE bump;
    node_health_loop drift-heal (compare echo vs stored MgrNodeInfo, heal via
    mirror_register_node) + pod-IP-reuse imposter detection (uuid echo != stored
    → different process on a stale addr → DON'T heal, WARN + treat df as failed);
    format → identity-only registration (stop stamping location); make
    --advertise REQUIRED + wire entrypoint/autumn-deploy/k8s launchers.
  - M2: reshard runbook (docs/ops.md) + k8s per-pod ClusterIP + Service port gen.
  - M3: reshard e2e/chaos (boot cpuset len 2 → write → restart len 4 →
    list-nodes shows 4 ports → read back zero-movement → restart len 1 → read).

Next steps:
  1. Commit + push M1a (extent_node.rs + cluster.sh + docs).
  2. M1b: start with DfResp echo fields (extent_rpc.rs) + EN handle_df echo +
     node_health_loop drift-heal. WIRE bump to v20 (record fingerprint).
