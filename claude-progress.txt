Date: 2026-06-01 (F250 — autumn-kvcache vLLM KVConnectorV1 adapter, Phase 3a)
TaskStatus: not_completed
Task scope: User said "开始实现" the vLLM connector (route A, native
  KVConnectorBase_V1, daemon-less, reuse autumn-client). Design = docs/
  autumn_kvcache_plan.md §13. Feature registered as F250 in feature_list.md.

=== WHAT IS DONE (verified) ===
  F250-A · Shared util `python/autumn_kvcache/autumn_kvcache/_keys.py`
    (build_tenant_suffix / full_key / tenant_prefix). sglang_backend.py
    refactored to import it (no behavior change). ALSO broadened the
    defensive sglang import from `except ImportError` -> `except Exception`
    (a present-but-broken sglang stack — flashinfer version-check
    RuntimeError in this env — must not crash adapter import).
  F250-B · `vllm_connector.py`:
    - `AutumnKVConnector(KVConnectorBase_V1)` + `AutumnConnectorMetadata`,
      mirrors vLLM's reference SharedStorageConnector (per-request-prefix,
      per-layer). Defensive vLLM import (_VLLM_AVAILABLE=False fallback;
      module imports WITHOUT vLLM installed — verified).
    - scheduler: get_num_new_matched_tokens / update_state_after_alloc /
      build_connector_meta / request_finished.
    - worker: register_kv_caches / start_load_kv (sync, Phase 3a) /
      wait_for_layer_load (noop) / save_kv_layer / wait_for_save / get_finished.
    - `_AutumnKVStore` helper = the autumn-facing data plane (key fmt +
      batched put_from/get_into + head existence) — vLLM/torch-free, unit-
      testable. Key = kvc/{tenant}/vllm/{prefix_hash}/{layer_name}.
    - torch helpers (_slot_mapping_for_blocks / _extract_layer / _inject_layer
      / _byte_view) — run only under a real vLLM worker.
  F250-C · `tests/test_vllm_dataplane.py` — PASSES against a real 1-node
    cluster: align/prefix_hash purity, key format, 64KiB x 4-layer ZC
    save/load byte-exact round-trip, exists() semantics, graceful miss.
  Regression check: existing sglang `tests/test_smoke.py` STILL PASSES
    (sglang=False fallback) — F250-A refactor caused no regression.
  coco /findbugs review (deep, GPT-5.5): 4 findings, ALL FIXED + re-verified:
    P1 #1 prefix_hash ignored LoRA/mm context -> added extra_keys
      (_request_extra_keys folds lora+mm into the hash).
    P1 #2 scheduler probed hardcoded "layer.0" (worker saves real layer
      names) -> would always-miss. Fixed: layer-name-INDEPENDENT completion
      marker (_AutumnKVStore.mark_present written in wait_for_save /
      is_present probed by scheduler). THIS was the bug that would have
      broken cross-instance hit entirely.
    P1 #3 empty block_ids silently saved/loaded empty KV -> record alloc
      blocks in update_state_after_alloc (_blocks_to_ids) + empty-slot guard
      in build_connector_meta/start_load_kv (skip + warn, never persist zeros).
    P2 #4 sglang clear() wiped whole tenant prefix incl vllm pool -> now
      pool-scoped (pool_prefix(tenant,"kv")); verified in smoke log.
    Re-ran both tests after fixes: vLLM data-plane PASS (now incl marker),
    sglang smoke PASS (clear scoped to .../kv/).

=== WHAT IS NOT DONE (why not_completed) ===
  F250-D · vLLM e2e (the real verification of the vLLM-facing half):
    - The connector lifecycle (scheduler/worker split, slot_mapping math,
      extract/inject tensor ops) is WRITTEN but NOT e2e-tested — there is no
      vLLM-running verification yet. Mirrors SharedStorageConnector but the
      details (block_ids shape, scheduled_new_reqs fields, tensor layout) need
      validation against a real vLLM.
    - Blocker/decision: vLLM is NOT installed; installing into the system env
      would likely downgrade torch 2.9.1 and BREAK the installed sglang
      0.5.10. Plan: isolated venv. NOT yet done (awaiting user go-ahead on the
      heavy install + a model run on the 2xH200).
    - README "Using autumn-kvcache as vLLM L3" section: not yet written.

=== NEXT STEPS ===
  1. Address any coco /findbugs P0/P1 in vllm_connector.py.
  2. Isolated venv: pip install a vLLM version compatible with the GPU; read
     the INSTALLED KVConnectorBase_V1 to reconcile any signature drift vs the
     v0.11.0 API this was coded against.
  3. e2e: two `vllm serve` instances + --kv-transfer-config AutumnKVConnector;
     assert 2nd instance gets cross-instance prefix-cache hit from autumn.
  4. README + flip F250 passes -> completed.

Env: 2xH200, torch 2.9.1+cu129, sglang 0.5.10 installed, vLLM NOT installed.
Data plane reused: autumn.BatchClient.get_into/put_from + Client.batch_head +
  _bridge (run/run_on/new_loop). Persistence only via partition
  (feedback_no_parallel_data_plane). Host clean, no leftover daemons.
