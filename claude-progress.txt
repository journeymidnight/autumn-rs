Date: 2026-05-08
TaskStatus: completed
Task scope: F160 — F119-C tightening on handle_copy_extent (close eversion=0
            silent-skip loophole) + clear Audit 2 #2 (vp_refs concurrent
            deletion race in handle_stream_punch_holes / handle_truncate)
            after re-verification.

Why:
  F119-C closed the `eversion=0 silent-skip` loophole in `handle_read_bytes`
  and `build_read_future` by dropping the `req.eversion > 0 &&` clause
  from the eversion comparison. The same loophole still existed in
  `handle_copy_extent`'s Ok(None) branch (line 3074) and Err(_) branch
  (line 3086), both used by `run_recovery_task` and `handle_re_avali`.

  Production callers fetch ExtentInfo from the manager before dispatch
  so eversion is normally fresh. But defense-in-depth matters: removing
  the `> 0 &&` clause closes a future-bug class where uninitialised
  eversion would bypass the EC-shape mismatch detection and copy shard
  bytes as if they were full payload (the F119-D corruption shape).

How:
  Drop `req.eversion > 0 &&` from both Ok(None) and Err(_) branches of
  handle_copy_extent. The check now reads simply `if req.eversion < ev`,
  matching the F119-C invariant.

Audit 2 #2 cleared:
  Re-verified the snapshot-and-mutate sequence in
  crates/manager/src/rpc_handlers.rs:1100-1203 (handle_stream_punch_holes).
  The whole block runs inside a synchronous closure under one
  borrow_mut. `needed_addrs` (line 1162-1172) and the in-memory
  `s.extents.remove` (line 1190) happen in the SAME synchronous scope
  without any `.await`.

  After the closure returns, borrow_mut drops; the etcd-mirror await
  follows. During that await, a concurrent handle_sync_partition_vp_refs
  cannot increment vp_table_refs on an extent that's already been
  removed from s.extents (since vp_table_refs is a field on
  MgrExtentInfo stored in the map; if the extent is gone, the increment
  silently no-ops, not a corruption).

  The agent's claimed race in Audit 2 #2 is not reachable today. NOT a
  bug. Documented as cleared in feature_list.md.

Files modified:
  - crates/stream/src/extent_node.rs:
      * handle_copy_extent: drop `> 0 &&` from 2 eversion-check branches
      * mod f160_copy_extent_eversion_tests: 1 new unit test
  - feature_list.md: F160 entry + Audit 2 #2 cleared note
  - claude-progress.txt: this file

Verification:
  - cargo build --workspace --exclude autumn-fuse: clean.
  - cargo test -p autumn-stream --lib: 39/39 pass (was 38; +1 new F160
    test that constructs an extent with eversion=7 and asserts a
    copy_extent request with eversion=0 returns FailedPrecondition).
  - cargo test -p autumn-manager --lib: 30/30 pass.
  - cargo test -p autumn-partition-server --lib -- --test-threads=1:
    118/118 pass.

Out of scope (deferred to future features):
  - F154 candidate (manager-side ecConversionInFlight etcd persistence)
    — perf hygiene only after F153 closed corruption.
  - Cross-replica byte-divergence detection — would require either
    end-to-end value CRC at API boundary or cross-replica byte
    comparison at every read; structural.
  - EC reconstruction integrity — would need shard-level CRC.
  - sealed_length cross-source consistency — periodic reconciliation.
  - F157-MED-2 reader-pin protocol — longest deferred structural item.
    Re-evaluation: MED-2's failure mode is "spurious NotFound" on
    in-flight Get vs concurrent GC delete (POSIX preserves bytes on
    open fds; physical delete only on refs+vp_table_refs both 0;
    extent-node's get_extent returns NotFound after extents.remove —
    clean error). Severity is availability (occasional read failures
    during normal GC) more than correctness. Reader-pin would reduce
    user-visible errors but isn't closing a silent-corruption surface.

Pre-existing (NOT caused by F160):
  - autumn-rpc lib test `init` symbol missing — pre-existing build error.
  - autumn-manager test ec_integration::replication_stream_works fails
    on this box both with and without F160.

Session-level summary (F151-F160 across this loop session):
  F151: Python bindings asyncio-native
  F152: 3 etcd-first manager handlers + truncate sync_data
  F153: per-extent EC conversion lock (post-failover double-dispatch)
  F154: RETRACTED (was: flush_one_imm_local F148-A invariant violation
        — re-inspection showed agent over-claim, not a bug)
  F155: rkyv checked decode (replaces from_bytes_unchecked)
  F156: min-replica commit_length majority quorum
  F157: extent .meta CRC32C + decode_last_table_locations skip-on-corruption
  F158: WAL record per-record CRC32C (V1 envelope)
  F159: save_meta durability + apply_extent_meta_durable ordering fix
  F160: handle_copy_extent F119-C tightening + Audit 2 #2 cleared

  Net: 9 verified bugs closed; 5 audit findings cleared; 1 agent
  over-claim retracted.

Next steps:
  - Remaining audit candidates require structural change (MED-2 reader
    pin, cross-replica detection, EC shard CRC, periodic reconciliation)
    or are perf hygiene (F154 EC CAS). Priority should be
    operationally-driven from production telemetry.
