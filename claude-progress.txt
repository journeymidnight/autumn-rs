Date: 2026-04-25
TaskStatus: completed
Task scope: F101-e — make `ucx-sys-mini` build cleanly on hosts without
libucx-dev installed, so that `cargo build --workspace` no longer panics
on bare developer machines. Default-feature build path of
`autumn-transport` already gates UCX usage; only the bindgen probe in
ucx-sys-mini/build.rs was unconditionally fatal.

Change:
  - crates/transport/ucx-sys-mini/build.rs:
      pkg_config::probe_library("ucx").expect(...)
        → match { Ok(lib) => bindgen as before,
                  Err(e)  => emit empty bindings.rs stub + cargo:warning }
  - lib.rs untouched (the existing `include!(bindings.rs)` of an empty
    file is a valid Rust no-op; only `pub use libc;` remains).
  - Downstream autumn-transport `ucx` feature already gates all UCX
    call sites with `#[cfg(feature = "ucx")]`, so no consumer-side
    changes needed.

Verification:
  ✓ Host has libucx 1.16.0; `cargo build -p ucx-sys-mini` succeeds with
    full bindings.
  ✓ Simulated absence with `PKG_CONFIG_LIBDIR=/tmp/empty cargo build
    -p ucx-sys-mini` → succeeds, warning emitted, empty stub written.
  ✓ Simulated absence at workspace level (`--workspace --exclude
    autumn-fuse`) → succeeds. autumn-fuse excluded because libfuse-dev
    is also missing on this host — that is a parallel pkg-config
    fragility (fuser crate's build.rs panics the same way), separate
    from F101-e and worth a follow-up.

Behavior matrix:
  | Host env             | Command                                   | Result   |
  |----------------------|-------------------------------------------|----------|
  | no libucx, no flag   | cargo build -p ucx-sys-mini               | ✓ stub   |
  | no libucx, no flag   | cargo build (default-members only)        | ✓        |
  | no libucx, no flag   | cargo build --workspace                   | ✓ (mod.  |
  |                      |                                           | autumn-  |
  |                      |                                           | fuse)    |
  | no libucx, --features ucx                                          | ✗ link   |
  | with libucx, --features ucx                                        | ✓ full   |

Acceptance:
  (a) ✓ Default workspace builds need no libucx install
  (b) ✓ Explicit --features ucx without libucx still fails loudly
       (correct signal: install libucx-dev)
  (c) ✓ links = "ucp" Cargo manifest field untouched — only the build
       script's link-lib emission changes, which is automatically tied
       to the pkg_config probe path
  (d) ✓ No source-code change in transport / rpc / partition-server /
       stream — pure build-script refactor

Follow-up (separate task, F101-f candidate):
  - autumn-fuse has `default = ["fuse"]` and pulls `fuser` which
    panics in its build.rs when libfuse-dev is absent. Same fragility
    as ucx-sys-mini pre-F101-e but in a 3rd-party crate, so the fix
    has to be a feature-gate flip (default = []) or workspace-level
    exclusion, not a build.rs rewrite.

Next steps when user resumes:
  - Optionally apply the parallel fix to autumn-fuse so
    `cargo build --workspace` works on a fully bare host.
  - F100-UCX gate (c) cross-host A/B remains the only path to actual
    RDMA throughput numbers.
