Date: 2026-09-11 (5)
TaskStatus: completed
Task scope: dashboard 五项改造（用户 2026-09-11 提的 5 点）。其中第 2 点
  「Policy advisories 有问题，要重新思考」是核心，第 3 点「apply split/merge 前要先
  compact，检查是不是做了」的答案是**没做**，本轮补上。

【1. 面板拆 tab】examples/dashboard/static/index.html 重写成六个 tab
  （Overview / Partitions / Servers / Nodes / Policy / Logs），tab 名进 URL hash
  所以视图可分享。轮询只取**当前 tab 需要的** endpoint —— 每个 /api/* 都会 fork 一个
  autumn-op 子进程，原来每 5 秒固定 3 个，现在 Partitions/Servers/Nodes 上只有 1 个。

【2. split 判据重设计 —— 这是本轮的主体】
  现象（用户截图）：part 32 携带 73.8G、9 个 extent，但 row 只有 797.8K、
  live size 624.8K、0 iops、0 B/s，策略每个窗口发一次
  `split part 32  size>53687091200 (est_live 73 GiB, lsm 0 MiB)`。
  **判据错在把"携带字节"当成 split 的触发器。** split 切的是 key range，跟着 key
  range 走的只有 LSM；大 value 躺在 log_stream 里，CoW split 之后两个孩子**指着
  同一批 extent**，不 major compact 根本分不开。所以对这种分区，split 买不到任何
  东西，只买到一笔 compaction 账单 —— 而那正是它每个窗口在建议的事。
  改法（`crates/manager/src/policy.rs`）：
  - **一条 split 建议必须点名一个 split 能缓解的瓶颈**，只有三个，各有各的度量：
    请求率（一分区=一个 P-log 线程，~30K ops/s → `req_per_sec`）、
    字节率（一分区=一条 log_stream，~350 MB/s → `write+read_bytes_per_sec`）、
    LSM 大小（compaction/memtable 的工作量，也正是切一刀真能减半的那部分 →
    `size_bytes`）。
  - `SPLIT_SIZE_HARD` → `SPLIT_LSM_HARD`，**阈值 50 GiB 一个字节没动**，只把它量的
    东西从 `effective_size_bytes`（含 payload）换回 `size_bytes`（LSM 常驻）。
    小 value 负载两个度量本来就重合，所以那类负载的行为**逐位不变**，只有 VP 的
    假阳性消失。
  - 新增 `SPLIT_BW_HIGH` = 175 MiB/s（实测单 log_stream 天花板 ~350MB/s 的一半，
    跟 QPS 阈值取"用掉一半预算"是同一条规则）。这条以前**完全没人看** ——
    `write_bytes_per_sec`/`read_bytes_per_sec` 收上来了但没有任何判据消费它，
    而 1400 ops/s × 1 MiB 对 `SPLIT_QPS_HIGH` 来说是空闲，对那条流来说是打满。
  - 携带字节保留它真正答得好的两个问题：速率触发器下面的**地板**
    （`SPLIT_SIZE_MIN`，太小的不值得切）和 merge 的**否决**（两个胖分区不能并成
    一个）。`PolicyCandidate.size_bytes` 仍然报携带字节（"它有多大"就是含 payload 的）。
  - merge 的 cold 判定补上 `MERGE_BW_LOW`（= SPLIT_BW_HIGH/10，与 QPS 同样的 10×
    迟滞）：只看 QPS 会把 1.4 GB/s 的一对判成冷。
  - `HOT_COLD_MIN_HOT_SIZE_BYTES` 原本写成 `SPLIT_SIZE_HARD/2`；它量的是携带字节，
    从一个量 LSM 的阈值推导是范畴错误，改成独立的 25 GiB（值不变）。

【3. split/merge 前先 compact —— 原来没做，现在做了】
  PS 侧 `handle_split_part` 在 `has_overlap != 0` 时直接拒（`cannot split:
  partition has overlapping keys`），而 manager **看不见这个标志**，于是策略每个
  窗口发一次必被拒的 split。（2026-09-10 修过"被拒也进冷却"，那是限流，不是收敛。）
  - wire 加 `PartitionLoad.has_overlap`。**刻意没有**用现成的
    `sst_out_of_range_bytes > 0` 去推断：那是越界记录的**字节数**，一个 has_overlap=1
    而共享表里恰好没有越界 key 的分区它读 0 —— 正是会被放行的那一种。
  - PS 侧 `PartitionMetrics.has_overlap` 做跨线程镜像，`PartitionData::set_has_overlap`
    是**唯一写者**（Cell 在分区线程上，report_load_loop 在主线程只够得着 metrics）。
  - `unblocking_compact()`：split（或 merge 的任一侧）本该发，但还 overlap，就改发
    `POLICY_KIND_MAJOR_COMPACT`，reason 写明它在给谁让路；受 `compact_inflight` 与
    `compact_cooldown_sec` 两道闸门约束。overlap 清掉后下一个 tick 拓扑动作自己回来。
  - 诚实地写在注释和文档里：若某个策略开了 split 却关了 compact，这条候选会被
    `kinds_from_switches` 滤掉、**什么都不会发生**。那是对的结果，而且是**看得见的**
    结果，不像原来的拒绝循环。（预置策略里只有 aggressive 开 split，它 compact 也开。）

【4/5. Nodes 与 Servers 两个 tab —— 它们存在是因为汇总答不了它们要答的问题】
  - `NodeCapWire.disks`（新）：逐盘 total/free/extent_bytes/online/faulted。节点级
    total/free 是这些行的和，而 `[空, 满, 满, 满]` 的节点汇总起来是"半空"。
    `faulted` 取自 manager 的 `faulted_disks`（= 节点自己说这块盘坏了），与 `online`
    分开：后者还混着"节点根本没应 df"，只有前者是关于**盘**的证据。
    数据一直在 df 里，`node_health_loop` 以前只求和，现在把逐盘行留在 `NodeCap` 上。
  - `GetClusterOverviewResp.ps_servers`（新）：**注册在案**的每一台 PS + 心跳年龄。
    从分区列表推导出来的服务器清单只能看见"当前拥有分区"的 PS，所以最值得看的
    两种状态恰恰是它表达不了的：一台什么都不服务的 PS，和一台停止心跳的 PS。
    `last_heartbeat_secs_ago = u64::MAX`（JSON null）= **本任期 leader 没见过**，
    渲染成 unknown 而不是 dead（新 leader 的心跳表是空的）。

【wire】38 → 39，`MIN = MAX`（本树纪律：停机升级）。三处加法：
  `PartitionLoad.has_overlap`、`NodeCapWire.disks`、`GetClusterOverviewResp.ps_servers`。
  feature_list 的 BUG-WIRE36-UNDEPLOYED 已更新：线上仍是 35，差距变成 35→39，
  性质不变（还是同一次停机升级）。

【测量】
  - 单测：manager lib 364 / partition-server lib 226 / rpc lib 59 全绿。
    policy 一族 62 条（新增 7 条）。
  - 集成：`system_merge --include-ignored` 12/12（103 s）、`system_split_overlap` 4/4。
  - **消融两条，各自变红**：①硬触发器指回 `effective_size_bytes` ⇒
    `carried_payload_alone_does_not_advise_a_split` 红；②删掉 `split_candidates` 里
    的 has_overlap 分支 ⇒ `overlapping_partition_gets_compact_instead_of_split` 与
    `unblocking_compact_respects_inflight_and_cooldown` 红。
  - **真集群 e2e（第 3 点的关键一环）**：单节点 + 双盘 EN，写 60 个 key、compact、
    `autumn-op split 10` → 两个孩子（10 / 14）**都**报 `has_overlap=1`
    （oor_bytes 248477 / 247680），说明新字段真的从 PS 穿过 wire 到了 manager 再到
    `info --detail`；两边各 `compact` 一次后 **5 秒内双双回到 0**。
  - dashboard 三条：`api_contract.sh`（真集群：页面字节 = 磁盘上的
    index.html、/api/ops 两列、/api/overview 的 ps_servers + 两块盘、
    /api/partition 的 has_overlap）、`render_check.js`、`tabs_smoke.js` 全过。
  - clippy：改动区域零新增告警。

【独立评审(CLAUDE.md 规则 15)】
  **fable 跑不了**：配额在本会话已耗尽（reset 还有 ~23 小时）。**coco 也跑不了**：
  Trae access token 过期（"log out and sign in again"），GPT-5.5 / GLM-5.1 两个模型
  都失败。改派一个独立 agent（另起上下文、对抗式 prompt）评审，抓到 15 条，
  **全部处理**。⚠️ **fable 那一遍仍然欠着**，配额恢复后应当补跑。

  【高危 2 条，都是"我的理由和代码对不上"】
  1. **merge 侧的 compact 是按一个并不存在的前置条件写的，而且那句话会直接印给运维。**
     我实测坐实了：两侧 has_overlap 都是 1 的一对**照样合并成功**，60 个 key 全读回，
     幸存者 reopen 后自己把标志重算成 0；全树唯一的 has_overlap 闸门是
     `handle_split_part`。而我的 reason 文案写的是"merge is refused until…"。
     改法：①文案按 op 分叉（split=硬前置/merge=卫生，明说"not refused"）；
     ②**只 compact 幸存者** —— 受害者马上被 merge 删掉，compact 它是白干
     （这是评审算出的真实代价，不只是措辞问题）；③新增断言:reason 必须含
     "not refused"，且 overlap 的**受害者**不得产生 compact。
  2. **被挡住的 split 在 compaction 期间完全看不见了。** 三条兜底
     （unblocking / major / minor）全被同样的闸门挡掉 ⇒ `policy-candidates` 对
     集群最大的分区**一行都不显示**，比改之前（至少还有一行 SPLIT）更差。
     改法：`unblocking_compact` **只看 compact_inflight，不再看 compact 冷却** ——
     冷却是为了别对没变化的**债务水位**重复建议，而 has_overlap 是**标志**，
     一次 compaction 跑完还留着它就说明没干成，等完剩下的窗口毫无意义。
     消融：把冷却闸门加回去 ⇒ 新测试变红。
     **剩余缺口如实写在注释里**：compaction 真在跑的那段时间仍然没有 advisory 行，
     状态只在 `info --part N --detail` 和面板 drawer 里。

  【中危 8 条，全部是注释/文档说过头，本仓库把这个当真缺陷】
  - "has_overlap == 1 就是让下次 compaction 变成 major 的原因" —— 错，major 来自
     `CompactTask.is_major`，是 `MSG_MAINTENANCE` 写死 true；has_overlap 只是压掉
     "表太少就跳过"。改成如实写，并点明：将来若加 minor 档的 compact op，这个环就不收敛了。
  - "被拒的 split 要付一次完整的 freeze-drain" —— 错，那个检查是 handler 解码后的
     第一句，只花一个 RPC 往返。理由改成真正成立的那条（它是永远无法被采纳的建议）。
  - "这条注意事项已经写进 reason 文案" —— 并没有。改成如实说它在 docs/ops.md。
  - docs/ops.md 里旧的那句"policy-candidates 用 est_live 判 split"就在新段落上面三行，
     两句话互相打架 —— 删旧的。
  - `SPLIT_SIZE_HARD` 改名后还有三处残留（policy.rs 的 hot/cold 文档、
     key_namespace_split_design.md §5 的阈值表和 §3.3、被 cfg(any()) 排除的旧测试）。
  - **"sustained"比听起来弱**：PS 报的是 5 秒均值、bucket 60 秒 last-wins，
     所以"N 个 bucket 全触发" = **相隔一分钟的 5 个 5 秒采样**（300 秒里只覆盖 25 秒），
     不是积分。这是从 QPS 触发器继承的走样，但大 value 的字节率比请求率更抖，
     新触发器更依赖它 —— 写进常量文档。
  - `MERGE_BW_LOW` 是对**整个 pair 且双向**求和：10× 迟滞对的是**合并之后**那个
     分区（正确的比较对象），对单侧是 20×。文档没说清，补上。
  - **"小 value 负载行为逐位不变"不精确**：`est_live` 还算上 log_stream(WAL) 与
     meta_stream 的字节，所以硬触发器对所有负载都**同点或略晚**，不是零变化。
     改成如实描述，并说要校准得先在 perf 机上量。
  - merge 的 reason 文案漏了新的字节率闸门，DryRun 看不出是哪一维拦的 —— 补上。

  【低危 5 条】
  - `NodeCap.disks` 少了 `apply_df_disk_health` 那道**节点归属过滤** ⇒ 一个 sentinel
     写错的盘会显示在别的节点名下。补上同一道过滤。
  - `DiskCapWire.faulted` 的文档声称它与 `!online` 不同 —— 在**逐盘**这一层今天它们
     是同一个 bit（都来自同一份 df），页面那个三态 pill 因此有一支永远走不到。
     修法不是删掉第三态，而是**让它变成真的**：新增 `reported` 字段 ——
     "注册表里属于这个节点、但节点这次 df 根本没提它"（运维把某个 --data 目录去掉了）。
     这也顺手补上了评审推断的 LOW-13（我验证了它的前提是错的：`DiskFS::open` 在
     EN 启动时是 fallible 的，开不了的盘会让整个节点起不来，不会静默消失）。
  - 我自己新加的断言又抓到一处：未上报的盘在 autumn-bytes 那一列还印 `0B`（假零）—— 改成 —。
  - 面板两个 drawer 在对象消失时只清了选中态、没清内容 ⇒ 已下线的 PS/节点详情
     永远留在屏幕上。加 `clearDrawer`。
  - split 冷却也会把"分离用的 compaction"压住一小时（评审说 arguably fine）——
     行为不动，写进注释。

  【评审判定为干净的四块】RefCell 借用（两个 compose 函数都是同步、无 await、
  各只有一个调用方）、wire 改动完整性（所有字面构造点逐一核过）、has_overlap 镜像
  （set_has_overlap 确是唯一写者，初始 store 在唯一构造点上）、cooldown_key 去重
  （当前候选集无碰撞，且顺序正确）、面板 danger 门控与定时器。


【本会话后续(提交 e05549c 之后)】
  - 用户要求 git pull:origin/main 多 15 个提交(stream 抢先 roll quiesce 修复、
    PS 诊断、rpc 提交队列有界等)。仓库配置 ff-only ⇒ 把本地这 1 个提交 rebase 上去,
    现为 **fe82703**(父 1b7bace)。唯一冲突是本文件:上游是**叠放的日期条目**,
    其 (4) 是一次**未关闭的数据丢失事故**(含 GC 门槛待办),所以按叠放解决,
    上游五条全部原样保留。两个自动合并的源文件与本改动无语义交集
    (上游那边是 PS delegate 路径的诊断等待 tick + manager 一行注释)。
    上游 8 个文件都不碰 PartitionLoad/policy/dashboard/WIRE_VERSION ⇒ 39 仍是唯一一次抬版本。
  - 用户指出进度行显示 `69% · 11895046144 / 17179981824` 无法阅读。根因:线上刻意
    传原始计数、由渲染方负责单位,而 CLI 的 `human_bytes_or_count` 只认 gc/forcegc,
    漏了同样按**字节**计的 ec-convert 与 recovery(正是 16 GiB extent 那一行);面板则
    完全没有这个函数。修:CLI 与面板(`fmtProgress`)都按 kind 选单位 ——
    字节(gc/forcegc/ec-convert/recovery)/SST 数据块(compact)/阶段(split/merge);
    后缀改 IEC(KiB/MiB/GiB,除数是 1024)。现显示 `69% · 11.1 GiB / 16.0 GiB`。
    CLI 注释原写 compaction "counts tables",实为 data blocks,一并改正。
  - 自查发现:去掉 unblocking_compact 的冷却闸门后,auto-policy 循环从 60 s 才重建一次的
    advisory_cache 里取候选、却最快 2 s 一跳,而 `sanitize_entry` 允许 `cooldown_sec = 0`
    ⇒ 同一条 compact 一个窗口内可被重复下发 ~30 次。按文件里已有的
    `REBALANCE_MIN_ACTUATION_COOLDOWN_SEC` 先例加 `COMPACT_MIN_ACTUATION_COOLDOWN_SEC`
    (60 s,低于所有预置策略的 120-240 s,只拦误配置)。消融:去掉即红。

【fable 独立评审(规则 15)】
  评审 fe82703 + stash@{0} 全部内容:无高危;中危 4、低危 4。H1/H2 修复、merge 幸存者路径、
  node_disks 归属过滤、wire 完整性均判定正确。全部已处理(本提交):
  - M1 compact 执行下限只按 MAJOR 键,而 minor 候选执行的是同一个 compact op ⇒
    cooldown_sec=0 时照样风暴。下限改为覆盖两种 compact kind。消融:只留 MAJOR ⇒ 新增的
    minor 断言变红。(第一次消融用 sed 把 candidate_to_cmd 里同样的两词也改掉了,minor 根本
    不下发,于是假绿;改成锚定下限臂独有一行、计数断言的精确替换后才坐实。)
  - M2 节点不应答 df 时,manager 把注册表里它的每块盘都补成 "not reported",页面再把原因写成
    "启动时漏了 --data 目录" —— 一台宕机被说成 N 块缺盘。改为:不应答 ⇒ 无盘行;页面明说
    "盘状态未知"。manager 侧新增 3 条单测 + 页面断言,两半消融各自变红。
  - M3 文档与两个 JS 夹具引用的 advisory 文案代码从不输出 ⇒ 改成真实输出,漂移可被测到。
  - M4 PS 指南说 size_bytes 无人写 —— 实为每 30 s 由 lsm_resident_bytes() 刷新
    (SIZE_REFRESH_INTERVAL 已核),且正是 SPLIT_LSM_HARD 的输入。
  - 低危:"新 leader 心跳表为空"的状态实际不出现(replay/register 都会预置);去掉没人上报的
    rebalance "moves" 单位;CLI 与页面追加同一套单位词;文档字段表补 reported/uuid。
  - L1(空表跳过路径不清 has_overlap)今天不可达:唯一的 CompactTask 生产者写死
    is_major:true,定时器也不再 compact ⇒ 按"今天不可复现不修",只在 unblocking_compact
    文档里记下这层耦合。

【最终状态】
  - 第二次 pull:上游又多两个提交(f440c40 账本新条目、08d4287 根 CLAUDE.md 重写)。
    rebase 后本地 main = 688b32d + fbea57a,领先 origin 2 个,未 push(用户只说了 pull)。
    冲突只在 feature_list.md —— 上游新增两条并保留 F-SPLIT-NEEDS-COMPACT,而本次把那条
    实现掉并删除。解法:四条新条目全留、删掉已实现那条、修好指向它的交叉引用,并给
    F-SPLIT-ADVICE-COUNTS-UNRECLAIMED 补一段**状态**(需求原文未动)——
    幻影体积不再是 split 的触发器,但①真集群验收未复验 ②根因(GC 门槛)未动
    ③`est_live` 仍把未回收垃圾全额计入,所以 merge 的否决与速率触发器的地板
    仍按幻影体积判断。上游那条"先修 GC 门槛"的结论依然成立。
  - 根 CLAUDE.md 新增规则 17(只有用户手改该文件)—— 本会话未碰它;规则 15 的
    "每次派新的 fable subagent、不要续用旧评审"本会话是照做的。
  - 全量(rebase 之后重跑):workspace check 零错误;rpc 62 / manager 367 /
    partition-server 226 / autumn-op 31;render_check、tabs_smoke、真集群 api_contract 全过。

Main gaps:
  - fable 对其发现所做的修复本身未再评审(M1/M2 已消融验证,其余为文档改正)。
  - coco 评审路径:用户 2026-09-11 令删除 coco skill,已删
    (`~/.claude/skills/coco-review`,备份在本会话 scratchpad)。评审此后只走 fable。
  - 账本里未实现的四条:本次新立两条(F-MERGE-COW-STALE-SEQ 未复现先写 harness、
    F-SPLIT-CARRIED-BYTES-UNBOUNDED 先量再做)+ 上游两条
    (F-SPLIT-ADVICE-COUNTS-UNRECLAIMED、F-EC-STARVES-FOREGROUND-APPEND)。

Next steps:
  1. 用户要求时 push(git push origin main)。
  2. 部署注意 wire 39 vs 线上 35(BUG-WIRE36-UNDEPLOYED)。
  3. 上游 (4) 的 GC 门槛待办(BUG-GC-ADVISORY-VS-SELECTION)仍开着。

Date: 2026-09-11 (4)
TaskStatus: not_completed
Task scope: 事故复盘 + 待办。写路径三个 bug 已修并上线(947bae0);数据损失已发生且不可逆;
  模型正在重新下载转换。GC 回收门槛的问题已定位但**未修**。

【本次数据损失的完整链条】
  under-seal(抢先 roll 封存在已 ack 之下,极端情况封在 0)
  → extent 变成 sealed && sealed_length == 0
  → sealed-empty sweep 认定"没有已 ack 的字节",合法地 punch + 删除
  → SST 里的 VP 还指着它 → 读报 "extent N not found"
  manager/CLAUDE.md 里对这个清理逻辑的安全性有白纸黑字的预警:
  "这是'不存在 under-seal 这类 bug'的断言,不是结构性保证;一旦复发,
   它会把'一个吵闹的卡死、字节还在盘上'变成'一分钟内 unlink'"。就是这样发生的。
  损失:三个模型(dsv4-flash / minimax-m25 / bge-m3)的大文件全部不可读,已清空重下。

【待办 1:GC 比例门槛对大 extent 过高(先前就存在,非本次引入)】
  log extent 固定 16 GiB,删数据后每个里死 1-3 GiB → 比例 0.06-0.19,
  默认门槛 0.4,于是一个都不够格;而 gc_debt 报的是真实死字节,
  于是 advisory 一直喊、GC 一直答 "no eligible extents to reclaim"。
  判据:`autumn-op gc --ratio 0.01 <part>` 立刻开始重写 → 确认是门槛,不是冷却、不是 EC。
  代码注释里已记录过这个现象,但上次只修正了指标(collectable_debt 也套门槛),
  没动门槛本身。方向:门槛应与 extent 大小挂钩(死字节绝对量也能触发),不要纯比例。
  临时手段:--ratio 0.01 手动跑,已用它回收了约 50 GB。

【待办 2:EC 与前台 append 的资源争用】
  单个转换把 EN 推到 ~950% CPU,append 从亚毫秒涨到几百毫秒。丢唤醒/卡死修好之后
  它不再致命,但仍会让写入变慢。未修。

【已验证的修复(30 分钟观察,负载下)】
  没有新的 sealed_length=0 出现(稳定 39)、没有 stale-extent WARN、
  大文件读始终正常、未读请求字节归零。

Next steps: dsv4 转换完成 → minimax(要等 nvme 空间)→ bge-m3 → 开 split+EC 做最终验证。

Date: 2026-09-11 (3)
TaskStatus: completed
Task scope: 找到并修复分区永久停止应答的真正根因——抢先 roll 不 quiesce。
  同一竞态还藏着一个更严重的 P0:静默丢弃已 ack 的数据。两半都有消融证明的测试。
  commit: 48c53b5。前两次诊断(worker 生命周期、imm 丢唤醒)一个是真 bug 但不是这个
  现象的因,一个被证伪已回退(d7bb21e)。

【根因】
  抢先 roll 在一次成功 append 的 end 达到 16 GiB 时触发,直接封存并换 tail,不排空。
  而 PS 在一条 stream 上并发跑 8 个 phase2,滚动方只是其中之一。同一时间窗两侧:
  - 在 ResetTail **之前**完成的兄弟:被连续前缀规则 ack 成功,offset 却在封存点之上。
    WAL 重放按 sealed_length 截断 → 已 ack 的字节在恢复时不存在。**静默丢数据**。
  - 在 ResetTail **时仍在飞**的兄弟:完成后被套用到新 extent 的状态(commit=0),
    落进 pending_acks 里一个永远够不到的 key,调用方的 oneshot 被永久扣住。
    PS 的 phase2 是无界 ack_rx.await → 分区停止应答 → 连接不再读 socket。

【为什么之前三次都找错】
  没有日志(这条路径只打 debug)、读正常(读不走 worker)、EN 空闲(早就 ack 完了)、
  撤掉负载也不恢复(sender 躺在活着的 map 里)、1600 秒超过所有下游超时(等的是
  BTreeMap 里的一个 oneshot,不是任何 I/O)。每一条都把我推向错误的层。
  真正收窄范围的是那条"分区收下请求后不回复"的诊断。

【修法】
  抢先 roll 走 SealCommit 排空(和其余六个发送点一致)。apply_completion 拒收
  extent_id 不是当前 tail 的完成——这是**可达的**兜底(软错误 tail 重载无法 quiesce,
  而且本次改动放大了它的窗口),不是不变式,注释里写明了。

Main gaps: EC 转换与前台 append 的资源争用未修(单个转换吃 9.5 核);FTW 上传待重跑。
Next steps: 构建 → 三 PS 并行重启 → 重跑上传 → 按用户要求开 EC 做验证(判据已先写死)。

Date: 2026-09-11 (2)
TaskStatus: completed
Task scope: 找到并修复分区永久卡死的真正根因——per-stream worker 的生命周期与它的
  "tail 已初始化"标记不是同一条命,而且退出的 worker 会注销掉自己的继任者。
  后果是三副本各截掉 1.7 GiB。已修、fable 独立评审、评审项全修、两处消融各自验证。

【线上现象】
  - 三个副本在同一时刻各丢 1.7 GiB,append 带 commit=0;随后 manager 把该 extent
    封在 8 MiB,而节点上有 3.3 GiB(UNDER-SEAL)。
  - 分区停止应答写入,客户端每分区堆 100-220 MiB 未读请求;无任何 WARN/ERROR。

【机制(评审纠正了我第一版的说法)】
  没有任何东西"把 commit 归零"——零从来没被覆盖过。新 worker 出生即 cursor=0,
  而唯一会把 cursor 抬到已提交长度的 SeedCursor 就在被跳过的那次初始化里。
    split → invalidate_stream(清 worker + 标记) → append spawn worker#2,初始化完成
    → worker#1 这才排空退出,按 key 删掉了 #2 的条目
    → 下次 append spawn #3,看到标记仍为 true 就跳过初始化
    → 第一个 append 报 "no tail set" → 被当软错误重载 tail → 重试在 cursor 0 处 lease
    → header.commit=0 → 每个副本照做,截断。

【修法】
  - 每个 worker 带 generation,两处删除都以它为门。
  - 顺序是承重的:把"清标记"挪到 generation 检查之前,所有显而易见的测试仍然全绿,
    但会让一个活着的 worker 在自己脚下重新初始化——两个 worker 用各自的 cursor
    写同一个 extent,比原 bug 更糟。已用测试钉死这个顺序。
  - "no tail set" 改为 fail-fast,不再软重试:下次再出生命周期 bug 会报错而不是截断。

Main gaps: 已卡死的 3 个分区需重启清除;FTW 上传停在 dsv4 8/52、minimax 29/34。
  EC 转换与 append 的 fsync 争用未证实(盘 34% util、CPU 1%),不作为根因。
Next steps: 构建 → 三个 PS 并行重启 → 换新 autumnfs 重跑两个 FTW 上传。

Date: 2026-09-11
TaskStatus: completed
Task scope: 修上传永久卡死——put_bulk 是唯一没有 deadline 的数据面调用;顺带让
  autumn-rpc 的 submit 队列的界第一次真正生效。已实现、fable 独立评审、评审项全修、
  ablation 验证。commit: dec82be(修复) + 50b649d(硬化)。

【要点】
  - 根因不在客户端一侧。实测:12 个上传的发送队列全满(各 1-4 MiB,recv 0)、30 秒内
    0 次上下文切换;而同一批连接的 PS 那头,92 条 socket 各压着约 4.6 MiB **没读走的**
    请求字节,进程 0% CPU。ps-1 与 ps-2 各积压 385 MiB,ps-0 干净。
    所以是 **PS 停止读取**,不是"回复丢了"。这一条本次没有修。
  - 客户端修的是"对这种停摆的反应":put_bulk_opts 直接调 call_vectored_bulk 而不走
    ps_call_with_timeout,于是 rpc_timeout 完全没生效,await_response 是裸 rx.await。
    停摆的服务端 = 永久停摆的客户端。现在 30s 超时 → 可重试 → MAX_PS_REFRESHES 后放弃。
  - submit 的界一直是假的:borrow().clone() + send().await,而 futures::mpsc 给每个
    sender 一个 buffer 之外的保留槽,所以每次调用自带一个槽,队列在**数量上无界**增长,
    然后调用方 park 在随后的 flush 上。park 在消息已入队之后 = 既不能进也不能退。
  - 评审抓到两处我写错的注释:stream 侧"worker park 住就不再消费 CQ"这个机制**不存在**
    (回复由连接自己 spawn 的 read_loop 消费);以及我引用了一个**从不存在的测试**。
    两条都已删除。教训同上一轮:推断要标成推断。

Main gaps: PS 为什么停止读取仍未定位。metrics 端点是空的,没有 inflight 计数。
  ps-0(6h 前重启过)干净,另两个已跑 2d3h ——重启大概率能解,但那是治标。
Next steps: (1) 定位 PS 侧停摆(怀疑 ps_conn_inflight_cap 达到后某个 in-flight 永不完成);
  (2) FTW 上传要靠新二进制或 PS 重启才能推完。

Date: 2026-09-10
TaskStatus: completed
Task scope: chaos 加入多 node 多 disk（用户 2026-09-10 定："chaos测试要加入多node多disk"）。
  一并交付的是这次多盘 chaos 直接测出来的两个缺陷 —— 一个在 harness 里（假阳性），
  一个在 manager 里（recovery 的 op 台账永远停在 running）。

【为什么这件事值得做:今天的盘级代码从没进过 chaos】
  harness 给每个 EN 只有一个 `--data` 目录。于是 9 月 9-10 号做的三件事 ——
  节点/盘两级放置(`choose_disk`)、坏盘触发重建、盘错误分类 —— 全部只有单测,
  一次都没在 nemesis 下跑过。"一台机器有多块盘"是 EN 自己划的界,
  harness 不划这条界,那条界后面的代码就永远不会被踩到。

【改法】
  `EnProcess.data_dir: PathBuf` → `data_dirs: Vec<PathBuf>`;每个 EN 一个 tempdir,
  里面 `disk{k}` 子目录当独立盘;`autumn-op format d0 d1 ...` 一次注册全部
  (它往每个目录都写同一个 node_id,所以 sentinel 读 `data_dirs[0]` 即可);
  EN 用 `--data d0,d1` 起。`find_extent_dat` 改成搜该节点的每一块盘 ——
  extent 落在哪块盘是节点自己的选择,harness 不能假设。
  旋钮 `AUTUMN_CHAOS_DISKS_PER_EN`(默认 2,`.max(1)`),设成 1 就能和单盘形态做 A/B。

【测出来的第一个:harness 自己的假阳性】
  seed 583 多盘跑失败,报"extent 25 在 node 16 上被腐化,它自己的节点 180 秒内
  从没说过,读会从这份坏副本上返回"。真相在 EN 日志里:
  `delete_extent: unlinked .dat + .meta extent_id=25` —— GC 在 scrub 走到 block 0
  之前就把这个 extent 回收了。**断言在要求一份已经不存在的字节被发现。**
  修法:注入时记下 `.dat` 路径和写进去的那 64 字节;验证时**先查日志**
  (所以"报了然后被删"仍算发现),再把剩下的分成"坏字节还在"(真发现)
  和"坏字节没了"(大声说明这次注入什么都没测到)。
  同一 seed 重跑,新分支如期命中并通过。

【测出来的第二个:recovery 的 op 台账永远停在 running(真 bug,已修)】
  同一轮还报 `op ... kind=8 target=0/20 still ACTIVE (state=1) after quiesce`。
  `note_recovery_dispatch` 每个 extent 只开一条台账、且只**复用**活着的那条;
  而 `apply_recovery_done` 有**五个**出口既不应用重建、也不留下任何还会再报的人:
    - EchoOfAppliedWork(重建早已落地,这是重发回声)
    - ReleasedAttempt(marker 被释放而执行者还在干)
    - 目标已在别的 slot(EC 转换中途改了布局)
    - replace_id 已不在该 extent
    - extent 在重建落地前被删
  五条全部把台账留在 RUNNING,而 `sweep_running_ttl` 按设计只收
  COMPACT/GC/FORCE_GC —— recovery 只要修复还没完成就该一直重试,不能按时间收。
  于是 `ops status` 对一个早已结束的 recovery 永远回答"running"。
  修:回声 → `complete_recovery`(SUCCEEDED,它描述的活确实成了);
  其余四条 → 新的 `abandon_recovery_entry`(FAILED + 原因)。
  **刻意不动的两条**:EC 转换在飞时的 defer、以及 DifferentAssignment ——
  这两条都留着一个活 marker,它自己的执行者还会来报。
  消融:把 recovery.rs 存起来跑新测试,五条出口**全部**报 `state=1`;
  加回修复,五条各自到达预期终态。

【测出来的第三个:rot 覆盖率断言把两种原因混成一种(harness,已修)】
  旧断言是"CorruptReplica 被选中却一次都没注入 ⇒ 本轮失败"。但同一个文件自己写着
  "a decline is per-tick, one round proves nothing" —— 两句话互相矛盾。
  拆成两种原因:`rottable_replicas` 现在返回 `RotTargets { ready, reachable }`,
  `reachable` 数的是"sealed + 多副本 + **至少有一个活着、没被 fence、没被隔离的持有者**"。
  reachable>0 而一次都没注入 = 有可注入的目标却没有任何节点 describe 过它 ⇒ 真失败;
  reachable==0 = 这一轮根本没有可用目标 ⇒ 大声记一笔,不判失败。
  **我第一版是错的,而且是同一类错**:只数了 sealed 的形状,没过滤持有者是否可达 ——
  那会在"唯一那个 sealed extent 的副本当时全被 fence 了"的轮次里去指控产品。
  是换了更细的仪器之后自己看出来的(第一版重跑 seed 66 报"1 个 sealed 可用",
  加上可达性过滤后 seed 66 直接过了)。

【测量(评审修完后重跑)】
  多盘 chaos,45 秒轮:**9/9 全过** —— 603、66、583、11、991、22、33、44、55。
  603 和 66 之前挂的正是第 3 条那个采样时刻问题,重新采样后自动消失。
  消融两条各自变红:①整个 recovery.rs 修复撤掉 ⇒ 五个关闭出口全红且各自报出自己的原因;
  ②只撤掉 Echo 的暗-slot 守卫 ⇒ 只有新加的那个 case 红。

【评审前的测量(留作对照)】
  多盘 chaos,45 秒轮:seed 583、991、11、22、33、44、55、66 PASS;603 FAIL。
  603 用 `AUTUMN_CHAOS_DISKS_PER_EN=1` 跑**同样失败**,所以与盘数无关。
  603 的主因已定谳并**三次独立复现**:CorruptReplica 与 EcConvert 撞在同一个 extent 上,
  三次都停在 `EC marker still pinned after quiesce (age 78~79s)`、`mismatches=0` ——
  见 feature_list 的 BUG-ROT-BLOCKS-ITS-OWN-REPAIR,docs/ops.md 也标了 KNOWN RED。
  单测:autumn-manager lib 359 全绿;集成 recovery_loop_drives 5 / corrupt_replica_rebuild 4 /
  offline_reconcile 4 / ec_integration 4 / scrub_isolates_rot 1 全绿(要用
  `--include-ignored`;`--ignored` 是只跑被忽略的,我先跑错过一次,0 passed 全 filtered)。
  改动区域 clippy 0。

【fable 评审(配额恢复后跑成)抓到 4 条,全部已改】
  1. **我夸大了覆盖范围(文档 + 注释)**。两块"盘"是同一个 tempdir 下的子目录、
     同一个文件系统 ⇒ `disk_stats()` 相同、全程 Online,而且没有任何 nemesis 弄坏盘。
     所以 `Full`/`Faulted`/"只重建那块盘的副本"**根本没被覆盖**,而我在 docs/ops.md
     和结构体注释里写了"已覆盖"。改成如实描述,并把缺口指向 F-CHAOS-DISK-FAULT。
  2. **rot 校验的"损坏已消失"桶把两件事混了**。`still_damaged()==false` 可能是
     文件没了、被截短、或字节被改写,三者不可区分;而如果 etcd 里 extent 还在、
     该节点还在 replicates 里、slot 的 avali 位还亮着,那就是"布局仍指着它,
     而文件在下面变了,没有任何一层吭声" —— 比这次注入要找的 rot 更严重。
     现在只有"extent 已不在 etcd"或"该节点已不是持有者/slot 已暗"才算无害。
  3. **覆盖率断言的采样时刻会说谎**。EN 学到"旧 tail 已 seal"主要靠 scrub 的
     manager 探测,而它的每-extent 退避是 8→16→32→64…tick(1 tick/s):一个开了
     ~30 秒的 tail 已在 +0/+8/+24 探过,下次要到 +56。所以轮末那一 tick 滚出来的
     extent,合法地在 20 秒内不会被 describe —— 拿那一刻的采样去指控产品是错的。
     改成**在 settle 之后重新采样**,`ready` 仍为空才指控。
     **这正是 seed 603/66 那个"可达却从没被 describe"的复现的真因。**
  4. **测试能因为错误的原因通过**。五个出口里四个都终结成 FAILED,只断言 state
     的话,万一 `_test_mark_recovery_inflight` 没钉上,case 3/4/5 会一起掉进
     ReleasedAttempt、照样 FAILED、照样绿 —— 三个出口等于没测。
     改成每个 case 断言**只有该出口会写的那句原因文本** + `Result` 变体,
     并补上两个"故意不关"的出口(EC 在飞、DifferentAssignment)断言仍是 RUNNING。
  另外评审给出一个我采纳的真缺陷:台账按 **extent** 键,不按 slot,所以同一 extent
  上另一个 slot 派发失败时 `record_recovery_failure` 会开一条无 marker 的 RUNNING 条目,
  而我的 Echo 分支会把它写成 SUCCEEDED、连带清掉那条失败原因。
  修法:Echo 只在该 extent **所有 slot 都可用**时才判成功;还有暗 slot 就说明活没干完,
  留给派发循环自己刷新。补了消融:单独去掉这个守卫,只有新加的那个 case 变红。

Main gaps:
  1. BUG-ROT-BLOCKS-ITS-OWN-REPAIR(新记账,`passes: false`):腐化的 extent 若正带着
     ConvertToEc marker,recovery 派发会跳过它,而 EC 转换又必然在内容校验上失败 ——
     只能等 24 次连续失败的给弃。会自愈,但把"尽快修腐化"变成"先等 EC 放弃"。
     **未验证**:79 秒时 marker 仍在≠它永远不走;给弃计数会不会在 marker 重取时归零,
     没测。定级前先补这条测量。
  2. `corrupt` nemesis 有一定概率"什么都没测到"(注入的 extent 被 GC 回收,
     或 20 秒内没有任何 sealed extent 被 describe)。现在会大声说明,不再假阳性,
     但也意味着单轮的 rot 维度覆盖是运气。
  3. F-DISK-FAULT-CLASSIFIER 另一半(EN 上报 online:false 前自证一次)仍未做。

Next steps:
  1. 把多盘 chaos 跑成常规:decommission_chaos.sh / vphead_chaos.sh 都走同一个 harness,
     确认它们在 DISKS_PER_EN=2 下也稳定。
  2. BUG-ROT-BLOCKS-ITS-OWN-REPAIR 先补测量(给弃到底几秒),再选 Scope 的 (a) 或 (b)。
  3. 加一个只针对盘的 nemesis:把某个活节点的**一块**盘弄成 Faulted,
     断言只有那块盘上的 slot 被重建、而节点仍是成员 —— 今天多盘只是让既有 nemesis
     跑在多盘形态上,盘级故障本身还没有专门的 nemesis。

─────────────────────────────────────────────────────────────────
（以下三条是 2026-08-30 那轮 chaos soak 与后续会话留下的自省/否决记录，与当次任务状态无关，
  单独保留是因为它们讲的是**方法**，不是某个已完成的改动。）
【本人两次仪器错误,同一形状,记以自省】
  1. 用 `ops history` 证伪 EC 假设 —— auto-policy 的 EC 转换绕过 op ledger,该工具对它天然失明。
     **空结果来自看不见目标的仪器,和阴性结果长得一模一样。** 被 subagent 顶回来,它是对的。
  2. 用 `dd 2>/dev/null | wc -c` 探测 —— I/O 错误和 0 字节读都得到 0,**无法区分**。
     据此断言存在"静默截断",并让 subagent 花两轮论证一个伪事实。
  两次都是"失败伪装成成功",而这正是我当天一直在别处指出的模式。
  `2>/dev/null` 之于 dd,等同于 `e.to_string()` 之于 PS 的错误链:**都丢掉了区分失败与缺席的通道**。

【2026-09-02 更正:FreeToken 从未挂起 —— 我的第三次同型仪器错误】
  事实: MiniMax 的加载**成功完成了**。串行专家构建 09:28:39 开始、09:49:38 打出
  "Free memory after initialization: 1.84 GiB" —— 21 分钟,慢但正常。
  我的错误: 用 `rchar` + 5 秒 CPU 采样判定"完全静止、0 I/O",据此宣布挂起。
  但串行路径走的是**文件 mmap**(expert_banks.py 注释明写 "reclaimable file mmap"),
  而 **mmap 的缺页不计入 rchar**。仪器看不见那种活动。

  三次同型错误,记在一起因为它们不是三个偶然:
    1. `ops history` 看不见 auto-policy 的 EC 转换(绕过 op ledger)-> 误证伪了正确假设
    2. `dd 2>/dev/null | wc -c` 分不清 I/O 错误和 0 字节读 -> 误报"静默截断",
       还让 subagent 花两轮论证一个伪事实
    3. `rchar` 看不见 mmap 缺页 -> 误报"FreeToken 挂起"
  共同形状: **用一个对目标现象天然失明的仪器测量,把空结果当成阴性结果。**
  而这恰恰是我当天一直在别处指出的那个模式(失败伪装成成功)的镜像版本。
  可操作的教训: 断言"什么都没发生"之前,先问这个仪器**能不能看见**要找的东西。
  具体到本仓库: 进程级 I/O 用 `rchar` 不用 `read_bytes`(FUSE 不走块设备),
  但两者都看不见 mmap —— 那要看 `minflt/majflt`。

【2026-09-03 --direct-read 已重测（早先那批数字曾被判作废）】
  作废的是 a83a1be（EC 尚不支持直读）上的数字：false 76 MiB/s vs true 22，慢 3.5 倍。
  `1de0005 feat(ec): serve direct reads from EC extents` 之后前提已不成立。重测（同集群、
  3 块真盘、EC 2+1、同一个 4 GiB 文件）：**direct-read=true 现在更快**——
  autumnfs CLI 1552~2053 vs false 769；fuse mount 936 vs false 498。默认开着是对的。
