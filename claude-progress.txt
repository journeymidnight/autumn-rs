Date: 2026-05-12
TaskStatus: not_completed
Task scope: F195 — eliminate env::var reads from production rs code,
            route all config via CLI args. Multi-wave migration.

Wave 1 (manager) — DONE this commit:
  - AutumnManager fields: report_disk_failure_window: Cell<Duration>,
    report_disk_failure_quorum: Cell<usize>. Defaults 60s / 3
    (match F192 env defaults).
  - Builder: set_report_disk_failure_config(window, quorum).
  - handle_report_disk_failure reads from self fields, no env::var.
  - Manager binary parses --report-disk-failure-window-secs and
    --report-disk-failure-quorum, applies via builder.
  - 4 F192 unit tests still pass (they use defaults).

Waves remaining:
  Wave 2 — autumn-stream:
    client.rs: BAD_NODES_TTL_SECS, INFLIGHT_CAP, APPEND_TIMEOUT_MS,
               READ_CHUNK_BYTES, SYNCED_POLL_MS, SYNCED_TIMEOUT_MS
    extent_node.rs: EC_CONVERT_PARALLELISM, RECOVERY_PARALLELISM,
                    INFLIGHT_CAP
  Wave 3 — autumn-partition-server (~16 vars):
    GROUP_COMMIT_CAP, INFLIGHT_CAP, BULK_INFLIGHT_CAP, MAX_IMM_DEPTH,
    MAX_WAL_GAP, SHUTDOWN_TIMEOUT_MS, MAJOR_COMPACT_PARALLELISM,
    FG_RATE_BYTES_PER_SEC, BG_RATE_BYTES_PER_SEC,
    FG_SATURATED_THRESHOLD, FG_QPS_QUOTA, GC_DEBT_HIGH_BYTES,
    COMPACT_PENDING_HIGH_BYTES, GC_COOLDOWN_SECS,
    COMPACT_COOLDOWN_SECS, CONN_INFLIGHT_CAP, MIN_BATCH,
    GC_READ_CHUNK_BYTES, GC_BATCH_RECORDS, GC_BATCH_BYTES,
    GC_RATE_BYTES_PER_SEC
  Wave 4 — autumn-rpc + binary-level:
    AUTUMN_RPC_FRAME_V1 (rpc/frame.rs OnceLock cache),
    AUTUMN_GROUP_COMMIT_CAP (autumn-client wbench),
    AUTUMN_PPROF_* (autumn-ps debug knobs)
  Wave 5 — cluster.sh + perf_check:
    Replace `env | grep AUTUMN_` leak with explicit --flag translation
    per binary.
