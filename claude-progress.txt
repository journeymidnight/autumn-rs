Date: 2026-06-13
TaskStatus: completed
Task scope: rolling upgrade R2（控制面 prost 迁移）。本会话交付 R2-A =
  etcd 核心元数据 7 前缀 rkyv→prost（设计 §2 硬前提）。R0+R1 已于前序
  会话完成。R2-B（RPC 结构 + 剩余持久前缀）待续；R3（数据面协商）未开始。

=== TWO LISTS REQUIRED BY THE LONG-TASK HARNESS ===

  Already implemented (this session, R2-A):
    - prost 基建：autumn-rpc 加 prost dep + prost_encode/prost_decode
      helper + U64U32Pair（named-pair 替 tuple，确定性编码保 F33 CAS）。
    - 7 核心持久类型双 derive（rkyv+prost）：MgrNodeInfo/MgrDiskInfo/
      MgrStreamInfo/MgrExtentInfo/MgrPartitionMeta/MgrPartitionVpRefs/
      MgrRegionInfo + MgrRange(嵌套)。
    - etcd 写（mirror_*/persist_extent/split/merge/recovery）+ replay 解码
      + F33 CAS baseline 全切 prost（这 7 前缀）。
    - 类型变更：shard_ports u16→u32（仅 manager 边界）、VpRefs.refs
      tuple→U64U32Pair。
    - WIRE_VERSION 1→2（MIN=MAX=2；v2=prost 格式，回滚 fail-closed）。
    - coco 2P1（混格式迁移=用户拍板 reset，文档化接受）+1P2（as u16→
      try_from）。live 端到端验证（reset/replay/split/rolling 全过）。
  Not implemented (R2-B + 后续，recorded):
    - R2-B：manager_rpc 全部 REQUEST/RESPONSE 结构（~100 个，含 CodeResp.code
      u8→u32 等大面 churn）+ 剩余持久前缀 node_override/decommissioned/
      extent_inflight/extentDeleteRetry/inode_leases 切 prost；最后移除这些
      类型的 rkyv derive。
    - R3：数据面连接级 hello 协商 + FooReqV2 冻结纪律。
    - upgrade_chaos.sh 双版本滚动 harness（依赖 R2 完成 + R3）。
  已稳定无需改：ownerLocks=mod_revision/psNodes=UTF-8/partitionLastOp=i64
    LE/cluster_id+cluster_version=ASCII。

Current summary:
  R2-A 已 commit。prost etcd replay 端到端实测通过（fresh/restart/split/
  rolling 全绿，零 decode error，零数据丢失）。
Main gaps:
  - R2-B 是大面 churn（~100 RPC 结构 + u8/u16 churn）；R3 待 R2 完成。
Next steps:
  - R2-B：按 RPC 簇逐步切 prost（每簇 green+commit），最后移 rkyv。
