Date: 2026-07-03
TaskStatus: completed
Task scope: 25 个随机 seed 的 in-process chaos 扫测(system_chaos 零丢数据不变量测试
  chaos_real_kill_split_merge_ec_fence_no_data_loss),完成后接本地 + 跨机 perf check
  (perf 阶段进行中,本条目记录 chaos 扫测闭环)。

Current summary:
  · 扫测配置:激进档(DURATION=45s, NEMESIS_INTERVAL=1500ms,= seed 583/603 回归同款),
    其余默认(K=3 M=1 ENs=5,全部 11 种 nemesis 动作)。25 个随机 seed 由 shuf 生成并
    记录:979809 578352 706409 574262 792262 499835 409033 715330 328074 261886
    295022 86399 30981 194595 49986 633034 153149 309742 64062 733988 247804
    18177 753653 122487 902341。
  · **结果:25/25 全 PASS,零失败零假阳性**。每轮五项校验全过(per-key 400 keys
    mismatches=0 not_found=0 / range=0 / write-liveness=0 / accounting=0)。
    单轮 71-93s;写入 acked 6k-14.4k/轮,nemesis events 9-16/轮。
  · 运行工程:排水门(TIME-WAIT<4000)逐轮生效(峰值 84K,门正常拦截);
    每轮清理测试泄漏的 /tmp 日志目录(system_chaos.rs:1971 .keep() 泄漏 +
    本次发现的 EN 数据目录泄漏,含 ANSI-color-in-baseline 的清理坑);
    结束后 /tmp 新增目录 = 0,无 stray 进程,live d02 集群 etcd 未被触碰
    (测试自建私有 etcd,已核实 support/mod.rs:859)。
  · 顺带修复 docs/ops.md 过时说明:该测试并不需要 127.0.0.1:2379 的 etcd
    (自建私有实例),并补记 toxiproxy/etcd PATH 依赖与 target/debug 前置构建。
  · 观察(非本次扫测所致):d02 live 集群当前只剩 5 EN + etcd,manager/PS 不在运行
    (perf 阶段按 perf-check skill 流程重建/重启该 bench 集群)。

Main gaps / risks:
  (无 —— 25/25 绿;已知 open 家族 seed=13/15 的 recovery-stuck 写 wedge 未在本次
   随机样本中出现,该家族仍由 memory project_recovery_stuck_blocks_alloc 跟踪。)

Next steps:
  1. 本地 perf check(perf-check skill;先 cargo build --release --workspace,
     release 二进制落后于 7-01/02 的 readability 提交)。
  2. 跨机 perf check(remote-autumn;::15 作 bench client,UCX pin mlx5_1:1)。
