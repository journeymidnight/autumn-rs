Date: 2026-07-18
TaskStatus: completed
Task scope: BUG-KVC-NO-HIT — vLLM kvcache 写了从不命中(hit 恒 0%) + 拖慢 prefill +
  20GB 写放大。真机复现(本地 3-EN 集群 + vLLM 0.23 + 真 Qwen3-VL-32B on H200; 装的
  autumn .so 旧于源码 → 先 maturin develop --release 对齐 namespace binding)。
根因(逐调用 trace):
  - 0% hit 是分层设计正确行为: KV 在 GPU prefix cache(HBM,非CPU); vLLM 先命中本地,
    只把"本地未命中残余 token"问外部; 同实例重复本地全覆盖 → external 永不 LOAD。
    **无需重启触发 hit**: llm.reset_prefix_cache() 清 GPU cache → 下请求 GPU miss →
    命中 autumn(实测 after-reset matched=1/load_ok=64)。
  - 真 bug: (1) 冗余重存(store 不查已存→本地命中的重复请求重存 64 层=20GB 放大);
    (2) 同步搬运挂关键路径(save_kv_layer 同步 .detach().cpu() 64 层 352MB D2H+每层
    sync, + 同步 put_from)。
修复(vllm_connector.py 纯 Python, 无 kill switch=用户明确不要):
  几乎所有工作移到后台单线程。save_kv_layer 只留 GPU gather(_extract_layer 出独立
  tensor, 解耦 paged block, 不 sync CPU); 后台 job CUDA event 等 gather 完 → D2H
  .cpu() + store-dedup(is_present) + 批量 put_from + marker 全在后台; marker 仅写全
  ACK 后发。背压 _MAX_INFLIGHT_SAVES=2(GPU staging 另按 vLLM token 预算天然有界),
  超了丢存, submit 失败回滚计数。读写走 ZC 批量(put_from/get_into=BatchClient ZC,
  ≥64K+UCX 自动 put_zc/EN-direct): write 一次批量全层; read 从逐层 64 次 get 改一次
  批量 get_into。
基准(1k-token, H200, no-hit 纯开销 vs 无 connector):
  TTFT +6.5ms(TCP)/+7.0ms(UCX)、TPOT ~0 (同步搬运时代是 +148ms) → 搬运移出关键路径
  后 no-hit 开销与传输无关(GPU gather 主导, 网络全异步)。传输只在 HIT 的 load 路径
  有别。
Review: coco(GPT-5.5 deep) 5 轮。actionable 全修: inflight 泄漏回滚(submit 失败)、
  doc 准确性、test importorskip 下沉(scheduler 纯测试不被 torch 缺席 skip)。
  bounded-by-design 记档不改(判 over-eng + huge GPU headroom): GPU staging=token 预算
  界; 重复请求仍走廉价前台 gather 后台 dedup(~6ms, 非 20GB); 跨 step in-flight 去重
  cross-process 无法干净做(worker mark 不通 scheduler), 残留有界自愈幂等。
Verify: 单测 33 绿(scheduler dedup + 后台 job dedup/写, torch 可选)。真机 e2e:
  dedup(warm 重复存 0); 异步(marks_at_gen=0 证 marker 在 generate 返回后发);
  正确性 O_extload==O_localcache 逐 token 一致(证异步 gather/D2H/load 无损);
  reset_prefix_cache 触发 hit。(GPU0 有他租 lerobot-train, e2e 用 GPU1; autumn 集群
  在 /data 不占 GPU。)
Docs: docs/ops.md + README(缓存分层语义、异步搬运、no-hit 开销数、reset 触发法)。
Next: 线上 redeploy vLLM 主机(connector 纯 Python, native 未改 → 重装 autumn_kvcache
  包即可)。用户驱动。

---- 上一子交付 ----
Date: 2026-07-18
TaskStatus: completed
Task scope: F-KEY-NS SD-3 = D1 fuse `fs/{tenant}/{volume}/` scoping (main agent 自己实现).
  停机批次第三子交付; 代码 + fable 对抗审 + 活集群测试完成, 待与 SD-1+SD-2 一起
  stop-world cluster reset 部署 (部署=用户驱动, 下一阶段).
Done (7 commit: 46ed227 core + bbb26ed→78a3530 inode + 03b49f5 schema + 9748d3d P0 +
  47e3159 autumnfs + a0f5774 tests):
  - **关键设计判定**: fuse 所有 KV 访问汇聚到 `state.rs` 8 个 `kv_*` choke point + read/write
    两处 batch 数据路径 —— `{volume}/` 只加这些点, **key.rs 与 38 调用点、全部 parse_* 零改动**
    (先前 per-builder KeyCodec 是错的层, 已 revert). `FsState.vol` + `wire()` prepend `{volume}/`,
    `kv_range_keys` 剥回; connect `connect_raw`→`scoped(fs, tenant)` (client 拥 `fs/{tenant}/` +
    剥回). 净 wire key = `fs/{tenant}/{volume}/[type][fields]`. 前缀 bounded 正确性已核
    (PS handle_range:1057 按 req.prefix 收窄). volume 段校验 `[a-z0-9._-]+`.
  - schema 戳 (Piece 4): `SCHEMA_VERSION=2` + `ensure_schema_version` (缺戳/符过/不符或坏字节
    fail-loud, ensure_root 入口 → fuse mount + PyO3 都覆盖).
  - autumnfs 迁移 (P1-3): client 加 `connect_volume(mgr,ns,tenant,volume)` +
    `NamespaceBinding::scoped_volume` (client 拥全前缀 `fs/{t}/{v}/` → autumnfs ~30 相对-key op
    零改动); autumnfs 加 `--tenant/--volume` + inode 改 manager 发号. server CLAUDE.md/ops.md 同步.
  - 挂载接线: fuse binary + PyO3 `autumn.Fs.connect` + autumnfs 均 `--tenant/--volume` 默认 default
    → 三者见同一 FS; 8 处 fuse 测试构造点改 scoped.
**inode 号=全局唯一 (用户拍板 B, 非 per-volume)**: lease/fence 平面按裸 ino key
  (`AcquireLeaseReq` 无 volume, manager `inode_leases/<ino>`, PS `fence_floors`), per-volume inode
  会跨 volume 撞车 (fable P1-2)。用户选 B (尊重 v25 冻结, 不动 wire): fuse 传空 volume + 去掉
  `init_root` 本地 seed → 全 inode 走 manager 全局计数器 (cluster-unique)。per-volume 机制 +
  冻结的 `AllocInodesReq.volume` 休眠待将来 volume-aware lease; manager `valid_alloc_volume` 校验
  冻结字段 (P2-4)。数据隔离靠 `{volume}/` key 前缀 (与 ino 无关), 不受影响。
Review: fable subagent (coco 替代, coco 大 diff 反复 hang) 对抗审 → 1 P0 + 2 P1 + 2 P2 + 文档漂移:
  - P0 batch 数据路径绕过 volume 前缀 (extent read get_many + write append put_many 只带
    `fs/{t}/` 缺 `{volume}/`) —— **自查 choke-point 完整性时先发现并修 (9748d3d), fable 确认修得全**.
  - P1-2 lease ino 冲突 → 拍板 B 全局 inode (上文). P1-3 autumnfs 未迁 → 已迁.
  - P2-4 volume 校验 → 已加. P2-5 schema v1 guard → reset 部署下 acceptable-risk, 跳过
    (probe 裸低位 key 路由脆; reset 后无 v1 数据; fail-loud v2→v3+ 仍在). 文档漂移 → 已修.
Verify: fuse core 13、manager lib 228、client 35、fs_alloc 5 单测 + fs_alloc_inodes 集成;
  **活本地集群 (manager+2EN+PS) 跑绿**: `system_fuse_ns.rs` cold-remount 读回 (证 P0 修复:
  batch append 写的 extent 冷重挂 scan 能读回) + 双 volume 隔离 (同 ino=100 各读回自己字节) +
  既有 f247 全读写截断删 (无回归). workspace check 绿. python ext 因预存 compio-buf unstable 本机
  不可编 (fs.rs 照 host 参数同型写, 低风险).
Next: 全批 stop-world cluster reset 部署 SD-1+SD-2+SD-3 (用户驱动) → 活集群验证 (mount ls/cp,
  autumnfs 重灌 qwen32b, 双 volume 隔离). push 仍 hold (本地 ahead 10).
遗留(非阻塞): memory keys.rs / hermes vestigial tenant 参数彻底删; presplit fuse_split_ranges 归
  D8/F-PRESPLIT-PER-NS 部署工具; init_root 非 CAS root-create race 为 M0 既有 (未加剧);
  autumnfs 不写 schema 戳 (fuse mount 首挂时写); 将来 volume-aware lease = 独立 feature (需 wire).

---- 上一子交付 ----
Date: 2026-07-18
TaskStatus: completed
Task scope: F-KEY-NS SD-2 = D7 client binding + PS Layer-A + kvc/mem key changes,
  改为 **Prepend-only**（用户拍板："转全 Prepend，天然锁 scope"）。
Done:
  - client `NamespaceBinding` Prepend-only：Scoped 永远 prepend `{ns}/{tenant}/`
    （scope 构造锁死，删掉 Assert/BindMode/chunk helper）；Raw 给 admin/connect_raw；
    `bind_key` infallible；range clamp（`{ns}/{tenant}0` 上界 + strip）；connect 段
    校验 ns/tenant `[a-z0-9._-]+`（P2）。
  - built-in builder 改**相对 key**：memory `keys.rs`（agent_prefix/shared_prefix 去
    `mem/{tenant}/`，`_tenant` 参数暂留 vestigial）、kvc `_keys.py`（full_key/pool_prefix
    去 `kvc/{tenant}/`）、hermes `_agent_prefix`（去 `mem/{tenant}/`）。binding 拥有前缀。
  - PS Layer-A gate（put/put-zc/batch 必须落已注册 namespace，delete/get/range 不 gate，
    token-free，registry 非空即开）；`namespace-list` 富列表（sanctioned wire v25 fp
    刷新 `6bb3e2105b2845db`）；PyO3/CLI 必填 ns/tenant + connect 时 `validate_credential_scope`
    fail-fast（P2）；principal 改名；perf-check/bench_zc 改 bench/perf scope。
  - 61 connect 站点迁移（admin/mgr-only/fuse-pre-SD3 → connect_raw；数据面 → connect）。
Review: coco full-diff 反复 hang GPT-5.5（两次），scoped 审 authz.rs 通过；改用 **fable
  subagent 深审**（用户指示）—— 验证 core mechanism（bind-once/range clamp/Layer-A/
  credential 分类/PyO3 错误传播）solid，抓 2 P1 + 1 P2 + 3 P3。coco 3 + fable 6 findings
  全修：put_stream+Assert（Prepend-only 消解，chunk 走普通 put）、connect_with_credential
  只在 authz-off 跳过（否则 fail-fast）、PyO3 补 scope 校验、bench_zc 签名、hermes 双前缀、
  Layer-A live-from-bootstrap 部署文档订正（**SD-2 不可单独部署，必须与 SD-3 同批**：SD-1
  seed 让 registry 非空 → Layer-A 从 bootstrap 就 live → fuse pre-SD3 raw key 会被拒；
  测试 memory-mode 不 seed 所以测不到）、Assert 注释清扫（~12 处）、get_many_direct `.expect`、
  CLI typo。
Verify: 全 workspace 构建绿（含 examples）；client binding 8 / memory 22 / manager 225 /
  PS 202 / rpc 48 lib 测试过；2 集成测试过（Layer-A reject + putstream chunk in tenant
  range）；wire fp v25 `6bb3e2105b2845db`；py_compile 5 个 python 文件干净。
Next: SD-3（D1 fuse `fs/{tenant}/{volume}/` + per-volume next_inode + schema 戳 +
  autumnfs 改 scoped + fuse builder 改相对）→ 全批 stop-world cluster reset 部署。
遗留（非阻塞）：memory keys.rs / hermes 的 vestigial `tenant`/`_tenant` 参数彻底删是机械
  follow-up；rescope() 未做段校验（admin 控制，低风险）。

---- 上一子交付 ----
Date: 2026-07-17
TaskStatus: completed
Task scope: F-KEY-NS SD-1 = D2 注册表 + 整批 wire 冻结（opus teammate 实现，team-lead+用户 review）。
Done: etcd `namespace/<name>` 注册表（照抄 tenantAccount/ 模板）+ namespace-create/delete
  RPC(0x57/0x58)+SDK+autumn-op CLI + bootstrap 预注册 fs/kvc/mem + authz bridge（protected =
  手工清单 ∪ owner'd namespace）+ GET_AUTHZ_CONFIG leader-gate（P1，用户拍板：ensure_leader，
  PS poll 遇 NOT_LEADER 轮转、不清缓存——PS 侧本就正确，只补注释）。**整批 wire 一次冻结
  v24→v25 fp 76e8ba557f7fca2d**（namespace RPC + GetAuthzConfigResp.namespaces + AllocInodesReq.volume
  [冻结待 SD-3] + CODE_NAMESPACE_UNKNOWN=10；SD-2/SD-3 不再动 wire）。coco(GPT-5.5 deep) 3 findings：
  P2 replay 语义校验已修（key-suffix/name/prefix 必须一致，fail-loud 拒当 leader）、P2 delete
  非空检查 CLI 侧（manager 无 KV 数据面 client，架构决定，--force 跳过且不批删=reset 部署）、
  P1 leader-gate 已并入。测试全绿：rpc 48 / manager lib 223 / PS 197 / client 27 / etcd 集成
  (#[ignore]) 实测通过（bootstrap 持久化→leader 服务→follower 返回 NOT_LEADER→失败切换后
  继任 leader 服务 rehydrated 注册表）。
已知遗留（非阻塞）：PS 冷启动首次 fetch_authz_config 若首联 follower，会 WARN+轮转、
  authz-off 直到 5s poll 命中 leader（既有的"可用性优先"权衡，内网威胁模型下 OK；teammate
  可按需给首次 fetch 加 retry-with-rotation 关掉 ~5s 窗口）。--with-tenant 目前只置 owner 标记，
  完整 principal-create 包装延后。
Next: commit SD-1（本条）→ SD-2（D7 主体）→ SD-3（D1 fuse）→ 一次 stop-world cluster reset 部署。

---- 上一任务 ----
Date: 2026-07-17
TaskStatus: completed
Task scope: F-KEY-NS D1+D7+D2 停机批次实施设计（main agent = 设计）。
Done: doc §7「停机批次实施设计」写入 —— 三路 Explore（fuse/client/manager）file:line
  复核确认后，产出三个子交付的具体 change-list、整批 v24→v25 wire 冻结清单、
  部署 runbook、测试计划、回滚。用户拍板两项：(1) 落地顺序 D2 →(wire 冻结)→ D7 → D1；
  (2) 线上走 cluster reset，不迁移（§7.4 已按此简化：reset 清 etcd+EN 盘 → 全新起 →
  预注册 fs/kvc/mem → fresh mkfs → 重新上载 qwen32b 权重，无批删/无迁移工具）。
  两处 explorer 细化已在 §7 定稿：kvc _keys.py 非零改动（full_key/pool_prefix 加 tenant）；
  F186 chunk key 由 binding 在叶层 put() 前缀化 → 落进 tenant 区间（Layer A/authz/presplit
  覆盖）。三处 §3 文档订正记于 §7.6（AUTUMN_AUTH_PROTECTED_PREFIXES env 不存在等）。
Next: 交 opus teammate 实现 SD-1 = D2 注册表（照抄 tenantAccount/ 模板）+ 整批 wire 冻结
  （MIN=MAX=25 + namespace-create/delete RPC 0x57/0x58 + GetAuthzConfigResp +namespaces +
  AllocInodesReq +volume[冻结待 SD-3 接线] + Layer A 错误码）+ bootstrap 预注册 fs/kvc/mem。
  SD-1 落 → coco → commit（v25 wire 之后 D7/D1 不再动 wire）→ 再 SD-2(D7) → SD-3(D1)。

---- 上一任务 ----
Date: 2026-07-17
TaskStatus: completed
Task scope: F-SPLIT-AT-KEY (design doc D4) — operator/controller-specified split point.
Current summary (DONE — team-lead reviewed + COMMITTED [feat(split), was 03ddf78,
  amended to fold the valid_segment test]. Two coco passes: teammate deep coco
  found P2+P3, main-agent coco found P3 — both fixed:
    P2 = CLI namespace/tenant not validated as single path segments →
         `--tenant acme/prod` collides with `--tenant acme --at prod/`, breaking
         the (ns,tenant,suffix)↔raw-key 1:1. Fixed: valid_segment/reject_bad_segment
         enforce [a-z0-9._-]+ on ns+tenant (suffix stays free-form on purpose).
    P3 = --at-raw-hex duplicate silently overwrote → now fail-loud like --at/--at-hex.
  Concurrency note (recovered): the d4-split-at teammate sent 2 session-limit
  failures ~1:20pm PT; main agent picked D4 up believing it dead, restored the root
  CLAUDE.md architecture guide the teammate had wrongly stripped to a 63-line stub,
  reviewed, ran coco, committed. Teammate then RESUMED after its 3:30pm PT reset and
  finished the same work concurrently (its coco caught the extra P2). We clobbered
  files briefly; main agent messaged it to stand down, it went idle, then main agent
  reconciled (folded its valid_segment test + kept its more-accurate P2/P3 annotation).
  Deploy held for the D1+D7 stop-world batch, NOT deployed standalone):
  - Wire: `SplitPartReq.at_key: Option<Vec<u8>>` (RAW byte key — partition layer
    stays namespace-agnostic, D5). WIRE v23→v24, MIN=MAX=24, fp eeaf88720200e4da,
    registry entry added, same-commit stop-the-world deploy (deploy not mine).
  - PS `handle_split_part`: explicit path validates at_key STRICTLY inside the
    authoritative (start,end) (==start / ==end / out-of-range → InvalidArgument);
    SKIPS the SST median scan AND the `>=2 keys` gate (empty partition splittable
    = D8 presplit primitive). Median path (at_key=None) unchanged, still gated.
    Downstream CoW/duplicate_stream/has_overlap/major-compact UNTOUCHED. Added a
    local `hex_str` helper (no new dep). Manager `auto_dispatch_split` + ~40 test
    sites updated to `{part_id, at_key: None}`.
  - Client: `split()` delegates to new `split_at(part_id, Option<Vec<u8>>)`.
  - CLI (细化三 term rule — user面 speaks namespace/tenant, never raw prefix):
    `split <PART> --namespace <ns> --tenant <t> [--at <suffix>|--at-hex <hex>]`;
    tool assembles `{ns}/{tenant}/ ++ suffix`; EMPTY suffix = pair boundary
    `{ns}/{tenant}/`. `--at-raw-hex` = admin-only escape hatch (raw whole key).
    Friendly CLI precheck (pair ∩ partition range) — UX only, PS authoritative.
  - Tests: PS/wire integration `system_split_at_key.rs` (2: interval in/edge/out,
    empty+--at success, empty+median reject) + CLI unit tests (6: assembly,
    empty-suffix=pair-boundary, hex suffix, raw, classification) + WIRE registry.
    Old median path regression = existing split suite (system_range_split green;
    system_multi_split_chain flaky-by-timing on has_overlap, PROVEN pre-existing:
    baseline stashed run passed, my version passes when run alone).
  - Docs: docs/ops.md D4 runbook (new CLI form + manual verify); feature_list.md
    F-SPLIT-AT-KEY status annotation.
Verify: cargo build --workspace green EXCEPT autumn-fuse macOS link
  (`_fuse_mount_compat25`, known-unrelated, brief said don't fix). All touched
  crates + bins build. PS lib 196 green, integration 2 green, CLI 6 green.
Next steps: DONE (reviewed + committed). Deploy happens with the D1+D7 stop-world
  batch (WIRE v24 same-commit), not standalone. Downstream F-PRESPLIT-PER-NS
  consumes this primitive (namespace-create loops split --at over the pair
  interval) — separate feature.
Corrections to brief: WIRE was v23 (brief's "current v23" CONFIRMED, not v22→v23
  as the parenthetical hinted); the `>=2 keys` gate + median-by-key-count PS-side
  selection both CONFIRMED as described; no interaction坑 found with CoW/overlap
  (empty-tail seal already handled by compute_duplicate_stream, manager note 32).

---- F-KEY-NS design finalization: 三层模型 + tenant 强制 + 术语规则 (2026-07-17) ----
TaskStatus: completed
Scope: DESIGN ONLY (per new CLAUDE.md teammate rules: main agent = design).
  User decisions folded into docs/key_namespace_split_design.md + feature_list:
  (1) 三层模型 {namespace}/{tenant}/{instance} — instance = volume/model/agent;
  (2) tenant 结构强制（ClusterClient/CLI 必填）+ 命名自由（部署默认 "default"）
      + 内网轻量（不建配额/计费/生命周期，tenant 不注册）。论证过程存档于
      §3.7 细化三（内网事实砍掉计费/敌意租户/WAS 论据；幸存论据=防事故、
      单一布局、authz 前缀单位）；
  (3) 术语规则：prefix 是实现细节，API/CLI 只讲 namespace/tenant；授权 =
      principal → (ns, tenant) 集合；presplit 单位 = pair [ns/tenant/, ns/tenant0)；
  (4) principal 改名（原 authz "tenant"）：principal-create 替代 tenant-create
      （alias 保留），1:1 默认约定；
  (5) kvc key 补 tenant 层 kvc/{tenant}/{model}/…（随 D1 停机批次；纯缓存
      冷失效=零迁移；D6 的 auth_tenant/tenant_suffix 双命名随之退役）。
  §5 原待拍板第 4 项（注册表粒度/CLI 归并）随之解决；剩 3 项待拍板。
  同轮清掉了 doc 里 default 反转的全部残留（§3.7①④、§3.8 尾部、
  feature_list F-NS-FIRST-CLASS——后者即 coco P3）。
Next: 实现顺序不变 D4 → 停机批次(D1+D7+D2+kvc-tenant) → D6-fs/kvc → D8。
  实现按 CLAUDE.md 分工交 teammate（代码=opus，部署=sonnet）。


---- F-AUTHZ-BUILTIN D6-mem/kvc client wiring (2026-07-17) ----
TaskStatus: completed
Scope: client-side credential plumbing ONLY (user: 不用管线上，先写代码).
  PyO3 Client.connect + BatchClient grow (tenant=, credential=) kwargs,
  both-or-neither validated up front; workers set_tenant_credential after
  connect. kvcache vLLM connector + sglang backend read auth_tenant /
  auth_credential_file from extra_config (named auth_* to avoid clashing
  with the key-namespace tenant_suffix), thread to BatchClient AND the
  existence-probe Client (authz gates reads on protected prefixes too).
  hermes provider: AUTUMN_MEMORY_CREDENTIAL_FILE. ops.md: enablement
  runbook (credentials-first gradual axis; rollback = drop signing key).
Verify: python/ workspace does NOT compile on local stable rustc
  (pre-existing: standalone workspace pins compio-buf 0.8.3 needing
  unstable maybe_uninit_slice; wheel builds in Docker/maturin) — PyO3
  change validated by rustfmt parse only. kvcache offline tests 23/23.
  Provider/backends ast-parse clean. NOT live-tested (needs wheel build).
Next: enablement is an OPS decision (runbook ready); D6-fs waits on D1.

Task scope: F-KVC-LAYOUT-FP — BUG-KVC-TENANT follow-up: fold the KV *layout*
  versions into the model fingerprint. vLLM's page layout is an internal
  detail (no stability contract; README pins 0.23.0 but loader.py:5 already
  claims "v0.24") → same model on an upgraded vLLM kept the same tenant and
  could cross-read layout-incompatible bytes (same silent-garbage class).
  Fact-check vs the task brief: the CONNECTOR-side layout was ALREADY
  version-gated (_KV_STORAGE_FORMAT="v1" in the key path, F278/1b522fa) —
  missing were the vLLM-side version, tenant-level split, offline testability,
  and the bump-invariant cross-refs.

Code DONE (uncommitted — awaiting team-lead review; do NOT commit before review):
  - _keys.py: VLLM_KV_STORAGE_FORMAT="v1" (moved from vllm_connector so pure
    tests can pin it) + BUMP-invariant comment (change _extract_layer/
    _inject_layer/_byte_view/key scheme ⇒ bump).
  - _identity.py: vllm_runtime_version() (lazy vllm.__version__ →
    importlib.metadata → None); vllm_identity_sources folds "vllm" (FULL
    x.y.z — patch releases can change layout; re-warm is one-time/predictable,
    under-split is silent garbage) + "kv_layout"; gated on non-empty sources
    so version-only can't mask the unfingerprintable⇒None⇒loud-warning
    contract.
  - vllm_connector.py: imports the shared constant; loud warning when vLLM is
    importable but version undetectable; INVARIANT cross-refs at
    _extract_layer/_inject_layer. sglang path untouched (byte-identical
    default tenant re-verified by tests); fingerprint still computed once in
    __init__ (zero hot-path cost).
  - tests/test_tenant_identity.py: 23 green (17 prior + 6 new: version split /
    patch-version split / undetectable degrade / version-only stays None /
    kv_layout split / pinned "v1"+in-sources). test_smoke/test_vllm_dataplane
    collection errors are PRE-EXISTING (need numpy + autumn native wheel).
  - Docs: README (fingerprint list + upgrade note: ANY vLLM version change
    cold-invalidates the vLLM pool), ops.md, plan doc §13.3,
    feature_list.md F-KVC-LAYOUT-FP.
Live impact (deployed cluster): current tenant kvc/model-cfg_456cf7543b98_0_1/
  (195 keys) goes cold on redeploy — fingerprint gains vllm+kv_layout sources
  ⇒ new tenant; old keys (ttl=0) need manual batch_delete to reclaim.
Next steps: team-lead review (+coco per repo rule) → commit → redeploy vLLM
  hosts, verify startup log shows identity={..., 'vllm': '0.23.0',
  'kv_layout': 'v1'} and a new tenant; batch_delete the old tenant prefix.

---- Prior task (same package, committed a42c6a1 + deployed; progress notes kept) ----
Date: 2026-07-16
TaskStatus: completed
Task scope: BUG-KVC-TENANT — autumn_kvcache tenant carries no model identity →
  cross-model KV aliasing. Live incident: Qwen2.5-7B + 32B (both served via
  autumn_vllm_loader's fixed /model-cfg dir, shared tokenizer → same content
  hash) landed in ONE tenant kvc/model-cfg_0_1/ and 32B cross-read 7B's KV.
  Same-shape models would have been SILENTLY wrong.

Code DONE (COMMITTED a42c6a1, deployed live; new tenant kvc/model-cfg_456cf7543b98_0_1/ verified working):
  - NEW python/autumn_kvcache/autumn_kvcache/_identity.py (pure, no deps):
    vllm_identity_sources (arch shape + weights source + optional model_id;
    field names verified against vLLM v0.23.0 tag) → 12-hex deterministic
    fingerprint; tenant_cfg_from_vllm moved here (also fixes is_mla_model
    hardcoded-False → ModelConfig.use_mla).
  - _keys.py: build_tenant_suffix(cfg, model_fingerprint=None) — fingerprint
    slots between model and tp/pp; None ⇒ byte-identical legacy format.
  - vllm_connector.py: tenant = model + fingerprint (loud warn if
    underivable); identity sources logged at startup; miss-after-marker
    warning now states causes per TTL config (ttl=0 ⇒ never blames TTL,
    points at tenant/model mismatch); _AutumnKVStore exposes tenant/marker_ttl.
  - sglang_backend.py: default tenant UNCHANGED (no sglang invalidation);
    optional extra_config["model_id"] escape hatch.
  - __init__.py: lazy re-exports (PEP 562) so pure modules import without the
    autumn native module → offline unit tests possible.
  - NEW tests/test_tenant_identity.py: 17 tests green (uv run --with pytest,
    no cluster/engine/native module). Cluster-required smoke/dataplane tests
    unchanged (cannot run on this Mac — need 1-node cluster + maturin wheel).
  - Docs: README (tenant/fingerprint/model_id/upgrade), plan doc §5+§13.3,
    ops.md verify+cleanup runbook, feature_list.md BUG-KVC-TENANT.
Residual NOT closed (documented): same-arch finetunes from the SAME local
  path (no weights_path) still collide → model_id override; weights digest =
  future work. Upgrade = vLLM-pool cold cache; old kvc/model-cfg_0_1/vllm/*
  keys (ttl=0) need manual batch_delete to reclaim space (live cluster
  currently self-consistent per team-lead; NOT touched).
Next steps: team-lead review (+coco per repo rule) → commit → redeploy vLLM
  hosts with the new package and verify two models log two tenants.

---- Concurrent task (same day, separate) ----
Date: 2026-07-16
TaskStatus: not_completed
Task scope: F-KEY-NS design doc — 内置应用 key 命名空间 + 分裂规则（doc only,
  无代码改动, 未 commit — team-lead 明确指示 doc-only + 待 review）。
Done: docs/key_namespace_split_design.md（现状/问题分级/三个 size 口径钉死/
  设计 D1-D5/迁移/待拍板/前期调查订正）; feature_list.md 新增 F-KEY-NS 伞条目
  （子项 F-POLICY-SIZE-EST-LIVE / F-KEY-NS-FUSE / F-SPLIT-AT-KEY /
  F-PRESPLIT-DEPLOY-MODE, passes:false）。
Key findings: size_bytes 已复活（background.rs:309, F-PS-SIZE-BYTES-DEAD
  2026-07-05）= LSM 常驻字节 → policy 对 VP 负载失明; manager CLAUDE.md
  "size_bytes always 0" 陈述已过期; info 45.4GB = stream 全 extent 字节含
  未 GC 死字节; est_live 四组件（sealed 和/open_tail/gc_debt/open_tail_dead）
  全部已有 → 修口径是 manager-only 零 wire 改动。
Update (同日, review 后定稿): 用户拍板前缀 = fs/{tenant}/{volume}/（迁移用
  fs/default/default/）。doc §3.1 重写：per-volume 自持状态不变量 —— ROOT_INO=1
  per volume（ino 本就 volume 作用域）、next_inode 计数器 per volume（etcd
  autumn-rs/fs/{t}/{v}/next_inode + KV floor，消灭跨 volume 灾备/删除依赖）、
  rmtomb/schema 戳进 fs/{t}/{v}/\x04、MSG_ALLOC_INODES 加 volume 字段（WIRE
  bump = D1 唯一 wire 变更，§4 表已改）；autumnfs 非 CAS race 不被多 volume
  放大。§5.1 改"已拍板"，其余 3 项保持待拍板。feature_list F-KEY-NS 同步。
Update 2 (同日): §5.2 也拍板 —— 19GB 迁移走 A 重灌（team-lead 线上 autumnfs ls
  全树盘点：只有模型权重, qwen32b 19GB 在用 + 旧 qwen 7B 5.5GB 无消费者, 全部
  hf-mirror 可再生; 旧 7B 不再灌 → 回收 ≈16.5GB EN 空间/RF3, EN 池仅 100GiB）。
  doc §3.1 迁移段/选项表/§4/§5 + feature_list 已同步; 不写迁移工具。
Update 3 (同日): §5.4 也拍板 —— authz 彻底启用覆盖 fs/kvc/mem（用户原话
  "authz彻底都加上吧"）。doc 新增 §3.6 D6：P0 完整闭环 = D1+D6（前缀给可
  保护性, authz 才把它变成已保护; rmtomb INVARIANT 首次由机制保证）; 机制
  盘点订正 team-lead 一处（autumn-op 有 gen-signing-key/tenant-create/
  tenant-delete/mint-token, 不是没有）; 真缺口 = 接线（fuse mount/PyO3/
  autumnfs credential + 部署层 signing key/prefixes env）; 灰度 = 按前缀
  mem/→kvc/→fs/ + 凭据先行（无 audit-only, 不补）; 失败模式 = 漏凭据全线
  写失败(PermissionDenied terminal) + manager 不可达>TTL−300s 拒写（数据面
  新增宽限期=TTL 的控制面依赖）。§4 顺序改 D3→D6-mem→D1+D2→D6-fs/kvc→
  D4→D5a; §5 "client 裸 key 劝阻" 被 D6 消解, 待拍板剩阈值重标定/kvc 分档/
  TTL 取值与 Secret 投递。feature_list 加 F-AUTHZ-BUILTIN 子项。
Update 4 (同日): 两条新拍板 —— D7 写入强制归属 namespace（类 table, 取消裸
  key 写面; doc 新增 §3.7: SDK Namespace 句柄/Layer A 存在性检查只管写不管读/
  与 authz 合并身份分离强制/etcd namespace 注册表/不设 default 兜底/与 D1 同
  停机批次; 反对点写明: namespace 不下渗存储层、不与 token 强绑）+ D8
  per-namespace presplit（§3.8: 锚点从 bootstrap 移到 namespace-create =
  HBase per-table SPLITS; bootstrap presplit 退役, D5a superseded; kvc tenant
  维度仍靠 split --at）。订正 team-lead 两处: meta/stats、idx/ 不是裸键（在
  mem/{t}/{agent}/ 下, keys.rs:174-204）; perf-check key 是 partition-local
  构造（key_for_partition = start_key++"!"++tag, main.rs:25）→ 结构性看不见
  热点且 D7 下会写进他人 namespace 须重设计; 45s put 是 BUG-MGR-RETRY-CLASS
  不是热点证据。§4 顺序改 D3→D6-mem→D4→【停机: D1+D7+D2】→D6-fs/kvc→D8。
  feature_list 加 F-NS-FIRST-CLASS/F-PRESPLIT-PER-NS, F-PRESPLIT-DEPLOY-MODE
  superseded。
Update 5 (同日): D7 两次细化落档 —— (i) 未指明写入 → default namespace
  （强制归属但有默认值, HBase 同构; 无参 connect 零改动）; (ii) ClusterClient
  类型必须携带 namespace（NamespaceBinding 字段; Prepend/Assert 两模式, 内置
  三族 key builder 零改动; credential 与 ns 同点绑定 fail-fast, 二者不冗余=
  可写集合 vs 当前作用域）。关键取舍: default **前缀化** 而非隐式（不附和
  team-lead 的隐式倾向）—— 决定性论据: 隐式 default 的存量 key 会被后来的
  namespace-create 静默捕获易主; 另 authz 纯 starts_with 表达不了排除法区间
  (authz.rs:198/237), Layer A 恒真空转; 本集群迁移代价≈0（裸 key 仅可弃
  bench 数据）。default 语义: 无 owner 不受保护不可删; 保留名 fs/kvc/mem/
  default + 前缀无关冲突检测; Layer A 收窄为只管 put 类不管 delete（存量
  清理免开洞）; admin 例外 = raw() 只绕 client 钳制不绕 PS 强制。doc §3.7
  ①/①b/②/③/④/⑤ + §3.8 + §4 D7 行 + §5 已拍板块全部更新; feature_list
  F-NS-FIRST-CLASS 重写。注意: doc 已被 team-lead 提交过一版（git M 状态）,
  本会话工作区改动在其上, 本会话仍不 commit。
Next steps: 用户拍板 §5 剩余 4 项 → 按 doc §4 顺序实施（D3 先行）。

---- Concurrent task (same day, separate) ----
Date: 2026-07-16
TaskStatus: not_completed
Task scope: BUG-MGR-RETRY-CLASS — code-classified StreamClient manager-call
  retry + owner-epoch-fence self-heal. Live incident: post-rebalance stale
  owner_epoch (partition/17: manager expected 14396, PS sent 14364 forever) →
  every alloc_new_extent burned 20×500ms futile retries → autumnfs put 45 s /
  rm 30 s; the fenced partition NEVER self-healed (safety held, liveness gap).

Code DONE (uncommitted — awaiting team-lead review; do NOT commit before review):
  - stream client.rs: typed ManagerError{code,ctx,message} from
    check_manager_resp (+ alloc_new_extent_once inline check routed through
    it); retry_manager_call classifies — deterministic codes (NOT_FOUND/
    INVALID_ARGUMENT/PRECONDITION incl. fence/unknown) FAIL FAST unwrapped;
    transport/NOT_LEADER/CODE_ERROR keep rotate+retry (no double-rotate on
    NOT_LEADER). Owner fence (CODE_PRECONDITION + matcher) Display carries the
    load-bearing "LockedByOther" token → F270 poison path.
  - common store.rs: is_owner_epoch_fence_message NEXT TO ensure_owner_epoch
    (no new wire code → no WIRE bump) + producer-pairing test.
  - PS background.rs: is_locked_by_other matches whole anyhow chain ({e:#}) —
    .context() wraps no longer hide the fence marker.
  - PS lib.rs LIVENESS: sync_regions_once drops handles whose partition
    thread is_finished() → region map arbitrates same 2s tick: still-assigned
    → reopen with FRESH epoch; rebalanced-away → clean release. shutting_down
    Cell gates the sync tick so shutdown drains can't be resurrected.
  - Docs: stream CLAUDE.md note 30; PS CLAUDE.md self-eviction section;
    common CLAUDE.md; feature_list.md BUG-MGR-RETRY-CLASS entry; docs/ops.md.
  - Tests green: stream 111 (7 new manager_retry_tests), partition-server 196
    (+fence_classifier_tests), common 13 (+matcher pairing). Workspace builds
    (autumn-fuse macOS link fail = pre-existing, Linux-only). Clippy: no new
    warnings.
Next steps: team-lead review (+coco per repo rule) → commit → live validate on
  VKE (fenced partition recovers in ~2 s instead of never). Follow-up feature
  candidate (NOT implemented): two-phase rebalance handoff (drain old owner
  before new owner opens) to close the dual-owner window itself.

---- Prior task (paused, resume after this bug) ----
Date: 2026-07-15
TaskStatus: not_completed
Task scope: F-REGION-REBALANCE — active region→PS load rebalancing. Phase A =
  manual `autumn-op rebalance` (fix the sticky-assignment concentration the M2
  rollout exposed: all 32 partitions on one PS). Phase B = dashboard auto-policy
  rebalance switch (deferred). Then build + deploy on VKE + fix the live 32/0/0.

Phase A — CODE DONE + committed 289965e, pushed:
  - wire: MSG_REBALANCE_REGIONS (0x56) + RebalanceRegionsReq{max_moves} /
    RebalanceMove / RebalanceRegionsResp{code,message,moves,moved}; WIRE v21→v22
    (fp 57fe8f244d8fd5c1, MIN=MAX=22 same-commit deploy).
  - manager: pure compute_rebalance_moves (greedy most→least-loaded until gap≤1,
    max_moves-bounded, deterministic, registered-PS-only) + handle_rebalance_regions
    (leader-gated; apply in-memory + clear moved part_addr + mirror_partition_snapshot).
    5 unit tests green.
  - client: ClusterClient::rebalance_regions. autumn-op: `rebalance [MAX_MOVES]`.
  - docs: ops.md runbook + cheatsheet; feature_list; manager CLAUDE.md.
  - coco (GPT-5.5 deep): P0 dual-owner = handled by owner_epoch fencing
    (open_partition acquires epoch BEFORE commit_length, same as failover; coco saw
    manager code only); P1 wire MIN=MAX kept (matches every prior bump + stop-world
    model); P1 part_addr staleness FIXED; P2 CLI trailing-arg FIXED.
  - Build: workspace clean (autumn-fuse macOS link fail is pre-existing, Linux-only).

Phase A — LIVE-VALIDATED on VKE (2026-07-15, user-authorized stop-world deploy):
  - CP build run 3d6c41a0 Succeeded → image 289965e (WIRE v22).
  - Deployed: apply v22 + coordinated `kubectl delete pod` (all non-etcd) →
    9/9 converged to v22 in <2 min.
  - Post-deploy the cluster was again 32-on-one-PS (sticky, as expected).
  - `autumn-op rebalance` MOVED 21 PARTITIONS → 11/10/11 across all 3 PS (info
    confirms). Post-rebalance perf-check green: write 10,152 ops + read 82,401
    ops @ 7,490 ops/s across all 3 PS (write p99 spike = cold-reopen warmup).
  - F-REGION-REBALANCE Phase A works end-to-end on the live cluster.

Phase B — CODE DONE + committed (997f030 after rebase over an unrelated chaos fix),
pushed. WIRE v22→v23 (fp 6b0224cb5e4f13f3, same-commit).
  - POLICY_KIND_REBALANCE=7 + policy_kind_names row.
  - PolicyEngine::compute_rebalance_advisory (cluster-level, one candidate/tick from
    regions+ps_nodes counts; fires when per-PS spread > rebalance_gap_threshold(2)
    outside rebalance_cooldown_sec(120s) via last_rebalance_at) → recompute_advisory_cache.
  - actuate_candidate + actuate_action(dashboard Apply) REBALANCE arms →
    handle_rebalance_regions(rebalance_max_moves_per_tick=4), bounded/gradual.
  - 6th switch `rebalance` (SWITCH_ORDER [split,ec,compact,gc,merge,rebalance]);
    kinds_from_switches/preset[bool;6]/sanitize 6/kind_priority=1/str/describe/
    candidate_to_cmd/candidate_to_action. ON in balanced+aggressive presets.
    dashboard HTML toggle + switch_order/switches_to_dict/upsert; autumn-op
    policy-candidates kind map + feas.
  - config split: policy DEFINITION persisted (autoPolicy/config rkyv, Vec<bool>
    forward-compat → old config decodes, sanitize pads rebalance off); 3 thresholds
    in in-memory PolicyConfig (NOT persisted, like all advisory thresholds).
  - 4 rebalance_advisory_* tests + updated auto_policy switch tests green; clippy clean.
  - coco (GPT-5.5 deep): wire MIN=MAX kept (same-commit); dashboard Apply path +
    CLI kind map/feas wired (P2+P3 fixed).

NEXT: LIVE deploy+validate of Phase B needs USER AUTH (stop-the-world v23 deploy).
  Validate: arm a policy with rebalance ON → concentrate → advisory fires → armed
  controller converges gradually; dashboard switch toggles; default-off preserved.

DESIGN Q (user, answered): a policy carries only switches + interval + cooldown +
  max_actions; per-kind tuning (rebalance max_moves/gap/cooldown, and all other
  advisory thresholds) is GLOBAL in-memory PolicyConfig, not per-policy. Per-policy
  params would need MgrAutoPolicyEntry wire fields or a persisted PolicyConfig key —
  offered as a separate future enhancement.

Prior task (DONE, do not redo): F-EN-DYNSHARD M2 — per-pod EN Services eliminated,
  pod-IP self-register, live-validated (commit fd2bfbf). Rollout exposed the sticky
  region→PS concentration that THIS feature (rebalance) fixes.
  See [[vke-rolling-restart-concentrates-partitions]].
