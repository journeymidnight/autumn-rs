Date: 2026-06-13
TaskStatus: completed
Task scope: 生产就绪审计后的两处崩溃一致性 fail-open 修复(coco arch P0)。
  按"先复现再修"处理。升级方向仍为:全停全启安全(rkyv fail-loud),不做
  rolling(见 project_rolling_upgrade_paused)。

=== TWO LISTS REQUIRED BY THE LONG-TASK HARNESS ===

  Already implemented (this session):
    - PROD-FIX-1 META-FAILCLOSED:ExtentEntry.corrupt_meta flag;load_extents
      区分 present-but-corrupt / 非-NotFound 读错(都 quarantine)/ 真
      NotFound(默认 open);append(handle_append + build_append_future)/
      read(handle_read_bytes + build_read_future)/ commit_length 在
      quarantine 时拒绝;write_meta_locked 成功持久后清 flag。复现测试
      corrupt_meta_quarantines_extent_and_rejects_stale_append(先红后绿)。
    - PROD-FIX-1 EC-PREPARE-DURABLE:fsync_staging_dir helper,EC 2PC prepare
      新写+幂等早退+commit rename 后都 fsync 父目录(durable prepare record
      真正成立)。
    - coco findbugs 3 条全采纳(非-NotFound quarantine / build_read_future
      热路径拒绝 / 幂等早退也 dir-fsync)。stream 78 单测绿,workspace 绿,
      live smoke 通过。
  Not implemented (recorded, 生产就绪审计剩余项,见
    project_production_readiness_audit):
    - P0 能力:wire 鉴权/TLS + manager 管理授权;备份/DR。
    - P0 数据损坏(EC/split 崩溃窗,reproduce-first 再修):EC abandon
      fence-handover 语义漂移;EC commit rename↔meta 窗 participant state
      machine;split FREEZE_TTL vs 旧 proposal token/epoch。
    - P1:WAL 中间-corruption skip → fail-stop;recovery set_len(0) 忽略
      错误;region_epoch=0 旁路审计;V0 WAL CRC + VP offset;flush 发布
      早于 checkpoint;inflight malformed/auto-release。
    - META quarantine 的自动 recovery 上报(liveness 残留)。

Current summary:
  两处 fail-open 已改 fail-closed,复现+coco+live 验证。其余审计项已登记。
Next steps:
  - 生产前推进 P0 能力(鉴权/备份)+ EC/split 崩溃窗(各自先复现)。
