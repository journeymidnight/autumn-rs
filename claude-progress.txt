Date: 2026-06-17
TaskStatus: completed
Task scope: 分块 EC encode + 流式 peer-copy(EC-CHUNKED-CONVERT)。
  让 16–32 GiB extent 在 EC 下安全:消除 EC convert 的内存峰值(~2× extent)和
  whole-shard WriteShard 超 frame `payload_len: u32`(4 GiB)上限两个问题。
  (承接上一任务 u64 offset 拓宽 + max_extent_size 16 GiB,用户追加 "do chunked ec encode and peer copy"。)
Current summary:
  - `erasure::ec_encode_stripe`:RS over GF(256) 按 offset 逐字节 → stripe 可独立编码;字节级等价整 extent
    `ec_encode` 的对应切片(`ec_encode_stripe_matches_whole` 单测证明)→ on-disk shard 布局不变,读路径零改。
  - `handle_convert_to_ec` 改 stripe loop:逐 stripe 读 K 个 data 子区间(尾零填充)→ 编码 M parity →
    远端 shard 先发、coordinator shard 0 最后写。峰值 = (K+M)×EC_ENCODE_STRIPE_BYTES(默认 64 MiB → ~256 MiB)。
  - `WriteShardReq.shard_offset: u64`(WIRE v5→v6,header 36→44):远端按 offset pwrite + per-stripe sync_data;
    顺序 await-ack 让 durable 前缀单调;coordinator-自己-最后 保留 "coord staging 满 ⇒ 全员 durable staged" 不变量。
    2PC + EC-COMMIT-ATOMIC #5 marker 不变。
  - 流式 peer-copy `peer_copy_full_extent_to_dat`:short 时流式拉进临时文件,完整 + fsync 后才原子 rename 覆盖 .dat
    (coco P1:绝不先 set_len(0) live replica)。删整 Vec 缓冲的 `fetch_full_extent_from_sources`。
  - coco P1 #2:`write_shard_stripe_local` 加边界 `shard_offset + stripe_len <= sealed_length`(防 sparse 撑爆 + commit 污染)。
  验收:
  - cargo build --workspace 绿;rpc 30 / stream 全单测(含 erasure 17 / f119-d / f153 / ec_2pc / merge_ec_replay)/
    manager lib 163 全绿;新增 stripe==whole 字节级单测。
  - coco deep:2 项 P1 全修(peer-copy 临时文件原子替换 + 边界校验)。
  - **live e2e(隔离 4-EN, AUTUMN_EXTENT_EC_STRIPE_BYTES=2MiB 强制多 stripe,不碰 /data02):** put-stream 1.25 GiB
    → log_stream 在 1 GiB seal → force-ec-convert 走分块 prepare(EN 日志 "(chunked)")→ shard 实测 357,919,062 字节
    (≈sealed/3,~170 stripes)→ get-stream sha256 **MATCH**(1,342,177,280 字节);open tail 仍复制态。
  注:F203 后 set-stream-ec 只设策略 + 出 advisory,实际转换走 OP 驱动的 force-ec-convert(e2e 已据此修正)。
    max_extent_size clamp 下限 1 GiB(seal 需写 > 1 GiB)。
Main gaps:
  - 无(代码 + 单测 + coco + live e2e 全绿)。
  - 真 >12 GiB EC extent(shard > 4 GiB,frame 溢出的极端)未单独 live 复现(代价高);但 frame 上限由分块结构性消除,
    且 stripe==whole 字节等价 + 1 GiB/170-stripe e2e 已覆盖分块正确性。
Commits: 待 commit + push 到 main(承接 u64 commit 8c7d2df 之后)。
Next steps:
  - commit + push 到 main(solo-flow 直推)。
