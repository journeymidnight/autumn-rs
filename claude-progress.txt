Date: 2026-05-21
TaskStatus: completed
Task scope: F216-E — UCX end-to-end zero-copy READ data path for the
            kvcache (sglang HiCache L3). Relay topology (client <- PS <- EN,
            no partition-layer bypass). R4 (final read-path copy elimination)
            implemented + unit-tested + live-UCX e2e PASSED this session.
            Plan: /root/.claude/plans/flickering-sauteeing-pearl.md.

Current summary:
  The read path is now FULLY zero-copy EN -> PS -> client over UCX, and the
  client-facing WRITE hop (put_zc / MSG_PUT_ZC) was done earlier. R4 closed the
  last PS-side copies:
    - resolve_value / read_value_from_log / get_value (background.rs +
      rpc_handlers.rs) now return `Bytes` instead of `Vec<u8>`. The VP-over-UCX
      fast path returns `Bytes::from_owner(pb)` ALIASING the registered RegPool
      buffer (no pb->Vec copy; pb returns to the pool when the Bytes drops). The
      inline path returns a zero-copy `raw_value.slice(..)`. (`impl AsRef<[u8]>
      for PooledBuf` added so from_owner can back a Bytes.)
    - handle_get_zc returns the response as TWO segments `(head, value)`:
      head = [V0 frame header][zc_meta] (ps_zc_head, mirrors EN zc_read_head),
      value = the aliasing Bytes. The ps-conn inflight FU output changed
      Bytes -> (Bytes, Option<Bytes>); push_resp pushes head then value into
      tx_bufs so ONE write_vectored_all emits them as a single wire frame with
      NO concat copy. d=1 fast path uses write_vectored_all([head,value]) for ZC,
      plain write_all(head) otherwise (no write-path regression). On-wire bytes
      are byte-identical to the pre-R4 concat form -> client read path unchanged.
    - handle_get (rkyv GetResp, generic SDK) does value.to_vec(); the rkyv
      encode copies regardless, so the Bytes return does NOT regress it (copy
      just moves). Net value copies: VP-over-UCX get_into = 0; generic get = same.

  Builds clean WITH and WITHOUT `ucx`. Unit tests: PS 146, rpc 15, stream 56,
  transport 5 — all green both configs.

  LIVE UCX E2E PASSED (1-node, RoCE rc_mlx5, /tmp/autumn-r4):
    - put-zc + zc-get byte-identical at 1000B (inline), 256K (VP), 8M (VP).
    - interop both ways: put-zc -> normal get MATCH (handle_get rkyv path);
      normal put -> zc-get MATCH (R4 vectored path on a normally-written value).
    - rc_mlx5 active on PS + EN (20/20); zero panic/error/fallback/eversion/
      short in PS+EN logs; PS alive throughout.
    (Launch that worked: minimal detached
     `(AUTUMN_TRANSPORT=ucx AUTUMN_BIND_HOST="[fdbd:dc62:3:302::14]"
       UCX_TLS="^sysv,posix" AUTUMN_DATA_ROOT=/tmp/autumn-r4 ./cluster.sh reset 1
       > log &)` as its OWN command, then a SEPARATE poll + put-zc/zc-get/cmp.)

Earlier hops (all DONE + verified, prior sessions):
  - client<-PS READ (get_into / MSG_GET_ZC): TCP + UCX, large/small/not-found.
  - client->PS WRITE (put_zc / MSG_PUT_ZC): value as own iovec, sliced
    zero-copy on PS; byte-identical round-trip + put<->put-zc interop.
  - PS<-EN READ (R3, MSG_READ_BYTES_ZC + read_value_into_pooled): EN emits
    [zc_meta][value] 2 Bytes (no encode copy); RpcClient::call_into_pooled
    read_loop OWNS the PooledBuf, hands it back on success, drops->pool on
    cancel (no leak). RegPool made transport-agnostic (top-level mod).
  - primitives: recv_into seam, memh wiring (UCP_OP_ATTR_FIELD_MEMH),
    RegPool, call_into_dest, RLIMIT_MEMLOCK raise + mt_workers_shared.

Main gaps (NOT done — lower priority, next sessions):
  1. python BatchClient(zc=True) get_into into the pinned sglang page + register
     the sglang pool once for memh reads + maturin rebuild + re-bench kvcache
     (reads were parity; write transport ~2x diluted by Python/dispatch).
  2. PS->EN WRITE explicit source registration (rcache already makes it
     zero-copy; the value is already an Arc-Bytes slice end-to-end). Low value.
  3. Deep UCX multi-worker / DC-transport scaling (separate from the copy work;
     established as per-op overhead + process serialization, not a real collapse).

Next steps:
  - Re-bench kvcache over UCX with the full zero-copy read path (R3+R4) wired
    into the python adapter (item 1). The intra-host read is RPC/round-trip
    bound, so the copy elimination's win shows most on large values / fewer RPCs;
    the real cross-host lever remains one-sided RDMA (3FS model), TCP-incompatible.

Verification this session:
  cargo test -p autumn-partition-server (146), -p autumn-rpc (15),
  -p autumn-stream (56), -p autumn-transport (5) — all ok. Builds clean
  -p autumn-partition-server (no-ucx) and -p autumn-server --features ucx.
  Live UCX e2e as above.

Previous task (completed 2026-05-19): F216 autumn-kvcache MVP
  (Python-adapter-only sglang HiCache L3 backend, F216-A..D).
