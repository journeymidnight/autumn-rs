Date: 2026-07-03
TaskStatus: completed
Task scope: 修 UCX 8M 读 stall(用户批准路线:先 fix(a) 日志分级,再 #1
  ping-on-connect;后转 rndv/zcopy 试验)。结果:stall 已由**纯配置修复**
  (UCX_TLS 去 posix/cma),fix(a) 日志已提交,ping 被证伪撤回。

Current summary:
  · fix(a) 已提交(b4754ae rebase 后):client get_direct 副本 failover +
    PS ZC 代理读的静默重试按错误分级(超时→warn,预期 failover→debug,
    全副本失败仅含超时时 warn —— coco P3 采纳)。client 27/27、stream 90/90、
    clippy 0。**部署当轮即立功**:373 条 warn 把故障点改写为 PS→EN 同机段。
  · ping-on-connect(#1)完整走了实现→测试→真集群验证→**被证伪→撤回**:
    探活(MSG_TYPE_PING)全通过而 8M 读依旧卡 → 病灶不是死 endpoint 而是
    **并发大消息传输**;按"不留未证实机制补丁"原则还原 conn_pool.rs
    (过程记入 memory 防重走弯路)。
  · rndv/zcopy 试验矩阵(6 配置 × fresh 集群 × t16 8M bench,指标=PS 超时
    warn 数/读 ops/s)定谳:
    baseline(posix,cma) 178/210 · no-cma 197/105 · **no-shm 0/853** ·
    rndv-put_zcopy 258/25 · rndv-get_zcopy 223/69 · no-zcopy 191/100
    → **元凶 = posix shm 传输的并发大消息路径**(cma 无辜,rndv/zcopy 无关)。
  · no-shm(UCX_TLS=rc_mlx5,ud_mlx5,tcp,self)验证:8M 读 960 ops/s(8.05GB/s,
    p99 122ms,0 stall,超 6-25 参考 2.6×)、4K 读 771K(p99 0.19ms)、8M 写 183;
    **代价:4K 写 71.8K→13.8K**(posix 曾高效承载 PS→EN 小 append,rc loopback
    每 append 付延迟,p50 0.84→7.4ms)。大值负载(kvcache/fuse)净赚 4-9×。
  · 结论已固化:feedback_ucx_tls_positive_list(7-03 修订:RoCE 绑定集群禁加
    posix/cma;纯 loopback 维持 posix)+ perf-check skill UCX_TLS 段 +
    project_ucx_zc_read_regression_readability(全 sweep 表)。
  · PS 双监听代价分析(用户问):**PS 无需双监听** —— 病灶 hop 上 PS 是连接方,
    传输选择在拨号侧;若未来真要,fd 成本低(TCP 侧)、真实成本在寻址/wire
    (part_addrs 双地址 + GetRegions + 三个消费者适配)与端口空间×2。已记档。

Main gaps / risks(后续可选,无阻塞):
  1. posix 大消息为何坏未根解(UCX 包/内核/sglang uct 同租;6-25 曾好过)——
     现已绕开,根查属 UCX 层专门课题。
  2. no-shm 下 4K 写恢复(append 批量化调优)——仅 4K 写密集负载需要。
  3. 基线重录策略(多租户+cpuset+新 TLS 三重口径变化)——需用户定。

补充(同日):UCX_TLS 配置已产品化 —— cluster.sh 按 bind 地址自动推导
  (loopback→posix,cma,tcp,self;RoCE→rc_mlx5,ud_mlx5,tcp,self+NET_DEVICES pin,
  显式 env 恒优先;do_start 与 load_cluster_config 两处调用覆盖子命令);
  autumn-deploy 对 TRANSPORT=ucx 逐 host 推导并注入 systemd Environment=/
  process env;topology.conf/README/docs/baremetal_deploy.md/docs/ops.md(fuse
  runbook 的坏组合列表修正)/perf_check.sh(被证伪的 combined 建议改写)全部
  对齐。验证:三例推导单测、unit 渲染含 Environment 行、validate.sh 绿、
  零手工 env 的 RoCE ucx 集群子进程 /proc/environ 确认继承 + 8M 读 900 ops/s
  (7.55GB/s)0 warn。

Next steps:
  (本任务闭环。可选:用 no-shm 配置重录 ucx 基线;4K 写调优;posix 根查。)
