Date: 2026-05-24
TaskStatus: completed
Task scope: F233 — recovery backoff observability + fix flaky e2e test
  `split_merge_split_with_concurrent_writes` (user: "修复这个失败，并且补上
  backoff 可观测性"). Both in the merged recovery series' code, not F232.

Summary (F233):
  1. Backoff observability. Backoff and the F207 Recovery marker are
     independent (backoff only delays re-dispatch 2^N s cap 300s; never
     deletes a marker, never stops retries — candidates re-derived from
     s.extents every tick; no give-up). The failure REASON was being
     dropped: record_dispatch_outcome took ok:bool, BackoffState had no
     reason, recovery-stats showed only a count. Now:
       - BackoffState gains last_reason:String (dropped Copy);
         record_failure(.., reason:&str); new backoff_snapshot().
       - record_dispatch_outcome takes &Result<(),AppError>; 4 call sites
         pass &res (was res.is_ok()).
       - wire: new RecoveryBackoffEntry; RecoveryStatsResp.backoff Vec
         (kept backoff_entries count). Same-commit deploy.
       - autumn-op recovery-stats prints a backoff table + --json array.
     manager/CLAUDE.md note 30 documents the marker/backoff independence
     + the keep-the-Result invariant.
  2. Flaky-test fix. The panic was the seeding ps_put hitting F227's
     all-replica current_commit at tail-init when one of 2 nodes
     momentarily didn't answer commit_length. Production SDK + the test's
     own writer task already retry this (Internal→retryable); only the raw
     ps_put helper used .expect() with no retry. Fixed support/mod.rs::ps_put
     to bounded-retry (30×100ms) then panic with last error. Test-infra
     only; success path unchanged.

Verification (F233):
  cargo build --workspace --exclude autumn-fuse clean. manager lib recovery
  tests 18/18 (incl extended backoff assertions). system_merge --ignored
  full suite 9/9 (the previously-failing test now passes, no sibling
  regression). NOT committed yet — awaiting user go-ahead.

----- previous task (completed 2026-05-23) -----
Date: 2026-05-23
TaskStatus: completed
Task scope: merge branch `f222-f224-recovery-completion-fixes` into main.
  All source code auto-merged cleanly; only the two ledger files conflicted.
  Resolved an F-number collision — both sides independently used F222/F223:
    - branch KEEPS F222–F230 (recovery series; 54 source-comment refs intact).
    - main's per-link-transport  F222 -> F231 (deferred, ledger-only, 0 code).
    - main's RPC-frame cleanup   F223 -> F232 (1 rpc/CLAUDE.md ref updated;
      commit 7b06a72's message keeps "F223" as immutable history).
  feature_list.md: P10=F231, P12=recovery(F222-F230), P13=RPC-frame(F232).
Verification: cargo build --workspace clean (code auto-merged, +futures dep).

----- merged task: RPC frame single-version cleanup (F232, was F223; completed 2026-05-24) -----
Date: 2026-05-24
TaskStatus: completed
Task scope: F232 (renumbered from F223 on merge) — collapse RPC frame to a SINGLE permanent V1 (CRC) format;
            delete the V0 encoder runtime toggle + back-compat (per user:
            "参考别的系统到底应该打开 CRC 还是关闭；决定了就只留一个版本，不
            考虑后向兼容"). Decision = CRC ON, single version. Also corrected
            stale ledger status (F161/F163 passes:false→true via F165; F216-F
            SUBSUMED by F219; renumbered the deferred per-link-transport entry
            F219→F222→F231 (F222 later collided with the merged recovery
            series, so it took F231) to fix an id collision with the completed
            F219).

Current summary (F232, was F223):
  DONE + verified by build + unit tests (no live cluster needed — wire-only,
  behavior-neutral since the deleted toggle had zero callers; V1 was already
  the always-on default).

  frame.rs: encode / encode_header / encode_request_header now unconditionally
  emit the CRC frame (FLAG_CRC + payload_len incl. 4-byte CRC trailer);
  encode_v1 inlined into encode. Deleted V1_ENCODER_ENABLED OnceLock +
  set_v1_encoder_enabled + v1_encoder_enabled. compute_payload_crc
  de-dead-coded (used by client::send_vectored).

  De-V0/V1 naming (per user "全是V1，老的V0就不要了，省得引起疑惑"): one frame
  protocol, no version vocabulary. encode_v0 RENAMED -> encode_no_crc. All
  "V0/V1 frame" comments + test names across frame.rs / partition_rpc.rs /
  client.rs / rpc_handlers.rs / lib.rs / extent_node.rs(zc_read_head) reworded
  to "CRC / CRC-less frame". (Unrelated F157 extent-.meta and F158 WAL-record
  V0/V1 left untouched — different concepts.)

  KEPT on purpose (NOT back-compat): encode_no_crc (CRC-less) + the decoder's
  FLAG_CRC dispatch branch — they back the zero-copy value-response framing
  (call_into_dest/call_into_pooled recv into a caller dest, can't strip a
  trailer; integrity = transport's per F219). Production hand-builds that
  CRC-less header in ps_zc_head / zc_read_head; encode_no_crc now only backs ZC
  test fixtures.

  Verification: cargo build --workspace --exclude autumn-fuse clean WITH and
  WITHOUT --features ucx; rpc 16 / stream 56 / partition-server 146 lib tests
  green. Docs: feature_list F223 + F165 follow-up note; crates/rpc/CLAUDE.md
  wire-format section.

  NOTE: code committed? NO — awaiting user go-ahead to commit.

----- previous task (completed 2026-05-21) -----
Task scope: F219 — TCP recv-side copy elimination (read + write), reusing the
            regpool + size threshold from F216-E, PLUS removal of the ZC-path
            value CRC from the PS hot path (per user: "把 PS 里面 hot path 的
            CRC 全部删除，不要 enable 啥的"). User ask: "复用 regpool+threshold
            让 tcp 也尽量 zero copy".
----- merged task: recovery completion fixes F222–F230 (completed 2026-05-22) -----
Date: 2026-05-22
TaskStatus: completed
Task scope: F227 (WAS-faithful commit/seal — remove quorum) + F228 (manager
  background-loop resilience). Both root causes of the live incident (operator
  fenced node 1 mid-write -> stale_vp + 11-min silent node_health_loop freeze).
  F229 (PS/EN loop resilience) audited + logged as deferred.

== F227 — root cause 2 (durability invariant), passes: TRUE ==
Invariant (WAS/Azure): all-replica-ACK; NO quorum on the commit path.
  commit/seal length = min over COMMITTED members (safe >= acked BECAUSE of
  all-ack). Bug was F156/F210-B2 majority-quorum + min-over-responders + F178
  flush fsync-quorum. Truncation of beyond-commit (un-acked) bytes stays correct
  — no floor (user-corrected an earlier wrong proposal).
Code:
  - manager/rpc_handlers.rs: pure compute_commit_seal + recovering_nodes_for_extent
    (F207 ledger) + seal_durability_floor (AUTUMN_MGR_SEAL_DURABILITY_FLOOR=1).
    handle_stream_alloc_extent failover seal (req.end==0) + handle_check_commit_length
    call it. req.end>0 path unchanged. commit_length_for_extent (READ path) left
    quorum-min on purpose.
  - stream/client.rs: current_commit + await_extent_synced_to -> all-replica;
    ensure_tail_initialised current_commit failure propagates (?) not unwrap_or(0).
Verified: 5 pure-fn unit tests; manager 113 / stream 56 / etcd 3 lib green;
  LIVE e2e (cluster.sh reset 5): fence node 7 mid-write, kept writing full speed,
  8 keys written-during-fence get back exactly 8192 bytes, NO stale_vp.

== F228 — root cause 1 (loop resilience), passes: TRUE ==
Audited all 9 manager loops; only unbounded awaits were etcd unary_call +
  ConnPool::get_or_connect connect (EN/PS request RPCs already call_timeout).
Code:
  - etcd/lib.rs unary_call: timeout both call_with_sender + reconnect
    (AUTUMN_ETCD_REQUEST_TIMEOUT_MS=10s) -> reconnect+retry on timeout.
  - manager/lib.rs ConnPool::get_or_connect: bound RpcConn::connect
    (AUTUMN_MGR_CONNECT_TIMEOUT_MS=5s).
  - manager/lib.rs spawn_supervised (AssertUnwindSafe+catch_unwind+ERROR+1s
    restart) wraps all 9 loops; +futures dep; ps_liveness_check_loop &self->self.
  - 1A prevents hangs (catch_unwind can't rescue a stuck await); 1C catches panics.
Verified: LIVE e2e same run — before+after fence all heartbeats 0s
  (node_health_loop alive, vs incident's frozen); manager.log 0 panic / 0 ERROR /
  0 bg_loop restart; recovery dispatched+completed for node 7. Incident does not
  recur.

== F229 — PS/EN loop resilience, passes: FALSE (deferred, logged) ==
Same detached-spawn pattern on PS (heartbeat/report_load/region_sync/per-partition
  flush·compact·gc/conn) + EN (coalescer/reconcile). NOT fixed: PS loops on
  per-partition !Send Rc<RefCell> runtimes need per-loop restart safety (panic
  mid-flush leaves partition state mid-mutation) — bigger + riskier than the
  manager's clone-restart. See feature_list F229.

Build/test: cargo build --workspace clean; lib tests green (manager 113 / stream
  56 / etcd 3).

Commit: F227 + F228 committed this session (source + docs + ledgers). Did NOT
  commit write_result.json (bench artifact), .venv/, python/d.py, python/del_prefix.py
  (pre-existing untracked, unrelated). Branch f222-f224-recovery-completion-fixes.

== F230 — partition_loop reopen busy-spin (found during clean-env integration run), passes: TRUE ==
Ran etcd-gated integration suite (clean env: tests self-spawn ephemeral etcd +
  random ports, isolated from any live cluster). F227/F228-relevant tests all
  green: extent_failover/recovery/seal, f211_*, ps/manager_failover, leader_fence,
  apply_done_atomicity, split/range/multi_split_chain, merge (each in own process).
Found a PRE-EXISTING (spins on parent too) hang:
  system_merge::split_merge_split_with_interleaved_writes → partition_loop ~95%
  CPU spin. Root: on reopen, PartitionHandle drop closes drain_rx (EOF) before
  req_rx; idle select kept selecting the closed drain_rx (instant Ready(None)).
Fix (crates/partition-server/src/lib.rs partition_loop): drain_closed latch —
  stop polling drain_rx once EOF, keep serving req_rx, exit on req_rx EOF (NOT
  break-on-drain-EOF, which stranded queued requests → hang).
  + test helper crates/manager/tests/support/mod.rs PsRouter::client_for now
  retries with region re-resolve (mirrors prod SDK) instead of expect()-panic
  in the reopen window.
Verified: the test passes ~17s (was ∞). No regression (lib + reopen integration
  green; the 1 partition-server-lib failure under full parallel load is the
  pre-existing flaky f099i_d1_fast_path perf-counter assertion, passes in isolation).

Next (optional): F229 (PS/EN loop supervision/timeouts — F230 fixed one concrete
  PS-loop spin but the broader 1A/1C-for-PS/EN audit-fix is still deferred).
