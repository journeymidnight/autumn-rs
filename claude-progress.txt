Date: 2026-07-16
TaskStatus: not_completed

---- F-AUTHZ-BUILTIN D6-mem/kvc client wiring (2026-07-17) ----
TaskStatus: completed
Scope: client-side credential plumbing ONLY (user: 不用管线上，先写代码).
  PyO3 Client.connect + BatchClient grow (tenant=, credential=) kwargs,
  both-or-neither validated up front; workers set_tenant_credential after
  connect. kvcache vLLM connector + sglang backend read auth_tenant /
  auth_credential_file from extra_config (named auth_* to avoid clashing
  with the key-namespace tenant_suffix), thread to BatchClient AND the
  existence-probe Client (authz gates reads on protected prefixes too).
  hermes provider: AUTUMN_MEMORY_CREDENTIAL_FILE. ops.md: enablement
  runbook (credentials-first gradual axis; rollback = drop signing key).
Verify: python/ workspace does NOT compile on local stable rustc
  (pre-existing: standalone workspace pins compio-buf 0.8.3 needing
  unstable maybe_uninit_slice; wheel builds in Docker/maturin) — PyO3
  change validated by rustfmt parse only. kvcache offline tests 23/23.
  Provider/backends ast-parse clean. NOT live-tested (needs wheel build).
Next: enablement is an OPS decision (runbook ready); D6-fs waits on D1.

Task scope: F-KVC-LAYOUT-FP — BUG-KVC-TENANT follow-up: fold the KV *layout*
  versions into the model fingerprint. vLLM's page layout is an internal
  detail (no stability contract; README pins 0.23.0 but loader.py:5 already
  claims "v0.24") → same model on an upgraded vLLM kept the same tenant and
  could cross-read layout-incompatible bytes (same silent-garbage class).
  Fact-check vs the task brief: the CONNECTOR-side layout was ALREADY
  version-gated (_KV_STORAGE_FORMAT="v1" in the key path, F278/1b522fa) —
  missing were the vLLM-side version, tenant-level split, offline testability,
  and the bump-invariant cross-refs.

Code DONE (uncommitted — awaiting team-lead review; do NOT commit before review):
  - _keys.py: VLLM_KV_STORAGE_FORMAT="v1" (moved from vllm_connector so pure
    tests can pin it) + BUMP-invariant comment (change _extract_layer/
    _inject_layer/_byte_view/key scheme ⇒ bump).
  - _identity.py: vllm_runtime_version() (lazy vllm.__version__ →
    importlib.metadata → None); vllm_identity_sources folds "vllm" (FULL
    x.y.z — patch releases can change layout; re-warm is one-time/predictable,
    under-split is silent garbage) + "kv_layout"; gated on non-empty sources
    so version-only can't mask the unfingerprintable⇒None⇒loud-warning
    contract.
  - vllm_connector.py: imports the shared constant; loud warning when vLLM is
    importable but version undetectable; INVARIANT cross-refs at
    _extract_layer/_inject_layer. sglang path untouched (byte-identical
    default tenant re-verified by tests); fingerprint still computed once in
    __init__ (zero hot-path cost).
  - tests/test_tenant_identity.py: 23 green (17 prior + 6 new: version split /
    patch-version split / undetectable degrade / version-only stays None /
    kv_layout split / pinned "v1"+in-sources). test_smoke/test_vllm_dataplane
    collection errors are PRE-EXISTING (need numpy + autumn native wheel).
  - Docs: README (fingerprint list + upgrade note: ANY vLLM version change
    cold-invalidates the vLLM pool), ops.md, plan doc §13.3,
    feature_list.md F-KVC-LAYOUT-FP.
Live impact (deployed cluster): current tenant kvc/model-cfg_456cf7543b98_0_1/
  (195 keys) goes cold on redeploy — fingerprint gains vllm+kv_layout sources
  ⇒ new tenant; old keys (ttl=0) need manual batch_delete to reclaim.
Next steps: team-lead review (+coco per repo rule) → commit → redeploy vLLM
  hosts, verify startup log shows identity={..., 'vllm': '0.23.0',
  'kv_layout': 'v1'} and a new tenant; batch_delete the old tenant prefix.

---- Prior task (same package, committed a42c6a1 + deployed; progress notes kept) ----
Date: 2026-07-16
TaskStatus: completed
Task scope: BUG-KVC-TENANT — autumn_kvcache tenant carries no model identity →
  cross-model KV aliasing. Live incident: Qwen2.5-7B + 32B (both served via
  autumn_vllm_loader's fixed /model-cfg dir, shared tokenizer → same content
  hash) landed in ONE tenant kvc/model-cfg_0_1/ and 32B cross-read 7B's KV.
  Same-shape models would have been SILENTLY wrong.

Code DONE (COMMITTED a42c6a1, deployed live; new tenant kvc/model-cfg_456cf7543b98_0_1/ verified working):
  - NEW python/autumn_kvcache/autumn_kvcache/_identity.py (pure, no deps):
    vllm_identity_sources (arch shape + weights source + optional model_id;
    field names verified against vLLM v0.23.0 tag) → 12-hex deterministic
    fingerprint; tenant_cfg_from_vllm moved here (also fixes is_mla_model
    hardcoded-False → ModelConfig.use_mla).
  - _keys.py: build_tenant_suffix(cfg, model_fingerprint=None) — fingerprint
    slots between model and tp/pp; None ⇒ byte-identical legacy format.
  - vllm_connector.py: tenant = model + fingerprint (loud warn if
    underivable); identity sources logged at startup; miss-after-marker
    warning now states causes per TTL config (ttl=0 ⇒ never blames TTL,
    points at tenant/model mismatch); _AutumnKVStore exposes tenant/marker_ttl.
  - sglang_backend.py: default tenant UNCHANGED (no sglang invalidation);
    optional extra_config["model_id"] escape hatch.
  - __init__.py: lazy re-exports (PEP 562) so pure modules import without the
    autumn native module → offline unit tests possible.
  - NEW tests/test_tenant_identity.py: 17 tests green (uv run --with pytest,
    no cluster/engine/native module). Cluster-required smoke/dataplane tests
    unchanged (cannot run on this Mac — need 1-node cluster + maturin wheel).
  - Docs: README (tenant/fingerprint/model_id/upgrade), plan doc §5+§13.3,
    ops.md verify+cleanup runbook, feature_list.md BUG-KVC-TENANT.
Residual NOT closed (documented): same-arch finetunes from the SAME local
  path (no weights_path) still collide → model_id override; weights digest =
  future work. Upgrade = vLLM-pool cold cache; old kvc/model-cfg_0_1/vllm/*
  keys (ttl=0) need manual batch_delete to reclaim space (live cluster
  currently self-consistent per team-lead; NOT touched).
Next steps: team-lead review (+coco per repo rule) → commit → redeploy vLLM
  hosts with the new package and verify two models log two tenants.

---- Concurrent task (same day, separate) ----
Date: 2026-07-16
TaskStatus: not_completed
Task scope: F-KEY-NS design doc — 内置应用 key 命名空间 + 分裂规则（doc only,
  无代码改动, 未 commit — team-lead 明确指示 doc-only + 待 review）。
Done: docs/key_namespace_split_design.md（现状/问题分级/三个 size 口径钉死/
  设计 D1-D5/迁移/待拍板/前期调查订正）; feature_list.md 新增 F-KEY-NS 伞条目
  （子项 F-POLICY-SIZE-EST-LIVE / F-KEY-NS-FUSE / F-SPLIT-AT-KEY /
  F-PRESPLIT-DEPLOY-MODE, passes:false）。
Key findings: size_bytes 已复活（background.rs:309, F-PS-SIZE-BYTES-DEAD
  2026-07-05）= LSM 常驻字节 → policy 对 VP 负载失明; manager CLAUDE.md
  "size_bytes always 0" 陈述已过期; info 45.4GB = stream 全 extent 字节含
  未 GC 死字节; est_live 四组件（sealed 和/open_tail/gc_debt/open_tail_dead）
  全部已有 → 修口径是 manager-only 零 wire 改动。
Update (同日, review 后定稿): 用户拍板前缀 = fs/{tenant}/{volume}/（迁移用
  fs/default/default/）。doc §3.1 重写：per-volume 自持状态不变量 —— ROOT_INO=1
  per volume（ino 本就 volume 作用域）、next_inode 计数器 per volume（etcd
  autumn-rs/fs/{t}/{v}/next_inode + KV floor，消灭跨 volume 灾备/删除依赖）、
  rmtomb/schema 戳进 fs/{t}/{v}/\x04、MSG_ALLOC_INODES 加 volume 字段（WIRE
  bump = D1 唯一 wire 变更，§4 表已改）；autumnfs 非 CAS race 不被多 volume
  放大。§5.1 改"已拍板"，其余 3 项保持待拍板。feature_list F-KEY-NS 同步。
Update 2 (同日): §5.2 也拍板 —— 19GB 迁移走 A 重灌（team-lead 线上 autumnfs ls
  全树盘点：只有模型权重, qwen32b 19GB 在用 + 旧 qwen 7B 5.5GB 无消费者, 全部
  hf-mirror 可再生; 旧 7B 不再灌 → 回收 ≈16.5GB EN 空间/RF3, EN 池仅 100GiB）。
  doc §3.1 迁移段/选项表/§4/§5 + feature_list 已同步; 不写迁移工具。
Update 3 (同日): §5.4 也拍板 —— authz 彻底启用覆盖 fs/kvc/mem（用户原话
  "authz彻底都加上吧"）。doc 新增 §3.6 D6：P0 完整闭环 = D1+D6（前缀给可
  保护性, authz 才把它变成已保护; rmtomb INVARIANT 首次由机制保证）; 机制
  盘点订正 team-lead 一处（autumn-op 有 gen-signing-key/tenant-create/
  tenant-delete/mint-token, 不是没有）; 真缺口 = 接线（fuse mount/PyO3/
  autumnfs credential + 部署层 signing key/prefixes env）; 灰度 = 按前缀
  mem/→kvc/→fs/ + 凭据先行（无 audit-only, 不补）; 失败模式 = 漏凭据全线
  写失败(PermissionDenied terminal) + manager 不可达>TTL−300s 拒写（数据面
  新增宽限期=TTL 的控制面依赖）。§4 顺序改 D3→D6-mem→D1+D2→D6-fs/kvc→
  D4→D5a; §5 "client 裸 key 劝阻" 被 D6 消解, 待拍板剩阈值重标定/kvc 分档/
  TTL 取值与 Secret 投递。feature_list 加 F-AUTHZ-BUILTIN 子项。
Next steps: 用户拍板 §5 剩余项 → 按 D3(F-POLICY-SIZE-EST-LIVE) 先行、
  D6-mem 次之的顺序实施。

---- Concurrent task (same day, separate) ----
Date: 2026-07-16
TaskStatus: not_completed
Task scope: BUG-MGR-RETRY-CLASS — code-classified StreamClient manager-call
  retry + owner-epoch-fence self-heal. Live incident: post-rebalance stale
  owner_epoch (partition/17: manager expected 14396, PS sent 14364 forever) →
  every alloc_new_extent burned 20×500ms futile retries → autumnfs put 45 s /
  rm 30 s; the fenced partition NEVER self-healed (safety held, liveness gap).

Code DONE (uncommitted — awaiting team-lead review; do NOT commit before review):
  - stream client.rs: typed ManagerError{code,ctx,message} from
    check_manager_resp (+ alloc_new_extent_once inline check routed through
    it); retry_manager_call classifies — deterministic codes (NOT_FOUND/
    INVALID_ARGUMENT/PRECONDITION incl. fence/unknown) FAIL FAST unwrapped;
    transport/NOT_LEADER/CODE_ERROR keep rotate+retry (no double-rotate on
    NOT_LEADER). Owner fence (CODE_PRECONDITION + matcher) Display carries the
    load-bearing "LockedByOther" token → F270 poison path.
  - common store.rs: is_owner_epoch_fence_message NEXT TO ensure_owner_epoch
    (no new wire code → no WIRE bump) + producer-pairing test.
  - PS background.rs: is_locked_by_other matches whole anyhow chain ({e:#}) —
    .context() wraps no longer hide the fence marker.
  - PS lib.rs LIVENESS: sync_regions_once drops handles whose partition
    thread is_finished() → region map arbitrates same 2s tick: still-assigned
    → reopen with FRESH epoch; rebalanced-away → clean release. shutting_down
    Cell gates the sync tick so shutdown drains can't be resurrected.
  - Docs: stream CLAUDE.md note 30; PS CLAUDE.md self-eviction section;
    common CLAUDE.md; feature_list.md BUG-MGR-RETRY-CLASS entry; docs/ops.md.
  - Tests green: stream 111 (7 new manager_retry_tests), partition-server 196
    (+fence_classifier_tests), common 13 (+matcher pairing). Workspace builds
    (autumn-fuse macOS link fail = pre-existing, Linux-only). Clippy: no new
    warnings.
Next steps: team-lead review (+coco per repo rule) → commit → live validate on
  VKE (fenced partition recovers in ~2 s instead of never). Follow-up feature
  candidate (NOT implemented): two-phase rebalance handoff (drain old owner
  before new owner opens) to close the dual-owner window itself.

---- Prior task (paused, resume after this bug) ----
Date: 2026-07-15
TaskStatus: not_completed
Task scope: F-REGION-REBALANCE — active region→PS load rebalancing. Phase A =
  manual `autumn-op rebalance` (fix the sticky-assignment concentration the M2
  rollout exposed: all 32 partitions on one PS). Phase B = dashboard auto-policy
  rebalance switch (deferred). Then build + deploy on VKE + fix the live 32/0/0.

Phase A — CODE DONE + committed 289965e, pushed:
  - wire: MSG_REBALANCE_REGIONS (0x56) + RebalanceRegionsReq{max_moves} /
    RebalanceMove / RebalanceRegionsResp{code,message,moves,moved}; WIRE v21→v22
    (fp 57fe8f244d8fd5c1, MIN=MAX=22 same-commit deploy).
  - manager: pure compute_rebalance_moves (greedy most→least-loaded until gap≤1,
    max_moves-bounded, deterministic, registered-PS-only) + handle_rebalance_regions
    (leader-gated; apply in-memory + clear moved part_addr + mirror_partition_snapshot).
    5 unit tests green.
  - client: ClusterClient::rebalance_regions. autumn-op: `rebalance [MAX_MOVES]`.
  - docs: ops.md runbook + cheatsheet; feature_list; manager CLAUDE.md.
  - coco (GPT-5.5 deep): P0 dual-owner = handled by owner_epoch fencing
    (open_partition acquires epoch BEFORE commit_length, same as failover; coco saw
    manager code only); P1 wire MIN=MAX kept (matches every prior bump + stop-world
    model); P1 part_addr staleness FIXED; P2 CLI trailing-arg FIXED.
  - Build: workspace clean (autumn-fuse macOS link fail is pre-existing, Linux-only).

Phase A — LIVE-VALIDATED on VKE (2026-07-15, user-authorized stop-world deploy):
  - CP build run 3d6c41a0 Succeeded → image 289965e (WIRE v22).
  - Deployed: apply v22 + coordinated `kubectl delete pod` (all non-etcd) →
    9/9 converged to v22 in <2 min.
  - Post-deploy the cluster was again 32-on-one-PS (sticky, as expected).
  - `autumn-op rebalance` MOVED 21 PARTITIONS → 11/10/11 across all 3 PS (info
    confirms). Post-rebalance perf-check green: write 10,152 ops + read 82,401
    ops @ 7,490 ops/s across all 3 PS (write p99 spike = cold-reopen warmup).
  - F-REGION-REBALANCE Phase A works end-to-end on the live cluster.

Phase B — CODE DONE + committed (997f030 after rebase over an unrelated chaos fix),
pushed. WIRE v22→v23 (fp 6b0224cb5e4f13f3, same-commit).
  - POLICY_KIND_REBALANCE=7 + policy_kind_names row.
  - PolicyEngine::compute_rebalance_advisory (cluster-level, one candidate/tick from
    regions+ps_nodes counts; fires when per-PS spread > rebalance_gap_threshold(2)
    outside rebalance_cooldown_sec(120s) via last_rebalance_at) → recompute_advisory_cache.
  - actuate_candidate + actuate_action(dashboard Apply) REBALANCE arms →
    handle_rebalance_regions(rebalance_max_moves_per_tick=4), bounded/gradual.
  - 6th switch `rebalance` (SWITCH_ORDER [split,ec,compact,gc,merge,rebalance]);
    kinds_from_switches/preset[bool;6]/sanitize 6/kind_priority=1/str/describe/
    candidate_to_cmd/candidate_to_action. ON in balanced+aggressive presets.
    dashboard HTML toggle + switch_order/switches_to_dict/upsert; autumn-op
    policy-candidates kind map + feas.
  - config split: policy DEFINITION persisted (autoPolicy/config rkyv, Vec<bool>
    forward-compat → old config decodes, sanitize pads rebalance off); 3 thresholds
    in in-memory PolicyConfig (NOT persisted, like all advisory thresholds).
  - 4 rebalance_advisory_* tests + updated auto_policy switch tests green; clippy clean.
  - coco (GPT-5.5 deep): wire MIN=MAX kept (same-commit); dashboard Apply path +
    CLI kind map/feas wired (P2+P3 fixed).

NEXT: LIVE deploy+validate of Phase B needs USER AUTH (stop-the-world v23 deploy).
  Validate: arm a policy with rebalance ON → concentrate → advisory fires → armed
  controller converges gradually; dashboard switch toggles; default-off preserved.

DESIGN Q (user, answered): a policy carries only switches + interval + cooldown +
  max_actions; per-kind tuning (rebalance max_moves/gap/cooldown, and all other
  advisory thresholds) is GLOBAL in-memory PolicyConfig, not per-policy. Per-policy
  params would need MgrAutoPolicyEntry wire fields or a persisted PolicyConfig key —
  offered as a separate future enhancement.

Prior task (DONE, do not redo): F-EN-DYNSHARD M2 — per-pod EN Services eliminated,
  pod-IP self-register, live-validated (commit fd2bfbf). Rollout exposed the sticky
  region→PS concentration that THIS feature (rebalance) fixes.
  See [[vke-rolling-restart-concentrates-partitions]].
