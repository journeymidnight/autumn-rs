Date: 2026-06-16
TaskStatus: completed
Task scope: MERGE-EC-REPLAY —— merge survivor 重启永久 WAL-FAILSTOP 根因定位 + 修复 + 端到端救活。
Current summary:
  真因(debug 埋点实锤,非前两次假设):StreamClient::read_committed_bytes_from_extent 算对了
  committed_end=sealed_length(3GB),却用 `Ok(r) => return Ok(r)` 把 read_with_layout 的元组整个透传,
  EC 路径 ec_subrange_read 第二元素是 shard 相对长度(~1GB)→ PS WAL replay 拿它当停止边界,只读 1 个
  shard,跨 shard 边界记录截断 → WAL-FAILSTOP。merge 只是把 survivor VP-head replay 窗口移到这个 EC log
  extent(victim 自己从不 replay 它),触发早已存在的透传 bug。数据零丢失。
  修复:成功分支返回 (bytes, committed_end) 用本函数权威 committed_end。crates/stream/src/client.rs 一处。
  端到端验证:part 25 重启读满 3GB 打开、绑 9302、autumn-op info 4 分区无 warning、ls 对其范围正常返回。
  stream lib 86 全绿。
  走过的弯路(已 revert,登记在 feature_list):
   - 误判 committed_end 被 clobber 成 1 shard(其实 read_committed 算的是 3GB,被透传丢了)。
   - 误判 manager `sealed` bool=false(其实 true)。
   - merge eversion desync(manager=4/EN=3,真实存在但非本 bug 病因,不导致 WAL-FAILSTOP)→ deferred。
Main gaps:
  - 回归测试待补(需 StreamClient+manager+EC infra,现有 stream 集成测试用裸 TestConn)。
  - merge eversion desync 修复 deferred(reproduce-first,危害未证 + merge 区 revert-prone)。
Next steps:
  - commit crates/stream/src/client.rs 修复。
  - (可选)补 EC committed_end 回归测试;评估 merge eversion desync 是否值得修。
  - 用户需重启 gallery 进程(本任务多次 restart-ps 期间 gallery 掉了)。
