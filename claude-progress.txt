Date: 2026-06-25 (review 战役 — /loop 自驱:coco review + 每轮 chaos + 删 F### 注释)
TaskStatus: completed
LOOP 设定(用户 /loop 动态模式,3 条累积指令):① coco 参与 review,逐一处理冗余/逻辑混乱文件;
  ② 每轮重跑 chaos 保证稳定性;③ 清理时删 F### 这类 feature-number 标签,只留必要注释(实质 what/why)。
  每轮流程:挑一个冗余/乱文件 → dedup/简化 + 删 F### 标签(保留实质注释)→ coco findbugs 评 diff →
  build+clippy+全量单测 → system_chaos(merge/split/gc 种)→ commit+push → 下一轮。
  F### 删除解释(待用户确认):删 "F###:"/Item-N 标签 + "pre-F### 曾是 X" 考古;保留 invariant/结构说明
  (etcd-first、CAS baseline、refuse 理由)。CLAUDE.md/feature_list.md 是文档不算"注释",不动。
ROUND 1 COMMIT(d8234e2,已 push):rpc_handlers.rs handle_stream_punch_holes + handle_truncate 的两段
  重复块(recovery+EC inflight refuse + ref-drop loop)→ refuse_if_removed_extent_inflight +
  compute_extent_ref_drops 两 helper(纯读 s,全在既有只读 out 闭包内,不碰 CAS baseline/mirror/apply →
  refuse 顺序/push 顺序/membership-CAS/etcd-first 全保)。op-name 参数化错误信息一致。同时删该区 F###/Item-N
  标签 + pre-F### 考古,留实质。net -26(127/-153)。验证:164/164 + clippy 0 + **coco findbugs deep(GPT-5.5)
  未发现问题** + **system_chaos seed 603(90s,真 EN kill+split/merge/gc):Split+Merge OK,mismatch=0
  not_found=0 accounting=0 all invariants OK**。
ROUND 2 COMMIT(04041c6,已 push):rpc_handlers.rs handle_multi_modify_split + handle_multi_modify_merge 的
  verify-before-mirror eversion-drift 检测块(borrow store + iterate pre_bump_eversion + 比 live vs expected)
  → first_eversion_drift(&self, map) -> Option<(eid,expected,live)>。两调用点保留各自 refusal 编码
  (CodeResp vs MultiModifyMergeResp,响应类型不同必须留调用点)。检测仍在 etcd txn 前、仅 live 存在且
  eversion!=expected 才拒——语义不变。同删该区 F### 标签(verify rationale / last_op stamp / Phase-3 note)。
  net ~平(+2;价值=漂移检测单一真源)。验证:164/164 + clippy 0 + **coco findbugs deep(GPT-5.5)未发现问题**
  + **chaos seed 603(90s):Split+Merge OK,mismatch=0 accounting=0 all invariants OK**。
ROUND 3 COMMIT(7b319f4,已 push):manager/lib.rs 6 个 mirror_* 的 etcd kv 构造
  `(format!("<prefix>/<id>"), rkyv_encode(v).to_vec())` × ~12 → Self::kv_entry(prefix,id,value)
  泛型 helper(rkyv Serialize bound,write-once verbose)。每站点 1 行。(key,bytes) 字节级相同;txn 调用/
  value-CAS(put_delete_txn_cas)/F149 fence/顺序不动;ps_nodes(into_bytes 非 rkyv)留 inline。删 2 处 Item-N
  CAS 注释标签。net -14;rkyv_encode().to_vec() 14→3。验证:164/164 + clippy 0 + **coco findbugs(GPT-5.5)未发现**
  + **chaos seed 603(90s,每个 mirror_* 都被 create/alloc/punch/truncate/split/merge/register 走到):
  Split+Merge OK,mismatch=0 accounting=0(12ext/22membership) all invariants OK**。
ROUND 4 COMMIT(2c813d1,已 push):stream/extent_node.rs(#2 大文件,热路径)——已有 err_bytes(req_id,
  msg_type,code,msg)->Bytes helper(7 个 over-slot 站点在用),但 append/append-chain/read-bytes 的 7 个
  single-frame 错误路径仍 inline `let p=encode_status; let bytes=Frame::error(...).encode()` → 改用 err_bytes。
  字节级相同。3 个故意留:2 个 build-p-once+per-slot clone(微优化),1 个 build 未编码 Frame(共享 Ok 臂
  Frame::response 形状)。修 err_bytes 过期 doc。net -6。验证:90/90 + clippy 0 + **coco findbugs(GPT-5.5)未发现**
  + **chaos seed 603(90s,kill/unavailable 窗触发这些错误路径):Split+Merge OK mismatch=0 accounting=0 all OK**。
ROUND 5 COMMIT(dd2ee21,已 push):partition-server/background.rs——background_compact_loop +
  background_gc_loop 各有字节级相同的嵌套 fn random_delay()→ 提到模块级单一 fn,删两份嵌套。net -2。
  验证:167/167 + **coco findbugs(GPT-5.5)未发现** + **chaos seed 603 Split+Merge OK all invariants OK**。
  **coco INSPECT 全文件失败**(GPT-5.5 + GLM-5.1 都 exit=1 在 3.7k 行大文件上——本机 coco 不稳,记忆已知);
  本轮回退手动 dedup;findbugs 在小 diff 上正常。background.rs 其余无明显大冗余(lookup_in_sst_seq_opt/fullscan
  是故意不同算法,gc_* 是干净命名 accessor,do_compact/run_gc/process_gc_chunk 是热路径大函数留后)。
观察:① 干净 dedup 渐枯竭(R4 -6 / R5 -2);剩多为热路径大函数(do_compact/run_gc/partition-server lib.rs)需
  reproduce-first 谨慎,或全文件 F### 清理(judgment-heavy,量大);② coco inspect 在大文件上不稳,findbugs(小 diff)稳。
ROUND 6 COMMIT(da16a02,已 push):partition-server/rpc_handlers.rs——handle_batch_get/get_value_inner/
  handle_head/handle_range 各 inline 字节级相同的 region-epoch 检查(`if req.region_epoch!=0 && !=p.region_epoch
  { return Err((FailedPrecondition, format!("region epoch stale..."))) }`)。4 处同 (StatusCode,String) 返回 →
  抽 check_region_epoch(part_id,have,req_epoch)->Result<(),(StatusCode,String)>,各站点 `?` 调用。删 get 站点冗余
  注释 + 删 range 站点 "F-this" 占位标签 + "pre-this" 考古(留 gallery-bug load-bearing 理由)。net -24(R1 后最佳)。
  验证:167/167 + clippy 9 warning 不变(全 pre-existing)+ **coco findbugs(GPT-5.5)未发现** + **chaos seed 603
  Split+Merge OK,range verify errors=0(直接相关:handle_range 被 dedup),mismatch=0 accounting=0 all OK**。
Next(loop 续):⑦ stream/client.rs(5.2k,只做过 eversion policy)/ fuse/dispatch.rs(1.7k)找小 dedup;
  或「专门 F### 清理轮」(挑一文件系统删标签);⑧ 热路径大函数(do_compact/run_gc/partition-server lib.rs)最后,
  reproduce-first。split/merge Phase-2 kvs builder MEDIUM 跳。coco inspect 大文件不稳(findbugs 小 diff 稳)。
ROUND 7 COMMIT(4d991aa,已 push):stream/client.rs——9 个 manager-RPC wrapper 的相同尾
  (`note_manager_code(resp.code); if resp.code!=CODE_OK { return Err(anyhow!("<ctx> failed: {}", resp.message)) }`)
  → check_manager_resp(&self,code,message,ctx)->Result<()>,各 `?` 调用。9 处:nodes_info/list_node_states/
  stream_info×2/check_commit_length/punch_holes/truncate/extent_info/multi_modify_split。report_corrupt_replica
  用 "refused"(故意不同词)留 inline。net -16。验证:90/90 + clippy 无新增 + **coco findbugs(GPT-5.5)未发现**
  + **chaos seed 603 Split+Merge OK mismatch=0 accounting=0 all OK**。
ROUND 8 COMMIT(db3ddb0,已 push):crates/client/src/lib.rs(ClusterClient SDK)——6 个方法
  (put_opts/put_zc_opts/delete_opts/range/gc_with_params/maintenance)的相同 guard
  `if resp.code!=CODE_OK { return Err(code_to_error(resp.code, resp.message)) }` → check_ps_code(code,&str)->Result。
  借 message(仅错误路 clone)。两个 batch-loop(per-item results[idx]=Err+continue)+ delete(容 NOT_FOUND)留。
  **coco findbugs 抓 1 个 P3**(我 doc 写 "PS/manager" 误导——code_to_error 是 partition code 空间,manager
  CODE_NOT_LEADER=5 ≠ partition CODE_VALUE_TOO_LARGE=5)→ 改 doc 为 PS-ONLY + 警告。net -2(6 guard 集中)。
  验证:27/27 + **coco findbugs(GPT-5.5)1 P3 已采纳** + **chaos seed 603 Split+Merge OK range verify errors=0
  mismatch=0 accounting=0 all OK**。
ROUND 9 COMMIT(17a6649,已 push):stream/extent_node.rs——build_read_future 两处重复的 per-slot
  CODE_EVERSION_MISMATCH read response 构造(zc head vs 完整 ReadBytesResp frame)→ eversion_mismatch_read_resp
  (req_id, zc)->Bytes。corrupt-meta refuse loop + per-slot `req.eversion<ev` 两处。非批量 handle_read_bytes
  (返回 Ok、无 zc)留。net -9。验证:90/90 + **coco findbugs(GPT-5.5)未发现(等价抽取)** + **chaos seed 603
  (EC nemesis bump eversion 触发此路径)Split+Merge OK mismatch=0 accounting=0 all OK**。
教训(loop 选 target):fuse/dispatch.rs + ioring/daemon.rs 不被 system_chaos 覆盖(改了无法满足"每轮 chaos"要求,
  fuse 要 fuse_chaos.sh 慢)→ loop dedup 只在 chaos-validated crate(manager/stream/partition-server/client)做。
  bash classifier 本轮短暂不可用(infra flake),重试即恢复。
用户方向调整(R10):① AskUserQuestion 选了「F### sweep」,但自动 F### 脚本(第二 pass 的 replace('  ',' ')
  误伤所有注释缩进,345 行)→ **revert,放弃自动 F### sweep**(太易误伤,逐条手动太多)。
  ② 用户补充:Giant 函数(do_compact/run_gc/process_gc_chunk/recover_partition)**重点不是拆分,是分析逻辑
  看是否简洁清晰**。→ loop 转向:读 giant 函数、评估 clarity/conciseness、去重/简化(非机械拆分)。
ROUND 10 COMMIT(f2806b7,已 push):partition-server/background.rs do_compact 逻辑分析——chunk-finalize 序列
  (spawn_blocking finish → account_compact → compact_row_append(P-bulk 单写者)→ spawn_blocking SstReader →
  push TableMeta+reader)在 in-loop emit + final emit **重复近 verbatim(~30 行 ×2)**,仅差 final 先 set_discards。
  抽 emit_compact_chunk(...)->Result<u64> 单一 helper(in-loop/final/set_discards/post-reset 差异留调用点)。
  **是去重(简洁)非拆分凑行数;F148-A meta-publish await-ordering 路径不碰**。net -6(逻辑去重是重点非 LOC)。
  验证:167/167 + clippy 9 不变 + **coco findbugs(GPT-5.5)未发现(逐项核对等价:逻辑顺序/discards 语义/
  output_bytes/无新 race-leak)** + **chaos seed 603(split/merge/gc 触发 compaction)Split+Merge OK all OK**。
进度:10 轮全绿(R1-R10)。do_compact 分析剩余 finding(未做,记录):final-emit 后的 defer-discards else-if 分支
  (~25 行多为冗长道歉式注释,代码≈no-op debug log)可精简注释;loop 内 5× "bump_discards+entries_discarded+++continue"
  drop-entry 模式(闭包去重有 borrow 摩擦,价值小)。
ROUND 11 COMMIT(13ea3c6,已 push):giant 函数逻辑分析续——run_gc + process_gc_chunk 读完:**intricate 但复杂度
  是 GC 正确性内在的(streaming carry / stale-snapshot liveness 重验 / full VP-identity),注释充分,无可安全简化的
  冗余/convolution → 不动**(分析结论=已清晰)。唯一 clarity wart = do_compact 的 defer-discards else-if 分支:
  绑 last_reader 仅 `let _ =` 丢弃 + ~17 行注释描述了一个"先说这样做→其实为简单跳过"的**已放弃方案(注释了不存在的代码)**。
  → 改 `!new_readers.is_empty()` 等价条件(去掉无用绑定 + let _)+ 注释精简到实际决策。行为不变(分支只 debug log)。
  net -11。验证:167/167 + clippy 9 不变 + **coco findbugs(GPT-5.5)未发现** + **chaos seed 603 Split+Merge OK all OK**。
观察:process_gc_chunk 11 个参数是 "too many args" smell,但参数都真需要(immutable ctx + mutable 累加器),
  bundle 成 struct 是中等重构 clarity 收益有限,暂不做。process_gc_chunk:2599 `let _ = expires_at;`(下游真用了,vestigial)
  太marginal没动。
进度:11 轮全绿(R1-R11)。giant 函数分析:do_compact(R10 去重 chunk-emit + R11 简化 defer 分支)、run_gc/process_gc_chunk(清晰)。
ROUND 12 COMMIT(1fe0150,已 push):recover_partition 逻辑分析(读了前 ~360 行:meta-records union / SST-open /
  dedup-region setup)——intricate 但复杂度内在(post-merge 多源 dedup / CoW dedup / eversion-retry / 非单调 extent
  位置),注释足。**清晰 wart = replay-dedup 前导的 dead `let sst_max_seq=max_seq; let _ = sst_max_seq;`**(绑了立刻
  丢弃从不读;max_seq 直接用别处)+ 挂在 `let _ =` 上的 ~20 行设计注释 + 上方另一段 intro 注释还在描述**已废弃的
  single-sst_max_seq dedup**("`if ts <= sst_max_seq { continue; }` below"——该代码已不存在,replay 是 per-region)。
  → 删 dead 绑定 + 两段重叠注释合并成一段描述**实际** per-source-region dedup(+ 为什么:多源独立 PS-seq → union max
  会跳 survivor tail;FuturesOrdered 给 per-source contiguous-seq 不变量)+ 去 F### 标签。net -19。
  验证:167/167 + clippy 9 不变 + **coco findbugs(GPT-5.5)未发现(注释+dead-code only 无可执行变更)** +
  **chaos seed 603(split/merge reopens 驱动 recover_partition)Split+Merge OK all OK**。
ROUND 13 COMMIT(7b461fa,已 push):recover_partition 逻辑分析收尾 + R12 一致性 follow-up。
  ① 读完 replay loop 全段(7180-7548):intricate 但复杂度内在(CoW-dedup extent 选择 / committed-clamped
  chunked-carry 读含 10-attempt 重试 / 跨副本 self_heal_replay_chunk / fence-bump 绕过 ts-dedup / VP 经
  value_offset_in_record 重建 / corrupt-replica A5 隔离),注释足、无可安全简化的逻辑(R12 已是该函数唯一 wart)。
  深嵌套用的是**有意 de-indent**(chunk loop body 与 `loop {` 同 12-space 缩进)——不值 220 行 whitespace churn。
  ② 修 R12 残留的 2 处 stale doc 引用:lib.rs:5331(partition_loop FuturesOrdered 设计 doc)+ 5387 两处仍写
  `if ts <= sst_max_seq { continue; }`(该变量 R12 已删,recovery 现为 per-source `extent_dedup`)→ 改成概念化措辞
  "per-source dedup(skip a record whose seq is already covered by that source's SSTs)",对 rename 鲁棒。注释 only。
  验证:build+clippy(23 warn 不变,均 pre-existing)+ **167/167 ps 单测** + **coco findbugs deep(GPT-5.5)未发现
  (确认 5331/5387 注释文字调整,无可执行逻辑/状态机/并发/资源/错误处理变更)** + **chaos seed 603(58s,split/merge
  reopen 驱动 recover_partition):writes acked=10737 reads ok=19391,per-key mismatch=0 not_found=0、range=0、
  write-liveness=0、accounting=0(5ext/8membership)all invariants OK**。net -4(5/-4)。
ROUND 14 COMMIT(1fd8f6a,已 push):stream/extent_node.rs handle_convert_to_ec(全 crate 最大函数 458 行,EC 2PC 协调器)
  逻辑分析 + EC-2PC-participant RPC 去重。读完全函数:**intricate 但复杂度内在**(idempotent-skip /
  coordinator-prepared skip / F128 post-rename 崩溃恢复 / peer-copy / 分条 RS-encode / 2PC commit,每分支注释足),
  逻辑清晰——与 recover_partition 同结论。唯一真结构冗余:**WriteShard fanout(phase-1)+ CommitEcShard fanout
  (phase-2)两块 `match rpc_oneshot → decode → code!=OK 拒 → Err 臂` 同形**(仅 Resp 类型 + label 串不同)→ 抽
  free fn `ec_2pc_participant_rpc(sock, msg, payload, label, decode_code 闭包)`。**消息字节级相同**:label 串
  ("WriteShard to {addr} shard {i} @ {off}" / "CommitEcShard to {addr} shard {i}")承载 code=/transport 两条,
  decode-err 串由各站点闭包内保留("decode write_shard resp" / "decode commit_ec resp")。控制流 `?` 早退等价
  原 `return Err`。in-family 于 R6/R7/R8 的 check_* 抽取。net -4(43/-47;helper +28 但 2PC participant 错误映射
  现单一真源)。验证:build+clippy(22 warn 不变,changed-code 无新增)+ **90/90 stream 单测** +
  **coco findbugs deep(GPT-5.5;首跑双模型 transient 失败,重试 OK)未发现** + **chaos seed 603(58s,EC nemesis 驱动
  handle_convert_to_ec):writes acked=10466 reads ok=19687,per-key mismatch=0 not_found=0、range=0、
  write-liveness=0、accounting=0(5ext/8membership)all invariants OK**。
ROUND 15 COMMIT(99dfdaf,已 push):大函数清晰度分析 ×3 + handle_append 拒绝响应 boilerplate 抽取。
  分析:① ps sync_regions_once(243 行)= 6 段不同 pass(fetch+decode/build wanted/drop-for-reload/open-new
  含 F196 budget+BUG#3 backoff/prune backoff/F265 part_addr self-heal),各段注释足、无重复 pattern → 清晰,不动。
  ② stream load_extents(192 行)= 每 extent 线性流(owns-check → open .dat → META-FAILCLOSED parse/quarantine →
  insert → EC-COMMIT-ATOMIC marker replay),崩溃恢复 intricate 但内在,quarantine 动作已是局部闭包 → 清晰,不动。
  ③ stream handle_append(252 行,非批量 append 路径)= 真有**清晰度问题**:append 协议 10 个 guard 之间散落 10 份
  字节级相同的 6 行拒绝块 `return Ok(AppendResp{code:X,offset:0,end:0}.encode())`,淹没协议逻辑。
  → 抽**纯响应构造** free fn `append_reject(code)->HandlerResult`,每 guard 变单行 `return append_reject(CODE_X);`。
  **零语义风险**(只构造响应字节,不碰 seal/fence/commit/并发逻辑);`return Ok(AppendResp{` 全文件仅此函数有
  (批量路径用 `let resp_payload=`),success 站点(real offset/end)留 inline。net -35(25/-60)。
  验证:build+clippy(22 warn 不变,无新增)+ **90/90 stream 单测** + **coco findbugs deep(GPT-5.5)未发现
  (显式核对全 10 调用点 return 类型/错误码/offset-end 语义一致)** + **chaos seed 603(58s,kill+restart+commit-trunc
  nemesis 重压 handle_append 各 guard):writes acked=10635 reads ok=19594,mismatch=0 not_found=0、range=0、
  write-liveness=0、accounting=0(5ext/8membership)all invariants OK**。
ROUND 16 COMMIT(88b3b93,已 push):大函数清晰度分析 ×4(清晰即不动)+ manager handle_stream_alloc_extent 拒绝 boilerplate 抽取。
  分析(均 intricate 但内在复杂、注释足、不动):① ps open_partition(215)= 线性 setup→spawn 线程→await ready→建
  handle;`*_for_thread` clone + 22-arg partition_thread_main 是搬状态进线程的固有形,非冗余。② stream
  stream_extent_from_sources(181)= SEED13 over-promised-seal 调和(err_count==0&&unverified==0 守卫),3 个
  unverified++ skip 各带不同 warn 文案,无干净去重。③ ps sync_regions_once / ④ stream load_extents(R16 复核,R15 已记)。
  动手:**manager rpc_handlers.rs handle_stream_alloc_extent(全 manager 最大函数 594 行,seal+alloc+roll)**——读完全函数:
  复杂度内在(F227 lenient seal + BUG2-IDEMPOTENT-ROLL + Item3 membership-CAS + verify-at-apply + fallback alloc,
  16+ programming notes 覆盖),逻辑清晰。真结构冗余:**16 处 `return Ok(rkyv_encode(&StreamAllocExtentResp{code,
  message,stream_info:None,last_ex_info:None}))` 拒绝块**淹没 seal/CAS/verify 逻辑 → 抽 `Self::alloc_reject(code,
  message)`(纯 rkyv 响应构造,同 R15 append_reject)。**零语义风险**:只换 return 尾,seal/fence/CAS/verify 逻辑行
  一字未动;rkyv 同结构=同字节。idempotent no-op(Some/Some,1557)+ success 尾(无 return)正确保留 inline。
  16 站点:7 err(Self::err_to_code+to_string)+ 5 not_found + 4 precondition,按缩进/`;` 分组(replace_all 同形块 +
  个别 edit)。net -65(31/-96)。验证:build+clippy(4 warn 不变,无新增)+ **164/164 manager 单测** +
  **coco findbugs deep(GPT-5.5)未发现** + **chaos seed 603(58s,每次 extent roll/seal/split 走 handle_stream_alloc_
  extent):writes acked=10456 reads ok=19482,mismatch=0 not_found=0、range=0、write-liveness=0、accounting=0
  (6ext/10membership)all invariants OK**。
观察:directive#4 双结论再印证 + 扩到 manager:多数大函数 analyze 后清晰即不动(open_partition/stream_extent_from_
  sources/sync_regions_once/load_extents),个别有真拒绝-boilerplate(handle_append R15 / handle_stream_alloc_extent
  R16)→ 纯响应构造抽取 zero-risk(只碰 return 尾,不碰 guard/seal/CAS 逻辑)。**alloc_reject 关键:虽是最 guard-heavy
  manager 函数,但抽取不触任何 guard 逻辑,故风险等同 R15。**
进度:16 轮全绿(R1-R16),均 push(R16 本轮 push)。giant 战役覆盖:partition-server 4 + stream 2(handle_convert_to_ec/
  handle_append)+ manager 1(handle_stream_alloc_extent)。chaos-validated 区 clean dedup + analyze 目标已基本枯竭。
Next(loop 续):⑰ 剩余大函数多为 guard-heavy 控制面(handle_multi_modify_merge 388/split 276/force_ec_convert 278/
  merge_partitions 259/replay_from_etcd 344)——可继续 analyze + 仅抽 zero-risk 拒绝/构造 boilerplate;或报告战役完成、
  等用户定向(热路径 build_append_future/partition_loop 仍 reproduce-first 暂缓)。coco+chaos 每轮不变。

────────────────────────────────────────────────────────────────────────────────
ROUND 17 COMMIT(d9491af,已 push):handle_multi_modify_split 清晰度分析(清晰即不动)+ **manager rpc_handlers.rs 全文件
  CodeResp boilerplate 抽取(directive#1 大文件去冗余)**。
  分析:handle_multi_modify_split(276)= 清晰 Phase1(闭包内 compute,零 store 改)/Phase2(单原子 etcd txn,含
  partitions+regions+lastOp 防 orphan)/Phase3(apply),#6 split-inflight guard + F146 verify-BEFORE-mirror(first_
  eversion_drift),注释足 → 不动。CodeResp 拒绝在此仅 5 处、稀疏、自解释,非单函数 obscuring。
  动手:rpc_handlers.rs(5660 LOC,manager 最大文件)有 **24 处** `Ok(rkyv_encode(&CodeResp{code,message}))` 散落
  全 ~15 handler——manager 最常见响应形,无 helper → 抽 `Self::code_resp(code,message)`(纯 rkyv 构造,同 R16
  alloc_reject/R15 append_reject 家族)。**零语义风险**:只换响应构造尾,无逻辑改;rkyv 同结构=同字节。其它 Resp
  类型(StreamAllocExtentResp/UpsertPartitionResp/GetRegionsResp/ReconcileExtentsResp)非 CodeResp,不动。
  24 站点按 shape×缩进×return/`;` 分组:3 replace_all(OK-8tail×7 / ERR-12return×7 / ERR-16return×2 = 16)+
  8 个别 edit(OK-16/OK-12 tail / match-arm Err / `{code,message}` 一行 / CODE_PRECONDITION-msg / ps-not-found /
  2 个多行 format! 消息)。helper body 自身不转(用 `.await;` 锚点排除)。net -54(39/-93)。
  验证:build+clippy(4 warn 不变,无新增)+ **164/164 manager 单测** + **coco findbugs deep(GPT-5.5)未发现
  (显式核对返回类型/响应编码/错误码传播/早退/RAII guard/borrow-await 边界/跨 handler 行为均不变)** +
  **chaos seed 603(58s,register_ps/heartbeat_ps/get_regions/split 等全走 code_resp):writes acked
  mismatch=0 not_found=0、range=0、write-liveness=0、accounting=0(5ext/8membership)all invariants OK**。
观察:response-boilerplate 整合三件套完成:append_reject(AppendResp,R15)+ alloc_reject(StreamAllocExtentResp,R16)
  + code_resp(CodeResp,R17)——manager/EN 全部状态响应现走构造 helper,纯构造零风险。CodeResp 是跨 handler(非单
  giant 函数)的 directive#1 大文件去冗余,与 giant-函数 clarity 分析互补。
进度:17 轮全绿(R1-R17),均 push(R17 本轮 push)。giant 战役 + response-boilerplate 整合基本收口;chaos-validated
  区剩余目标多为 guard-heavy 控制面(已 analyze=清晰)或热路径 reproduce-first 暂缓。
Next(loop 续):⑱ 候选:其它 Resp 类型若也散落(UpsertPartitionResp 等,但每种仅 2-3 处,稀疏,价值低)可顺手;
  或继续 analyze 剩余 guard-heavy 大函数(merge/force_ec/replay_from_etcd)报清晰;或报告战役收口、等用户定向新方向。
  热路径 build_append_future/partition_loop 仍 reproduce-first 暂缓。coco+chaos 每轮不变。

────────────────────────────────────────────────────────────────────────────────
ROUND 18 COMMIT(4574547,已 push):handle_force_ec_convert 清晰度分析 + force_ec_resp 响应 boilerplate 抽取。
  分析:handle_force_ec_convert(278,F203 OP-driven EC 触发器)= 清晰逻辑流(inflight 检查 → 单 borrow 取 state+
  EC-stream → target-node 推导含 shuffle fallback → F209-D verify-BEFORE-acquire → 统一 inflight marker CAS),
  注释足 → 不动。**14 处 `Ok(rkyv_encode(&ForceEcConvertResp{code,message}))`**(ForceEcConvertResp 结构=
  {code,message},与 CodeResp 同形但独立 wire 类型)→ 抽 `Self::force_ec_resp(code,message)`(纯构造,同 R16/R17)。
  零语义风险:只换响应尾;14 站点全唯一(消息各异),逐个 edit;helper body 不转。
  **net ~平(+2;58/-56)** —— 14 中 7 个多行 format! 消息主导,struct→helper 调用行数近持平,本轮价值=一致性/集中化
  (非行数),诚实记录:response-boilerplate dedup 已进入"行数中性、仅剩一致性收益"的长尾。
  验证:build+clippy(4 warn 不变)+ **164/164 manager 单测** + **coco findbugs deep(GPT-5.5)未发现** +
  **chaos seed 603(58s):mismatch=0 not_found=0、range=0、write-liveness=0、accounting=0(5ext/8membership)
  all invariants OK**。
观察:{code,message} 响应类型有长尾(ForceEcConvert/ReportCorruptReplica/MergePartitions/CheckCommitLength/...
  多个结构同形但独立 wire 类型)。每个抽 per-type helper 价值递减(本轮已行数中性)。**结论:dense 响应-boilerplate
  已收口(append/alloc/code/force_ec 四 helper);剩余 per-type 是稀疏/行数中性长尾,不值逐个扫。** 真正的根因冗余是
  "多个同形 {code,message} wire 类型",但合并 wire 类型涉及 client decode 侧=语义改动,保守不做。
进度:18 轮全绿(R1-R18),均 push(R18 本轮 push)。giant-函数 clarity 分析 + dense 响应-boilerplate 整合均已收口。
  chaos-validated 区清晰目标基本枯竭:剩余大函数 analyze=清晰、boilerplate 长尾行数中性、热路径 reproduce-first 暂缓。
Next(loop 续):⑲ **campaign 收口信号明确**——建议向用户报告并请其定向新方向(如:授权热路径 build_append_future/
  partition_loop 的 reproduce-first 工作 / 扩 chaos 覆盖到 fuse·ioring 后再清那些 crate / 转性能或新 feature)。
  若 loop 续跑无新方向:仅在发现 genuine non-trivial 冗余/混乱时动手,否则记 analyze=清晰避免行数中性 churn。coco+chaos 不变。

────────────────────────────────────────────────────────────────────────────────
ROUND 19 COMMIT(47beda7,已 push):replay_from_etcd 分析 + **真·结构去重(非行数中性 boilerplate)**。
  R18 收口后用户无新方向、loop 同 prompt 再触发→按既定策略"只动 genuine non-trivial 冗余"。检查 manager/lib.rs 最大
  函数 replay_from_etcd(344):多数 per-prefix decode loop 各异(id-map 已用 replay_decode_id_map / inode_leases 是
  fail-loud 两阶段 / failed_deletes 独有),清晰。**真冗余 = node_override(1990-2008)+ decommissioned(2009-2027)两段
  字节级同构 loop**:同 decode MgrNodeOverride 进 HashMap<u64,_>,clear→parse_id(`?` fail-loud)→rkyv_decode
  (warn+skip,localizable 非 fatal)→insert,仅(目标 map / prefix const / warn label)不同 → 抽 `replay_node_override_map
  (kvs,prefix,label,&mut out)`(与 replay_decode_id_map 并列;后者 fail-loud 不同语义故独立)。两 loop→单一真源。
  warn 消息从字面 "...node_override entry" → "...{} entry"+label 渲染**字节级相同**(标准 tracing 位置格式参数)。
  net +7(45/-38;doc 注释占增量,**结构上消除重复**=真价值,非 R15-18 的行数中性响应 boilerplate)。
  验证:build+clippy(4 warn 不变)+ **164/164 manager 单测** + **coco findbugs deep(GPT-5.5)未发现(显式核对 fail-loud
  parse_id / warn+skip decode / clear+insert 顺序 / borrow_mut 生命周期 / 每调用点 prefix+label+map 正确)** +
  **chaos seed 603(58s)mismatch=0 accounting=0(6ext/10membership)all invariants OK**。注:replay node_override 路径
  需 manager-HA failover+持久 override 才直接触发(seed 603=EN-kill 不直接走);本轮为纯 behavior-preserving 抽取,
  靠 build+coco+types+164 单测 验证语义,chaos 验证无旁路回归。
观察:R19 证明 dense response-boilerplate 收口后,仍有零星 genuine 结构冗余(同构 loop)值得抽——但已是个位数、需逐函数
  挖,边际递减。replay_from_etcd 其余 prefix loop 各异(非冗余)。
进度:19 轮全绿(R1-R19),均 push(R19 本轮 push)。
Next(loop 续):⑳ campaign 深度收口。建议同 R18-Next:报告并请用户定向(热路径 reproduce-first 授权 / fuse·ioring 扩
  chaos / 转 perf·feature)。无新方向则继续"只动 genuine 结构冗余,否则 analyze=清晰"——但此类目标已个位数,可能多为
  no-change analysis 轮。coco+chaos 不变。

────────────────────────────────────────────────────────────────────────────────
ROUND 20 COMMIT(6541833,已 push):mirror_* 族分析(已 well-factored,不动)+ **真·跨函数结构去重:split+merge 的 eversion 快照**。
  分析:mirror_* 6 helper(R3 kv_entry 后已 well-factored,6-26 行)——唯一重复是 stream_cas 构造(2 处 4 行,trivial,
  按既定 bar 不抽)。handle_multi_modify_merge(388,最大未读 handler)= 清晰 Phase1(闭包+Phase1Result struct)/1.5
  (alloc fallback)/2(原子 etcd txn,uniform-CAS survivor baselines)/3(apply),注释足,MultiModifyMergeResp 仅 5 处稀疏。
  真冗余 = **split(2509-2521)+ merge(2786-2798)的 pre_bump_eversion 快照两段字节级同构**(F146 verify-at-apply
  baseline:遍历 stream-id 切片 → 取每 stream 的 extent_ids → 取 extent.eversion → 插 HashMap),仅 stream-id 切片不同
  (split 3 流 / merge all_streams 6 流)→ 抽 `Self::snapshot_stream_extent_eversions(state, stream_ids)`,与既有
  first_eversion_drift(R2,verify 侧)配对。两 13 行块 → 单一真源。net +1(27/-26,doc 占;**结构消除重复**)。
  验证:build+clippy(4 warn 不变)+ **164/164 manager 单测(含 split/merge 回归套)** + **coco findbugs deep(GPT-5.5)
  未发现(调用点行为与内联一致)** + **chaos seed 603(58s,Split+Merge nemesis 直接走两快照点):mismatch=0
  not_found=0、range=0、write-liveness=0、accounting=0(6ext/10membership)all invariants OK**。注:本轮改动**直接被
  chaos 覆盖**(split+merge),非 R19 replay 的旁路验证。
观察:R20 再得一个 genuine 跨函数结构去重(eversion 快照 split↔merge),且直接 chaos-validated。split/merge 族现共享
  compute_*/splice_*/first_eversion_drift/snapshot_stream_extent_eversions 多个 helper,verify-at-apply 两侧均单一真源。
进度:20 轮全绿(R1-R20),均 push(R20 本轮 push)。
Next(loop 续):㉑ genuine 结构冗余已个位数且零散。继续按既定策略:挖到 genuine 非平凡结构去重就动(coco+chaos),
  否则 analyze=清晰报告、不造 churn。强烈建议用户定向(热路径 reproduce-first 授权 / fuse·ioring 扩 chaos / perf·feature)。

────────────────────────────────────────────────────────────────────────────────
ROUND 21 COMMIT(ee6363e,已 push):转 client SDK(client/lib.rs)——**真·4-site 字节级结构去重:路由-重试 backoff+refresh 尾**。
  分析:call_ps_for_key(数据路径)vs call_ps_for_part(admin 路径)看似同构但**有意分歧**(F225:admin 路径短路
  deterministic 错误 PreconditionFailed/InvalidArgument/ValueTooLarge,数据路径绝不短路因 FailedPrecondition=stale
  epoch 必 refresh;payload 闭包 vs 固定;resolve_key vs resolve_part)→ 整体合并会 conflate 故意差异,**不合并**
  (同 rpc_handlers "21 leader 前缀" 否决教训)。**但**4 个路由-重试 loop(call_ps_for_key/part/get_into/get_range_into)
  的 backoff+refresh **尾**字节级相同(仅缩进 20 vs 12):`if attempt>=MAX{break} attempt+=1; refresh_backoff; sleep;
  refresh_regions`。→ 抽 `refresh_and_backoff(&mut attempt)->bool`(budget 用尽→false 调用方 break;否则 bump+backoff+
  refresh→true)。**只抽真正相同的尾,故意分歧的 resolve/payload/错误分类全留各调用点**。2 组 replace_all(20-space×2 +
  12-space×2)。net -5(23/-28)——真减行(4-site 去重 helper 摊销),给路由-重试步骤命名。
  验证:build+clippy(2 warn 无新增)+ **27/27 client 单测** + **coco findbugs deep(GPT-5.5)未发现** +
  **chaos seed 603(58s,workload SDK 走 call_ps_*;split/merge 触发 refresh-retry 即 refresh_and_backoff):mismatch=0
  not_found=0、range=0、write-liveness=0、accounting=0(5ext/8membership)all invariants OK**。
观察:R21 = 迄今最干净的尾期去重(4-site 字节级、net 真负、直接 chaos-validated、命名清晰),且**正确识别 call_ps_for_key/part
  的"看似 dup 实为有意分歧"不强合**(避免 conflate 教训)。client SDK 路由-重试族现共享 refresh_and_backoff 单一真源。
进度:21 轮全绿(R1-R21),均 push(R21 本轮 push)。
Next(loop 续):㉒ 继续按既定策略挖 genuine 结构去重(coco+chaos),否则 analyze=清晰。建议用户定向(热路径 reproduce-first
  授权 / fuse·ioring 扩 chaos / perf·feature)。client SDK 其余(range/batch_put/get_many fan_out 已是 F245 单一真源)。

────────────────────────────────────────────────────────────────────────────────
ROUND 22 — NO-CODE 分析轮(评估 2 个候选,均**正确否决**;无改动,HEAD 仍 ee6363e=R21)。
  hunt:① 评估 manager F211-C audit-entry 构造(7 处 MgrAuditEntry{op,node_id,extent_id:0,by,reason,result_code,
  result_message,ts_ns:0} 散落 fence/maintenance/clear/remove)→ 抽 audit_node_op(6 参) helper:**实做后 REVERT**——
  net +12(6 positional 参 wrap 行数≈struct)且**positional 参丢失 named-field 自解释性**(by/reason/code/message 不如
  `result_code:`/`result_message:` 清晰)= net-negative 重构;named struct 重复在此**更可读**(extent_id:0/ts_ns:0 显而易见)。
  ② lease handlers(acquire/release/heartbeat)leader-gate(`if etcd&&!leader { return XxxLeaseResp{NOT_LEADER,...,lease:None} }`)
  = 跨不同 Resp 类型的 struct 字面量、named-field 清晰 → 同 audit/force_ec 边际类,不抽。lease 各 handler 实质 registry/
  persist 逻辑各异,无 LOGIC dup。
  **结论(重要判断)**:GOOD-dedup(重复 LOGIC/loop,helper 既减行又保/增清晰——R19/R20/R21 那种)井已干。剩余"重复"是:
  (a) struct 字面量(named-field 往往更清晰,别抽成 positional helper);(b) 有意分歧(call_ps_for_key/part);
  (c) 热路径(enqueue admission preamble region_epoch+in_range+value-too-large+fence 3 处,BUG-LEASE-2 ordering load-bearing,
  reproduce-first 待用户授权)。**知道何时不抽 = 本阶段的判断力**。跳过 chaos(无 code 改动,R21 seed-603 已绿于此 HEAD code)。
进度:21 轮代码全绿 + R22 no-code 分析(R1-R21 均 push;R22 仅进度文档)。
Next(loop 续):㉓ **强烈建议用户定向**——good-dedup 井干,继续同 prompt 多半是 no-change 分析轮或 net-negative 风险。
  方向选项:(A) 授权热路径 reproduce-first(enqueue preamble / build_append_future / partition_loop);(B) fuse·ioring
  扩 chaos 后清那两 crate;(C) 转 perf 调优 / 新 feature / 指定文件。无方向则 analyze-only 报告、绝不造 net-negative churn。

ROUND 23 — NO-CODE(第 2 连续 no-change)。扫 manager/recovery.rs(1538 LOC:node_health_loop 233 / recovery_dispatch_
  loop 212 / apply_recovery_done 190 / dispatch_recovery_task 171 / dispatch_one_ec_conversion 117)——intricate 单一用途
  recovery/EC dispatch 循环,F207/F222/F224 不变量 load-bearing = revert-prone 高风险区,无低风险 byte-identical 重复块可抽。
  确认 good-dedup 井干。**收尾决定:cadence 延长到 1800s 收束**,停止每 4 分钟 churn;无新方向则保持 analyze-only,
  绝不在 recovery/CAS/热路径 高风险区为边际去重动刀。等用户定向(A 热路径授权 / B fuse·ioring 扩 chaos / C perf·feature·指定文件)。
进度:21 轮代码全绿(R1-R21,均 push)+ R22/R23 no-code 分析(井干确认)。campaign 在 chaos-validated crates 上的
  behavior-preserving 去重/分析**实质完成**。

────────────────────────────────────────────────────────────────────────────────
PREV:
Date: 2026-06-25 (review 战役 — 转向最大文件;manager/lib.rs ×2 + chaos 验证 + extent_node.rs ×1)
TaskStatus: completed
Task scope: 用户"不超过200行的不用处理,开始处理最大的几个文件(冗余/乱代码最多)"→"先搞一次 chaos 防 bug,然后 continue"。
  先用 5 个并行 Explore agent 测绘 5 大文件(38K LOC),挑干净低风险 win 动手;每次 build+clippy+全量单测;**已 push origin/main**。
CHAOS 验证(用户要求,跑在 manager 两 dedup 之后):system_chaos(真 EN 子进程 kill+restart + split/merge/EC/fence/gc nemesis
  + per-key durability + extent accounting 不变量)2 个 merge-family seed 全 PASS:
  · seed=603(历史 merge over-seal 触发种)90s/5000ms:**显式跑成 Merge OK + Split,accounting errors=0(12 extents,
    22 memberships)**——memberships(22)>extents(12)= CoW-shared refs≥2 被校验 = 正是我 splice_victim_extents refs 路径;
    mismatches=0 not_found=0 all invariants OK。
  · seed=13 90s/5000ms:mismatches=0 not_found=0 accounting errors=0 all invariants OK(本轮 Split 被 EC-inflight 跳过,merge 少)。
  结论:manager 两 dedup(merge splice + replay decode)在 chaos 下无回归、无数据丢失。+ merge 单测(f_merge_refs_leak_{cow,splice}_*)同绿。
COMMITS(本段 3 个,均 push):
  · c8aec02 stream/extent_node.rs(#2 大文件 8.8K):DiskFS 的 extent_path/meta_path/ec_staging_path/ec_commit_marker_path
    四个**字节级相同**(仅后缀 dat/meta/ec.dat/ec.commit 不同)→ 抽 extent_file_path(id, suffix) 单一真源
    (hashed 布局 {base}/{hash:02x}/extent-{id}.{suffix},与 autumn-op format 256 子目录 + remove_extent_files 共享)。
    纯路径字符串、非热路径。验证:build+clippy(0 新增)+ 全量 cargo test -p autumn-stream --lib 90/90。
5 大文件测绘(供后续;LOC + 顶级低风险 win):
  · partition-server/lib.rs 11,853(热路径,最 revert-prone,167 ps 单测)——agent 头号是 setter dedup
    (108-196,~22 个 set_*),但我判定 marginal(每 setter 已极简,set_clamped<T:Ord> 几乎不缩短 + f64 不满足 Ord)→ 跳过,over-refactor。
    真低风险但有价值的:enqueue_put/delete/batch_put 的 region_epoch+range+fence 前缀校验三处重复(6233-6650),
    但在热写路径——谨慎。其余(partition_loop drain、frame dispatch)= 热路径中风险。
  · stream/extent_node.rs 8,788(热 append/read/coalescer,note 24 revert-prone,90 stream 单测)——
    DiskFS 路径构造 4 函数泛化(387-415,低)、eversion-mismatch 错误编码 dedup(低)、build_append_future 前置校验抽取
    (低但热路径前段)。其余(recovery/re_avali finalize)中风险。
  · manager/lib.rs 6,595(控制面,164 单测)——**本轮做了 2 个**(见下)。剩:mirror_* txn 构造 pattern(低,DEFER),
    ConnPool::call / last_op_at_for dead-code(需先验证真未用)。
  · manager/rpc_handlers.rs 5,660(控制面,164 单测)——agent 头号"leader 前缀 21 处 dedup"**被我否**:每处编码
    不同 Resp 结构体(RegisterNodeResp/PunchHolesResp/...),只能上 control-flow 宏(藏 early-return)——meaty+opinionated,
    暂缓。真低风险:punch_holes/truncate 两 handler 的 ~40 行 ref-decrement+pending-delete 块字节级重复(Items 3/7/8)——
    但在 note33 CAS + F139/F145 guard 的 revert-prone 区,需逐行验证 guard 顺序,留作下个谨慎目标。
  · stream/client.rs 5,229(StreamClient,部分热路径,90 单测)——eversion-mismatch codec dedup(低)、
    cache 失效 2-liner→方法(低)、chunked-read loop dedup(低但需 read-path 回归)。EC scatter-gather/apply_completion 高风险。
COMMITS(2 个,均 push):
  · 11b88c9 manager/lib.rs:compute_merge_streams + splice_streams_without_new_tail 的两个**字节级重复块**
    (survivor 旧 tail 封存 + victim-extents 循环含 F-merge-refs-leak refs 处理)→ 抽 seal_survivor_old_tail /
    splice_victim_extents 两 helper。push 顺序不变;F-merge-refs-leak 完整 rationale 现只在 helper 一处
    (原 splice 是"see compute_merge_streams"指针)。纯 verbatim 抽取。验证:164/164 含
    f181_compute_merge_streams_extent_ids_order_and_refs / f_merge_refs_leak_{cow,splice}_* /
    merge_refs_invariant_holds_across_split_merge_cycles —— 直接钉死两函数 refs/seal/order 行为。net -3(87/-90)。
  · 269a239 manager/lib.rs:replay_from_etcd 的 5 个 id-map 解码循环(nodes/disks/streams/extents/partitions)
    → 泛型 Self::replay_decode_id_map::<T>(&kvs, prefix, &mut max_id)。其余前缀(owner_locks/ps_nodes/regions/
    last_op)真不同保留 inline。net ~平,价值=fail-loud replay_decode_err(note 39 升级安全)集中到一处。
教训:① Explore agent 会高估/编造 dedup(rpc_handlers "21 处 Bytes dedup" 实际每处 Resp 类型不同;
  partition setter "head win" 实际 marginal)——动手前必验证实际 pattern;② 大文件最干净的低风险 win 是
  **泛型函数抽取 / 字节级重复块抽取**(replay 5 循环 / merge 2 块),不是 control-flow 宏(藏 early-return,违保守);
  ③ 热路径(partition_loop/coalescer/append)+ CAS/guard 区(note33/F139/F145)的 dedup 留作逐行验证的谨慎目标,不抢跑。
Next steps(用户定夺):① 继续 manager/lib.rs 低风险(mirror_* txn pattern / 验证并删 dead-code);
  ② 谨慎做 rpc_handlers punch_holes/truncate 的 ~40 行 dedup(先逐行验证 note33 CAS + F139/F145 guard 顺序);
  ③ extent_node.rs DiskFS 路径 dedup **已做(c8aec02)**;stream/client.rs 读失败 eversion-stale/evict policy **已做(84d4f3c)**;
  ④ 热路径(partition-server/lib.rs enqueue 前缀、partition_loop)最后,reproduce-first 心态。
client.rs 测绘结论(记录,避免重走):chunked-read loop(read_with_layout vs read_shard_from_addr 两循环结构同)抽取
  需 async-closure-per-chunk(boxed-future 堆分配 / AsyncFnMut lifetime)——加复杂度非减,**不做**;eversion-mismatch
  DECODE(read_shard_chunk_from_addr vs ec_reconstruct)返回类型/校验/早退 vs match 不同,**不强抽**(只有 2 行
  EversionStale 构造重复,marginal)。真正干净的只有 5 处 cache-invalidation policy(已抽 read_err_fail_fast)。

NEXT 段 COMMIT(本段 1 个,已 push):
  · 84d4f3c stream/client.rs:read_replicated_with_failover + read_hedged_pair 的 5 处
    `if is_eversion_stale(&e){return Err(e)} extent_info_cache.remove(&ex.extent_id)` → read_err_fail_fast(&e,id)->bool
    (true=fail-fast 传播;false=evict+试下个副本)。早退 `return Err(e)` 留在调用点(显式)。仅这 5 处失败/hedge,
    2-attempt 重试循环(invalidate_extent_cache)+ EC reconstruct(新建 EversionStale/push needs_reconstruct)不碰。
    验证 90/90。net +11(21/-10,helper doc 占)。

────────────────────────────────────────────────────────────────────────────────
PREV:
Date: 2026-06-25 (review 战役 — policy.rs compute_* 拆子检查 ×2)
TaskStatus: completed
Task scope: 用户"continue"→"Push and continue"(承接 review 战役;next-steps ① policy compute_* 大函数拆子检查)。
  两次纯 behavior-preserving 拆分,每次 build+clippy+**全量 cargo test -p autumn-manager --lib(164/164)** 验证
  (硬性 [[feedback_run_unit_tests_after_refactor]])。**已 push origin/main**(用户 "Push")。
COMMITS(2 个,均 push):
  · df199a6 policy.rs:compute_maintenance_advisory 的 GC/major-compact/minor-compact 三臂抽成命名
    Option<PolicyCandidate> 子检查 helper(gc_advisory / major_compact_advisory / minor_compact_advisory,
    都是 (part_id, bs, cfg, now) 无 self 关联函数)。loop body → 3 行 out.extend(Self::*(..))。
    每臂 `if cond { out.push }` → `if cond { Some } else { None }`,零控制流变更、emission 顺序不变、
    每分区每 kind ≤1 candidate 不变。**是 DECOMPOSITION(拆子检查)非上轮否决的单一 deduped helper**——
    每 helper 保留各自 metric/阈值/reason 文案,无一处读起来更糟。验证:clippy policy.rs 0 新增;
    164/164 含 gc_advisory_*/compact_advisory_*/minor_compact_*/maintenance_advisory_partial_* 专项。
  · 0d16f35 policy.rs:PolicyEngine::compute_candidates 的两个视觉分隔 pass(SPLIT/MERGE)抽成命名私有 helper:
    - split_candidates(&self, args) = F183 per-partition SPLIT pass(317-364 体)
    - merge_candidates(&self, args) = F183 adjacent-pair MERGE pass(366-450 体)
    compute_candidates 变成小 orchestrator:split 在前 merge 在后(push 顺序不变)+ 保留 advisory_cache/_at 写。
    两 helper 都是 **只读 &self**(只读 self.metrics+self.config+args)——把每个 pass 的只读性显式化。net +27。
    cfg.clone() 每 helper 各 clone 一次(2 次/60s tick,可忽略;mirror compute_maintenance 既有写法)。
  验证:build 绿;clippy policy.rs **0 新增**(残留 3 个 pre-existing 在 inode_lease.rs:739 + lib.rs:169/231,
    我没碰那两文件);全量 164/164,含 policy_tests 的 split_*/merge_* candidate 专项(钉死 pass 行为)。
区分:compute_maintenance 本轮做的是 **DECOMPOSE(3 个独立命名子检查)**,不是上轮否决的 **DEDUP(单一参数化
  helper)**——后者仍不做(字段/阈值不同,合一读更糟)。extent_inflight.rs 看过——已高度 factored
  (acquire ~28 行 + 一堆小 helper + F208 sweep),无大函数可拆,强拆=over-refactor,跳过。
  manager CLAUDE.md note 16/18 仍准确(compute_candidates / compute_maintenance_advisory 仍是 public 入口,
  无架构/不变量变更)→ 不更新。
Next steps(用户定夺):① 继续纯拆分(policy compute_hot_cold_advisory ~170 行——但内部是 positional tuple
  (u64,u32,u32,u64,u64) + QPS/size 两维平行 + 控制流重 loop,full extract 有 continue→return None 转换雷,
  低风险只能拆 Phase-1 by_ps 建图,价值小;若做要先用 named struct 替 positional tuple 再拆;
  inode_lease.rs acquire_with_force_deferred ~166 行最大函数但 fencing 关键 + borrow-bridge 状态机,extract 笨重高风险);
  ② coco 恢复后控制面 bug-finding(recovery/rpc_handlers);③ 热路径巨文件(partition-server/lib.rs、extent_node.rs)更晚;
  ④ 顺手清 3 个 pre-existing clippy(inode_lease.rs:739 空行 + lib.rs:169/231 first().is_some(),非本次范围)。

────────────────────────────────────────────────────────────────────────────────
PREV:
Date: 2026-06-25 (review 战役 — manager 纯拆分,不依赖 coco)
TaskStatus: completed
Task scope: 用户"Continue"+"继续纯拆分(不依赖 coco)"+硬性要求"每次重构完都要跑 unit test"
  (记 feedback_run_unit_tests_after_refactor)。coco 本会话两次卡死(autumn_client findbugs + recovery.rs inspect),
  本机 coco 不稳,故改纯 behavior-preserving 拆分/dedup,build+clippy(-D)+**全量 cargo test -p autumn-manager --lib(164/164)** 验证。
COMMITS(2 个,recovery 已 push,policy 待 push):
  · 2b733d9 recovery.rs:apply_recovery_done 的 **3 个 byte-identical** best-effort marker-release 块(etcd put_and_delete_txn
    + 同样 10 行 rationale 注释 + warn + commit_extent_inflight_release)→ 抽 release_recovery_marker_best_effort helper。
    272→198 行(net -48)。成功路径(atomic put+delete,fail-loud `?`)+ 末尾 commit-only release **不动**(语义不同)。
    验证:recovery 单测(apply_failure_keeps_inflight_marker / apply_success_releases_inflight_marker / f126_*)+ 全量 164/164。
  · b76a08e policy.rs:4 处 compute_* advisory 的 window 提取前缀(buckets.rev().take(required_buckets).collect()+len 闸)
    → PartitionMetricsWindow::recent(n)->Option<Vec> 方法(let-else;双窗口 split-pair 用 tuple let-else)。net -10。policy_tests 覆盖。
教训:① coco 本机不稳时,纯拆分(build+全量单测验证)是不依赖 coco 的安全推进路径;② **优先重构有 unit test 覆盖的代码**
  (apply_recovery_done/policy compute_* 都被单测覆盖;node_health_loop/recovery_dispatch_loop 需 live cluster 无单测,refactor 风险高,本轮没碰);
  ③ dedup 只做 **byte-identical** 或前缀完全相同的(recovery 3 块/policy window 前缀);compute_maintenance 的 3 个 arm 结构相似但
  字段/阈值/filter 不同,抽成闭包重 helper 反而更难读 → 不强行 dedup(避免 over-refactor)。
Next steps(用户定夺):① 继续纯拆分别的 unit-tested 文件(policy compute_* 大函数可拆子检查 / extent_inflight.rs / inode_lease.rs);
  ② coco 恢复后回头做控制面 bug-finding(recovery/rpc_handlers);③ 热路径巨文件(partition-server/lib.rs、extent_node.rs)更晚。

────────────────────────────────────────────────────────────────────────────────
PREV:
Date: 2026-06-25 (review 战役 — autumn_client 第 2 个文件)
TaskStatus: completed
Task scope: 用户"Continue"——战役第 2 个低风险文件 autumn_client.rs(1401 LOC,数据面 CLI:KV ops+benchmark)。
  完成:拆文件 + perf-check 抽函数 + coco 抓 10 bug 修清晰的 5 个。invariant 保持(0 mgr_call(MSG_)。
COMMITS(均 push 前,4 个):
  · 4bec788 拆分:autumn_client.rs → 目录 bin autumn_client/{args.rs(Command/Args/parse_args/warns), main.rs}。
    val helper 类同 autumn_op。纯移动(git rename)。
  · 338de4c perf-check 抽函数:~490 行 Command::PerfCheck arm → cmd_perf_check(client, 12 字段, manager);main() 730→241。
    踩坑:① 插入 fn 要在 `#[compio::main]` 属性**之前**(否则属性脱离 main → async-main 报错);② 重命名 args.manager→manager
    后 `Arc::new(manager.clone())` 是 Arc<&str> 逃逸到 'static 线程 → 改 manager.to_string()。
  · 6107182 修 coco 的 5 个 bug:
    #1(P1)perf-check Barrier 死锁:--ramp-ms 下 connect 失败的线程在 barrier 前 return → 连上的线程永等 + main join 卡死。
       修:失败线程也 barrier.wait()(读写两阶段)。
    #3(P2)parse_args flag 缺值 raw[i] 越界 panic(--manager/--transport/ls/perf-check 多处)→ val(raw,i) helper(同 autumn_op #9)。
       踩坑:sed 漏了**多行** `var = raw[i]\n.parse()`(partitions/pipeline_depth/bulk)+ `raw[i]\n.parse()`(ucx-regpool)——
       要补 `= raw[i]$` 和 `^\s*raw[i]$` 两个 sed;且 clippy 不刷新二进制,smoke 前必 cargo build。最终 16/16 缺值→usage,0 panic。
    #2(P2)put-stream --chunk-size 0:loop 不前进(无限空 chunk)+ div_ceil(0) panic → bail + saturating_add。
    #4(P2)progress 线程 loop{}+drop 不停 → 跨阶段串扰 → AtomicBool stop flag + join。
    #6(P2)读把 Ok(None)(key 缺失)计成功 → 虚高吞吐+掩盖数据缺失,perf-check 失去 write+read sanity → 只数 Ok(Some(_))。
  coco findbugs 复核本轮**卡死(~14min 0 输出,infra flake 非 finding)**——杀掉;5 修 build+clippy+#3 smoke 独立验证过,故 commit。
BACKLOG(autumn_client niche,记录):#5 bulk+direct-read 忽略 direct_read;#7 bulk 部分失败用连续序号重放读错 key(叠加 #6);
  #8 zc-get 按服务端长度无上限分配易 OOM;#9 total_ops*value_size 无 checked_mul;#10 put/del 跳过 `-` 前缀 key。
Next steps(用户定夺):① autumn_client niche(#5/#7 是 benchmark 正确性,可一起修);② 进下一个文件
  (manager 控制面 lib.rs/rpc_handlers.rs 低风险,或热路径巨文件 partition-server/lib.rs、extent_node.rs)。

────────────────────────────────────────────────────────────────────────────────
PREV:
Date: 2026-06-25 (代码 review/refactor 战役 — 启动 + autumn_op pilot)
TaskStatus: completed
Task scope: 用户"和 coco 合作,通读代码 refactor 必要代码,简化,拆分文件/函数;准备花 2 周 review 所有代码"。
  用户拍板:① 激进度=**保守**(behavior-preserving 拆分 + 明显净化;行为变更要 reproduce-first;热路径只抽函数不改逻辑);
  ② 起点=**低风险 pilot 先跑通流程**。
工作流(已验证可用,本战役标准循环):coco inspect(逐文件挖 bug)/ arch(跨组件)→ 我提拆分/修复 → 应用(纯移动零逻辑变更
  OR reproduce-first 修复)→ cargo build+clippy(-D warnings)+test+CLI smoke(+热路径 chaos/perf)→ coco findbugs 复核 diff →
  更新该 crate CLAUDE.md → 一个可 review 的 commit。
代码地形(战役优先级):113K LOC / 11 crate;**5 个巨型文件占 ~38K(1/3)**:partition-server/lib.rs 11.8K(热路径,最 revert-prone)、
  stream/extent_node.rs 8.8K(热 append/read+coalescer,note 24 明令 revert-prone)、manager/lib.rs 6.6K、manager/rpc_handlers.rs 5.7K、
  stream/client.rs 5.2K。大函数:recover_partition ~1.1K、fuse handle_request ~726、partition_thread_main、process_gc_chunk/compact_row_append、
  handle_convert_to_ec、build_append_future、replay_from_etcd。
PILOT 完成(autumn_op.rs CLI,3053 LOC,3 commit):
  · eab8e78 拆分:autumn_op.rs → 目录 bin autumn_op/{main.rs 2051, args.rs 1009}(Args/Command/parse+parse 助手+tests 进 args.rs;
    Cargo path 改 src/bin/autumn_op/main.rs)。纯代码移动(git 认出 rename),build clean + 12 单测 + CLI smoke。
  · coco inspect(GPT-5.5)挖到 **10 个真 bug**(非 trick,都符合代码自身不变量)。用户选**修清晰的 6 个**:
  · 2153d2f 修 5 个:#7 fuse_split_ranges floor-stride 偏斜(非 256 约数尾分区吞 remainder)→ 比例边界 (i*256)/n + reproduce-first 测
    (n=6 spread 4→1);#4 info 用 sealed_length==0 判 open → 改权威 .sealed(note 32);#6 run_info 不查 resp.code → 三个 resp 全 bail;
    #1 format 不查 RegisterNodeResp.code 就写哨兵 → bail;#2 disk_id unwrap_or(0) 静默伪格式化 → 先全解析再写(no partial)。coco findbugs 清。
  · cfbd06c 修 #9:parse() 缺 flag 值/positional 直接 raw[i] 越界 panic → 加 val(raw,i) helper(越界即 usage),热点 flag-dispatch 不动;
    rebuild 后 15 个缺值用例全 usage(0 panic;stale 二进制还 panic=reproduce 证据),好输入仍 parse。coco findbugs 清。
BACKLOG(niche,用户暂不修,记录待逐个拍板):#3 format 目录不去重→幽灵磁盘;#5 info probe open-extent 不按 shard 路由(仅 multi-shard);
  #8 --json info --part 缺→无 JSON+rc0;#10 parse_byte_size 无 u64 上溢检查。
教训:① cargo clippy 不刷新 target/debug 二进制——CLI smoke 必须先 cargo build(踩过 stale 二进制假阳);② bin 拆 module=改成目录 bin
  (main.rs+子模块)+Cargo path;③ coco inspect 出 bug 不出结构,结构拆分是我的判断;④ 巨型文件先拆 cohesive 块(args/display)再拆大函数。
RUN() 拆分完成(d2f9080 + 398c201,均 push 前者已 push 后者待定):用 python 脚本(body-brace 匹配 + 统一 dedent +
  args.json→json,Format 额外传 manager/transport)把 21 个命令臂抽成 cmd_*(client,json,fields) free fn,
  **run() 1066 → 63 行纯 dispatcher**(每臂一行调用;仅 Info/Bootstrap 委托 run_info/run_bootstrap、RegisterNode stub 例外)。
  纯代码移动(臂体逐字),build+clippy(-D)+13 单测+dispatch smoke(死 manager 下每命令路由到 manager call)全绿。
  脚本踩坑:struct-variant 臂 `Command::X { fields } => {` 的 brace 匹配要从 `=> {` 的 body brace 算,别从 pattern brace
  (第一版漏算损坏 ExtentHealth/AuditLog,git checkout 重做)。后续可把 21 个 cmd_* 挪进 commands.rs 子模块再缩 main.rs 文件体量。
autumn_op PILOT 全部完成:拆文件(args.rs)+ 6 bug 修复 + run() 1066→63。
Next steps(用户定夺):① autumn_op 收尾项(cmd_* 挪 commands.rs / display.rs 抽 + run_bootstrap·run_info 也可抽)可选;
  ② 进下一个文件(autumn_client.rs / manager 控制面等低风险先,热路径巨文件后);③ 每周节奏。

────────────────────────────────────────────────────────────────────────────────
PREV:
Date: 2026-06-24 (EN-down stall 定位)
TaskStatus: completed
Task scope: 用户"先复现+定位再说"(EN-down ~30s stall finding)。**定位完成:不是 failover-latency bug,
  是 RF=N-on-N-nodes 容量耗尽。** reproduce-first via autumn-client(无 fuse bridge),未改生产代码。
确定性证据(/tmp/fuse_rmw/stall.log vs stall5.log,同脚本仅 NODES 不同):
  · **3 EN, RF=3**:kill 1 → 剩 2 健康 < RF=3 → select_nodes 无法在不含死节点下组 3 副本新 extent →
    F227 all-replica-ACK append 永不完成 + 新 extent alloc 退回死节点 → **单次 put 卡满 90s CLI timeout(等价无界)**,直到 EN 回来。
  · **5 EN, RF=3**:kill 1 → 剩 4 健康 ≥ RF=3 → 新 extent 落健康节点,append 透明 roll off 死副本 extent →
    **所有 put/get <0.1s,PS retries=0,manager 都没来得及标 Suspected**。零 stall。
  · READ 任何规模都容忍 1 死副本(min-quorum 读)——所以 eni 完整性测试一直过(读 OK),只有 RF=N 时写卡。
结论:此前"~30s availability stall"框架不完整——真因是**用恰好 RF 个 EN(3)跑测试 = 节点丢失零写容量**
  (Ceph/HDFS 在 RF=3/3 节点掉 1 也停写,是副本系统通性,非 autumn bug)。fuse 单线程 dispatcher + 30s bridge
  REPLY_TIMEOUT 只是把 stall 放大成 EIO,不是病根。**之前设想的"stream 层 fast-fail stale EN conn"是误诊,不做。**
修正动作:更新 d9adb41 的文档(fuse CLAUDE.md "Restart behaviour" / README / 本文件)——把"~30s stall"
  改成准确的"RF=N 容量耗尽,>RF 不 stall"。fuse_chaos/fuse_en_restart_chaos 用 3 EN 故只验 EN-restart **完整性**
  (快重启,读全程 OK),不测 writes-during-EN-down(那只会撞 RF=3 容量墙)。要测写可用性需 provision >RF EN。
Next steps(用户定夺):commit 文档修正(solo-flow);无代码改动。生产建议:EN 数 > RF。

────────────────────────────────────────────────────────────────────────────────
PREV:
Date: 2026-06-23 (EN/PS 重启 测试)
TaskStatus: completed
Task scope: 用户"多测试 en ps 重启的情况"(承接 RMW-GET-SWALLOW 修复)。
结果:**EN 重启 fuse 数据完整性 OK(4 轮×3 EN + remount,6 durable 4K..10M + 4 RMW 镜像 byte-exact);
  PS 重启 OK(已有 F1 + fuse_rmw_chaos)。新增一个 AVAILABILITY finding(非数据丢失):EN 在线期间发起的
  op 会 stall ~30s 才 failover**。新增 scripts/fuse_en_restart_chaos.sh 作 EN 重启完整性守卫。未 commit。
覆盖缺口:fuse_chaos 只 kill PS/manager/fuse,从不重启 EN(数据面);system_chaos 有真 EN kill+restart 但不经
  fuse 挂载。本次补 fuse 挂载下 EN 重启。
做法+结论:
  · 先给 fuse_chaos 加 E1(EN kill,write-during-down + manifest verify)——但 ① 放在 F1 PS-respawn 后 6s,
    与 PS region reconvergence 混淆;② write-during-EN-down 每 op stall ~30s(daemon 单线程串行化)→ 慢/噪。
    → 撤掉 E1,改成独立 fast 脚本。
  · scripts/fuse_en_restart_chaos.sh(从 /tmp/fuse_rmw/en_integrity.sh 提升):EN kill + **2s 快重启** + 健康后
    verify(durable sha + RMW 镜像)。隔离 correctness(EN 重启是否损坏/丢数据)与 availability(EN-down 慢)。
    实测 PASS:baseline + after-clean-rmw + r1..r4(每 EN)+ remount 全 integrity OK。
AVAILABILITY finding(报告用户,未修):EN kill -9 后,仍在飞行/新发的 op 走 dead replica 的 stale conn(或 PS 侧
  VP resolve / commit-length 探测重试 dead replica)在 30s rpc_timeout 内不被 fast-fail;fuse bridge REPLY_TIMEOUT
  也是 30s → op 在 ~30s 时 EIO 而非 ms 级 failover;daemon 单线程 → 一个 stalled write 串行化后续写。EN 回来/被标
  Suspected(F276 读避让只在 ~30s 检测后才生效)后自愈。**非 fuse 专属——任何 client 碰 kill 的 EN stale conn 都中招。**
  修法 = stream 层 stale-EN-conn 更快 fast-fail(缩短 first-attempt timeout),属共享 conn-pool 路径(revert-prone),
  reproduce-first 已具备,等用户拍板是否动刀。
Next steps(用户定夺):commit 守卫脚本+文档(solo-flow);是否追 availability 修(stream 层,谨慎)。
验收: scripts/fuse_en_restart_chaos.sh PASS;/tmp/fuse_rmw/eni.log + restart_soak.log(慢路证据)。

────────────────────────────────────────────────────────────────────────────────
PREV:
Date: 2026-06-23 (RMW-GET-SWALLOW)
TaskStatus: completed
Task scope: 用户"chaos 测试 fuse,我发现 fuse 经常出问题"。
结果:**现有 fuse_chaos.sh 全绿(cp/append-only),但审计 + 确定性复现抓到一个真 P0 数据损坏(fuse RMW 路径),
  已修 + coco 通过 + 加永久回归守卫(T4)**。未 commit/push(等用户拍板,solo-flow)。
ROOT CAUSE — fuse 部分覆盖写(RMW)吞 get 错误 → 成功写却伪造零:
  crates/fuse/src/extent.rs write_region 的 RMW 分支(offset 落在已有 extent 内)用
  `state.kv_get(&ck).await.unwrap_or_default()` 读现值。transient/hard get 错误被 collapse 成空 Vec →
  zero-fill 未触及前缀 [start,offset) + 截断未触及后缀 + put → 在**成功的写**上伪造零/丢字节。
  cp-only 工作负载只走 append("Fresh territory"分支),从不进 RMW → fuse_chaos T1-T3 漏掉这条路径。
复现(reproduce-first,确定性,/tmp/fuse_rmw/repro.sh):get/put 各有独立 10-refresh(~13s)预算 →
  kill 持有 PS 让 RMW 的 get 在死亡期耗尽预算硬错,+16s 重启让随后的 put(新预算)落盘。
  1MiB 文件部分覆盖 256K@256K:**修复前 dd rc=0(成功)但回读 prefix[0,256K) 全零** = 伪造零确诊;
  **修复后 dd rc=1(EIO) + 文件完好(==A)** = 安全失败(app 重试)。
FIX:`state.kv_get_opt(&ck).await?.unwrap_or_default()` —— 传播硬错误(→ EIO),仅真实缺失 key(Ok(None),
  mapped extent 不该出现)当稀疏空值。镜像同文件 clean_beyond_eof 既有 barrier。零热路径开销。
验证:repro 修后安全(dd EIO+文件完好);fuse 44 lib 单测绿;coco findbugs deep(GPT-5.5)未发现问题;
  新增 scripts/fuse_rmw_chaos.sh(单 PS,部分覆盖 + PS kill+restart + prefix-never-zeroed 断言)永久守卫。
  注:先在 2-PS fuse_chaos 里加 T4,但 kill PS 触发 migration → verify 读卡 part_addr reconvergence(非本 bug,
  是已知 wedge 家族)→ 改成独立单 PS 脚本(无 migration,robust),已撤 fuse_chaos T4。
教训:cp/append-only chaos 漏 RMW(原地覆盖)路径;read-modify-write 的"读"硬错误必须传播(=clean_beyond_eof 早已确立的
  barrier),unwrap_or_default 在 RMW 上 = 数据损坏。属 feedback_no_let_underscore_swallow 家族。
Next steps(用户定夺):commit(solo-flow,不 push);可选:把 RMW 覆盖扩到 system_fuse_read 集成测试。
验收: 见 feature_list RMW-GET-SWALLOW + /tmp/fuse_rmw/(repro + run1 损坏证据 + run2_fixed 安全)。

────────────────────────────────────────────────────────────────────────────────
PREV:
Date: 2026-06-23 (深夜)
TaskStatus: completed
Task scope: 用户"再压测,配合上 chaos"。用官方 system_chaos(真 EN 子进程 kill+restart + split/merge/EC/
  killfence/fence/gc nemesis + per-key durability + accounting 不变量)验证两个 .max(1) 修复。
结果:**两个 over-seal 修复在 chaos 下验证通过,无数据丢失,无新 bug**。
  · 含历史触发该 over-seal 家族的 seed 13/603 在内,3/3 seed(13/603/42,moderate 配置 90s/5s interval)
    全 PASS:**mismatches=0、not_found=0、accounting errors=0、all invariants OK、0 WAL-FAILSTOP**。
  · 先用激进配置(240s/2500ms interval/含频繁 kill EN)跑 seed 13/603 失败——但失败 = **verify 期 part_addr
    路由 wedge**(某分区 listener addr 在最后一波 nemesis 后还没 reconverge,harness verify 在 attempt=0 就放弃),
    **非 over-seal bug(0 WAL-FAILSTOP)、非数据丢失(mismatches=0)**。诊断确认:同样的 nemesis 集合换成有收敛
    窗口的 moderate 配置(5s interval),seed 13/603 立即干净通过(not_found=0)→ 那是我配置太猛、nemesis 打到
    verify 之前不留收敛窗口的 timing 假阳性,不是真 bug。
教训:chaos nemesis interval 不能压到 verify settle 以下(默认 30s 跑能过;240s+2.5s 间隔会把 part_addr 收敛窗口耗光)。

────────────────────────────────────────────────────────────────────────────────
PREV(SECOND SOAK):
Date: 2026-06-23 (晚)
TaskStatus: completed
Task scope: 用户"继续压测一小时+各种op,查还有无 bug"(在两个 .max(1) 修复之后)。
SECOND SOAK 结果:**两个修复在规模上验证通过 + 数据完整,未发现新产品 bug**(被磁盘填满+我自己的操作失误打断,~30min)。
  · 用修后二进制起 6-EN/8-presplit 同款 soak。跑 ~30min:全程 admin churn(split/compact/gc/set-stream-ec/
    force-ec-convert/merge)+ 大小 v 混合负载 + 24 durable key 校验。
  · **修复验证(核心正面结果)**:把这个被 churn 30min 的集群 cold-restart(等价崩溃重启)→ **0 WAL-FAILSTOP、
    0 open-failures、10/10 分区全开**(修复前 merge survivor 会 WAL-FAILSTOP 开不了)→ 两个 .max(1) 修复在规模上成立。
  · **数据完整**:24/24 durable key byte-exact(其中 3 个 part-19 因 95% 磁盘 + 90GB 大分区恢复慢,等 part19 settle 后读回 byte-exact)。
  · **未发现新 bug**:soak 期间的 DATALOSS 全是已知"大分区在 admin churn/负载下读瞬时不可用"特征(QPS ceiling ~30K +
    merge 可用性缺口),全部自愈;0 panic、0 WAL-FAILSTOP、0 open-fail。
  · **环境/操作失误(非产品 bug,自我复盘)**:① 负载配置对磁盘过猛——admin churn 把数据堆进 90GB 大分区,GC 跟不上,
    /data05 30min 填到 95%,提前收尾;② 清理用 kill 进程组误杀了集群(cluster.sh 把集群进程放在 master 进程组里);
    ③ 诊断脚本 `cd` 后用相对 `target/release/autumn-client` 路径 3 次→ rc=127 误报"data lost"(实际数据完好,用绝对路径即 21-24/24)。
  · 清理:停集群 + 杀 etcd + wipe /data05/autumn-soak(1.6TB,释放磁盘)。
教训:soak 负载要配磁盘配额/限速;清理用 cluster.sh stop 别杀进程组;诊断脚本一律用绝对二进制路径。

────────────────────────────────────────────────────────────────────────────────
PREV(FIRST SOAK + 两个 .max(1) 修复, committed 80f29aa + 05f0293):
Date: 2026-06-23
TaskStatus: completed
Task scope: 用户要求"压测一小时(大v+小v)+ admin-op churn(split/merge/gc/convert-ec)"。soak 跑满
  3600s 抓到一个真 P0 → 确定性复现 → 根因 → **修复 + 验证 + coco 通过(未 commit)**,故 completed。
  驱动脚本全在 /tmp/soak/(env/status/ops/tick/verifier/adminops/watchdog/run_soak/restart_verify/repro)。
FIX(本会话核心成果)— merge survivor 重启不可开 = `.max(1)` over-seal 空 splice log tail:
  根因 = crates/manager/src/rpc_handlers.rs handle_merge_partitions(~3352)把每条冻结流的 commit_length
  捕成 `(resp.end as u64).max(1)`。victim 空 log tail(commit=0)被 .max(1) 顶成 1 → compute_merge_streams
  把空 extent 封成 sealed_length=1 → 冷重开 survivor WAL 回放读它"要 1 字节实得 0"→ WAL-FAILSTOP → 永久
  不可开。.max(1) 是 STALE 兜底(note 32 早已让空 tail 封 0、!ex.sealed 闸、无 0-skip),被它遮蔽。
  修 = 去掉 .max(1) 传真实 commit(含 0)。安全:handle_check_commit_length 副本不可达时返回 Err(已捕获),
  故 OK+0 = 真空(非被掩盖的失败,split 侧 unwrap_or(0).max(1)→abort 修的是那个 F227 隐患,这里不适用)。
  验证:把"round 1 即崩"的同一确定性 repro 用修后二进制重跑 → **6/6 轮 open-failures=[]**、survivor 读回
  byte-exact、log_stream seal 状态干净(无 size=1 雷、无 dangling)、merge 单测 17/17、coco findbugs deep
  (GPT-5.5)未发现问题。
FOLLOW-UP(reproduce-first,未做):split 侧 .max(1)(partition-server handle_split_part ~1228/1233/1238)=
  同一潜在 bug(空 CoW tail 封 1;回放通常跳过故尚未复现)。soak part-93 的 `rowStream extent 157 not found`
  是另一个(row-stream-checkpoint)侧面,本修未关闭。
Result: 6-EN TCP /data05、EC-off bootstrap、presplit 8;并发小v(4K inline)+大v(8M VP)perf-check 循环;
  24 个 durable 已知 key(8×8M+16×2K)散布全 keyspace sha256 连续校验;admin-op 每 ~200s 轮转 split/
  compact/gc/set-stream-ec+force-ec-convert/merge(靶子随机分散)。**活集群上数据完整性全程满分**
  (durable 24/24 byte-exact、refcount_leaks=0、orphans=0、0 panic、ACCOUNTING_OK),EC 转换 e2e 真编码
  (4 extent parity 填充 eversion bump),六类 admin-op 功能均正确。
🔴 P0(未修,reproduce-first 待办)— 重启后单分区不可开 = 数据不可用:
  merge 热点 survivor part 93(反复 merge 堆成 63GB/105-SST)在 cluster.sh start(保留数据,非 reset)后
  **永远开不了**:其持久 meta_stream TableLocations checkpoint 引用 row-stream SST extent=157,但 157 已被
  GC/compaction 从 manager 回收(当前 manager 元数据干净、无 stream 列 157)。
    ERROR partition thread failed: read SST from rowStream extent=157 offset=535224764:
          extent_info failed: extent 157 not found part_id=93  → open 重试 48 次 30s 退避永不成功。
  活集群期间 part 93 一直可用(走已打开的内存 SstReader),**仅冷重开从持久 stream 时暴露悬挂引用**。
  影响:9/24 durable key(part 93 range 0x9f–0xff)重启后不可用,其余 9 分区+15 key 正常。值是否真丢
  (vs 仅 checkpoint 失配可 repair-metastream 修)未定——需查 157 的 SST 数据是否还在 log_stream/其它 SST。
  类别:checkpoint-before-reclaim 顺序 / GC-vs-checkpoint 竞争(同 GC-stale-cache / manifest-no-CRC 家族)。
  **原始 soak 日志被 restart 截断,必须 reproduce-first 重跑+保日志再 root-cause,勿凭推断在 revert-prone 区动刀。**
🟠 次要 finding:重载下 merge 让 survivor 整段 range 读不可用 60–90s(远超标称 ~2s region_sync),还能把并发
  8M 读 workload 拖死 50min(一个 large perf-check run 卡 age 3003s,按 PID kill);整小时 32 条单发 GET-FAIL
  全在 merged range、活集群全自愈。
建议:① accounting checker 增"stream 引用的每个 extent 必须存在"不变量(活集群即可抓);② reproduce-first
  脚本注入 merge→gc→restart 序列+保 manager/ps 日志定位 157 删除点;③ 修 = checkpoint 必须先于 row-extent
  reclaim 更新(或 GC relocate-then-punch 时同步刷 TableLocations)。
REPRODUCED(2026-06-23,/tmp/soak/repro.sh v2,干净集群,ROUND 1 即触发):merge survivor part 15 重启后不可开。
  首次 repro 踩坑=残留 etcd squatter :2379([[project_clustersh_etcd_clean_broken]])让 repro bootstrap 到 soak
  的 etcd(18 分区、range 重叠、merge 全失败)→ 只杀我自己的 soak etcd+进程(保 soak 数据)重跑→round1 复现。
精确根因——同族两变体(merge survivor 冷重开不可开):
  · soak part 93: rowStream extent 157 not found(meta_stream checkpoint 引用已回收 ROW extent)。
  · repro part 15: WAL-FAILSTOP log_stream extent 68 got 0 of 1 expected bytes(被 splice 进来的 victim LOG tail
    被 over-seal:sealed_length≥1 但服务副本 0 持久字节;68 在 manager log 0 次出现=victim 的 log tail 被 merge
    splice 进 survivor)。
  两者皆 **split/merge 非幂等+客户端重试** 家族([[project_split_retry_cascade]];manager note 32a
  BUG2-IDEMPOTENT-ROLL 只修了 split 的 seal_and_roll)。触发链:merge 可用性缺口(survivor F185 freeze-drain
  切换期不可用 60-90s)→ autuop/客户端重试 merge(repro 日志:merge 15 36→connection error→merge ok)→ splice
  tail 在 phantom/未静默 commit 上被 over-seal。compute_merge_streams seal 有 !ex.sealed 幂等闸(lib.rs ~2762/
  2833)非裸双封;错的是捕获的 victim_sealed/survivor_sealed(F185 freeze-drain 连接抖动下未完全静默,或 F227
  phantom-commit)。
Next steps(用户定夺):#2 reproduce 已完成(确定性复现+根因+保日志)。#3 fix 高风险——F227 lenient-seal 是用户
  既定决策、merge handler 被 F138/F145/F146/F149/Item3-CAS 重重保护——需再 trace 一层 victim_sealed 来源+谨慎改
  +coco+repro 复验,本会话未动刀(遵守 reproduce-first/不盲修)。#1 detector=post-merge restart-openability 断言
  (accounting checker 看不到 seal-length over-seal),repro 即其体现。
验收: 见 /tmp/soak/logs/FINDINGS.md + /tmp/soak/repro_out/(round1 保留日志 + REPRODUCED_ps.log) + 本会话记忆
  [[project_soak_merge_hotspot_unopenable_p0]]。集群:repro 集群留活(part 15 坏)可 inspect。

────────────────────────────────────────────────────────────────────────────────
PREV(F278, completed + committed 1b522fa,未 push):
Date: 2026-06-22
TaskStatus: completed
Task scope: F278 — vLLM connector 跨实例正确性,真 Qwen3-VL e2e 抓修 3 个 P0(F250 一直只测 metric)。
Result: PASS。用户问"QwenVL 带图 prefix cache 好用吗"→ 跑真 vLLM 2 实例 e2e(Qwen3-VL-4B,GPU4/5)抓到
  connector 此前跨实例根本产不出正确输出(F250-D 只看命中 metric 掩盖了)。修 3 个 pre-existing P0:
  ① mm_hash 漏 key(0.23 在 mm_features[].identifier,非旧 mm_hashes)→ imageB false-hit imageA;
  ② save 没跑(没维护基类 _connector_metadata → has_connector_metadata False → attention 装饰器跳过
     save_kv_layer,只剩 marker 无 layer → load 全 miss 垃圾)→ bind/clear 调 super();
  ③ KV 布局 reshape(2,-1) 打乱 K/V(0.23 是 (num_blocks,2,block_size,..) → layer[slot//bs,:,slot%bs])。
  加固:marker 仅全 layer save 成功才发(_saved_count==len)+bind 重置;key 加 _KV_STORAGE_FORMAT=v1 防升级
  误读;dataplane key 断言同步。e2e 修后:inst2 load imageA→red、text→Paris、imageB→blue(对+不 false-hit);
  autumn markers=3 layer=108 全在 /vllm/v1/;dataplane 12/12;coco 3 轮收敛。教训:cache e2e 必须验输出正确性。
  与 mm_hash 修复一起 commit(用户拍板),未 push。

────────────────────────────────────────────────────────────────────────────────
PREV(F277, completed + committed d42fc85,未 push):
Date: 2026-06-22
TaskStatus: completed
Task scope: F277 — autumn Python 客户端 put 族 + vLLM/sglang kvcache adapter 接 TTL(默认不过期)。
Result: PASS。dataplane e2e(新 §6 TTL)绿:ttl_secs=1 store → 立即 is_present/exists 真;sleep 3s 后
  marker 惰性过期(is_present False)、layer 仍在(exists True)= marker-before-layer 不变量成立。
Current summary: 缺口定位:partition 层 TTL(F085:expires_at 全程 + 读路径惰性过期 + SST min_expires_at +
  后台清理)、PutReq/BatchPutOp.expires_at、Rust SDK put_many(&[(k,v,expires_at)])+ttl_to_expires_at 早已完整;
  唯一缺口 = Python binding 把 expires_at 恒写 0。本次:
  ① python/src/lib.rs:Op::Put/PutFrom/BatchPutFrom + BatchJob + run_job/run_batch 加 ttl_secs;
     Client.put/put_from/batch_put_from + BatchClient.put_from 加 #[pyo3(signature=(...,ttl_secs=0))]。
     ttl_secs==0 走原快路径(零行为变更);非 0 转 expires_at 经 put_many 写(ZC 写路径保持 from_static 零拷贝)。
  ② vllm_connector:_AutumnKVStore(ttl_secs),marker TTL=ttl_secs、layer TTL=ttl_secs+grace(300s)→ marker
     永先过期,杜绝「marker 在/layer 过期→部分层 load miss 静默 continue→未初始化 KV 错误输出」;extra_config.ttl_secs 注入。
  ③ sglang_backend:ttl_secs 透传 batch_set_v1(put_from)+set(put);自管 existence,无 marker 复杂度。
Verify: cargo check -p autumn-python 绿(仅既有 per_worker_cap dead_code warning);两 adapter py_compile 绿;
  maturin 重建 wheel 装进 /data/f250_vllm_venv;1-node TCP cluster(127.0.0.1:9001,AUTUMN_EXTENT_BASE_PORT=20000,
  先杀 /data02 旧 checkout 的 :9001 孤儿 manager)跑 dataplane 全 11 项 PASS。
Coco: coco findbugs deep(GPT-5.5)3 轮收敛。R1:① ttl_to_expires_at 整数溢出 → saturating_add(crates/client);
  ② grace 非严格 → 保留+load-miss debug→WARN;③ 负 ttl 静默永不过期 → 两 adapter fail-fast raise。R2:新真 bug =
  marker_ttl+300 顶过 u64::MAX → layer 写失败但 marker 仍发布(false-positive)→ ① TTL 饱和 u64::MAX ② marker
  仅在该 req 所有 layer save 成功才发布(_save_failed 门控,save_kv_layer 收 put_from 返回值)。R3 收敛:§6 不证
  batch TTL → 加 §6b(裸 1s BatchClient.put_from 过期);README ttl_secs 语义澄清。fresh-cluster e2e 12 项全 PASS。
Main gaps: 无;连接器 save-gate / _save_failed 逻辑仅 py_compile + 逻辑审,未经真 vLLM e2e(F250-D 隔离 venv 才有)。
Next steps: commit(solo-flow,不 push);如需可跑 F250-D 真 vLLM 两实例验 save-gate。
验收: 见 feature_list F277 acceptance。

────────────────────────────────────────────────────────────────────────────────
PREV(F250-D, completed + pushed 412666f):
Date: 2026-06-21
TaskStatus: completed
Task scope: F250-D — autumn-kvcache vLLM connector 真 e2e(隔离 venv 两实例跨实例 prefix 命中)+ README。
Result: PASS。inst2(GPU5,本地 cache 空)首见 inst1 存过的 282-token prompt → 272 token 从 autumn 跨实例命中
  (vllm:external_prefix_cache_hits_total 0→272 + prompt_tokens_by_source{external_kv_transfer} 0→272)。
  唯一 connector 改动 = __init__ 收 kv_cache_config 第三参(0.23 要求,向后兼容)。sglang smoke 无回归;
  新建 python/autumn_kvcache/README.md。下一步:commit + coco + 拆 vllm 实例/cluster 释放 GPU。
Current summary: F250-A/B/C 早已实现(连接器全 KVConnectorBase_V1 方法齐)。本次做 F250-D:
  ① 隔离 venv /data/f250_vllm_venv 装 vllm 0.23.0 + torch 2.11(不动系统 torch 2.9.1/sglang)。
  ② 重建 native autumn wheel(maturin, cp312, 匹配 HEAD)装进 venv;装 autumn_kvcache editable。
  ③ 起 1 个 3-replica TCP cluster(manager 127.0.0.1:9001)——坑:Ray 占 10100-10110 撞 EN ctl 口,
     用 AUTUMN_EXTENT_BASE_PORT=20000 移到 20001/21001([[project_bench_env_traps_20260610]]);
     另:harness shell errexit+pipefail 吞输出,集群起停一律走 /tmp/f250_*.sh 脚本文件规避。
  ④ F250-C dataplane smoke vs live cluster 全 PASS(save/load 字节级往返)。
  ⑤ 真 bug 修复(F250-D reconcile):vLLM 0.23 要求 external V1 connector __init__ 收第三参
     kv_cache_config 并转给 super()——连接器原是 2 参 → pydantic ValidationError 起不来。
     已改 vllm_connector.py:__init__(vllm_config, role, kv_cache_config=None) + try/except 向后兼容
     2 参老版本。editable 安装即生效。
  ⑥ 进行中:起两个 vllm serve(Qwen3-8B,GPU4:8101 / GPU5:8102)验证 inst2 跨实例 cached_tokens>0。
Main gaps: 两实例 e2e 跨实例命中尚未实测通过;README "Using autumn-kvcache as vLLM L3" 待写。
Next steps: inst1 ready 后跑 /tmp/f250_verify.py;过了写 README + 更新 F250 passes + commit(connector 修复)。
验收: 见 feature_list F250 acceptance(cross-instance prefix hit cached_tokens>0)。

────────────────────────────────────────────────────────────────────────────────
PREV(F276, completed + pushed c7f8d69):
Date: 2026-06-20
TaskStatus: completed
Task scope: F276 — 读路径主动绕开 Suspected 节点 + EC 主动重建(不等超时)。
Current summary: alloc_extent 早已排除 Suspected(select_nodes/online_node_ids);本任务把同样的避让
  扩到 READ 路径(此前读侧对 node 状态零感知,只在 RPC 失败后才 failover,坏节点每读吃一个超时;EC 更糟,
  先读坏分片等失败再重建)。实现全在 crates/stream/src/client.rs:
    ① SuspectedCache + maybe_refresh_suspected():读入口(read_with_layout)触发 TTL(2s)门控 + 非阻塞
       后台 poll 现有 MSG_LIST_NODE_STATES(滤 NODE_AUTO_STATE_SUSPECTED),当前读用略陈旧快照、绝不等
       manager RTT;idle 不 poll;poll 失败保留旧快照(不扩大避让集 → 不 strand 读)。
    ② 纯函数 replicated_read_order():F258 rotated start + avali 资格 + suspected-to-back(软降级,健康
       优先,suspected 副本保留为末位回退;空快照 = 逐字节等同 pre-F276 热路径不变)。hedge 改用两个
       健康-优先槽位。
    ③ ec_subrange_read:命中数据分片的 node 若 Suspected,直接进 needs_reconstruct(不发该分片读),
       现有 ec_reconstruct_shard_subrange(K 健康分片+parity,first-K-wins)立即重算 → 不卡 join_all 5s
       超时。ec_reconstruct_shard_subrange 不改(first-K-wins 本就不等死分片)。
    ④ 三条 VP 读路径全覆盖(coco 抓到 v1 只改 copy path):ZC 代理 read_value_into_pooled 用同序;
       client-direct extent_read_descriptor 用新 helper healthy_eligible_slots 剔除 Suspected(外部 SDK
       hash 轮转,排序无效)。= [[project_vp_read_paths_fragmented]] 陷阱。
  设计经用户 2 问确认:传播=客户端 poll 现有 RPC;读语义=软降级+回退。
Main gaps: 无 wire 变更、无新 CLI/config;纯 client-side 软提示,correctness 不依赖它。
Next steps: coco findbugs deep 已重跑通过(GPT-5.5,2 P2 全修)。已 commit(solo-flow),**未 push,等用户拍板**。
  可选后续:cluster 实测 kill-node 后读不停顿(README 已给手动步骤);把 Fenced/Maintenance override 也纳入
  读侧避让(本次只按用户原话做 Suspected)。
验收: cargo build -p autumn-stream 绿 + 0 新增 clippy(残留同型 cast 均 pre-existing);
  cargo test -p autumn-stream --lib 90/90 含 f276_suspected_read_tests 8 个纯函数测试;
  coco findbugs deep(GPT-5.5)2 P2(VP 三路径分叉)已全修。

────────────────────────────────────────────────────────────────────────────────
PREV(FGA-03):
Date: 2026-06-19
TaskStatus: completed
Task scope: FGA-03 — gallery 自适应 HLS 段大小(高码率视频 .ts 不撞 64MB PS inline cap)+ 接上
  /transcoding/ 前端独立面板(转码中视频不进主 /list/)。详见 feature_list.md FGA-03。

== FGA-03 (DONE, 本次) — 自适应 HLS 段 + 转码中面板 ==
  根因:transcode 每个 .ts 段 = 单条 inline put 的 KV value,`-hls_time` 写死 20s;高码率源 20s 段 ~106MiB
    > inline cap 64MiB → put 被 CODE_VALUE_TOO_LARGE 拒 → transcode 永久失败。WARN:
    `write .hls/…/seg005.ts failed: value 106633036 bytes exceeds … inline cap`。
  修(examples/gallery/src/main.rs):
    ① pick_hls_time(ffprobe duration → bytes/sec → seg_secs=48MiB/rate,clamp[2,20]);run_hls_ffmpeg 收 seg_secs
       参数替换写死的 "20"。普通码率仍满 20s。
    ② 兜底:copy 后 max_ts_bytes>64MiB(源关键帧比 seg_secs 稀疏时 -c copy 切不动)→ 清产物改 reencode,
       reencode 加 -force_key_frames expr:gte(t,n_forced*seg_secs) 段边界强插关键帧 → 段可靠受界。
       与原"copy 失败→reencode"合并进 reencode_reason 分支。
    ③ HLS 段维持 inline put(未改 striped);视频原件上传仍是 FGA-02 striped,未碰。
  前端(examples/gallery/static/index.html):新 Transcoding card(空则 hidden),2s 轮询 /transcoding/;name
    离集合=done→refreshList 刷主列表;failed 留面板带 ⚠+删除。修 uploadOne:视频 /put/ 回 JSON{"name":..},
    旧码把整段 JSON 当 name;改 parse .name + 只 selectFile 真进 files 的。
  验证:build 绿 + clippy 0 gallery 警告 + test 8/8(+4 pick_hls_time)+ ffmpeg arg 串实跑(testsrc 复现
    copy 出 1 段稀疏关键帧、reencode -force_key_frames 出 3 段)。前端面板待用户浏览器实测。

────────────────────────────────────────────────────────────────────────────────
PREV(FGA-02 squash 单 commit 3c3be55):

== FGA-02 (DONE, 本次) — gallery 大视频流式上传 + striped → HLS ==
  改动全在 examples/gallery/src/main.rs + README:
  ① 去 gallery 冗余 Rc<Mutex<ClusterClient>> → Rc<ClusterClient>(client 本就为 Rc 共享并发设计;29 处
     .lock().await 机械去掉)。去锁是前提:PutStreamHandle 借 &ClusterClient 要在"读浏览器 socket→发 chunk"
     全程持有,有外层锁会串行堵住其他请求。
  ② /put/ 按 is_video_ext 分流:视频 → cleanup_derived+delete_stream 清旧件 → stream_upload()
     (put_stream_begin → 网络小块攒满 4MiB(STRIPE_CHUNK_SIZE)就 send → commit;错则 abort);内存 O(4MiB)、
     value 无单条上限。非视频 → 原样内联 put + meta + thumb 失效。
  ③ download_to_file(转码源,只被视频转码调用)head+get_range → get_stream+next_chunk 顺序写临时文件
     (striped 的 head 只返回 28B meta)。
  ④ 删原件(转码完 + /del/)delete → delete_stream(级联删 chunk+meta;inline 退化普通 delete)。
  ⑤ 全表扫(/list/ + 启动恢复)用 is_chunk_key() 跳过 SDK 的 \xff\xfe… chunk 命名空间;body 上限 1GB→64GiB。
  ⑥ /get/ 维持 inline-only(head+get_range)—— 用户拍板。前端视频走 HLS、从不 /get/ 原件,原件转码后即删;
     make_chunk_key/StripeMeta 是 client pub(crate),gallery 无法对 striped 随机 seek。
  验证:cargo build -p gallery 绿;clippy -p gallery 0 gallery 警告;cargo test -p gallery 4/4。
  播放修复(用户报 leeesovle-cat.mp4 看不了;Chrome 不行 / Safari 行)—— 两个真 bug,均与 storage 无关
  (ffprobe 证明 18 段连续全 200、有 ENDLIST、源被 ffmpeg 干净解析→往返字节精确):
  ① 前端播放器选择写反(Chrome 头号根因):attachHls 先判 canPlayType('application/vnd.apple.mpegurl'),Chrome 149
     对此返回 "maybe"(真值)但不能真正原生解 .m3u8 → 走 video.src=m3u8、readyState=4、无 error、currentTime 卡 0
     = 看不了;Safari 原生 HLS 是真的所以能播。修 index.html attachHls:先 Hls.isSupported() 用 hls.js,原生仅作
     Safari 回退。用户 Chrome 实测:补丁后走 hls.js、currentTime 推进、seek 150/350s OK。Chrome 全部视频头号原因。
  ② HLS 首帧 PTS≠0(transcode copy 路缺口):源 MP4 edit-list 偏移被 -c copy 保留 → 首段从 1.4s 起 → 播放器 0 处无
     媒体易卡。修 run_hls_ffmpeg 加 -copyts -start_at_zero(input)+ -muxdelay 0 -muxpreload 0(output)两路都加。
     实测:make_zero 单独无效;本组合→首帧 keyframe@PTS0、0 起始源不回归。存量文件已就地重 mux 修。
  ③ 缓存(用户最终决定 — 测试程序):re-transcode/re-upload 复用同名 URL,长 max-age 会服旧。中途试过 mtime→ETag
     条件 GET(commit 一度做过,已弃)。用户拍板"测试程序直接全部 no-cache":index/get/hls/thumb 全部
     cache-control: no-cache(JSON 端点本就 no-store)。永不服旧。整个 gallery 改动 squash 成单 commit、force-push。
  待办(交给用户):重启 gallery(重建后的二进制)即可在 Chrome 播放;README live e2e。

────────────────────────────────────────────────────────────────────────────────
PREV(committed 7d9dd02 等): /loop chaos 测试。三项修复 + 提交:BUG #1(GC stale-cache fast-punch,数据丢失)、
  BUG #2(seal-and-roll 重试不幂等 → split-child-open wedge)、GC-回收-leak(coco 在 BUG#1 提的 P1,本次关闭)。
  代码 + 单测 + 加压 chaos(含多配置 soak)+ coco 全绿。

== 全族 chaos 验证矩阵(本会话延伸,全绿)+ selfheal 脚手架修复(commit 53e8d55)==
  覆盖轴全绿:in-proc system_chaos(6 配置矩阵 + 6 fresh 随机种子 + EC 几何 3/1·2/2·4/2 + 2× 120s
    深跑 30/19 nemesis 事件,均 "all invariants OK" mismatches=0 not_found=0)、单机 TCP/UCX、跨主机
    TCP/UCX、kvcache(native wheel 重建 v7→v8,154 轮 PASS)、etcd(551 keys 零丢 + 重选举)、
    enospc(163 keys,FULL-but-online + 2s 自愈)、selfheal(S1 bit-rot 自愈 12/12 byte-exact + S2 fail-loud)。
  selfheal 脚手架 BUG(非 autumn bug):selfheal_chaos.sh 没设 AUTUMN_EXTENT_BASE_PORT → EN ctl 端口 :10101
    撞本机 Ray 的 10100 段 → reproduce 2× root-cause → 加 =20000(对齐 etcd/enospc/kvcache 三脚本)→ 端到端 PASS。

== CHAOS-ACCOUNTING-CHECKER(本次提交)— storage 会计不变量进 system_chaos ==
  动机:gap 分析(用户 extent-10 提议 + coco arch)发现现有 chaos 只校验用户数据,不校验 storage 会计。
  新增:system_chaos verify 收尾断言每 extent `refs==列它的 stream 数` + `vp_table_refs==0` + 无 dangling
  membership。纯 test 改动,读 manager etcd(extents//streams/)为真相源,无新 RPC、无生产代码改动。
  一次覆盖 extent-10 orphan 类 + CoW double-free + GC leak 回归。
  关键设计:revision-pinned 一致快照(读 extents/ 取 header.revision,streams/ 按同 rev 读 — 防跨 commit
  phantom 误报,coco P2)+ 非空守卫(clean 但 0 extent/membership 判失败,防 vacuous pass,coco P2)+
  纯函数 check_accounting_invariants 抽出 + 6 单测。
  验证:6 单测(每类违规会 fire)+ 11 e2e(多 seed/EC 几何/深 churn,errors=0 且 memberships>extents 证明在
  校验 CoW refs≥2 共享 extent)+ coco 2 轮(P2×2 已修)。

== BUG2-EC-APPLY-FAIL(本次提交)— EC convert apply 失败被吞 → 半永久 wedge ==
  coco arch gap 分析发现 + 核实的真 bug。recovery.rs:1300 `let _ = apply_ec_conversion_done(...)` 吞错 +
  无条件 release marker;apply 的 etcd txn 失败但未丢 leadership 时,etcd 留 marker+pre-EC、内存清 marker,
  drain-only dispatch loop 永不重派 → 读 EVERSION_MISMATCH 直到 failover。修:抽 finalize_ec_dispatch_after_convert,
  仅 apply Ok 才 release,Err 保留 marker 等下 tick 重派(F119-D/F153 幂等)。reproduce-first:抽出方法单测 red→green。
  验证:2 单测 + 164 lib + 2 EC-heavy chaos(accounting errors=0)+ coco 未发现问题。

== 设计原则:禁止 `let _ =` 隐藏错误(用户 2026-06-20)+ 全仓审计 ==
  T1b 根因即此。原则记 feedback_no_let_underscore_swallow。3 高危 crate 并行审计:stream 0 BAD(fsync/save_meta
  已 P0-A/B/D fail-closed)、partition-server 0 BAD、manager 4 BAD(均 recovery.rs,T1b 同类):
  :860 `let _ = apply_recovery_done`(F222 once-only 恢复完成丢失→槽位卡死节点+marker 卡到 10min sweep);
  :237/:260/:338 `let _ = etcd.put_and_delete_txn(删 marker)`(Recovery marker etcd 泄漏,违反 I3 原子释放)。
  已修(commit ab42dd1)::237/:260/:338 换 `if let Err(e){warn!}` 后保留原"无论如何释放内存"语义(最小忠实,
  不改控制流);:860 区分良性 Precondition 与真错。coco 3 轮收敛(R2 抓到我误改 `?` 的 P2 回归→改回 log-and-continue;
  R3 仅 P3 文档已修)。教训:无吞错修复=换 log 别改 `?`/atomicity。2 个 deferred(completion retry queue / 原子 release,
  reproduce-first)。164 lib + recovery-churn chaos + coco 全过。
  剩余:~12 OK-BUT-LOG(safe 但静默)留轻量后续;coco 早前 gap 分析的其它项(inode_leases replay 非 fail-loud 等)见
  feedback_no_let_underscore_swallow / project_chaos_coverage_gaps_20260619。

== GC-RECLAIM-EVICT (DONE + 本次提交) — 关闭 no-refetch 回收 leak ==
  修法:alloc_new_extent 在 authoritative 封存路径 evict 旧 tail 的 extent_info_cache → GC 下次拉权威 sealed 状态回收。
    NOT synthesize sealed_length=seal_commit(coco P1:manager already_sealed 保留旧 L≥seal_commit,本地写 c<L 会让 GC
    搬前 c 删整 extent → 丢 [c,L))。多出的 cache-miss RPC 现安全(seed=603 wedge 已由 BUG2 修)。no-refetch 跳过留作兜底。
  验证:stream82 单测;seed 583 ×3(不回归 BUG#1,GC 见正确长度不误删)+ 603 ×2 + 760-763 = 9/9 全过;coco 未发现问题
    (v1 synthesize 被 coco P1 否 → 改 evict)。

== BUG #1 (DONE + COMMITTED 1346e0d) — GC stale-cache fast-punch ==
  GC F201 fast-punch 信了 StreamClient extent_info 陈旧缓存(alloc 封旧 tail 不 evict)→ 把已封满载 live VP 的
  extent 当空删掉(seed 583 丢 q000056)。修法:authoritative_sealed_length helper,读到 !sealed 一律 SKIP(no-refetch,
  数据安全,零新增 RPC,不触发 BUG#2 时序)。验证 11 seed 加压全绿 + coco。

== BUG #2 (DONE + COMMITTED 本次) — seal-and-roll 重试不幂等(chaos seed=603,WIRE v8)==
  根因:alloc_new_extent(Some(commit)) 在 manager 成功(封 T、滚新 OPEN tail T')但响应丢 → 写入端用同一 seal_commit
    重试 → manager 把当前全新的 T' 按 stale commit 封过头 → T' 永不可恢复 → split 子(CoW 共享 log stream)回放它
    撞 WAL-FAILSTOP 永不 open → key 不可达(mismatches=0,数据没物理丢)。
  修法:SealCommit 握手返回 (commit, tail_extent_id);alloc_new_extent(stream, Some(commit), seal_extent_id);
    manager 仅当当前 tail==seal_extent_id 且 OPEN 才封,否则幂等空操作返回当前 tail;coco P1:!tail.sealed 闸
    (已封则 fall through already_sealed 分配新 open tail)。WIRE v8。不动 F227 lenient-seal。
  验证:stream82/PS167/rpc30 单测 + WIRE registry 绿;修复前 seed 603 3/3 挂、修复后 seed 603 ×5 + 回归 0 挂
    (14 次加压 chaos);coco(P1 已修+重验)。

(本会话无遗留 deferred 项 —— GC-回收-leak 已由 GC-RECLAIM-EVICT 关闭,见上。)

────────────────────────────────────────────────────────────────────────────────
PREV(committed 3309b8b/ed7ac11): 删 vp_table_refs 双计数 Stage 1。Stage 2(未做):迁移后塌缩 extent_can_delete=
  refs==0 + 版本化解码物理删 vp_table_refs/vp_deps + 清 partitionVpRefs/。
Backlog(本任务外):note33 extent-state RMW 未 value-CAS;manifest TableLocations 无 CRC;extent unlink 不 fsync
  父目录;MERGE-REFS-RECOMPUTE(复现失败,已加回归守卫)。

ROUND 28 — fuzz round + 教训记录(无 code 改动)。fuzz-monitor 模式:试 3-min soak(seed 7, AUTUMN_CHAOS_DURATION_SECS=180)
  → **预期内命中已知环境 artifact**:loopback 端口耗尽([[project_chaos_long_soak_port_exhaustion]])——180s 持续负载
  churn 穿 ~55k 端口范围,TIME-WAIT 飙到 21902 → verify 阶段 EADDRNOTAVAIL(os err 99)→ not_found=398(假阴,非丢数据)。
  确认非 main 回归:drain-gate TIME-WAIT<4000 后,**seed 7 @ 默认 30s 干净通过**(mismatch=0 not_found=0 accounting=0
  all invariants OK)。**教训:此 loopback 测试机不能跑 >~60s chaos soak(无 drain-gate+连接复用前),future fuzz 轮
  应变 SEED @ 默认 30s,不要拉长 duration。** main 在 EN-kill(603/42/7)+ PS-failover + inode-lease 全 chaos 场景健康。

ROUND 29 COMMIT(待 push):**genuine dedup 复活——R22/23 的"井干"结论被修正**。回归 directive#4(分析未读 giant 函数)读
  handle_re_avali(171,sealed-replica 修复路径)= 清晰线性(forward-shard / op-lock / get_extent / mgr-info /
  apply_meta_durable fail-closed / eversion / F206 EC 短路 / already-up-to-date / recovery permit / stream_extent_from_
  sources / sync+save_meta fail-closed),不动。但发现:**manager R17 抽了 code_resp,stream crate 从没查过**——
  extent_node.rs 有 **20 处** `Ok(rkyv_encode(&CodeResp{code,message}))` 散落 re_avali/require_recovery/delete_extent/
  convert_to_ec handlers,无 helper → 抽 free fn `code_resp(code,message)`(同 manager R17 形)。纯 rkyv 构造零语义风险。
  20 站点:7 CODE_OK(3 组 replace_all)+ 13 unique-message(逐个 edit,含 5 多行 format!)。**net -36(53/-89)真减行**。
  验证:build+clippy(22 warn 不变)+ **90/90 stream 单测** + **coco findbugs deep(GPT-5.5)未发现** +
  **chaos seed 603(58s,EN kill 走 re_avali/recovery/delete):mismatch=0 not_found=0、range=0、write-liveness=0、
  accounting=0(5ext/8membership)all invariants OK**。
教训:R22/23 宣告"井干"过早——只查了 manager 的 code_resp,漏了 stream crate 同形 20 处。**应跨所有 chaos-validated
  crate 系统查同一 pattern**(append_reject/code_resp/alloc_reject 类响应构造)。下轮:查 partition-server/client 是否也有
  未抽的 CodeResp/Resp 构造 boilerplate。
进度:22 轮代码全绿(R1-R21 + R29),R22-R28 分析/fuzz。

ROUND 30 — 跨 crate response-boilerplate 系统清查(R29 教训的 follow-up)+ R29 fresh-seed 验证。
  系统查 partition-server + client 是否也有未抽的 CodeResp/Resp 构造 boilerplate(R29 发现 stream 漏抽,本轮确保无其它漏网):
  · partition-server/rpc_handlers.rs:MaintenanceResp/HeadResp/GetRedirectResp 各≤3、RangeResp/GetResp×2、无 CodeResp
    = 稀疏,无 dense 单类型 boilerplate → 干净。
  · partition-server/lib.rs:PutResp×8——但 PutResp={code,message,**key**}(3 字段,key 各异)且全在**写热路径**
    (enqueue_put/group-commit responder/partition_loop)→ 非 clean {code,message} 且热路径,**正确 defer**。
  · client/lib.rs:0 个 server CodeResp(SDK 是 decode 侧,用 AutumnError)→ 干净。
  **结论:CodeResp response-boilerplate 现已跨所有 chaos-validated crate 抽净(manager R17 + stream R29;ps 热路径/稀疏、
  client SDK 侧)。R29 是此 pattern 最后一个 genuine。** 其它 helper(append_reject/alloc_reject/force_ec_resp/
  refresh_and_backoff/replay/eversion-snapshot)都是 crate/函数特定,无跨 crate 扩散。
  验证:**chaos fresh seed 99 @30s 验 R29 代码(5340552):all invariants OK(400 keys,3ext/3membership)**——R29 改动在
  新 nemesis 调度下稳。
进度:22 轮代码全绿(R1-R21+R29)。response-boilerplate campaign 真正收口(R29 修正了 R22/23 过早"井干"+R30 系统确认)。
