Date: 2026-06-17
TaskStatus: completed
Task scope: u64 offset 拓宽 + max_extent_size 16 GiB flag。
  把 stream/storage 层每个 extent 字节位置从 u32 拓到 u64,让单 extent 突破 4 GiB；
  max_extent_size 从写死 3 GiB 改成默认 16 GiB + `autumn-ps --max-extent-size-bytes` flag。
  动机:extent 越大→数量越少→大规模下 manager/etcd 元数据压力越小。
  前提:用户明确「现在没有任何部署,不需要管兼容问题」。
Current summary:
  编译器驱动的级联改造(改核心类型→迭代 cargo build 修 `as u32` 级联):
  - wire(extent_rpc 二进制热路径): AppendReq.commit / AppendResp.offset+end /
    ReadBytesReq.offset+length / ReadBytesResp.end / CommitLengthResp.length 全 u32→u64
    (header 28→32, AppendResp 9→17, ReadBytesReq 24→32, …);manager 手镜像
    ExtCommitLengthResp.length 同步(逐字节 wire-compat)。
  - 持久格式: ValuePointer 16→24B(offset/len u64), partition_rpc SstLocation.offset/len +
    TableLocations.vp_offset, SST MetaBlock.vp_offset(read_u32→read_u64), TableMeta.offset/len,
    StreamAllocExtentReq.seal_commit Option<u64>, MultiModifySplitReq.*_sealed_length,
    GetRedirectResp.value_offset/value_len。WAL 记录 val_len 保持 u32(单值≤4GB 不变量)。
  - 计数仍 u32(in_flight / shard 数 / 块内偏移≤64KB / 单值长≤4GB)。
  - manager compute_commit_seal / commit_length_on_node / probe_extent_on_node /
    compute_duplicate_stream / compute_merge_streams 全 u64;PS recover_partition WAL 重放
    buf_base/cur_off/record_extent_off u64;reader sst_base u64 + block_cache 键(u64,u64)。
  - **EC 读分块修复(coco P1 #4)**: read_with_layout 对 EC extent 直接走 ec_subrange_read
    绕过 256MiB 分块;16GiB extent K=3 → shard ~5.33GiB > frame payload_len:u32(4GiB)上限。
    在唯一收口点 read_shard_from_addr 内部按 read_chunk_bytes(256MiB)分块,所有 EC 路径自动受界。
    旧 3GiB 默认 shard ~1GiB 不触发,16GiB 默认才暴露 → 属本次新增能力的真 bug,已修。
  - WIRE fingerprint v4→v5(MIN=MAX=5,pre-R3 同 commit 部署);registry 注册 5254fafce73f6ffe。
  - max_extent_size_bytes(): OnceLock 默认 16GiB,clamp[1,64]GiB;两处 StreamClient 建点都读它;
    autumn-ps 新 --max-extent-size-bytes flag → set_max_extent_size_bytes。
  验收:
  - cargo build --workspace 绿;rpc 30 / manager lib 163 / stream+PS 全单测绿。
  - manager 集成 3 个 recovery 测试在并发下偶发失败 = 进程级 e2e 资源竞争,已 git stash
    对照 baseline(HEAD)确认相同 3 个偶发失败 → 非本次回归(单独跑各自全绿)。
  - coco deep review:7 项,#1/#2/#3/#7 全是「旧持久格式/滚动升级兼容」→ 用户明确不管兼容 +
    全停全启 rkyv-fail-loud + WIRE v5 MIN=MAX 强制无混跑 → 出范围;#4(EC frame 溢出)真 bug 已修;
    #5(ZC 读)被 length>read_chunk_bytes 提前 bail 覆盖;#6(EC materialize 内存)= F193 已知 won't-do 区。
  - **live e2e(隔离 memory-mode 4-EN 集群, /tmp, 高端口 19xxx, --cpuset 130-135, 不碰 /data02)**:
    put-stream 4.5GiB(1152×4MiB chunk 入单 log_stream extent)→ 单 extent 实际 4,831,907,398 字节
    > u32::MAX(4,294,967,295)→ get-stream 回读 sha256 **MATCH**(后段 chunk VP offset > 4GiB,
    走 u64 ReadBytesReq.offset);kill -9 PS 重启 → recovery 重放 SST(paged u64 vp_offset)+ WAL
    (u64 record offset)→ get-stream 再读 **MATCH**;--max-extent-size-bytes 8GiB flag 生效。
Main gaps:
  - 无(代码 + 单测 + coco + live e2e 全绿)。
  - EC > 12GiB extent 的 frame-溢出修复(read_shard_from_addr 分块)逻辑与既有复制路径同构,
    单测覆盖 stitch 正确性;触发需 >12GiB EC extent(实测代价高),未单独 live 复现该极端但逻辑直白。
Commits: 待 commit + push 到 main。
Next steps:
  - commit + push 到 main(solo-flow 直推 main)。
