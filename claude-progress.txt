Date: 2026-05-17
TaskStatus: not_completed
Task scope: F211 series — Operator-Driven Node Lifecycle (plan
            only, no code yet). Refactor autumn-rs node-death
            judgment from "manager auto-detects + immediate
            recovery" to HDFS decommission style "OP-driven
            fence + manager exposes facts". Addresses:
              (a) EC convert coordinator 永久死亡时 marker 卡死
                  导致 F138 互斥锁死 recovery 的死局
              (b) 当前节点故障判定过于激进 (心跳 10s 超时立即
                  dispatch recovery), 网络抖动引发不必要的
                  跨节点重建
              (c) 缺乏 fencing 防护，假死复活节点可对 sealed EC
                  extent 造成 split-brain

Design summary (full plan in
/Users/zhangdongmao/.claude/plans/ec-convert-marker-coordiator-enumerated-adleman.md):
  - Manager 自动跟踪 Online ↔ Suspected (心跳), 不自动进 Down
  - OP policy script 综合外部证据 (ssh/k8s/监控) + manager 事实
    后通过 admin RPC 显式 fence
  - Fence 是 cleanup 同步点: bump owner-lock revision +
    auto-abandon 关联 EC marker + 让 recovery 接管副本重建
  - EC convert reissue 不自动, 由 OP script 自己发现 advisory
    后调 force_ec_convert
  - 业界对标: HDFS decommission 模式 / Ceph noout + osd out
  - Fencing 复用 owner-lock revision (新加 shard read/write/
    commit fence), 假死 coord 复活后写入被拒

子 feature 拆分 (passes:false, 待实现):
  F211-A  NodeStateTracker (Online ↔ Suspected, auto in-memory)
  F211-B  Health reporting RPCs (list_node_states /
          extent_health_report / list_ec_inflight_markers)
  F211-C  Operator fence / maintenance / clear / remove admin
          RPCs (含容量预检 #5 + zombie 防护 #2 + maintenance
          TTL #6)
  F211-D  Owner-lock revision bump on fence + shard
          read/write/commit revision fence (含读路径 fence #4)
  F211-E  Recovery loop gated by Fenced state + 指数退避 (#7)
  F211-F  EC convert auto-abandon on fence + Suspected-window
          dispatch 跳过 (#3)
  F211-G  Python OP policy script (python/node_policy.py)
  F211-H  Recovery 节流 + 优先级 (per-source/target/global
          concurrency limits, #1)
  F211-I  Operator action audit log (#8)

落地顺序: A → B → I → C → D → H → G → E → F
  - 1-7 (A,B,I,C,D,H,G) 是基建, 可一起合到 main
  - 8-9 (E,F) 是行为切换, 必须 G policy script 就绪后才能上线

向后兼容警告 (per feedback_warn_on_backward_incompat):
  - 节点故障默认不再自动重建 (env var AUTUMN_MGR_RECOVERY_GATE=
    auto_disk 临时回滚)
  - EC convert 不再 stuck 等运维 etcdctl (fence 后自动 abandon)
  - Recovery 现在有并发上限 (默认 max_global=64)
  - 新 etcd prefix: node_override/, ec_convert_advisory/,
    decommissioned/, mgr_audit_log/ (回滚需 etcdctl 清空)
  - 9 个新 MSG_ 常量; WriteShardReq/CommitEcShardReq/
    ReadShardReq 加 revision (rkyv 字段扩展, partition server
    需同步升级)
  - Fenced/Decommissioned 节点 register 会被拒

关键不变量 (本 plan 完全不动):
  - F138 marker 在阻塞 recovery (marker 删除仍是同步点)
  - F149 leader fence
  - F119-D EC convert 幂等 guard
  - F153 per-extent ec_conversion_locks
  - F207 marker rich state + replay
  - Reed-Solomon 编码确定性
  - owner-lock revision 协议 (新增使用点, 协议本身不变)

Status: plan approved by user, feature_list.md entries written
(F211-A through F211-I, passes:false). 代码实现尚未开始, 由用户
后续推进。

Next steps:
  - 实现按落地顺序逐个 sub-feature 推进
  - 每个 sub-feature commit 时更新 feature_list.md 的 passes
    字段
  - F211-E 切换前确认 F211-G policy script 已部署 + 监控渠道
    准备好
  - 阶段化部署: F211-A/B/I/C/D/H/G 合一波 (基建),
    F211-E/F 单独上线 + 观察

Previous task (completed): F210-H3 Tier 2 split commit_length
fence-enforcing primitive + dedicated fence-free probe RPC.
